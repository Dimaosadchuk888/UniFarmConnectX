# Database connection - Set via Replit Secrets for security
# DATABASE_URL should be configured in Replit Secrets, not here
# NEON_PROJECT_ID should be configured in Replit Secrets, not here

# Настройки для выбора БД
DATABASE_PROVIDER=neon
USE_NEON_DB=true
USE_LOCAL_DB_ONLY=false