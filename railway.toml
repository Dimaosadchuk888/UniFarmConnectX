# Railway configuration for UniFarm Connect v1.0.37 - FORCE REBUILD
# FORCE REBUILD: Fixed Vite config with ES modules compatibility
[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run build && echo 'BUILD COMPLETED SUCCESSFULLY' && NODE_ENV=production PORT=3000 tsx server/index.ts"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[deploy.envs]
NODE_ENV = "production"
PORT = "3000"
FORCE_RESTART = "2025-08-08-14-50"
BUILD_VERSION = "1.0.37"
VITE_FIX = "true"

# Build configuration
[build.envs]
NODE_ENV = "production" 