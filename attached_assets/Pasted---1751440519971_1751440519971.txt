Отлично, вот промпт в формате глобального исследования, без вмешательства в код — только проверка, диагностика, верификация. Передаём агенту для запуска полного технического аудита текущей системы:

⸻

🔍 ПРОМПТ ДЛЯ ГЛОБАЛЬНОГО ИССЛЕДОВАНИЯ (без изменений кода)

📌 Цель: провести полное техническое исследование текущего состояния системы, определить, какой пользователь реально подключён на preview-странице, и проверить, насколько данные интерфейса соответствуют данным в базе.

⸻

1. 🔐 Идентификация текущего пользователя
 • Определи, какой user_id сейчас подключён на preview-странице.
 • Проверь JWT токен в localStorage:
 • user_id = ?
 • telegram_id = ?
 • Что возвращает useUser() из userContext?
 • Что уходит в mutation при покупке Boost и UniFarm пакетов (BoostPackagesCard.tsx, FarmDeposit.tsx)?

⸻

2. 💳 Проверка балансов и кошельков
 • Баланс UNI Token на UI — сколько отображается?
 • Баланс TON на UI — сколько отображается?
 • Сравни эти данные с тем, что хранится в базе данных по user_id, который определён в п.1.
 • Проверь, откуда UI получает балансы: getWalletDataByUserId() или getWalletDataByTelegramId()?

⸻

3. 🧪 Проверка бустов (TON Boost)
 • Попробуй (в режиме анализа) купить Boost пакет:
 • Какой ID уходит в запрос?
 • Какой payment_method используется?
 • Какой кошелёк задействован (user_id)?
 • Списывается ли сумма с баланса? Или отклоняется?
 • Создаётся ли транзакция в базе?
 • Обновляется ли баланс после покупки?

⸻

4. 🧾 Проверка истории транзакций
 • Какие транзакции отображаются на странице?
 • Есть ли последние Boost/Deposit действия?
 • Показываются ли транзакции только того юзера, который реально подключён?
 • Совпадают ли транзакции UI с тем, что есть в БД?

⸻

5. 🧭 Системная консистентность
 • Нет ли конфликта между user_id=48 и user_id=50?
 • Не происходит ли подмена одного ID другим?
 • Все ли данные UI строго соответствуют данным, полученным из базы по user_id?

⸻

6. ⚠️ Обязательные требования
 • Не менять никакой код
 • Не сбрасывать и не обновлять localStorage вручную
 • Только исследование: диагностика, логика, сопоставление

⸻

📌 Ожидаемый результат отчёта:
 1. Какой ID реально подключён?
 2. Совпадают ли балансы UI ↔ БД?
 3. Работает ли Boost?
 4. Есть ли скрытые конфликты между 48 и 50?
 5. Что мешает полностью перейти на одного пользователя?

⸻

Готов передать это агенту. Если хочешь — могу дополнительно оформить это как чеклист или встроенный формат в систему.