🛠️ 🔍 Промт для технической проверки аномалий с балансом и фармингом UniFarm:
❗ Важно: не вносить никаких изменений в код и БД, только диагностика и анализ логов, таблиц и логики исполнения.
Цель:
Найти причину, почему у части пользователей (в том числе у ID 25) после пополнения TON-средств:

Баланс пополняется, но через 1–2 минуты средства исчезают с кошелька без следов;
Средства не зачисляются в фарминг, хотя были отправлены;
При следующих действиях (повторном пополнении или новом фарминге) всё работает корректно;
Проблема проявляется периодически, не на каждой транзакции.
⚠️ Важно:
Это техническое задание составлено как прототип.
Названия файлов, методов и сервисов, указанные ниже (например, wallet.service.ts, saveTransaction(), farming_positions, rollback и др.), — ориентировочные и могут не совпадать с тем, что используется в вашем проекте напрямую.

Исполнитель должен самостоятельно соотнести указанные точки проверки с фактической архитектурой проекта, файлами, названиями функций, таблиц и логикой работы системы.

🔎 1. Проверить логику обработки транзакций и баланса
Определить, какие процессы запускаются через 1–3 минуты после пополнения (cron-задачи, фоновые jobs, пост-хуки).
Проверить backend-логику обработки TON транзакций:
выполняются ли действия вроде deleteTransaction, rollback, clearInvalidTransactions, resetBalance;
есть ли механизмы, которые удаляют или обнуляют транзакции при подозрении на дубликаты или ошибки;
анализировать дедупликацию по tx_hash, description, comment, bounce, payload.
🗄️ 2. Проверить таблицы:
transactions, wallet_balances, farming_transactions, farming_positions, wallet_events.
По user_id = 25 (и другим случаям, если есть) за последние 3 дня:
Найти транзакции, у которых статус был CONFIRMED, а затем они исчезли или стали FAILED;
Определить, происходили ли балансовые изменения без подтверждающей транзакции;
Проверить удаление транзакций с нетипичными параметрами (например, bounce = false, длинный comment).
📦 3. Проверить фарминг
Проверить, почему иногда происходит списание средств с кошелька, но позиция в фарминге не создаётся.
Возможно, вызывается wallet.withdraw(...), но из-за ошибки или отката позиция (farming_position) не фиксируется.
Анализировать: в каких случаях происходит сбой при зачислении в фарминг — особенно при последовательных действиях пользователя.
🔁 4. Проверить параллельные и фоновые процессы
Есть ли конфликтующие процессы, например:
SyncWalletTransactions, RecalculateWalletBalances, RollbackFailedTxs, AutoFarming;
Проверить, могут ли они выполнять откат транзакции или пересчитывать баланс в промежутке после пополнения.
⚖️ 5. Сравнить успешные и неуспешные кейсы
Найти два пополнения подряд:
одно — полностью успешное;
второе — пропало с баланса.
Сравнить их параметры:
comment, payload, bounce, длину, тайминги createdAt, confirmedAt, deletedAt, структуру tx_hash.
📌 Итоговая задача:
Необходимо точно установить, на каком этапе или при каких условиях происходит автоматическое или ошибочное списание средств с баланса пользователя после пополнения или при попытке отправки в фарминг — без создания позиции и без появления в истории транзакций.
Проверка — только аналитическая, никаких изменений в продакшн-системе или БД.