Вот техническое задание для агента в твоем стиле — чтобы он **проверил Telegram WebApp**, **запустил его в браузере**, **открыл DevTools (F12)** и **выписал все ошибки, предупреждения и подозрительное поведение**, **не влезая в код**, а **только через исследование интерфейса и консоли браузера**:

---

# 🧾 Техническое задание: Проверка консольных логов и DevTools без вмешательства в код

## 🎯 Цель:

Провести **исследование состояния консоли** (DevTools) при загрузке и работе Telegram WebApp UniFarm, выявить:

* Ошибки JavaScript
* Предупреждения (`console.warn`)
* Сетевые ошибки (network 4xx/5xx)
* Проблемы с загрузкой ресурсов (шрифты, изображения, файлы)
* Нарушения CORS или CSP
* Проблемы с WebSocket
* Все, что может повлиять на стабильность работы WebApp

---

## 📦 Что агент должен сделать:

### 1. **Открыть WebApp в Telegram**

* Запустить WebApp, как обычный пользователь
* Дождаться полной загрузки интерфейса

### 2. **Открыть DevTools**

* Нажать `F12` или `Cmd+Opt+I`
* Перейти на вкладки:

  * **Console**
  * **Network**
  * **Application** (опционально: проверить localStorage/sessionStorage, если есть)

### 3. **Выписать в чат:**

* Все **ошибки** из вкладки **Console**
* Все **предупреждения**
* Все **сетевые ошибки** из вкладки **Network** (4xx, 5xx, CORS)
* Проблемы с загрузкой шрифтов, картинок, скриптов
* Статус **WebSocket соединения** (ошибки, reconnect, ping/pong)
* Статус **initData из Telegram** (есть ли, корректно ли парсится)

---

## 📌 Ограничения:

* ❌ **Не менять код**
* ❌ **Не редактировать конфигурации**
* ✅ Только **анализ через браузер**
* ✅ Только **визуальное и консольное исследование**

---

## 📝 Ожидаемый формат ответа:

```plaintext
🛑 Ошибки Console:
- Uncaught TypeError: Cannot read properties of undefined (reading 'balance')
- WebSocket connection failed: wss://... code 1006

⚠️ Предупреждения:
- Deprecation warning: useEffect cleanup function returned nothing
- Content Security Policy: The page’s settings blocked the loading of a resource

🌐 Сетевые ошибки:
- GET /api/v2/user/profile — 401 Unauthorized
- GET /fonts/Inter.woff2 — 404 Not Found

🔄 WebSocket:
- Initial connection: OK
- Ping/pong: работает
- Ошибки: нет

📦 Storage:
- sessionStorage содержит telegram_user
- localStorage пустой

📤 Telegram initData:
- Передается через X-Telegram-Init-Data
- Распарсилось успешно
```

---

Хочешь, я сразу создам шаблон для ответа агента, чтобы он мог просто вставить туда свои наблюдения?
