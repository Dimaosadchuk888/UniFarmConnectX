Вот обновлённая, финализированная версия технического задания для агента, **с твоим уточнением о глубоком уровне проверки всей системы** и запретом на поверхностный анализ:

---

📌 **ЗАДАЧА: Глубокий аудит модульной структуры, маршрутов и кода проекта UniFarm**

---

### 🎯 Цель:

Выявить **все несостыковки** между структурой модулей, маршрутами, импортами, названиями файлов и фактической реализацией **на уровне кода**, по **всей системе UniFarm**.

---

### 🔎 Что конкретно нужно сделать:

#### 1. Исследовать **всю систему целиком**, включая:

* 📁 Все модули (auth, wallet, user, farming, missions, boost, etc.)
* 🧠 Бизнес-логику (файлы `*.service.ts`, `*.logic.ts`, `*.controller.ts`, `*.model.ts`, `*.schema.ts`)
* 🌐 API маршруты (`routes.ts`, `index.ts`, `api/v2`, legacy)
* 📄 Все вспомогательные и подключаемые файлы (utils, middlewares, Telegram, админка и т.п.)
* 🔁 Точки входа, импорты, экспорты
* 🗂 Любые нестандартные структуры (`routers.ts`, `indexRoutes.ts`, `apiRouter.ts` и пр.)

---

### 🚫 **Категорически запрещено:**

* Делать **поверхностную или выборочную проверку**
* Ограничиваться только файлами маршрутов или `RedMap`
* Пропускать "неявные" подключения, вложенные импорты, абстракции
* Сравнивать только по названиям — проверять нужно **реальный работающий код**

---

### ⚠️ Особое внимание:

* 📌 Случаи, когда:

  * Один и тот же маршрут описан в двух разных местах (дублирование)
  * Импорт ведёт на файл с другим назначением (например, импорт `router.ts`, но используется `routers.ts`)
  * Файл подключён, но не используется
  * Названия функций или путей в коде не совпадают с тем, что заявлено в маршрутах
  * Несовпадение между `routes.ts`, `controller.ts`, `service.ts`, `model.ts`

---

### 📋 Что ты должен выдать:

Создай файл **`MODULE_INCONSISTENCY_REPORT.md`**, где:

1. Перечисли **все модули по очереди** (в алфавитном порядке или по структуре проекта)
2. Для каждого модуля отрази:

   * 🔍 Проверенные файлы
   * 🚫 Обнаруженные проблемы: несостыковки в коде, неправильные маршруты, дубли, неиспользуемые импорты, лишние файлы
   * ✅ Что соответствует идеально
3. В конце составь:

   * 📌 Список **критических пробоев** (могут вызывать баги, потери данных, ошибки авторизации и т.п.)
   * 📌 Список **технического долга** (нужно привести к единому стилю, но пока работает)
   * 📌 Список **чистых модулей** (всё аккуратно и соответствует RedMap + роутинг + структура)

---

### 🛡 Важно:

* Не делай никаких изменений в коде
* Используй инструменты навигации, поиск по проекту, кросс-проверку между слоями
* Проверяй всё как для **production code review**: строго, точно, глубоко

---

Если нужно — могу выдать шаблон Markdown-отчёта для фиксации результатов.
