Понял. Ниже — **чёткое и технически корректное сообщение для агента**, чтобы он провёл **точечную диагностику поведения Boost-пакетов** по **ID пользователя 25**, **без изменения кода**, только с проверкой БД и логики.

---

🔍 **ЗАДАНИЕ: Диагностика ошибки при покупке Boost-пакетов (TON Boost)**

📌 **Контекст**:
После последнего обновления:

* ✅ Проблема с дублированием депозитов решена.
* ⚠️ Но теперь выявилась **новая ошибка** — **некорректное списание TON при покупке Boost-пакета**.

---

### 🚨 **Что происходит (системное поведение)**:

1. Пользователь покупает Boost-пакет (TON Boost):

   * С баланса **списывается сумма**, но **затем возвращается обратно**.
2. Бывает и наоборот: Boost **активируется**, система считает его открытым, начисляет бонусы и уведомления, **но деньги с баланса вообще не списаны**.
3. **ID пользователя:** `25`

---

### 🧩 **Что нужно сделать (без вмешательства в код):**

#### 1. **Проверить транзакции в БД**

* Все связанные с Boost-покупкой транзакции (`type = 'BOOST_PURCHASE'`) для user\_id = 25
* Были ли они:

  * `created` или `rolled_back`
  * реально списаны (`status = 'completed'`) или отменены
* Есть ли следы "возвратных" транзакций — возможно, система сама отменяет их?
* Присутствуют ли в транзакциях поля `is_duplicate`, `duplicate_of_id`, `tx_hash_unique`, `metadata`?

#### 2. **Проверить таблицу Boost-пакетов**

* Убедиться, что Boost-пакет действительно записан как активный (TON Boost), даже если списание не прошло.
* Какие поля сохраняются при создании пакета: user\_id, сумма, время, статус.
* Есть ли `transaction_id` или ссылка на конкретную транзакцию — не пустая ли она?

#### 3. **Проверить логи событий и WebSocket**

* Какие события шлются по WebSocket:

  * `balance_update`, `boost_activated`, `transaction_created`
* Есть ли события **в неправильном порядке** или задвоенные?

#### 4. **Проверить возможные причины возврата средств**

* Есть ли логика/триггер, которая **откатывает списание**, если Boost-пакет не успел активироваться?
* Используется ли механизм `rollback` или ручной откат транзакции через сервис?
* Есть ли попытка создать Boost-пакет до завершения транзакции → и система ошибочно "отменяет" списание?

#### 5. **Дополнительно: проверить**

* Нет ли конфликтов между `WalletService` и `BoostService` при распределении баланса.
* Логика по типу "если Boost уже активирован, не списывать повторно" — могла ли она сработать некорректно?

---

### 🧾 Цель:

Нам нужно **выявить причину**:

* Почему Boost может активироваться **без списания средств**?
* Почему средства могут **списываться, а потом возвращаться**?
* Какой модуль или логика **отменяет** транзакцию, и почему?

📍 Всё это — **без вмешательства в код**, только **через базу и логи**.

---

Если агенту нужны конкретные таблицы, поля или схемы — могу их дополнительно прислать.
