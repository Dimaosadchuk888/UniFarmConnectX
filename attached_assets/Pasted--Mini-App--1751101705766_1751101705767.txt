✅ Промт: Полная проверка реферальной системы Mini App (без изменения кода)

⸻

🔒 ОГРАНИЧЕНИЯ:

НИ В КОЕМ СЛУЧАЕ не изменять код, не комментировать, не добавлять, не удалять. Только анализ, отладка и проверка.
Твоя задача — полностью исследовать поведение системы как тестировщик: глазами пользователя и в логах.
Если ты что-то хочешь изменить — сначала отправь отчёт и жди отдельного разрешения.

⸻

🧪 ЗАДАЧА: Полностью проверить, как работает передача данных в Mini App → запись в БД → генерация реф-ссылки → отображение её на UX.

⸻

🔍 Проверь все следующие этапы:

⸻

1. Telegram передаёт данные в Mini App
 • Загружается ли telegram.initData?
 • Работает ли telegram.initDataUnsafe.user.id?
 • Присутствуют ли все нужные параметры: id, username, auth_date, hash?
 • Есть ли ошибки в консоли при старте приложения?

📌 Добавь в отчёт скрин или лог initDataUnsafe.

⸻

2. Передача данных на сервер
 • Происходит ли отправка на backend?
 • Покажи fetch/axios запрос, который отправляется.
 • Какие поля в теле запроса?
 • Покажи ответ сервера (успешно или с ошибкой)?

📌 Сделай скрин или вставь пример запроса и ответа.

⸻

3. Запись в базу данных (Supabase или др.)
 • Создаётся ли новая запись при первом заходе юзера?
 • Какие поля записываются?
 • Присутствует ли telegram_id, username, ref_code, ref_link?
 • Не блокирует ли база запись (уникальные ограничения, ошибка в политике доступа)?

📌 Приложи скрин таблицы с новой записью, покажи ID, дату, ref-ссылку.

⸻

4. Генерация и хранение реферальной ссылки
 • Создаётся ли реферальная ссылка (в виде URL)?
 • Записана ли она в базу (в отдельном поле)?
 • Правильный ли формат ссылки?
 • Нет ли дубликатов или пустых значений?

📌 Приложи пример реф-ссылки из БД.

⸻

5. Отображение на интерфейсе
 • Отображается ли реферальная ссылка на экране?
 • Загружается ли она из БД через API или client fetch?
 • Есть ли DOM-элемент, в который она должна попадать?
 • Не скрыта ли она стилями (display: none, opacity: 0)?
 • Нет ли условий, которые блокируют её появление (if (!ref_link) return null)?

📌 Покажи, как она (или не она) отображается в браузере. Приложи скрин интерфейса.

⸻

📋 Итог отчёта:
 • Для каждого этапа: ✅ / ❌
 • Подтверждение: скриншоты, логи, ссылки.
 • Комментарии по каждому этапу — только факты: “есть/нет”, “отображается/не отображается”, “передаётся/не передаётся”.