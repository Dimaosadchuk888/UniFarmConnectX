Вот формулировка задачи для агента:

---

### ✅ ЗАДАЧА: Обновление всех ссылок на актуальный хост UniFarm

#### 🎯 Цель:

Заменить устаревшие ссылки на актуальную рабочую ссылку проекта:

```
https://uni‑farm‑connect‑x‑elizabethstone1.replit.app
```

#### 🔍 Контекст:

В системе до сих пор используются устаревшие или временные домены в следующих точках:

* Вебхуки (`/api/v2/admin-bot/webhook`)
* Telegram Mini App manifest (`/tonconnect-manifest.json`)
* TON Connect иконка (`/tonconnect-icon.svg`)
* Все прямые вызовы внутри клиента (`fetch`, `axios`, `useApi`, и т.д.)
* Переменные окружения (`.env`, `.env.production`, `.env.preview`)
* Vite config (если прописан `base` или `server.proxy`)
* Server endpoints (если жёстко указаны абсолютные пути)
* Supabase webhook targets (если используются)
* Admin Bot config (`config/adminBot.ts`)
* Документация (README, .md файлы)

#### 📌 Задачи по шагам:

1. **Провести скан по проекту**:

   * Найти **все вхождения** старых ссылок: `http`, `https`, `replit`, `.app`, `.dev`, `connect`, `x-`, `preview`, `localhost`, `127.0.0.1`, и т.д.
   * Зафиксировать, где они используются (файл + строка + контекст).

2. **Заменить вручную**:

   * Заменить все устаревшие ссылки на **`https://uni‑farm‑connect‑x‑elizabethstone1.replit.app`**
   * Убедиться, что ссылки корректно закодированы, если вставляются в JSON или env.

3. **Проверить функциональность**:

   * Проверить загрузку манифеста
   * Проверить отображение иконки
   * Проверить срабатывание вебхуков (через curl или вручную)
   * Проверить фронт через Telegram Mini App

4. **Подготовить отчёт**:

   * Что именно было заменено (файлы + строки)
   * Были ли исключения или временные ограничения
   * Список всех файлов, где были внесены изменения
   * Подтверждение, что всё работает с новой ссылкой

#### ⚠️ Важно:

* Не удалять и не изменять ничего кроме ссылок
* Не использовать относительные пути там, где был абсолютный
* Не менять структуру JSON, если это влияет на парсинг в Telegram или TON Connect

---

Если нужно, я могу сразу сгенерировать примерный grep-скрипт или поиск по VSCode для агента.
