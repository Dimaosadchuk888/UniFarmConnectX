✅ Техническое задание №1 — Глубокий аудит Telegram-авторизации и окружения UniFarming

🧭 Цель:

Провести полный исследовательский аудит Telegram Mini App UniFarming, чтобы:
 • определить, почему при входе через Telegram у всех пользователей подгружается один и тот же аккаунт с user_id = 48,
 • выяснить, в каком окружении (прод, дев, превью и т.д.) находится приложение,
 • и проверить, действительно ли система корректно воспринимает Telegram-пользователя через initData.

⸻

🔒 ВАЖНО:

Агенту запрещено вносить любые изменения в код, базу, сервер или окружение.
Цель — только диагностика и верификация текущего состояния системы.

⸻

📋 План аудита (по этапам)

1. Анализ среды исполнения (окружение)
 • Определить, в каком режиме развернуто приложение:
 • production, preview, development, test, sandbox
 • Указать точный URL/домен, к которому привязана текущая версия.
 • Выяснить, где размещён фронтенд (Replit, Railway, Vercel, Render, и т.д.).
 • Проверить, есть ли NODE_ENV, IS_PREVIEW, IS_PRODUCTION, IS_TEST, .env переменные.
 • Вывести эти значения в отчёт — ссылкой и скриншотом (или логом) подтверждающим окружение.

2. Проверка Telegram SDK и initData
 • Убедиться, что при входе через Telegram SDK действительно отрабатывает и передаёт initData.
 • Найти, где в коде используется Telegram.WebApp.initDataUnsafe.user.id или initData.
 • Проверить: если initData отсутствует — какие значения подставляются?
 • Определить: сохраняется ли Telegram ID в БД или в local/session storage, и как система его извлекает.

3. Диагностика подстановки user_id = 48
 • Найти все участки, где:
 • user_id подставляется вручную
 • используется testUser, defaultUser, mockUser
 • явно присутствует user_id = 48
 • Уточнить, работает ли это только в dev/preview-режиме или в любом случае.

4. Фактическая проверка поведения в Telegram
 • Зайти в Mini App с двух разных Telegram-аккаунтов.
 • Проверить, какая информация отобразится:
 • Баланс
 • Имя пользователя
 • Рефералы
 • ID (если можно отследить)
 • Зафиксировать: происходит ли подмена на user_id=48 в любом случае.

⸻

📌 Финальный отчёт должен включать:

Раздел Что проверить Найдено Комментарий
1. Окружение Какой режим окружения? preview / prod / и т.д. Подтверждено по URL, env
2. SDK и initData Передаётся ли Telegram ID? Да / Нет Как извлекается
3. user_id Подставляется ли user_id=48? Да / Нет Где и когда
4. Поведение При входе с разных Telegram-аккаунтов, кто отображается? Всегда user_id=48 / корректный Скриншоты / Network


⸻

🧠 Методика:
 • Использовать только инструменты анализа: console.log, network, devtools, debug, Postman, curl, logs.
 • Не производить правок ни в одном из следующих мест:
❌ .env
❌ config
❌ auth.ts
❌ mock.ts
❌ user.service
❌ базы данных

⸻

✅ Цель после выполнения:
 • Получить исчерпывающий отчёт, который подтверждает, где и почему система работает неправильно.
 • На основе отчёта будет принято решение о дальнейших правках (если потребуется).