📋 PROMPT: Финальный аудит системы UniFarm (Production Readiness Check)

📅 Дата: (автоматически ставить сегодняшнюю дату)
📁 Файл аудита: /audit/final-audit-YYYY-MM-DD.md
🎯 Цель: Провести полный независимый аудит всей системы UniFarm, основываясь исключительно на текущем состоянии кода, файлов, модулей и зависимостей.
🚫 Не использовать предыдущие MD-файлы или устаревшие отчёты. Только живая структура проекта и фактический код.
🛠 Никаких изменений — только проверка, анализ и составление отчета.

⸻

📦 1. Корневая структура проекта

Проверить:
 • Количество файлов в корне (есть ли тестовые, .sh скрипты, устаревшие HTML, JS и т.п.)
 • Наличие README.md, docs/, package.json, tsconfig.json, env, config/
 • Правильность иерархии директорий (client, modules, core, routes, types и т.д.)
 • Удалены ли лишние каталоги (attached_assets/, __trash/, debug/, old_versions/)

📍 Вывод: Структура чиста / требует очистки. Указать путь и тип нарушений.

⸻

📁 2. Модули и архитектура

Проверить:
 • Количество модулей в /modules/ и /client/src/modules/
 • Каждый модуль должен содержать: controller.ts, service.ts, routes.ts, types.ts (если применимо)
 • Есть ли дублирующие или устаревшие модули (например, admin vs adminBot)
 • Связи между модулями (wallet ↔ boost ↔ farming) — явно или через import

📍 Вывод: Архитектура модульная / есть проблемы. Описать слабые связи, дубли, циклы.

⸻

🧠 3. Синтаксис и стиль кода

Проверить каждый файл на:
 • Незакрытые скобки, синтаксические ошибки
 • Использование устаревших или небезопасных JS/TS конструкций
 • Дублирование логики (например, обработка баланса/депозита в 2+ модулях)
 • Использование any, @ts-ignore, TODO, FIXME

📍 Вывод: Синтаксис чистый / требует рефакторинга. Привести примеры строк.

⸻

📚 4. Зависимости (package.json)

Проверить:
 • Содержимое dependencies и devDependencies — корректно ли распределены
 • Актуальность библиотек (если есть outdated — указать)
 • Есть ли лишние зависимости (не используются в коде)
 • Название проекта, версия, скрипты (start, build, test, preview)
 • Подключён ли Sentry, TonConnect, shadcn, ESLint, Prettier, Tailwind

📍 Вывод: Пакеты чисты / требует очистки. Указать версии и проблемы.

⸻

🔐 5. Безопасность

Проверить:
 • middleware: requireTelegramAuth, jwtAuth, rateLimiter, hmacMiddleware
 • Есть ли fallback токены или user_id в коде (|| 42, || 'secret', demo_mode)
 • Переменные .env — нет ли реальных токенов в .env.example
 • Уязвимые endpoints (без авторизации, без валидации)
 • Прямые SQL-запросы или debug-обходы

📍 Вывод: Уязвимостей нет / есть критические проблемы. Указать файл и строку.

⸻

🖥 6. UI/UX и адаптивность (client/src)

Проверить:
 • Страницы: Wallet, Farming, Boost, Transactions, Referrals
 • Компоненты: карточки, табы, графики, Boost-пакеты
 • Работает ли адаптивность на всех экранах (desktop, mobile)
 • Есть ли undefined, NaN, отсутствие fallback’ов
 • Присутствует ли глобальная обработка ошибок и состояний загрузки
 • Используются ли актуальные библиотеки (lucide-react, tailwind)

📍 Вывод: Интерфейс стабильный / ломается. Указать проблемные компоненты.

⸻

🔄 7. Бизнес-логика и API

Проверить:
 • Работают ли запросы wallet → boost → farming → referral → withdraw
 • Есть ли дублирующие/неиспользуемые endpoints
 • Подключены ли все endpoints к Supabase (таблицы, транзакции, балансы)
 • Проверить WebSocket (подключение, reconnect, лимиты)

📍 Вывод: Логика цельная / есть обрывы. Указать endpoints и модули.

⸻

🧪 8. Preview vs Production

Проверить:
 • Replit Preview: создаётся ли тестовый пользователь? Отображаются ли Boost-пакеты?
 • Production: нет ли конфликтов auth логики? Установлен ли JWT токен?
 • Проверка наличия страниц: /set-jwt-token.html, /check-preview-auth.html

📍 Вывод: Preview работает / не работает. Production готов / есть риски.

⸻

✅ Финальный вывод

Создай итоговый файл:

/audit/final-audit-YYYY-MM-DD.md

В нём укажи:

1. Общие замечания

2. Найденные проблемы по категориям

3. Потенциальные уязвимости

4. Рекомендации (по приоритетам)

5. Оценка готовности к продакшену (в % и по категориям)

6.


Риски запуска без правок

⸻

📌 Важно:
 • ❌ Никакие правки в коде не делать
 • ✅ Только анализ, исследование и документирование
 • 💬 Каждый блок должен содержать пояснения, примеры и строки кода (если найден баг)
 • 🗂 Все результаты должны быть сохранены в /audit/final-audit-YYYY-MM-DD.md

⸻

Когда агент завершит аудит, мы сделаем финальный обзор и примем решение о продакшене.