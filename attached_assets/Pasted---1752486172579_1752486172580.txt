Вот чётко сформулированное **техническое задание** для агента — задача по **глубокому исследованию административного сектора** и **Telegram-бота**, который выполняет функции админ-панели:

---

## 🧾 ТЕХНИЧЕСКОЕ ЗАДАНИЕ: Исследование административной панели через Telegram-бота

### 🎯 Цель:

Провести **глубокий аудит административного функционала**, включая Telegram-бот, через который осуществляется управление системой и контроль операций. Проверить, что **всё подключено, отображается и функционирует корректно**.

---

### 📋 ЗАДАЧИ И ОБЯЗАТЕЛЬНЫЕ ПУНКТЫ ПРОВЕРКИ:

#### 1. 📲 **Подключение Telegram-бота к системе**

* Убедиться, что бот подключен к backend и маршрутам админки.
* Проверить `telegramBot.ts`, `adminBot.ts` или аналогичные файлы.
* Проверить, какой **Telegram ID** используется для определения админов (`isAdmin(user.id)` и др.).

#### 2. 📌 **Команды администратора**

* Пройтись по всем доступным командам админ-бота:

  * `/start`
  * `/stats`
  * `/users`
  * `/balance`
  * `/withdrawals`
  * `/missions`
  * `/moderate`
  * и другие
* Проверить, что:

  * команды обрабатываются;
  * нет ошибок в логике или некорректного отображения;
  * ответы содержат актуальную информацию из базы данных.

#### 3. 💰 **Фиксация выплат через админ-панель**

* Убедиться, что бот отображает:

  * все **ожидающие выплаты**
  * статусы выплат (pending / done / failed)
  * детали по каждой выплате: сумма, адрес, пользователь
* Проверить, что при подтверждении выплаты (если предусмотрено) происходит правильный переход статуса в базе.

#### 4. 🔗 **Маршруты и подключение к backend**

* Проверить, какие **маршруты API** вызываются ботом (например, `/api/admin/withdrawals`, `/api/admin/users`, `/api/admin/moderate`)
* Убедиться, что запросы корректно формируются и данные приходят в ответ.

#### 5. 📊 **Информационные команды и отчёты**

* Проверить команды, которые:

  * показывают общее число пользователей
  * показывают статистику депозитов / фарминга
  * предоставляют системные логи (если есть)

#### 6. 📋 **Форматы и валидация данных**

* Убедиться, что бот:

  * правильно обрабатывает типы данных (TON, UNI, адреса)
  * не допускает пустых, ошибочных или кривых значений

#### 7. 🧩 **Согласованность с остальной системой**

* Проверить, что все действия, произведённые через бот:

  * фиксируются в базе
  * отражаются в административной панели (если есть Web-интерфейс)
  * не конфликтуют с другими действиями (например, выплаты через Web и через бот)

---

### ⛔️ ЗАПРЕЩЕНО:

* Вносить изменения в код
* Менять команды, токены или Telegram-бота
* Модифицировать логику базы данных

---

### ✅ ФОРМАТ ОТЧЕТА:

В отчёте агент должен отразить:

* Список всех доступных команд
* Какие команды работают корректно
* Какие команды не работают (с указанием причины)
* Подключение к API и таблицам БД
* Подключен ли бот к маршрутам выплат и статистики
* Есть ли ошибки, пробелы или отсутствующие данные
* Вывод о **готовности Telegram-бота как админ-панели к эксплуатации**

---

💡 **Примечание:** это ТЗ является **инструктивным прототипом**, агент должен сверяться с **реальными файлами проекта**, чтобы подтвердить или опровергнуть соответствие.

Готов оформить и шаблон для отчёта, если нужно.
