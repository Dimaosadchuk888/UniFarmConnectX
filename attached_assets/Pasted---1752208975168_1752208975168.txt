Вот чётко оформленное **техническое задание для агента** на **регрессионную проверку** после выполнения **Фазы 1** из плана оптимизаций:

---

## ✅ ТЕХНИЧЕСКОЕ ЗАДАНИЕ

**Название:** Полная регрессионная проверка после реализации Фазы 1 оптимизации UniFarm
**Исполнитель:** Агент
**Дата начала:** *(указать)*
**Цель:** Убедиться, что после внесённых изменений в рамках Фазы 1 (BalanceManager, Referral Rewards, Transaction Types) вся система UniFarm работает корректно и **без ошибок**, на всех уровнях.

---

### 📦 ЧТО УЖЕ ИЗМЕНЕНО (Фаза 1):

1. **Централизация управления балансами**

   * Весь функционал по работе с балансом переведён на `BalanceManager`
   * Удалены прямые SQL-операции с `supabase.update()` и `insert()` в модулях

2. **Исправление логики реферальных наград**

   * Referral-награды больше **не начисляются от покупки Boost**
   * Теперь начисление происходит **только от дохода farming/boost**

3. **Стандартизация типов транзакций**

   * Введены новые значения в `TransactionType`: `FARMING_DEPOSIT`, `BOOST_PURCHASE`, `BOOST_INCOME` и т.д.
   * Обновлены все места, где создаются транзакции

---

### 🔍 ЗАДАЧА: РЕГРЕССИОННАЯ ПРОВЕРКА

Тебе необходимо пройтись по **всей логике приложения**, которая была затронута при реализации Фазы 1, и **подтвердить, что всё работает как раньше или лучше**. Ниже — подробный чек-лист:

---

### ✅ ЧЕК-ЛИСТ ДЛЯ РЕГРЕССИОННОЙ ПРОВЕРКИ

#### 🔹 A. **Farming (UNI)**

* [ ] Депозит в UNI farming проходит успешно
* [ ] Баланс списывается через `BalanceManager`, сумма отображается корректно
* [ ] Начисление ежедневного дохода работает, баланс обновляется
* [ ] Транзакции создаются с типами `FARMING_DEPOSIT`, `FARMING_REWARD`
* [ ] При доходе от фарминга — **рефералу начисляется награда**
* [ ] Никаких ошибок в логах и интерфейсе

#### 🔹 B. **Boost (TON farming)**

* [ ] Boost можно купить с внутреннего кошелька
* [ ] После покупки — **не начисляется реферальная награда**
* [ ] Доход от Boost начисляется корректно, создается транзакция `BOOST_INCOME`
* [ ] При доходе от Boost — реферальная награда начисляется правильно
* [ ] Баланс в UNI и TON отображается верно после начислений

#### 🔹 C. **Referrals**

* [ ] При наличии реферальной структуры (уровни 1–3) все начисления работают
* [ ] Отсутствуют дубликаты, неправильные суммы, баги с rounding
* [ ] Проверить `modules/referral/rewardDistribution.ts` — все уровни проходят расчёт

#### 🔹 D. **История транзакций**

* [ ] Все транзакции создаются с **корректным типом (`TransactionType`)**
* [ ] Отображаются в истории на клиенте
* [ ] Корректно фильтруются по типу
* [ ] Нет “UNKNOWN” или пустых типов

#### 🔹 E. **Баланс**

* [ ] Все действия с балансом (депозит, фарминг, бонус, boost) проходят только через `BalanceManager`
* [ ] Значения баланса отображаются корректно
* [ ] Нет дублирующихся начислений
* [ ] Баланс никогда не уходит в минус
* [ ] Атомарность проверена при транзакциях

---

### ⚠️ ДОПОЛНИТЕЛЬНЫЕ ТРЕБОВАНИЯ

* [ ] Все тесты должны быть **проверены вручную на staging** или dev-среде
* [ ] Пройтись по всем связанным таблицам в Supabase и проверить данные вручную
* [ ] Протестировать как минимум **3 разных пользователя** (без реферала, с 1 уровнем, с 3 уровнями)
* [ ] Все баги, найденные во время регрессии, **немедленно задокументировать и не продолжать интеграции, пока не решены**

---

### 📎 ФОРМАТ ОТЧЁТА

Предоставить подробный **отчёт по каждому блоку** выше, с пометкой:

* ✅ Пройдено, ошибок не найдено
* ❌ Ошибка найдена: (описание)
* ⚠️ Нужна проверка: (описание, при необходимости — гипотеза)

---

Готов прислать шаблон репорта или создать Google Sheet для отслеживания — дать?
