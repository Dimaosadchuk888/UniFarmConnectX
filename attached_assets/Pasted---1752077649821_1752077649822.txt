Отлично! Ты очень грамотно подводишь команду к архитектурной чистке. На основе твоего последнего сообщения я могу оформить короткое, но точное **ТЗ для команды разработчиков / архитекторов**, чтобы они устранили **асинхронность между логикой и базой данных**.

Вот краткий формат такого ТЗ:

---

# 🧩 ТЗ: Синхронизация логики UniFarm с базой данных Supabase

**Источник:** `UNIFARM_SYNCHRONIZATION_AUDIT_REPORT.md`
**Цель:** Привести структуру БД в полное соответствие с используемой бизнес-логикой

---

## ✅ Синхронизированные модули (✅ ничего делать не нужно):

* `UNI Farming`: поля `uni_deposit_amount`, `uni_farming_rate`, `uni_farming_active` работают корректно
* `Referrals`: структура `deepReferral.ts` соответствует данным в БД
* `User Balance`: поле `balance_uni` синхронизировано

---

## ⚠️ Проблемные участки — требуется доработка:

### 1. **Тип транзакции `FARMING_DEPOSIT`**

* ❌ **НЕ существует в БД**
* 💡 Добавить в справочник типов транзакций
* 📌 Использовать для списания UNI при входе в фарминг

### 2. **TON Boost**

* ❌ Все boost-пакеты **захардкожены в коде**
* ❌ Таблица `boost_packages` отсутствует
* 💡 Создать таблицу с полями:

  * `id`, `ton_amount`, `yield_rate`, `duration_days`, `title`, `bonus_uni`

### 3. **TON Wallet**

* ❌ Нет полей для хранения TON-адресов
* 💡 Добавить в таблицу `users` поля:

  * `ton_wallet_address`, `ton_wallet_status`, `ton_linked_at`

### 4. **`farming_sessions` и `boost_purchases`**

* ⚠️ Используются в логике начислений, но:

  * ❓ Не подтверждено существование таблиц в Supabase
* 💡 Проверить наличие, при отсутствии — создать

---

## 📊 Итог:

* **Общий уровень синхронизации:** 75%
* **Критический минимум для исправления:**

  * [ ] Добавить тип транзакции `FARMING_DEPOSIT`
  * [ ] Обновить структуру БД для поддержки TON
  * [ ] Создать таблицу `boost_packages`
  * [ ] Убедиться, что `farming_sessions` и `boost_purchases` действительно есть и используются

---

Хочешь, чтобы я на основе этого сделал `SQL DDL` (команды создания недостающих таблиц/полей)? Или оформить это в виде Markdown-файла `UNIFARM_DB_FIX_TODO.md`?
