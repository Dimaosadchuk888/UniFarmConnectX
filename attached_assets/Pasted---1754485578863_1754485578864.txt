Вот точный и официальный промт для агента под задачу аудита:

---

📌 **ЗАДАЧА: ПОЛНЫЙ АУДИТ СИСТЕМЫ И БАЗЫ ДАННЫХ**
(🔒 без внесения изменений в код или данные)

🎯 Цель:
Провести **детальную проверку всех полей и таблиц базы данных**, которые подключены к приложению, и подтвердить:

1. Что каждое поле действительно **используется системой**,
2. Что **все поля синхронизированы с приложением**,
3. Что **нет нарушений** в структуре — например, устаревших или неиспользуемых связей,
4. Что **у каждого аккаунта** данные подтягиваются и отображаются корректно,
5. Что **не существует аккаунтов или записей**, которые подключены “не туда” или работают с рассылающимися данными.

---

🔍 Что нужно сделать:

1. **Перечислить все поля и таблицы**, которые **реально используются в приложении**,
   — Без теоретических выводов, без предположений из кода.
   — Только **факт подключения к рабочей системе**.

2. По каждому полю:

   * Указать, к какому **разделу приложения** оно подключено (фарминг, бусты, кошелёк, рефералы и т.д.).
   * Какую **функцию** оно выполняет.
   * Как часто и через какие механизмы обновляется (если применимо).
   * Проверить, что нет "битых", пустых или устаревших связей.

3. Провести **диагностику синхронизации**:

   * Проверить, что все данные для каждого пользователя **корректно синхронизированы**.
   * Убедиться, что в базе нет **висящих, рассинхронизированных или дублирующихся записей**,
     которые больше **не обрабатываются системой**, но продолжают существовать.

4. Проверить наличие **устаревших или незадействованных полей**,
   которые больше не используются в бизнес-логике, но всё ещё присутствуют в структуре.

---

🛑 **Ничего в коде и данных не менять.**
Только ручная или инструментальная проверка и полный фактологический отчёт.

📄 **Формат ответа:**

* Таблица или список:

  * `Имя поля`
  * `Таблица`
  * `Описание`
  * `Привязка к разделу`
  * `Комментарий по синхронизации`

* В конце:

  * Список всех **выявленных нарушений/рассинхронизаций** (если есть)
  * Подтверждение, что **вся структура БД соответствует активной системе** — либо указание, где есть отклонения.

Жду полный отчёт по результатам аудита.
