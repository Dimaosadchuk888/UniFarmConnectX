🛠️ ЗАДАЧА ДЛЯ АГЕНТА: ПРОВЕРКА КОРРЕКТНОСТИ ОТОБРАЖЕНИЯ TON-ДЕПОЗИТОВ

⸻

🧩 Проблема

После успешного пополнения TON-кошелька:
 • Транзакция отображается в истории
 • Хэш-транзакции указан корректно
 • НО:
 • Баланс TON не увеличивается
 • Тип транзакции в истории указан как UNI_FARMING, а не TON_DEPOSIT
 • Визуально транзакция отображается с некорректным тегом/категорией

⸻

🔍 Цель текущего этапа

Провести полную диагностику системы, чтобы выявить, почему:
 1. Не обновляется balance_ton после подтверждённой TON транзакции
 2. Созданная транзакция в системе имеет неверный тип (UNI_FARMING)
 3. Возможно, вызывается не тот метод или передаётся неверный tx_type

⸻

✅ Что нужно проверить
 1. Внутри processTonDeposit() и связанных методов:
 • Правильно ли вызывается balanceManager.addBalance(...)
 • Есть ли нужная проверка успешной TON-транзакции (по BOC/hаsh)
 • Какой тип транзакции передаётся в UnifiedTransactionService.create(...)
 2. Внутри UnifiedTransactionService:
 • Определение type: 'TON_DEPOSIT' — используется ли корректно
 • Возможно ли, что стоит дефолтный тип UNI_FARMING в момент создания
 3. Сторона фронтенда:
 • Отправляется ли верный тип при POST /api/v2/wallet/ton-deposit
 • Корректно ли передаётся amount, wallet_address, ton_tx_hash и другие параметры
 4. База данных:
 • В таблице транзакций — записан ли type корректно
 • Обновляется ли balance_ton при подтверждении депозита

⸻

🛑 Строгие ограничения
 • Запрещено вносить любые изменения в код (ни backend, ни frontend), пока не будет:
 • Полной диагностики
 • Подробного отчёта по найденной причине
 • Получено разрешение на конкретные изменения
 • Особенно важно: не производить “на глаз” замену tx_type или вызов addBalance, пока не станет понятно, на каком уровне нарушена логика.

⸻

📎 Примерная полезная информация

Транзакция, которая иллюстрирует проблему:

🌾 UNI Farming
20.07.2025, 08:56
TON deposit from blockchain: 00a1ba3c2614f4d65cc346805feea960


⸻

🎯 Цель:

Подготовить подробный технический отчёт с перечислением:
 • Где и почему ошибка возникает
 • Какую строку или логику нужно будет изменить (с указанием места)
 • Как избежать повторения этой ошибки в будущем