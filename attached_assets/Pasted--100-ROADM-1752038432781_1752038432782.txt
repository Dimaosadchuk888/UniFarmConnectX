Вот точный и однозначный **промпт для агента**, чтобы он **привёл систему к 100% соответствию `ROADMAP.md`**, с учётом всех важных условий: без дубликатов, с проверкой наличия и состояния файлов перед любыми действиями:

---

📌 **Промпт для агента: Привести систему к 100% соответствию с ROADMAP.md**

---

🎯 **Цель:** Привести всю систему UniFarm к **полной (100%) синхронизации с `ROADMAP.md`**, на основе отчёта от 09.07.2025 и выявленных критических пробелов.

---

### 🔍 Основные правила перед любыми действиями:

1. **Перед созданием любого файла, маршрута, метода или схемы:**

   * Проверь, существует ли уже **соответствующий файл или модуль** в проекте.
   * Если файл **есть** — **проведи аудит его текущего состояния** и **расширяй/дорабатывай его**, не создавая дубликатов.
   * Если файла **нет** — создавай **только один, корректно структурированный** файл на своё место в модуле.

2. **Никаких дубликатов, конфликтующих реализаций или побочных эффектов.**
   Чистота кода, единообразие, интеграция с существующей архитектурой — приоритет.

---

### 🔧 Что нужно реализовать:

#### ✅ Приоритет 1 (Критический):

* **Wallet Module (40%)**:

  * `POST /api/v2/wallet/deposit`
  * `GET /api/v2/wallet/transactions`
  * `POST /api/v2/wallet/transfer`
    🔎 Убедись, что `createDeposit()` и `depositSchema` уже есть, но **роут не подключен** — **подключи** аккуратно в `wallet/routes.ts`

* **User Management (56%)**:

  * `PUT /api/v2/user/profile`
  * `GET /api/v2/user/stats`
  * `GET /api/v2/user/search/:query`
  * `POST /api/v2/user/update-settings`
    🔎 Убедись, есть ли базовые заготовки (контроллер, схема) — **доработай**, не дублируй.

* **Authentication (71%)**:

  * `GET /api/v2/auth/session`
    🔎 Проверь, реализована ли логика в `AuthController` и `AuthService`, но **отсутствует endpoint** — добавь корректно.

#### ✅ Приоритет 2 (Важный):

* **UNI Farming (71%)**:

  * `GET /api/v2/farming/rates`
    🔎 Создай или подключи `rates` в `FarmingController` — если логика ставок реализована, просто экспонируй.

* **Telegram Integration (33%)**:

  * `GET /api/v2/telegram/webapp-data`
  * `POST /api/v2/telegram/set-commands`
    🔎 Убедись, что методы не дублируются в `telegram/routes.ts` — добавь аккуратно.

#### ✅ Приоритет 3 (Финальный этап):

* Обнови `ROADMAP.md` с актуальным статусом:

  * Перечисли все endpoint'ы
  * Пометь завершённые модули
  * Удали устаревшие записи

* Убедись, что нет **расхождений между кодом и ROADMAP.md**

---

### 📋 Результат:

* Все endpoint'ы из `ROADMAP.md` реализованы и подключены
* Нет дубликатов файлов и маршрутов
* Чистый, поддерживаемый код
* Обновлённый `ROADMAP.md` с 100% актуальной информацией
* Финальный статус соответствия: ✅ **100% синхронизация**

---

Если хочешь, могу также выдать шаблон финального аудита или контрольный чеклист для верификации соответствия.
