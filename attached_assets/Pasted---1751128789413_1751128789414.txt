🔬 Задача: Комплексное исследование всей системы перед продакшеном

Цель — проверить текущее состояние проекта по всем ключевым направлениям, подтвердить отсутствие багов, дубликатов, обходных решений и убедиться в 100% готовности к продакшену. Исправления пока не вносить, только диагностика и отчёт.

---

📌 Основные направления исследования:

1. Дублирование и обходные пути
- Есть ли дублирующие файлы (например, UserServiceV2.ts, ReferralCodeDisplay и т.п.)?
- Используются ли временные или обходные версии компонентов/сервисов вместо основных?
- Есть ли подключённый неиспользуемый, конфликтующий или устаревший код?

2. Архитектурная целостность
- Используются ли только основные, оригинальные компоненты и сервисы?
- Все ли зависимости и подключения построены на стабильной основной структуре?
- Есть ли раздвоение логики (например, конфликт версий V1 и V2)?

3. Работа реферальной системы
- Возвращается ли корректный ref_code из API?
- Отображается ли ref_code в интерфейсе (раздел “Друзья”) через основной pipeline?
- Обрабатывается ли ref_code в UserContext согласно спецификации (ref_code и refCode)?
- Правильно ли ref_code сохраняется и извлекается из localStorage?
- Работает ли копирование реферальной ссылки?
- Как интерфейс ведёт себя при отсутствии ref_code?

4. Миссии, farming, бонусы
- Совпадает ли количество активных миссий в интерфейсе с данными API?
- Активны ли и корректно ли отображаются функции бонусов и farming?

5. Кэш и устаревший код
- Загружается ли устаревший JS-код на фронтенде?
- Есть ли признаки, что браузер кэширует удалённые или старые скрипты?
- Корректно ли настроены заголовки Cache-Control, no-store, ETag и прочие механизмы?
- Используется ли Service Worker, localStorage или иные механизмы, где может “залипать” старый код?
- Есть ли неиспользуемые зависимости или старые версии библиотек?

6. Полное исследование всей системы (главное дополнение)
- Проверь абсолютно все модули и компоненты проекта без исключения.
- Пройдись по всей бизнес-логике: все функции, методы, сервисы, утилиты.
- Проверь каждый API endpoint по отдельности — корректность запросов, ответов, ошибок, обработки.
- Исследуй UX на всех страницах и сценариях, включая крайние и нестандартные кейсы.
- Проанализируй все системные и конфигурационные файлы — настройки сборки, деплоя, кеширования, окружения.
- Никаких пропусков — каждый файл, каждая строчка кода должна быть рассмотрена и проверена.

---

📋 Формат ответа:

✔ Компонент/модуль/API: [что работает правильно]  
✖ Компонент/модуль/API: [что не соответствует] → [описание проблемы и где искать]  
❓ Подозрения: [возможные, но не подтверждённые проблемы]

---

🔍 Дополнительные требования:

- Убедись, что никаких дубликатов файлов (UserServiceV2.ts и подобных) нет, либо они удалены, либо заменены оригинальными.  
- Запрещено использование обходных версий компонентов/сервисов, если есть основная версия.  
- Проверить, что реферальная ссылка отображается через основной pipeline, временные компоненты (ReferralCodeDisplay и т.п.) удалены.  
- Очистить все следы устаревшего кода и кеша:  
  • Версионирование файлов (query-параметры, например ?v=2) для обновления кеша браузера  
  • Проверить и очистить серверный кеш (Cache-Control, no-store и пр.)  
  • Проверить localStorage, Service Worker и прочие механизмы кеширования  
- Проверить отсутствие любых временных костылей и обходных решений — только чистый и стабильный код.  
- Проверить исправление ошибки “Data validation failed after type correction” на корневом уровне, а не подавлением.

---

🎯 Итог:  
Дать чёткий, краткий отчёт по каждому пункту — что работает и что нет, с объяснениями и рекомендациями. Исправлять пока не нужно, только диагностика и выводы.

---

Спасибо!