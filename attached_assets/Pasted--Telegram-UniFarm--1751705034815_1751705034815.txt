# ✅ Техническое задание: Полный аудит Telegram-бота UniFarm

## 📌 Цель:
Провести глубокую проверку админ-панели бота UniFarm, определить:
- какие функции доступны,
- работают ли выплаты,
- обрабатываются ли запросы на выплату,
- правильно ли срабатывают подтверждения/отклонения,
- как взаимодействие происходит с базой данных и бекендом.

---

## 📁 Что требуется сделать:

### 1. 🔍 Исследовать файл Telegram-бота
Найти основной файл, содержащий реализацию Telegram-бота (`bot.ts`, telegramBot.ts, bot/index.ts и пр.)

Для этого:
- Определить используемую библиотеку (например, node-telegram-bot-api, telegraf и т.д.)
- Определить, где и как бот запускается (init-функция)

---

### 2. 🛠️ Проверить админ-панель:
Исследовать, какие админ-команды реализованы в боте. Например:
- /admin или /panel
- /payouts — список заявок на выплаты
- /confirm <id> — подтверждение выплаты
- /reject <id> — отклонение заявки
- /broadcast — рассылка

Для каждой команды указать:
- Есть ли она в коде
- Как обрабатывается (в какой функции)
- С какими таблицами в базе работает
- Требует ли авторизации по admin_user_ids
- Работает ли сейчас (не закомментирована ли)
- Используются ли webhook или polling

---

### 3. 💸 Проверить модуль выплат:
- Где происходит логика обработки выплат?
- Какие данные записываются в таблицу выплат (например, `payout_requests`)?
- Как бот получает информацию о новых выплатах?
- Как происходит подтверждение выплаты (что именно обновляется в БД)?
- Есть ли связь с внешними кошельками (или это ручное подтверждение)?
- Какие статусы существуют (`pending`, approved, `rejected`)?

---

### 4. 📬 Уведомления пользователям
- Получает ли пользователь уведомление о подтвержденной/отклоненной выплате?
- Если да — где реализована эта отправка? Через какую команду или коллбэк?

---

### 5. 🔐 Авторизация админов
- Где хранится список администраторов? (`admin_user_ids`)
- Есть ли защита админ-команд от обычных пользователей?
- Есть ли логирование действий админа?

---

### 6. 🧪 Тестовый режим
- Есть ли в коде переменная для "тестового режима" (например, TEST_MODE, `IS_DEMO`)?
- Если есть — влияет ли она на работу выплат?

---

## 🔍 Ожидаемый формат отчета:
Таблица или структурированный список, в котором по каждой функции будет указано:

| Команда / Функция | Описание действия | Статус (работает / нет) | Файл | Рекомендации |
|-------------------|--------------------|--------------------------|------|---------------|

---

## ❗Важно:
- 🔒 Ничего не изменять в коде
- Только исследование и выводы
- Если какие-то модули недоступны или закомментированы — отразить это в отчете

---

## 🎯 Цель:
Убедиться, что админ-панель работает стабильно, выплаты фиксируются и реализованы корректно, а система защищена от злоупотреблений.