✅ Промт для агента: Финальное исследование после переноса секретов (без вмешательства в код)

⸻

🧪 Цель:
Провести полную проверку системы на предмет корректности подключения всех переменных из Replit Secrets и отсутствия побочных нарушений после удаления хардкод значений.

⸻

📋 Что конкретно нужно проверить:

🔐 1. Проверка переменных окружения (Replit Secrets)
 • Убедиться, что все требуемые переменные действительно подключены в среде:
 • SUPABASE_URL
 • SUPABASE_KEY
 • TELEGRAM_BOT_TOKEN
 • ADMIN_BOT_TOKEN
 • JWT_SECRET
 • APP_DOMAIN
 • TELEGRAM_WEBAPP_URL
 • TON_BOOST_RECEIVER_ADDRESS
 • VITE_TON_BOOST_RECEIVER_ADDRESS
 • VITE_APP_URL
 • Вывести результат: какая переменная найдена, какая отсутствует.
 • Проверить, нет ли опечаток в названиях (например, APP_DOMAN вместо APP_DOMAIN)

⸻

⚙️ 2. Проверка работоспособности всей системы (без redeploy)
 • Проверить: поднимается ли проект в Replit без ошибок (если переменные есть)
 • Пройтись по всем модулям:
 • 🟣 AdminBot — успешно ли подключается к Telegram?
 • 💳 TON Boost Module — инициализируется ли адрес получателя?
 • 🧾 Farming / Wallet / Transactions — читается ли база из Supabase (если SUPABASE подключен)?
 • Проверить, не выпадает ли ошибка в логах Replit, связанных с process.env.

⸻

🧩 3. Проверка, не поврежден ли UI/Frontend
 • Открывается ли WebApp корректно (по адресу TELEGRAM_WEBAPP_URL)?
 • Не пропал ли какой-либо компонент на UI?
 • Балансы, транзакции, формы — всё ли доступно?
 • Провести имитацию обычного запуска и проверку кнопок (не кликая, просто анализ DOM/рендер)

⸻

📁 4. Проверка .replit и Replit окружения
 • Подтвердить, что .replit больше не содержит SUPABASE_KEY и SUPABASE_URL
 • Если содержит — агент должен сообщить, в каком именно блоке (строке) они остались

⸻

🟡 Важно:
 • ⚠️ Не вмешиваться в код
 • ⚠️ Ничего не удалять вручную
 • ⚠️ Цель — только провести исследование текущего состояния

⸻

📌 В формате отчета:
 • ✅ Какие переменные окружения установлены, какие отсутствуют
 • ✅ Какие модули загружаются корректно, какие нет
 • ✅ Присутствует ли что-то в .replit, что может быть уязвимостью
 • ✅ Общая оценка — можно ли безопасно делать Redeploy