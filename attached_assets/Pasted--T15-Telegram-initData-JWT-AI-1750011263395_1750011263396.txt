🟥 ПРОМТ: T15 — Аудит и восстановление авторизации через Telegram (initData + JWT)

📌 РОЛЬ:
Ты — AI-разработчик проекта UniFarm. Задача: провести ПОЛНЫЙ АУДИТ Telegram авторизации, чтобы выяснить:
• Почему initData пустой
• Почему JWT не выдается
• Почему API возвращает 401 Unauthorized
❗ Ничего не создавать и не исправлять до завершения полного анализа!

🔒 ПРАВИЛА:
1. ❌ Не создавать новых функций, пока не будет полностью выявлена причина ошибки
2. 🔍 Проверка только модуля авторизации (auth), initData, JWT, UserContext
3. ⚠️ Не менять переменные окружения и не править client/index.html

✅ ОБЯЗАТЕЛЬНО ПРОВЕРИТЬ:
• client/src/main.tsx — передаётся ли initData от Telegram
• client/src/contexts/UserContext.tsx — корректно ли обрабатывается initData
• core/middleware/telegramMiddleware.ts — парсинг initData
• modules/auth/controller.ts — логика входа через Telegram
• modules/auth/service.ts или authService.ts — формируется ли JWT
• core/supabase.ts — создаётся ли пользователь через Supabase
• server/index.ts и routes.ts — есть ли POST /auth/telegram
• client/src/services/authService.ts — вызывается ли запрос к API

📋 СТРУКТУРА ОТЧЁТА:
# АУДИТ АВТОРИЗАЦИИ TELEGRAM

🧩 INITDATA
- [x] client/src/main.tsx — window.Telegram.WebApp.initData: undefined
- [ ] Причина: приложение открыто не через Telegram Mini App
- [x] middleware работает, но ничего не получает

🔑 JWT
- [ ] authService.ts — JWT не генерируется
- [ ] user не создаётся в Supabase
- [x] API возвращает 401 Unauthorized

📂 КЛЮЧЕВЫЕ ПРОБЛЕМЫ:
1. initData не передаётся из Telegram → приложение запущено вне Mini App?
2. JWT не создаётся → пользователь не найден или не создаётся
3. UserContext не обрабатывает initData → login flow не работает

✅ ПРЕДЛОЖЕНИЯ (НЕ ВЫПОЛНЯТЬ):
1. Проверить, открыт ли проект через Telegram Mini App
2. Настроить правильный WebAppURL в BotFather
3. Убедиться, что initData передаётся и создаёт пользователя

📊 ВЫВОД:
Авторизация не работает из-за отсутствия initData, отсутствия логики генерации JWT и некорректной цепочки создания пользователя через Supabase. Требуется вмешательство только после полной диагностики и подтверждения.