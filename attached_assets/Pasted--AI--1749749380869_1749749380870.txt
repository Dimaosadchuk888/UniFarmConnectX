Ты — профессиональный AI-разработчик. Твоя задача — провести Тотальную пред-продакшн проверку проекта UniFarm Connect. Ты не вносишь никаких изменений в код. Только читаешь, анализируешь и составляешь отчёт по всем системным аспектам.

---

## ❗ ПЕРЕД НАЧАЛОМ:
- Работай в режиме Read-Only: ничего не правь, не удаляй и не комментируй.
- Цель — найти возможные ошибки, риски и уязвимости до выхода в продакшн.

---

## 📋 ЧТО ПРОВЕРИТЬ (ТОТАЛЬНЫЙ СПИСОК):

### 1. 🔐 Авторизация и безопасность
- Присутствие auth middleware на всех защищённых маршрутах
- Проверка, что Telegram initData валидируется (если используется)
- Проверка конфигурации JWT (длина, секрет, срок действия)

### 2. 🌐 API и маршруты
- Совпадение маршрутов клиента и сервера (`client/api` ↔ `server/routes`)
- Проверка, нет ли дублирующих маршрутов (например, /api/users и `/api/v2/users`)
- Актуальность всех endpoint’ов (`GET`, POST, PATCH, `DELETE`)

### 3. 🗃️ Работа с базой данных
- Есть ли SQL-инъекции (прямые запросы без параметров)
- Используются ли правильные связи в моделях (foreign keys, constraints)
- Проверка на лишние/неиспользуемые таблицы или поля

### 4. 🧩 Модули и структура
- Проверка, что все модули /modules/ действительно используются
- Нет ли дубликатов (например, `/UniFarmConnect/modules/*`)
- Все контроллеры подключены и обрабатываются

### 5. 📦 .env и секреты
- Проверка, что в репозитории нет .env или .env.local
- Секреты вынесены в переменные окружения, нет прямых ключей в коде

### 6. ⚙️ Настройки продакшна
- NODE_ENV установлен корректно
- PORT, DATABASE_URL, JWT_SECRET, API_KEYS заданы
- Присутствуют healthcheck (`/health`, `/api/v2/health`)

### 7. 📊 Логирование и мониторинг
- Используется structured логгер (например, `pino`)
- Нет console.log в продакшн-коде
- Ошибки логируются корректно (с кодом, сообщением, trace)

### 8. 📦 Frontend
- Проверка всех API-запросов в client/services на актуальность
- Совпадают ли названия и методы с backend
- Присутствует ли fallback на случай ошибок API (loading/error state)

### 9. 🚦 CI/CD и деплой
- Настроен ли автоматический деплой (например, на Railway/Vercel)
- Есть ли .github/workflows или vercel.json, railway.json
- Проверка структуры Dockerfile и docker-compose (если есть)

### 10. 📃 Документация
- Наличие README.md, env.example, routes.md или аналогов
- Описание всех переменных и ключевых модулей
- Легко ли новичку понять структуру проекта?

---

## 📄 ОТЧЁТ О РЕЗУЛЬТАТАХ:

В конце напиши:

- ✅ Что соответствует ожиданиям (всё чисто и готово)
- ⚠️ Что вызывает подозрения (не критично, но желательно улучшить)
- 🟥 Что нужно обязательно исправить перед продакшном
- 🔒 Уровень риска по каждому из 10 пунктов (низкий, средний, высокий)

---

## 🎯 ЦЕЛЬ:
Провести максимально полную проверку всего проекта перед деплоем. Убедиться, что система готова к запуску, не содержит уязвимостей, технического долга и неиспользуемых компонентов.