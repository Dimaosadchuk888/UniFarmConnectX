⸻

🧩 ТЕХНИЧЕСКОЕ ЗАДАНИЕ — Т22: Очистка дублирующих сервисов в модулях referral и dailyBonus

📌 Роль:

Ты — агент AI, который выполняет профессиональную доработку проекта UniFarm строго по выявленным ошибкам. Все действия должны быть изолированы в рамках одной задачи и не выходить за пределы описанного задания.

⸻

🛑 Жёсткие ограничения:
 1. Нельзя удалять, перемещать или изменять другие файлы, кроме referral и dailyBonus.
 2. Нельзя изобретать архитектуру заново или переписывать бизнес-логику.
 3. Нельзя переименовывать файлы без прямого указания.
 4. Нельзя удалять дублирующий файл, если он используется — сначала проверь подключения.
 5. Обязательная проверка всех import на использование service.ts и service-supabase.ts (или service_supabase.ts).

⸻

🧠 Контекст задачи:

В модулях referral и dailyBonus обнаружены дублирующие сервисы:
 • modules/referral/service.ts
 • modules/referral/service-supabase.ts ❗️возможный дубликат
 • modules/dailyBonus/service.ts
 • modules/dailyBonus/service_supabase.ts ❗️возможный дубликат

Необходимо:
 • Проверить, какой файл реально используется в контроллере и маршрутах.
 • Удалить неиспользуемые дубликаты.
 • Обновить импорты, если потребуется.
 • Согласовать архитектуру, чтобы в каждом модуле остался только один актуальный и используемый сервис.

⸻

✅ Ожидаемый результат:
 • Только один service.ts файл в каждом из модулей: referral и dailyBonus.
 • Удалены все неиспользуемые дубли (service-supabase.ts, service_supabase.ts).
 • Импорты приведены в порядок.
 • Все подключено корректно: controller.ts → service.ts
 • Система запускается без ошибок (ты не обязан запускать, но должен учитывать, чтобы не было import на несуществующие файлы).

⸻

📤 Формат отчета:
 1. Какие дубли были обнаружены?
 2. Какие из них реально используются?
 3. Что было удалено?
 4. Какие импорты были обновлены?
 5. Подтверждение, что осталось по одному сервису на модуль.

⸻

🎯 Цель:

Привести структуру модулей referral и dailyBonus к чистому, однозначному виду без дублирующих файлов. Устранить архитектурный конфликт и подготовить к продакшн.

⸻