Вот аккуратно и точно составленный **промпт для агента/разработчика**, который нужно передать для диагностики **проблемы с выводом средств через UniFarm**:

---

### 🔍 **Диагностика проблемы с выводом средств через UniFarm**

Проблема проявилась следующим образом:

1. **Я подал заявку на вывод средств через UniFarm.**
2. **Баланс списался, но не сразу — был небольшой лаг.**
3. **Транзакция вывода не появилась в списке транзакций.**
4. **Админ-бот, через который я должен подтвердить вывод, вообще не прислал никаких уведомлений.**
5. Это указывает на то, что, **вероятно, админ-бот подключён некорректно или не получает ивенты.**

---

### 🎯 Цель:

Нам нужно сейчас **точечно и без правок кода** провести **диагностику всех ключевых звеньев**, чтобы выявить, **на каком этапе** происходит сбой.

---

### ❗️Важно:

* **Никаких правок или фиксов без моего одобрения.**
* Сейчас только **диагностика**, вывод точек входа и **описание вариантов решения** с оценкой рисков.

---

### ✅ Что нужно сделать:

1. **Найти все файлы и модули**, которые участвуют в процессе вывода:

   * Подтверждение запроса на вывод.
   * Создание транзакции.
   * Списание баланса.
   * Отправка уведомления в админ-бот.
   * Проверка очереди вывода, если она есть.

2. **Прописать:**

   * Где и когда происходит создание транзакции.
   * Где логика отвечает за отправку сообщения в бот.
   * Где происходит обновление баланса.
   * Есть ли логика отложенного выполнения (например, CRON или worker).
   * Какое поведение системы при сбое на одном из этапов (есть ли откат, ручной фоллбек и т.д.).

3. **Проверить:**

   * Приходил ли вызов на отправку в админ-бот.
   * Была ли транзакция создана в БД.
   * Был ли вызван `wallet/withdraw` → `transactions.ts` → `withdrawals.ts`.
   * Какой статус у этой заявки в БД (pending/confirmed/failed?).
   * Работает ли WebSocket-сервис, через который приходит апдейт баланса/уведомления.

---

### 🔧 Технический контекст:

* Мы работаем на **продакшне**, данные пользователей реальные.
* Система должна быть стабильной — **любой спонтанный фикс может вызвать критические последствия**.
* Потенциально может быть проблема с кешем или расхождением между балансом и фактами в БД (это надо проверить).

---

### ⏱ Ожидаемый результат:

* Перечень всех точек в коде, участвующих в процессе вывода.
* Анализ: на каком этапе произошёл сбой.
* Возможные причины, без предположений, только с фактами.
* Предложение вариантов решения (например: перезапуск сервиса, чистка очереди, исправление конфигурации).
* После — согласование и только потом действие.

---

Если нужна дополнительная информация (ID пользователя, ID запроса на вывод) — запрошу дополнительно.

---

Хочешь, чтобы я сделал короткую версию или перевёл на английский — дай знать.
