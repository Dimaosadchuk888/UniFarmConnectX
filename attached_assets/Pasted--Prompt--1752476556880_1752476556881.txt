Вот готовый **финальный Prompt / Техническое задание для агента**, чтобы он выполнил проверку именно по твоему аккаунту на Replete Preview и подтвердил стабильность всей системы **TON Boost**:

---

# 📋 ФИНАЛЬНОЕ ТЕХНИЧЕСКОЕ ЗАДАНИЕ

### 🎯 Цель

Провести **финальную валидацию работоспособности TON Boost** на тестовом аккаунте, подключенном через **Replete Preview**, и подтвердить, что система полностью готова к приёму реальных пользователей и депозитов.

---

### 🔍 Что требуется сделать:

#### 1. Проанализировать кабинет пользователя в Replete Preview:

* Определить **общее количество депозитов**, открытых в этом кабинете
* Посчитать **общую сумму farming\_balance**
* Проверить: все ли депозиты корректно **суммируются**

#### 2. Проверить начисления:

* Посчитать **реальное начисление за 5 минут** (из последних транзакций типа `TON_BOOST_INCOME`)

  * Например: транзакции вида `+0.023611 TON` — найти 3–5 последних
* Вычислить фактический доход за **сутки** на основе формулы:

  * `суточный доход = farming_balance × farming_rate`
  * `доход за 5 минут = суточный доход ÷ 288`

Сравнить:

* ✅ Совпадает ли `начисление каждые 5 минут` с расчетной ставкой по формуле?
* ✅ Правильно ли установлена ставка (`farming_rate`) в базе?

#### 3. Проверить стабильность:

* Убедиться, что **farming\_balance не сбрасывается**
* Проверить, что **новые депозиты суммируются корректно**
* Убедиться, что **все транзакции фиксируются** и содержат нужные `metadata`
* Отсутствуют ли ошибки с `json`, `null`, `undefined`, сломанными транзакциями

---

### ✅ Ожидаемый результат

Ты должен подтвердить, что:

* ✅ Все депозиты отображаются и суммируются корректно
* ✅ Процентная ставка соответствует фактическим начислениям
* ✅ Начисления каждые 5 минут соответствуют ожидаемой логике
* ✅ Транзакции отображаются корректно
* ✅ Система **готова к приёму реальных участников**, без рисков потери баланса или сбоев

---

### ⛔️ Условия

* **Код НЕ менять**
* Проверка только через **анализ данных**, SQL, метаданных, транзакций
* Проверить **только кабинет**, подключенный через **Replete Preview**
* Если что-то не работает — **конкретно указать причину и в каком месте**

---

📌 *Вывод должен быть основан только на фактах и данных, а не на догадках. Ответ должен быть кратким, структурированным и окончательным.*

⏳ *Срок: как можно скорее, но с полной проверкой всех уровней.*

---

Готов отправить это агенту от твоего имени?
