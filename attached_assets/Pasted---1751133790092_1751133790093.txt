📦 Цель: Провести глубокий аудит *каждого файла проекта* — кода, логики, структуры, состояния системы. Важно удостовериться, что:
- Весь код работает корректно
- Все компоненты согласованы между собой
- Нет ошибок, утечек, дубликатов, мёртвого кода или неиспользуемых участков
- Система готова к финальной сборке и продакшену

⛔ Никаких изменений в коде вносить нельзя — только диагностика и подробный отчёт.

---

📌 Что требуется проанализировать:

### 1. 🔁 Каждый файл проекта
- Пройтись вручную по всем файлам проекта: frontend, backend, утилиты, конфигурации, хуки, сервисы, модули и вспомогательные структуры
- Проверить, что файл:
  - Используется системой
  - Работает без ошибок
  - Не дублируется (например, UserService.ts и `UserServiceV2.ts`)
  - Не содержит закомментированных устаревших блоков
  - Не является временным или тестовым (например, demo, mock, temp, Backup, `Copy`)
  - Не является частью устаревшей версии, которая могла быть забыта в репозитории

### 2. 🧩 Функциональная связность
- Все ли импорты корректны? Нет ли «мертвых импортов»?
- Все ли функции/методы/классы действительно вызываются?
- Нет ли «зависших» или неиспользуемых фрагментов логики?
- Все ли props, hooks, context и передача данных используются по назначению?

### 3. 🧪 Функциональный след
- Убедись, что каждый файл в проекте выполняет роль в общей бизнес-логике.
- Пройтись по каждому route/page/component и проследить:
  - Откуда вызывается?
  - Что делает?
  - Возвращает ли то, что должен?
  - Участвует ли в реальном пользовательском сценарии?

### 4. 🛠️ Системные и конфигурационные файлы
- .env, vite.config.js, package.json, .replit, .gitignore, tsconfig.json и т.п.
- Проверь:
  - Все ли переменные окружения актуальны?
  - Нет ли лишних зависимостей или dev-зависимостей?
  - Совпадают ли названия и пути с рабочими директориями?

### 5. 🧠 Глубинный анализ логики
- Есть ли файлы, где логика повторяется или дублируется с другими?
- Есть ли подозрения на устаревшую реализацию, не адаптированную под текущую архитектуру?
- Присутствуют ли куски кода, созданные в обход основной архитектурной модели (например, временные костыли)?
- Все ли именования и типы согласованы? Нет ли пропущенных типов или any в TypeScript?

---

📋 Формат ответа:

```text
✔ [Путь к файлу] — работает, используется, ошибок не найдено  
✖ [Путь к файлу] — ошибка / устаревший / дублируется → [описание проблемы]  
❓ [Путь к файлу] — возможно, не используется или не подключён → [что проверить дополнительно]