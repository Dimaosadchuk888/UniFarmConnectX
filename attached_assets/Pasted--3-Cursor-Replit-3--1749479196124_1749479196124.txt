Вот полное Техническое задание №3 — для Cursor або Replit агента.

⸻

🧩 ТЕХНИЧЕСКОЕ ЗАДАНИЕ №3

Тема: Устранение конфликтов зависимостей и корректная настройка package.json
Цель: Разделить frontend и backend зависимости, устранить конфликты версий и скриптов

⸻

🔍 Проблемы, которые нужно устранить:

Проблема Описание
🔴 Конфликт версий Один и тот же пакет (например, typescript, axios) используется с разными версиями в root и client/package.json
🔴 Смешанные зависимости Backend-зависимости (express, drizzle-orm, pg) находятся в client/package.json, хотя относятся к серверу
⚠️ Скрипты конфликтуют В корне проекта и в client разные dev, start, build команды, которые не согласованы между собой


⸻

📌 Инструкция для агента:

Ты — технический агент UniFarm. Твоя задача — устранить конфликты в зависимостях и навести порядок в package.json файлах.

1. Проверь 3 файла:
   - package.json в корне проекта
   - client/package.json
   - server/package.json (если есть)

2. Выполни следующие шаги:
   • Удали все frontend-зависимости (vite, react, react-dom, vite-plugin, и т.п.) из корневого package.json
   • Удали все backend-зависимости (express, drizzle-orm, pg и т.п.) из client/package.json
   • Все backend-зависимости должны быть только в server/package.json (если он есть, иначе — в корневом)

3. Проверь и выровняй версии общих пакетов (`typescript`, `dotenv`, eslint и др.), если они дублируются

4. Проверь скрипты:
   • В корне должны быть общие скрипты (например,ма: Устранение конфликтовесли нужно запустить client + server)
   • В client — только frontend скрипты (`vite`,АДАНИЕ №3 `preview`)
   • В server — только backend (`build`, start, `dev`)

5. Убедись, что все package-lock.json файлы удалены и пересозданы (или только один в корне, если используется workspaces)

‼️ Запрещено:
- Удалять функциональные пакеты, если они реально используются
- Перемещать зависимости без анализа их использования
- Менять структуру папок


⸻

✅ Ожидаемый результат:
 • client/package.json содержит только frontend-зависимости
 • server/package.json содержит только backend-зависимости
 • В корне нет дублирующихся или конфликтующих зависимостей
 • Скрипты согласованы между всеми частями
 • Отсутствуют устаревшие или неиспользуемые зависимости

⸻

🟢 После выполнения:
 • Приложи финальные версии package.json для каждой части проекта
 • Укажи, какие зависимости были удалены/перемещены
 • Убедись, что npm install работает в каждой части проекта без ошибок

После выполнения перейдём к ТЗ №4 — настройка переменных окружения и устранение хардкодов.