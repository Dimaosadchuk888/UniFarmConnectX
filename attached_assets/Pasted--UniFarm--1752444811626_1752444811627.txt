Отлично, вот структурированный **чек-лист по всем текущим проблемам** UniFarm, который можно передать на техническую проверку агенту. Он полностью заточен под **исследование без изменений кода**, с чёткими задачами, цепочками и приоритетами.

---

# ✅ ЧЕК-ЛИСТ ДЛЯ ИССЛЕДОВАНИЯ ПРОБЛЕМ UniFarm

**Цель:** Провести технический аудит всех ключевых проблем в начислениях и отображении, **без вмешательства в код**.

---

## 🔢 ПРОБЛЕМА №1: Не отображаются транзакции TON Boost

| Параметр             | Значение                                                                                            |
| -------------------- | --------------------------------------------------------------------------------------------------- |
| 🐞 Симптом           | Отображается **одна транзакция**, несмотря на наличие 10+ депозитов                                 |
| 📌 Проверить         | - Сколько пакетов у пользователя в `ton_farming_data`?                                              |
|                      | - Сколько транзакций создаётся в планировщике?                                                      |
|                      | - Обрабатываются ли пакеты по отдельности или агрегируются?                                         |
|                      | - Какой **тип транзакции** используется? (`FARMING_REWARD` vs `TON_BOOST_REWARD`)                   |
| 🔄 Цепочка вызова    | `tonBoostIncomeScheduler.ts` → `TonFarmingRepository` → `UnifiedTransactionService` → БД → API → UI |
| 🧠 Цель исследования | Подтвердить: начисление идёт **по всем пакетам** или только по сумме?                               |

---

## 🔢 ПРОБЛЕМА №2: UNI Farming — одна транзакция на весь депозит

| Параметр             | Значение                                                                      |
| -------------------- | ----------------------------------------------------------------------------- |
| 🐞 Симптом           | Одна транзакция раз в 5 минут, хотя открыто **несколько пакетов UNI**         |
| 📌 Проверить         | - Есть ли в системе поддержка **нескольких farming пакетов**?                 |
|                      | - Где и как считается `deposit_amount`?                                       |
|                      | - Используется ли отдельная таблица или поле для каждого пакета?              |
| 🔄 Цепочка вызова    | `farmingScheduler.ts` → `calculateUniFarmingIncome` → `BatchBalanceProcessor` |
| 🧠 Цель исследования | Подтвердить: начисление идёт **по всем пакетам** или только общая сумма       |

---

## 🔢 ПРОБЛЕМА №3: CRON планировщики – не запускаются или не обрабатывают всё

| Параметр             | Значение                                                                                        |
| -------------------- | ----------------------------------------------------------------------------------------------- |
| 🐞 Симптом           | - После перезапуска не запускается TON Boost CRON                                               |
|                      | - Логов нет, доход не начисляется                                                               |
| 📌 Проверить         | - Работает ли `tonBoostIncomeScheduler.start()` после рестарта?                                 |
|                      | - Есть ли `setInterval` или `node-cron`? Как они себя ведут?                                    |
|                      | - Обрабатываются ли все активные пользователи в цикле?                                          |
| 🔄 Цепочка вызова    | `server/index.ts` → `tonBoostIncomeScheduler.ts` → `TonFarmingRepository.getActiveBoostUsers()` |
| 🧠 Цель исследования | Убедиться: **CRON запускается, не падает**, корректно обрабатывает всё                          |

---

## 🔢 ПРОБЛЕМА №4: Тип транзакций — неправильный для отображения

| Параметр             | Значение                                                                    |
| -------------------- | --------------------------------------------------------------------------- |
| 🐞 Симптом           | Транзакции существуют в БД, но **не отображаются в UI**                     |
| 📌 Проверить         | - Какие типы транзакций используются?                                       |
|                      | - Какой фильтр стоит на фронтенде? (`FARMING_REWARD` vs `TON_BOOST_REWARD`) |
|                      | - Есть ли `metadata.original_type`, и обрабатывается ли оно в UI?           |
| 🔄 Цепочка вызова    | `UnifiedTransactionService.createTransaction()` → запись в `transactions`   |
| 🧠 Цель исследования | Подтвердить: типы транзакций влияют на **отображение в истории**            |

---

## 🔢 ПРОБЛЕМА №5: `farming_balance = 0` у всех активных пользователей

| Параметр             | Значение                                                                |
| -------------------- | ----------------------------------------------------------------------- |
| 🐞 Симптом           | Все активные пользователи TON Boost имеют `farming_balance = 0`         |
| 📌 Проверить         | - Обновляется ли `farming_balance` при покупке?                         |
|                      | - Почему данные старых пользователей не обновились?                     |
|                      | - Какие поля участвуют в расчёте дохода?                                |
| 🔄 Цепочка вызова    | `activateBoost()` → `TonFarmingRepository` → `ton_farming_data`         |
| 🧠 Цель исследования | Установить: данные обновляются ли при покупке и влияют ли на начисление |

---

## 🔢 ПРОБЛЕМА №6: Отсутствует структура множественных TON Boost пакетов

| Параметр             | Значение                                                              |
| -------------------- | --------------------------------------------------------------------- |
| 🐞 Симптом           | Все пакеты TON Boost складываются в один баланс                       |
| 📌 Проверить         | - Есть ли структура для хранения каждого пакета?                      |
|                      | - Сохраняется ли история каждой покупки?                              |
|                      | - Отдельно ли начисляется доход по каждому пакету?                    |
| 🔄 Цепочка вызова    | `boost/service.ts` → `activateBoost()` → `ton_farming_data`           |
| 🧠 Цель исследования | Подтвердить: можно ли в будущем начислять доход **по каждому пакету** |

---

## 🧾 Финальный результат, который ожидается

* [ ] Подробный отчёт по каждой проблеме
* [ ] SQL-запросы, которыми проверялась каждая гипотеза
* [ ] Скриншоты/логи, подтверждающие поведение
* [ ] Рекомендации по следующему шагу (без изменения кода!)

---

Если ты хочешь, я могу сейчас сформулировать **инструкцию для агента** и отправить её в виде готовой задачи. Напиши — и я сформирую.
