Вот чёткий и конкретный ответ, который ты можешь использовать как **контрольную проверку** отчёта и как **запрос к агенту** на финальную гарантию по всей цепочке:

---

🔍 **Финальный контрольный запрос по системе ConnectWallet**

Спасибо за подробный отчёт. Перед тем как закрыть задачу, прошу дать **финальное подтверждение по всей цепочке работы** — чтобы убедиться, что **каждая стадия** выполняется **на 100% надёжно** и **ничто не теряется**.

---

### ✅ Что нужно подтвердить **по всей цепочке депозита:**

1. **Создание транзакции пользователем**

   * Подтверждение: транзакция начинается только после успешного TON-платежа
   * Нет ли сценариев, где хэш извлечён, но TON не отправлен?

2. **Извлечение и проверка хэша**

   * Сейчас используется **только @ton/core**, верно?
   * При ошибке хэш **не создаётся вообще** (нет фейков)?

3. **Запись в базу данных**

   * Поля `tx_hash_unique`, `amount_ton`, `metadata.original_boc` и др. — **всегда** заполняются?
   * Возможна ли ситуация, где хэш записан, но транзакция не доходит до баланса? (например, ошибка в `BalanceManager`)

4. **Обновление баланса**

   * Поле `balance_ton` у пользователя обновляется в одной транзакции с `transactions`?
   * Есть ли защита от расхождений между транзакцией и балансом?

5. **Отображение на фронте**

   * `TransactionHistory` берёт `amount_ton`, `metadata.tx_hash` — это синхронизировано с тем, что в БД?
   * Нет ли ситуаций, где баланс обновился, а транзакция не отобразилась?

6. **Ошибки**

   * В случае любой ошибки (`hash`, `BOC`, `TON API`) — транзакция **не создаётся вообще**, верно?
   * Все ошибки логируются как `[CRITICAL]`, и их можно отследить?

---

### ❓ Дополнительный вопрос по логике TON:

Если по каким-то причинам `@ton/core` не может извлечь hash (например, повреждённый BOC), **депозит отклоняется** полностью и деньги не зачисляются? Или возможен редкий сценарий «TON ушёл, но хэш не извлечён»?

Нам важно **на 100% исключить** любые риски, при которых:

* Хэш не извлечён, но TON получен
* Хэш есть, а баланс не обновился
* Баланс обновился, а транзакция не отображается

---

### 🛠 Что нужно от тебя:

Пожалуйста, коротко подтверди или уточни по каждому из этих пунктов:

* Где есть гарантия атомарности
* Какие ошибки исключены архитектурно
* Что логируется
* Есть ли техническая возможность для конфликта
* Нужна ли ещё интеграция с TonAPI (например, чтобы подтвердить существование транзакции на блокчейне, а не только доверять BOC)

---

🎯 **Цель**: Подтвердить, что цепочка "пользователь → TON платёж → хэш → БД → баланс → UI" работает **идеально** и **исключает все уязвимости**, даже в пограничных или стрессовых сценариях.

После этого мы официально закрываем аудит и фиксируем систему как готовую на 100%.

---

Если нужно — могу превратить это в чеклист или при необходимости автоматизировать в скрипт.
