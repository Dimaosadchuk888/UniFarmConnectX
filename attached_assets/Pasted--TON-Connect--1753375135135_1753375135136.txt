🧩 ЗАДАНИЕ: Диагностика внешнего пополнения через TON Connect (личный кабинет)

🔐 ВАЖНО:

Запрещено вносить любые изменения в код, БД, конфиги или окружение. Только диагностика. Всё только по согласованию.

⸻

📌 КОНТЕКСТ:

В системе реализована возможность пополнения личного кабинета (User #25) напрямую через внешний TON-кошелёк с помощью TON Connect.
Вчера всё работало штатно. Сегодня — критический сбой:

⸻

❗ ПРОБЛЕМА:
 • Пользователь отправляет оплату на кошелёк системы через TON Connect.
 • Транзакция успешно поступает на blockchain и отображается в интерфейсе.
 • Баланс обновляется на 1–2 секунды — и пропадает обратно.
 • Через 1–2 следующих попытки пополнения — старые депозиты “всплывают” вместе с новыми, после чего снова исчезают.
 • Иногда баланс через 3–5 минут возвращается, иногда — нет.
 • Подозрение на ошибку в отображении, дедупликации или сбросе транзакции.

⸻

🧠 Возможная причина:

Есть предположение, что система может содержать механизм проверки транзакции или отложенной валидации депозита, из-за чего:
 • после поступления средств сначала запускается внутренняя проверка транзакции (например, по hash, payload, bounce, активности адреса),
 • и только после успешного прохождения всех условий происходит финальное зачисление и отображение в интерфейсе.

Если такая логика существует — её надо выявить и зафиксировать.

⸻

🔍 ЧТО НУЖНО ПРОВЕРИТЬ:
 1. Как фиксируется входящая транзакция от внешнего кошелька через TON Connect:
 • Используется ли bounce: true
 • Сохраняется ли она в transactions, и как определяется type, tx_hash, description.
 2. Есть ли в базе фактическая запись этой транзакции после поступления?
 • Возможно, запись создаётся, а потом удаляется, сбрасывается или перезаписывается.
 3. Не срабатывает ли логика, которая считает транзакцию “ошибочной” или “непрошедшей” и убирает её?
 4. Есть ли проблема с кешированием или отображением?
Например, пополнение проходит, но не доходит до UnifiedBalanceService или не попадает в интерфейс.
 5. Проверить весь путь: от входящего BOC → контроллер → транзакция → обновление баланса → отображение.
 6. Выяснить, есть ли встроенные задержки, проверки, дебаунсы или условия, которые могут откладывать отображение транзакции или временно скрывать её.

⸻

📍 Пример транзакции (24.07.2025, 16:32):

TON deposit from blockchain: 
+1.000000 TON


⸻

✅ Цель:

Изолировать и точно определить, на каком этапе происходит сбой:
 • фиксация транзакции,
 • сохранение баланса,
 • отображение в UI,
 • либо откат или отложенная валидация.

Результат нужен в виде отчета по всей цепочке от поступления TON до его отображения на балансе.