Вот готовое **техническое задание для агента** — строгое, без вмешательства, только диагностика:

---

🔧 **Техническое задание: Диагностика системных уведомлений WebSocket-соединения**

---

### 📌 Проблема:

В интерфейсе слишком часто появляются **системные уведомления** типа:

* ❗️ *"Соединение с сервером потеряно"*
* ✅ *"Соединение с сервером восстановлено"*

Это **не пользовательские уведомления** (не про депозиты, выводы и т.д.), а технические, **предположительно генерируемые WebSocket-слоем**.

---

### 🎯 Цель:

Провести полную диагностику:

1. Кто именно в коде **отправляет** эти системные уведомления?
2. Через какой механизм они попадают на фронтенд?
3. Какая **UI-компонента отображает** эти уведомления?
4. Как **отключить** только *технические уведомления*, **не затрагивая бизнес-оповещения** (депозиты, выплаты, бонусы и т.д.)

---

### 🔍 Что нужно исследовать:

#### 🔌 WebSocket-слой:

* Где и как обрабатываются события `onDisconnect`, `onReconnect`, `onError`
* Кто вызывает отображение уведомления при обрыве/восстановлении соединения?
* Есть ли глобальный обработчик (например, в `webSocketContext`, `NotificationContext`, `GlobalEvents.ts`, и т.д.)

#### 💬 Компонент уведомлений:

* Через какую функцию/хук отправляется уведомление:
  Примеры: `notify()`, `showNotification()`, `addToast()`, `NotificationContext.push()`
* Найти конкретный `message` или `type`, по которому можно отличить системные уведомления от пользовательских

---

### ⚠️ ВАЖНО:

**❌ Никаких изменений, пока не будет полной карты источников!**
Наша цель — **найти**:

* Конкретный участок кода, отвечающий за вывод системных сообщений (WebSocket)
* Возможность **отключить только их**, не повредив всю систему уведомлений

---

### ✅ Ожидаемый результат:

* Где именно генерируется уведомление о потере WebSocket-соединения
* Как оно передается в систему уведомлений
* Какие возможны пути безопасного отключения или фильтрации этих уведомлений (предварительно, без реализации)
* Предложение: **как элегантно убрать системные сообщения**, не затронув бизнес-уведомления (о депозитах, бонусах, выводах и т.п.)

---

🧠 Предположение: возможно, уведомление генерируется из `NotificationContext`, `webSocketContext` или обёртки WebSocket-клиента (`useWebSocket`, `SocketManager`, `AppInitializer` и т.д.) — всё это нужно исследовать.

⏱ Приоритет: высокий, но **действовать с осторожностью. Только анализ.**

Жду карту и выводы.
