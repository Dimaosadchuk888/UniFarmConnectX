Отлично, вот чёткое и жёстко ограниченное **техническое задание для агента** для проверки того, что **у новых аккаунтов начисления UNI фарминга происходят автоматически и без ошибок**:

---

## ✅ **Задача для агента: Проверка автогенерации `uni_farming_rate` для новых пользователей**

**Цель:**
Убедиться, что **у новых пользователей `uni_farming_rate` задаётся автоматически** при старте фарминга, и начисления происходят без ручного вмешательства.

---

## 📌 Что агент должен сделать:

### 1. **Ориентироваться строго по `ROADMAP.md`**

* Найти, в каком модуле или файле происходит установка `uni_farming_rate`
* Определить, устанавливается ли оно:

  * при регистрации нового пользователя
  * при первом запуске фарминга
  * или вручную (что является ошибкой)

---

### 2. **Проверить:**

* Что происходит при создании нового пользователя (`POST /api/v2/users/`)
* Что происходит при запуске фарминга (`POST /api/v2/farming/start`)
* Добавляется ли `uni_farming_rate` в базу данных автоматически

---

### 3. **Проанализировать:**

* Файлы:

  * `modules/user/service.ts`
  * `modules/farming/service.ts`
  * `modules/farming/logic/rewardCalculation.ts`
  * `core/scheduler/farmingScheduler.ts`
* Проверить: читается ли `uni_farming_rate` или вычисляется по умолчанию

---

## 🧪 Критерий успеха:

* ✅ Для новых пользователей при старте фарминга в таблице `users` появляется значение `uni_farming_rate` (например, `0.01`)
* ✅ Планировщик (`farmingScheduler`) видит новых пользователей и начисляет награды
* ❌ Нет случаев, когда `uni_farming_rate` остаётся `NULL` без ручной правки

---

## 🔴 Запрещено:

* Вносить изменения в код
* Модифицировать данные в базе
* Исправлять поведение
* Отклоняться от проверки только farming и user

---

## 📤 Ожидаемый результат:

* 📑 Где и как устанавливается `uni_farming_rate`
* ⚠️ Есть ли риск, что оно не устанавливается при каком-то сценарии (например, Telegram WebApp, прямой вызов API)
* ✅ Подтверждение, что **новые аккаунты корректно обрабатываются без ручных действий**

---

Хочешь, я сымитирую отчёт, как будто агент уже всё проверил и дал тебе выводы?
