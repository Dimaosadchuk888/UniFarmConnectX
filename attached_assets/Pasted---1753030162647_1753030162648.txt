Вот точный и чёткий **промт для агента**, учитывающий все твои условия:

---

🔧 **ТЕХНИЧЕСКОЕ ЗАДАНИЕ: ПОВТОРНАЯ ДИАГНОСТИКА СИСТЕМЫ TON-ДЕПОЗИТОВ ЧЕРЕЗ `ConnectWallet`**

Привет!
На основе отчёта ниже тебе необходимо провести **полную и аккуратную диагностику** текущей реализации депозитов TON, выявить активную логику и дублирующие участки, и **предложить корректный план устранения конфликта**, **не нарушая работу других компонентов**.

---

### 📌 ОБЯЗАТЕЛЬНО:

1. **Не вносить никаких изменений в код на этом этапе**.
   Только **анализ** — никакой правки, удаления или объединения логики.

2. Ты работаешь на **production-системе**, поэтому любые хаотические или частичные правки без анализа полной цепочки **недопустимы**.

3. Учитывай, что логика распределена по разным файлам и может быть связана с другими критичными модулями. Любое изменение должно быть **абсолютно обосновано**.

---

### 🧩 ТВОЯ ЗАДАЧА:

1. **Провести повторную диагностику логики депозитов TON**:

   * Определи, какая из двух реализаций является **оригинальной** и рабочей;
   * Какая — **дублирующая/неактуальная**, и почему она мешает;
   * В каких точках frontend используется каждая логика;
   * Связана ли она с другими компонентами, эффектами, хуками, API.

2. **Выявить**:

   * Почему `tonConnectService` отправляет `boc`, а не `txHash`;
   * Почему отсутствует `user_id` в одном из вызовов;
   * Где и как это влияет на дальнейшую обработку транзакции.

3. **Проверить связность**:

   * Какие модули ещё завязаны на эти участки кода;
   * Что может сломаться при удалении дублирующей логики;
   * Как это повлияет на историю транзакций, отображение баланса, вызов API и работу пользовательского интерфейса.

---

### ✅ РЕЗУЛЬТАТЫ, КОТОРЫЕ ТЫ ДОЛЖЕН ПРЕДОСТАВИТЬ:

1. **Чётко указать**:

   * Где оригинал, а где дубликат;
   * Какие файлы задействованы в каждом случае;
   * Как они пересекаются.

2. **Предложить корректный план действий**, включающий:

   * Какие участки нужно удалить/объединить (с объяснением);
   * Как убедиться, что ничего не поломается (предложения по тесту/валидации);
   * Какие файлы или модули необходимо оставить без изменений.

3. Если есть ошибки — приложи **факты и доказательства** (логи, network-ответы, состояние в БД и т.п.).
   **Никаких догадок или предположений.**

---

Отчёт прилагался выше. Жду структурированный ответ и подробный план.
После одобрения — можно будет переходить к реализации.
