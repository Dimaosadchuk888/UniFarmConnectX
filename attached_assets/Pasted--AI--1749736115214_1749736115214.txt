Ты — профессиональный AI-разработчик. Твоя задача — выполнить конкретное техническое задание, строго в рамках инструкции. Работай точно, локально и без побочных эффектов.

---

## ❗ ПЕРЕД НАЧАЛОМ РАБОТЫ:
- Убедись, что понял задачу. Если есть сомнения — НЕ начинай выполнение без уточнения.
- Удостоверься, что работаешь в нужной ветке (`production`) и используешь активный сервер (UniFarm Connect).
- Проверь, какие модули и файлы разрешено изменять. Работай ТОЛЬКО с backend модулями и server/routes.ts.

---

## 📌 ПРАВИЛА ВЫПОЛНЕНИЯ:
1. Не выходи за рамки задачи.
2. Не меняй архитектуру или структуру проекта.
3. Не трогай другие части системы, даже если в них есть ошибки.
4. Работай только с перечисленными endpoint'ами.
5. Используй только уже существующие структуры: controller, service, model.

---

## 🧩 ТВОЯ ЗАДАЧА:
Восстановить и реализовать недостающие API маршруты во фронтенде проекта UniFarm Connect.  
Цель — устранить критические и частично реализованные endpoint'ы по приоритету.

---

## 📋 СПИСОК ОТСУТСТВУЮЩИХ И НЕРЕАЛИЗОВАННЫХ ENDPOINTS:

### 🔴 HIGH PRIORITY:
- /api/v2/missions/active (GET)
- /api/v2/missions/complete (POST)
- /api/v2/missions/stats (GET)
- /api/v2/missions/user/{userId} (GET)
- /api/v2/wallet/withdraw (POST)
- /api/v2/uni-farming/deposit (POST)
- /api/v2/uni-farming/harvest (POST)

### 🟠 MEDIUM PRIORITY:
- /api/v2/boosts/packages (GET)
- /api/v2/referrals/stats (GET)
- /api/v2/ton-farming/active (GET)
- /api/v2/ton-farming/purchase (POST)

### 🟡 LOW PRIORITY:
- /api/v2/airdrop/register (POST)
- /api/v2/user-missions (GET)
- /api/v2/users/generate-refcode (POST)
- /api/v2/me (GET)

### ⚠️ Частично реализованные:
- /api/v2/daily-bonus/status (GET) — доработать при необходимости
- /api/v2/daily-bonus/claim (POST) — добавить метод
- /api/v2/users/profile (GET) — проверить соответствие логики
- /api/v2/wallet/balance (GET) — проверить формат
- /api/v2/transactions (GET) — проверить соответствие

---

## ⚙️ ДОПОЛНИТЕЛЬНЫЕ УКАЗАНИЯ:
- При реализации endpoint'ов соблюдай единый стиль кодирования.
- Не используй @ts-ignore, any, temp или мок-заглушки.
- Если логика уже частично существует в другом модуле — подключи её, не дублируй.
- Строго соблюдай формат данных, ожидаемый frontend'ом.
- Обязательно проверь: маршрут прописан в server/routes.ts, контроллер реализован, сервис подключен.

---

## 📄 ФОРМАТ ОТЧЁТА:
В конце своей работы напиши:

- ✅ Что было реализовано (по endpoint'ам)
- ⚠️ Что было найдено, но требует уточнения или не реализовано
- 🧪 Какие проверки были проведены (ручной тест, Postman, curl и т.д.)

---

## 🎯 ЦЕЛЬ:
Реализовать отсутствующие и частично сломанные endpoint'ы, восстановить стабильность API, обеспечить полную совместимость между frontend и backend.
Работа должна быть выполнена чисто, безопасно и без побочных эффектов.