# 🧭 AGENT PROMPT: Полный аудит базы данных UniFarm (только чтение)

## 🎯 Цель
Провести **глубокий аудит текущей структуры базы данных UniFarm** и сравнить её сMPT: Полный аудит базы данных UniFarm (только ч

Никаких изменений. Только сбор и проверка. Результат — чёткий отчёт, без предположений, без фантазий.

---

## ✅ Задачи

1.базы данных UniFarm** и сравнить её с **фактическим использованием(`schema: public`).
2.лный аудит базы данных UniFarm (только чтение)

## 🎯 Цель
Провести *
3.🧭 AGENT PROMPT: Полный аудит базы данных UniFarm (тол(поиск по коду, интерфейсам, SQL/ORM-запросам).
4.rm (только чтение)

## 🎯 Цель
Пров
   - какие используются, какие нет,
   - где есть несовпадения типов,
   - где поля есть в коде, но нет в базе (и наоборот).

---

## 📦 Вывод

Подготовь подробный отчёт в виде таблицы:

| Table        | Column       | DB Type   | Nullable | Default | Used in Code | Code Type | File(s) / Line(s)     | Комментарий                  |
|--------------|--------------|-----------|----------|---------|--------------|-----------|------------------------|------------------------------|
| transactions | tx_hash      | text      | YES      | NULL    | ✅           | string    | modules/.../service.ts | Есть в коде, нет в БД / OK   |
| referrals    | reward_uni   | numeric   | NO       | 0       | ❌           | —         | —                      | Есть в БД, не используется   |

---

## 🛠️ Инструменты и шаги

### 1. Сбор схемы БД:
Выполни SQL-запрос:

```sql
SELECT table_name, column_name, data_type, is_nullable, column_default
FROM information_schema.columns
WHERE table_schema = 'public'
ORDER BY table_name, ordinal_position;

Сохрани как schema_audit_raw.csv.

⸻

2. Сканирование кода:
 • Используй grep, rg, find или IDE для поиска полей и таблиц в коде.
 • Зафиксируй, где и как используется каждое поле: файл, строка, тип, контекст.
 • Проверь не только запросы, но и интерфейсы, схемы, типы, модули и сервисы.

⸻

3. Сравнение и анализ:
 • Сопоставь каждое поле из базы с кодом.
 • Отметь:
 • поля, которые не используются,
 • поля, которые есть в коде, но отсутствуют в базе,
 • поля с конфликтами типов или nullability.

⸻

4. Формирование отчёта:
 • Финальный файл: db_schema_audit.md (или .csv)
 • Структура по таблицам
 • В конце — список выявленных проблем:

[ISSUE-TX-01] transactions.tx_hash
  • Используется в коде (transactions/service.ts:121)
  • В базе данных отсутствует
  • Ошибка: возможные сбои при фиксации TON-транзакций


⸻

⛔ Ограничения
 • Не менять базу данных.
 • Не менять код.
 • Только анализ, только чтение.
 • Не придумывать поля или названия, если их нет в базе.

⸻

📌 Отчёт считается завершённым, если:
 • Есть список всех таблиц и полей.
 • Указано, какие используются в коде, какие нет.
 • Все поля сравниваются по типу и наличию.
 • Выявлены все проблемы и даны чёткие ссылки на строки кода.