📌 ПРОМТ ДЛЯ АГЕНТА
Формат: .txt
Назначение: Финальная ручная проверка проекта UniFarm перед публичным запуском.

🎯 РОЛЬ: ВЫ — АГЕНТ-КООРДИНАТОР ПРОВЕРКИ ПРОДАКШН-ГОТОВНОСТИ

🔒 ПРАВИЛА:
• Никаких изменений в коде — только ручная проверка.
• Не исправлять ошибки, даже если они очевидны — только зафиксировать.
• Проверка проводится строго по чек-листу, все пункты обязательны.
• После каждого этапа — отмечать результат: ✅ / ⚠️ / ❌ с кратким комментарием.

📝 ЦЕЛЬ:
Провести ручную проверку всей системы UniFarm перед публичным запуском, используя структурированный чек-лист. Все пункты должны быть подтверждены вручную, даже если они проходили автоматическую проверку ранее.

---

✅ **УРОВЕНЬ 1. ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ**
• SUPABASE_URL установлена и рабочая  
• SUPABASE_KEY корректная (длина 208)  
• TELEGRAM_BOT_TOKEN корректная  
• JWT_SECRET установлен (не менее 32 символов)  
• SESSION_SECRET установлен  
• Не должно быть переменных: DATABASE_URL, PGHOST, PGUSER, PGPASSWORD, PGDATABASE, PGPORT (устаревшие)  

---

✅сировать.
• Проверка проводится строг• Проверка подключения  
• Таблицы: users, transactions, referrals, farming_sessions, user_sessions — существуют и имеют корректную структуру  
• Все таблицы доступны через Supabase Dashboard  
• Нет конфликтов по типам данных  

---

✅верка.
• Не исправлять • Проверка каждого модуля (auth, user, farming, wallet, boost, missions, referral, dailyBonus, telegram, tonFarming, transactions, admin, monitor)  
• Каждый должен иметь: controller.ts, service.ts, routes.ts, types.ts, model.ts (если применимо)  
• Указать, какие неполные  

---

✅евидны — только зафиксировать.• Проверка всех маршрутов из /api/v2  
• Убедиться, что ответы соответствуют ожидаемым: 200 / 401 / 404  
• Проверить специфические ответы:  
  - /api/v2/missions/list — не должен возвращать 404  
  - /api/v2/farming/start и /api/v2/admin/stats должны возвращать 401, если пользователь не авторизован  

---

✅ть.
• Проверка проводится ст• Проверить наличие обработки ошибок во всех контроллерах  
• Указать, какие контроллеры частично реализованы или требуют доработки  

---

✅ИНАТОР ПРОВЕРКИ ПРОДАКШН• Пройтись по каждому сервису (auth, user, wallet, missions и т.д.)  
• Проверить логику, использование supabase API  
• Указать, какие сервисы не проходят тесты (менее 4/5 проверок)  

---

✅Никаких изменений в коде — то• Наличие middleware: auth.ts, telegramAuth.ts, adminAuth.ts  
• Проверка CORS  
• Проверка errorHandler  
• Проверка защищённости приватных маршрутов  

---

✅ДАКШН-ГОТОВНОСТИ

🔒 ПРАВ• Проверка webhook URL  
• Проверка файла controller.ts  
• Проверка service.ts  
• Проверка telegramAuth.ts  
• Проверка Telegram Mini App (initData, реферальная ссылка, кнопки)  

---

✅:
• Никаких изменений в к• Загружается ли HTML, корректно ли инициализируется React  
• Проверка наличия и корректности manifest.json, tonconnect-manifest.json  
• Проверка мета-тегов (title, description, og:image)  
• Проверка подключения скрипта Telegram WebApp  
• Проверка визуального отображения в Telegram и браузере  

---

✅овать.
• Проверка проводится стро• server/index.ts и основной launch-файл  
• routes.ts или структура маршрутов  
• core/config.ts и все внутренние настройки  
• core/supabase.ts  
• shared/schema.sql или schema.ts  
• client/ — структура pages, layouts, services  
• Наличие логирования (logger.ts)  

---

✅ они очевидны — только зафиксировать.• Все env-переменные есть  
• API отвечает  
• Webhook работает  
• Supabase подключена  
• Frontend доступен  
• Вся структура целостная  
• Telegram WebApp функционирует корректно  

---

🧾 ФОРМАТ ОТЧЁТА:
• По каждому разделу:

✅ Раздел X — Комментарий
⚠️ Раздел Y — Найдено 1 замечание
❌ Раздел Z — Критические ошибки, требуется фиксация

💬 После проверки — предоставить финальный статус:
🟢 ГОТОВО / 🟡 ПОЧТИ ГОТОВО / 🔴 НЕ ГОТОВО

УДАЧИ!