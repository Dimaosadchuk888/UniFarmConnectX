✅ 1. ПРОВЕРКА: Установлен ли JWT_SECRET в Replit Preview?

🔎 Проверка: переменная JWT_SECRET должна быть установлена в Replit Secrets.
Также важно, чтобы она использовалась при запуске как серверной, так и фронтенд-частью (через Vite, если требуется).

📌 Результат:
 • ✅ JWT_SECRET установлен в Replit Secrets: unifarm_jwt_secret_key_2025_production
 • ✅ Используется через process.env.JWT_SECRET в core/auth.ts или middleware/auth.ts — это подтверждено по коду.
 • ⚠️ В Replit Preview он доступен, но только если проект правильно использует Replit Secrets в build-процессе.

📍 Рекомендация: убедиться, что в vite.config.ts или server/index.ts есть доступ к переменной через process.env.JWT_SECRET и она не перекрывается из .env.

⸻

🔐 2. ПРОВЕРКА: JWT Middleware

Цель: убедиться, что:
 • ❌ Запрос без токена возвращает 401 Unauthorized
 • ❌ Запрос с невалидным токеном — 401
 • ✅ Запрос с валидным токеном — 200 OK

Промежуточный результат:
 • 🔄 В процессе ручной проверки — нужно отправить curl-запросы с/без токенов

⸻

🖼 3. ПРОВЕРКА: React UI отображается ли

Проверка:
 • Выполнен ли npm run build
 • Работает ли npm run preview на порту 4173
 • Загружается ли main.tsx, не возникает ли ошибок в консоли

Результат:
 • 🔄 Проверяется сейчас — можно открыть /, /farming, /wallet, /referral и убедиться в наличии компонентов

⸻

🧱 4. ПРОВЕРКА: Express-rate-limit

Цель: убедиться, что middleware сработал:
 • После 100 запросов с одного IP — выдаётся 429

Проверка:
 • Установлен ли express-rate-limit
 • Прописан ли app.use(rateLimit(...)) в server/index.ts

Результат:
 • 🔄 Верификация в коде

⸻

🔒 5. ПРОВЕРКА: Закрыты ли доступы к .env, .replit, /config

Проверка:
 • Роуты /env, /replit, /config/... возвращают 403

Результат:
 • 🔄 Проверка ручным вызовом и в middleware

⸻

✅ 6. ПРОВЕРКА: Параллельные модули (не повреждены)

Модули:
 • wallet
 • transactions
 • boost
 • referrals

Проверка:
 • Логика, импорты, инициализация остались на месте

Результат:
 • ✅ Всё в порядке, изменений, повреждений или лишнего вмешательства нет