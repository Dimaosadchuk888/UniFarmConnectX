# ЗАДАНИЕ ДЛЯ АГЕНТА  
🔍 «Схема фактических запросов и взаимодействий UniFarm (без вмешательства в код)»

## Цель
Дать мне развёрнутую, точную картину того, **как сейчас в реальности проходят все основные запросы и взаимодействия** в проекте UniFarm:  
- от фронтенда до базы данных (Supabase),  
- включая промежуточные слои (контроллеры, сервисы, репозитории, cron-процессы, WebSocket-события).  

## Что сделать (только аудит, никаких изменений)

1.модействия** в проекте U(папкиНИЕ ДЛЯ АГЕН# ЗАДАНИЕ ДЛЯ АГ# ЗАДАНИЕ ДЛЯ# ЗАДАНИ# ЗАДАНИЕ `ws/`)  
   иАГЕНТА  
🔍 «Схема факт  

2. СоставитьЕНТА  
🔍 «Схема фактических запродля каждого главного сценария:  
   - Авторизация / JWT-проверка  
   - Покупка Boost-пакета  
   - Начисление Boost-дохода (cron)  
   - Депозит UNI и начисление Farming  
   - Получение Daily Bonus  
   - Создание и активация Referral  
   - Подключение TON-кошелька  
   - Любые WebSocket-обновления баланса  

   Для каждого сценария показать последовательность:  
  цессы, WebSocket-события).  

## Что сделать (только аудит, никаких изменений 
   (если идёт cron, то `CronJob → Service …`).  

3.и взаимодействий UniFarm (без вмешательства в код)»

## Ц а какие идут через сервис-слой.  

4. Отметить, где уже есть централизованная валидация/логика, а где запросы «голые» (без проверки).  

5.ную картину того, * не вносить ничего в БД — только читать и описывать.

## Формат ответа

- Markdown-документльства в код)»

## Цель
Дать мне развё 
  c разделами по сценариям и схемами вида:

 1. Покупка Boost
UI: src/components/Boost/BuyButton.vue
↓ POST /api/boost/buy
Controller: boost/controller.ts:buy
↓ BoostService.buy()
↓ TransactionService.create(type: BOOST_PURCHASE)
↓ Таблицы: boost_purchases, transactions

- Сводная таблица:

| Сценарий | Маршрут | Слой до БД | Прямой SQL? | Таблицы | Тип транзакции |
|----------|---------|------------|-------------|---------|----------------|

- Краткие комментарии: что централизовано, а что «голый SQL»; где уже есть проверки, где нет.

## Ограничения
*грации Supabase**.  

2.в репозитории и Supabase.  
* Опираться только на текущий код и схему.  
* Никаких выдумок — только факты.

## Ожидаемый результат
Полная, чёткая картина текущих запросов и взаимодействий, понятная мне без просмотра кода.  
Файлия** в проекте UniFarm:  
- + (по желанию) SVG-диаграмма путей.