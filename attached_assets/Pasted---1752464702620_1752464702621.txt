Вот готовое **Техническое задание (ТЗ)** для агента, основанное на твоем уточнении. Оно направлено на **глубокое исследование логики TON Boost** при множественных депозитах, с фокусом на проблему замещения, недоначисления и архитектурных ограничений:

---

# 🧾 Техническое задание: Аудит логики множественных депозитов в TON Boost

**Проект:** UniFarm
**Модуль:** TON Boost
**Дата:** 14 июля 2025
**Исполнитель:** \[Агент / разработчик / аудитор]
**Тип задачи:** Аналитика без изменения кода

---

## 🎯 Цель

Провести технический аудит механизма обработки **множественных покупок TON Boost**. Проверить, как работает текущая архитектура:

* поддерживаются ли *одновременные активные депозиты*
* как обновляется баланс
* как начисляется доход
* как это фиксируется в транзакциях
  Задача — **выявить, корректна ли текущая логика**, при которой новый пакет **замещает** предыдущий, и не происходит **суммирования всех покупок**.

---

## 🔍 Задачи и ключевые вопросы

Проследить полную техническую цепочку при **множественных покупках Boost-пакетов**. Ответить на следующие вопросы **на основе данных в БД** (без вмешательства в код):

### 1. **Структура хранения пакетов**

* Сколько строк создаётся в `ton_farming_data`, если пользователь купил 2 и более пакетов?
* Перезаписывается ли запись или создаются новые?
* Есть ли механизм для хранения истории всех купленных пакетов?

### 2. **Поведение `farming_balance`**

* При новой покупке: баланс **суммируется** с предыдущим или **перезаписывает**?
* Каков итоговый `farming_balance`, если купить 2, 3, 5 пакетов подряд?

### 3. **Транзакции BOOST\_PURCHASE**

* Сколько транзакций создаётся при множественных покупках?
* У всех ли указана верная сумма (`amount_ton`)?
* Присутствует ли `metadata.original_type = 'TON_BOOST_PURCHASE'`?
* Есть ли несоответствие между суммами транзакций и итоговым `farming_balance`?

### 4. **Начисления дохода (TON\_BOOST\_INCOME)**

* Доход начисляется с **каждого пакета отдельно** или только с последнего?
* Какой расчётный баланс участвует в `FARMING_REWARD` (TON)?
* Все ли транзакции фиксируются с `metadata.original_type = 'TON_BOOST_INCOME'`?

### 5. **Сравнение с UNI Farming**

* В UNI Farming все депозиты суммируются, и доход начисляется от общей суммы.
* Почему TON Boost работает иначе? Это ограничение логики или баг?

---

## 📌 Методология

* Использовать SQL-запросы к таблицам `ton_farming_data`, `transactions`, `users`
* Пример для пользователя `user_id = 74` (имеет 9 пакетов)
* Проверить последовательность покупок, начислений, отображение в истории
* Проверить соответствие `farming_balance` общей сумме покупок
* Сравнить, фиксируются ли все суммы в транзакциях

---

## 📤 Выходные материалы

* Таблица или сводка по каждому из 5 пунктов выше
* SQL-запросы и вывод по каждому
* Цепочка: покупка → `ton_farming_data` → транзакции → начисление дохода
* Краткое заключение:

  * ⚠️ Ошибки, если есть
  * ✅ Что работает корректно
  * ❌ Что не работает
* Отдельный блок: рекомендации (если понадобится доработка архитектуры)

---

## 🚫 Ограничения

* **Никакого изменения кода или БД**
* Только чтение данных и анализ логики
* Только фактическая техническая диагностика

---

## ✅ Результат

Получим **подробный отчёт**, отвечающий на главный вопрос:

> Может ли TON Boost корректно обрабатывать множественные депозиты, или текущая система теряет/перезаписывает данные и доходы?

---

Готов адаптировать под формат GitHub-issue, Notion, PDF или вставить в текущую документацию.
