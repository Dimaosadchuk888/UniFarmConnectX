Вот чёткая и полная задача для агента:

---

### 🔐 ЗАДАЧА: Проверка критической уязвимости — привязка к аккаунту ID=74

#### 🚨 Проблема:

Система в текущем виде **небезопасно привязана** к аккаунту с ID `74`. При авторизации через Telegram **некоторые новые пользователи видят данные 74-го пользователя**, включая:

* Баланс
* Историю транзакций (включая депозиты и бонусы)
* Потенциально — farming, boosts, рефералов и т.д.

#### 🎯 Цель:

Полностью устранить **жёсткую привязку к ID 74** по всей системе. Привязка к конкретному аккаунту **не должна использоваться нигде** в production-логике. Все данные должны грузиться только:

* По текущему авторизованному пользователю
* С валидацией Telegram ID (или guest ID, если включено)

---

### 📋 Что нужно сделать:

#### 1. **Аудит всей системы на использование `userId = 74` или его жёсткой подстановки**

Проверить:

* `api/`, `modules/`, `contexts/` — где-либо прописан `74` (в коде, в моках, в логике)
* Стартовые данные (например, fallback-поведение, если user не найден)
* Компоненты, которые делают запросы без `auth context`, например `getUserWallet(74)` вместо `getUserWallet(currentUser.id)`
* Использование `74` в тестах или dev-заглушках

#### 2. **Проверка `fallback-логики`**

* Есть ли `fallback`, который подставляет ID=74, если Telegram auth не сработал
* Как система реагирует, если пользователь не найден или не авторизован
* Проверить TelegramInitializer / TelegramWebAppCheck
* Проверить `requireTelegramAuth` middleware (на сервере)

#### 3. **Проверка всех API-ендпоинтов**

* `/api/users/profile`
* `/api/wallet`
* `/api/farming`
* `/api/referrals`
* `/api/daily-bonus`
* `/api/missions`
* `/api/boosts`

На каждом убедиться:

* Авторизация работает строго по токену Telegram
* Никакой пользователь с ID=74 не возвращается случайно

#### 4. **Проверка WebSocket-обновлений**

* Проверить, не шлёт ли сервер данные по 74 аккаунту в broadcast-режиме
* Проверить, какой `userId` подписан на события

#### 5. **Проверка Frontend логики**

* Провести авторизацию новым Telegram-аккаунтом
* Убедиться, что:

  * Баланс и транзакции отображаются корректно
  * Нет утечек данных другого пользователя
  * Все функции (farming, boosts, withdraw) работают от **нового userId**

---

### ✅ После всех проверок:

#### 📌 Агент должен предоставить:

* Полный список мест, где раньше использовался `74` и что с этим было сделано
* Подтверждение, что система работает от реального авторизованного пользователя
* Подтверждение, что ни в одной части системы не используется фиксированный ID
* Подтверждение, что fallback логика безопасна и не раскрывает чужие данные

---

### 🛡 Цель: Перевести систему в **Production-режим**, где:

* Каждый пользователь видит только свои данные
* Авторизация Telegram надёжна
* Нет зависимости от тестовых аккаунтов или моков
* Поведение корректно даже при ошибках (fallback-режим)

---

Если нужно, могу также подготовить шаблон проверки `requireTelegramAuth` и типовой user flow через Telegram для ручного теста.
