Вот полный вариант уже в формате **Технического Задания (ТЗ)**:

---

### 📌 **Техническое задание: Аудит системы авторизации и работы с базой данных (без внесения изменений в код)**

---

#### ✅ **Цели задачи:**

1. Провести полный аудит работы с базой данных и механизмов авторизации в проекте UniFarm.
2. Определить конкретные причины ошибок авторизации, работы сессий и нестабильного подключения к БД.
3. Составить подробный отчет с результатами аудита и рекомендациями без выполнения изменений в коде.

---

#### 🚫 **Жесткие ограничения:**

* ❗ **Категорически запрещено:**

  * Вносить какие-либо изменения в исходный код проекта.
  * Выполнять миграции или правки в базе данных.
  * Перезапускать сервисы без согласования.

* ✅ **Разрешено:**

  * Выполнять только диагностические действия и анализ логов.
  * Использовать временное логирование и диагностику через отдельные скрипты (без влияния на рабочую систему).

---

#### 📋 **Текущие проблемы (на основе анализа):**

##### 1. **Подключение к БД (Neon):**

* ⚠️ Фикс `db-connect-fix.js` не применяется ко всем модулям.
* ⚠️ Некоторые модули могут пытаться использовать Unix Socket вместо TCP/IP.
* ⚠️ В базе данных отсутствуют пользователи в таблице `auth_users`.
* ✅ При изолированном подключении к БД соединение успешно устанавливается.

##### 2. **Настройки CORS:**

* ⚠️ Отсутствует заголовок `Access-Control-Allow-Credentials: true`.
* ⚠️ Используется wildcard `*` в `Access-Control-Allow-Origin`, что блокирует работу cookies.
* ⚠️ Не настроены разрешения для передачи telegram-специфичных заголовков.

##### 3. **Работа с сессиями:**

* ⚠️ Cookies не устанавливаются или не передаются корректно.
* ⚠️ Невозможно восстановить сессии пользователей.
* ⚠️ Нет создания новых пользователей при первой авторизации.

##### 4. **Ошибки сервера:**

* ❌ Частые ошибки 502/503 из-за проблем с подключением к БД.
* ❌ Необработанные ошибки промисов (`unhandledrejection`).
* ❌ Проблемы с WebSocket соединениями.

---

#### 🛠️ **Рекомендации по проверкам (без внесения изменений):**

##### 📌 Для базы данных:

* Убедиться, что переменные окружения принудительно указывают на Neon DB.
* Проверить, что `db-connect-fix.js` импортируется в самом начале всех модулей.
* Выполнить диагностику подключения к БД через отдельный тестовый скрипт.

##### 📌 Для CORS:

* Проверить конфигурацию CORS на наличие заголовка `Access-Control-Allow-Credentials: true`.
* Проверить, что для cookies используется конкретный домен, а не `*`.
* Добавить telegram-специфичные заголовки в разрешенные (например, `telegram-data`).

##### 📌 Для сессий:

* Проверить установку и передачу cookies (флаги Secure, SameSite).
* Убедиться, что сессии не истекают слишком быстро.
* Проверить работу механизма создания новых пользователей при первом входе.

##### 📌 Для авторизации через Telegram:

* Получить пример `initData` и проверить его обработку.
* Проверить валидацию данных от Telegram, включая проверку подписи.

---

#### 📅 **Результаты работы предоставить в виде отдельного отчета, который должен содержать:**

1. ✅ Чек-лист по каждому пункту аудита (что проверено, какие данные получены).
2. ✅ Таблицу с найденными проблемами и потенциальными причинами.
3. ✅ Подробные логи диагностики (если использовались диагностические скрипты).
4. ✅ Рекомендации по решению проблем (отдельно указать те, что требуют изменения кода).

---

Хотите, чтобы я сразу сформировал это как `.txt` файл? 😊
