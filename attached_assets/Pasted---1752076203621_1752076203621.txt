Вот обновлённое **техническое задание для агента**, строго учитывающее, что **названия файлов могут отличаться**, и всё нужно находить **через `ROADMAP.md`**:

---

# 🧠 Техническое задание: Аудит цепочки списания UNI при открытии фарминга

**Цель:**
Пользователь сообщает, что при открытии фарминг-пакета **монеты не списываются с баланса**. Необходимо **проследить полную цепочку открытия фарминга**, выявить, **на каком этапе возникает сбой**, и подготовить отчёт **без внесения изменений в код**.

---

## 📌 Общие инструкции

🔒 **Категорически запрещено:**

* Вносить изменения в код, БД или переменные среды
* Делать выводы по догадкам — только по `ROADMAP.md` и реальным файлам

🧭 **Обязательно:**

* Изыскание всех точек анализа вести **строго через `ROADMAP.md`**
* Проверять только те модули и функции, которые напрямую связаны с:

  * открытием фарминга
  * списанием средств
  * сохранением фарминг-сессии
  * начислением процентов

---

## 🧩 Что проверить — вся цепочка A → Я

### ✅ 1. Открытие фарминга

* Найти через `ROADMAP.md`, какой API-метод обрабатывает открытие фарминг-пакета
* Проверить, как он обрабатывает:

  * сумму депозита
  * идентификатор пользователя
  * вызовы других функций (например, списания или создания сессии)

---

### ✅ 2. Списание UNI

* Найти, где в рамках открытия пакета вызывается логика списания средств
* Определить:

  * создаётся ли транзакция
  * обновляется ли баланс в базе
  * вызывается ли механизм проверки достаточности средств

---

### ✅ 3. Сохранение фарминг-сессии

* Проверить, записывается ли новая фарминг-сессия в базу
* Отслеживается ли:

  * дата старта
  * сумма UNI
  * привязка к пользователю

---

### ✅ 4. Начисление процентов

* Найти, какой модуль отвечает за автоматическое начисление фарминг-дохода
* Проверить:

  * входит ли пользователь в пул начисления
  * есть ли у него активная сессия и `uni_farming_rate`
  * создаются ли транзакции дохода

---

## 📤 Что должен выдать агент в отчёте

1. 📁 Список всех **найденных через ROADMAP** файлов, задействованных в этой цепочке
2. 📌 По каждому этапу:

   * что **происходит** в коде
   * что **не происходит**, хотя должно
3. ❌ Указание точки, на которой **обрывается логика**
4. ✅ Вывод, вызвана ли функция списания и записи фарминга
5. 🧩 Заключение: где сбой и что его вызывает (в рамках анализа, без исправлений)

---

Если всё устраивает — могу сразу сгенерировать симуляцию финального отчёта от агента. Хочешь?
