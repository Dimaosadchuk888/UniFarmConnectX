🧪 ТЗ для агента: TON Boost – Финальная валидация всей цепочки

Дата: 13 июля 2025
Статус: Критическая архитектурная проблема подтверждена, требуется фактологическая ревизия перед внесением правок.

📌 Цель

Провести финальную проверку всей цепочки файлов, которые участвуют в работе TON Boost — от frontend-компонентов до базы данных, включая контроллеры, сервисы, репозитории, планировщики и модель пользователя.

Необходимо:

Подтвердить или опровергнуть каждую из обнаруженных проблем,
Указать точные места (файлы, строки, поля БД),
Не допустить никаких предположений — только ссылки на факты в коде,
Обязательно пройти в обе стороны: от фронта до БД и обратно.
📂 Что нужно проверить

1. 📱 Фронтенд (TON Boost UI)
Файлы:

client/src/components/ton-boost/TonFarmingStatusCard.tsx
client/src/components/ton-boost/ActiveTonBoostsCard.tsx
client/src/components/ton-boost/BoostPackagesCard.tsx
Проверить:

Какие поля приходят из API (deposits, balance_ton, ton_farming_balance)?
Используется ли в UI поле ton_farming_balance?
Как рассчитываются суммы и что показывается пользователю?
Подтвердить, что ton_farming_balance нигде не визуализируется — если так, указать строки.
2. 🧩 Контроллеры и сервисы
Файлы:

modules/boost/controller.ts
modules/boost/service.ts
modules/boost/TonFarmingRepository.ts
Проверить:

В методе purchaseBoost() — какие параметры передаются?
Метод purchaseWithInternalWallet() — где происходит:
списание баланса;
передача данных в activateBoost();
Метод activateBoost() — какие поля обновляются в таблице (в upsert)?
Подтвердить, что ton_farming_balance или farming_balance не обновляется — указать строки.
3. 🔁 Планировщик начислений дохода
Файл:

modules/scheduler/tonBoostIncomeScheduler.ts
Проверить:

Откуда планировщик получает userDeposit?
Убедиться, что используется balance_ton, а не farming_balance.
Указать строку, где это происходит.
Если используется fallback-загрузка баланса из users, указать, что это искажает расчёт.
Подтвердить, что фактически используется весь баланс.
4. 🗄️ База данных (таблица users или ton_farming_data)
Проверить:

Есть ли поле ton_farming_balance в структуре таблицы users?
Обновляется ли оно после покупки?
Есть ли поле ton_farming_deposit, и используется ли оно?
Есть ли накопленные значения (ton_farming_accumulated, ton_farming_last_update)?
Подтвердить, что при покупке Boost никакое поле из них не заполняется.
5. 💸 Финансовое поведение и расчёт дохода
Проверить:

Какая ставка используется для пользователя (например, 1.5% в день)?
От какой суммы рассчитывается доход (balance_ton или farming_balance)?
Подтвердить, что пользователь с 5 TON в Boost получает доход с 800+ TON — если да, показать путь данных.
📌 Вывод

Ожидаемый формат отчёта:
Разделы по каждому слою архитектуры:

✅ Раздел: Планировщик
Файл: tonBoostIncomeScheduler.ts
Строка: 117
Комментарий: используется balance_ton вместо farming_balance
Факт: Подтверждено

✅ Раздел: База данных
Таблица: users
Поле: ton_farming_balance
Факт: Присутствует, но не обновляется