Вот пошаговая **инструкция по диагностике**, чтобы точно и безопасно выявить проблему с **отображением баланса TON** и **отсутствием транзакций** в личном кабинете, несмотря на успешное зачисление на админский кошелёк:

---

### 📌 **Общие Условия**

**❗Важно: код менять категорически нельзя!**
Если потребуется что-то изменить, добавить или удалить — необходимо:

* Сначала **описать проблему в чате**
* **Указать, что именно нужно изменить**
* **Обосновать причину**
* Только после этого — с согласия можно проводить правки

---

### ✅ **Проверка маршрута пополнения TON**

#### 1. **Отправка TON пользователем**

* Убедиться, что отправка прошла и выдан **hash** транзакции
* Убедиться, что сумма списалась у пользователя

#### 2. **Поступление на админский кошелёк**

* Проверить через **TON Explorer**, что:

  * транзакция есть
  * сумма верная
  * хэш транзакции валиден

#### 3. **Фиксация в БД**

Перейти в `transactions` таблицу и проверить:

* появилась ли новая запись
* корректно ли записались поля:

  * `type: "deposit"`
  * `userId`
  * `amount`
  * `status: "confirmed"` (или `pending`, если в процессе)
  * `hash`
  * `method: "ton"`

#### 4. **Запись TON баланса**

Проверить таблицу **wallet** или **userBalance**, где хранится TON-баланс:

* поле `tonBalance` (или аналог)
* соответствует ли оно ожидаемой сумме после пополнения

Если нет:

* Проверь, где и при каких условиях выполняется **обновление баланса** после подтверждённой транзакции
* Обычно это делается в сервисе: `walletService.ts`, `transactionService.ts` или `tonWebhookHandler.ts`

---

### 🧪 **Фронт + API: отображение баланса и транзакций**

#### 5. **Проверка эндпоинтов**

* GET `/api/wallet/` — должен возвращать объект с текущим балансом
* GET `/api/wallet/:userId/transactions` — должен содержать список транзакций с TON-пополнением

Если `GET /api/wallet/` не возвращает `tonBalance`:

* Проверить `walletController.ts` → `getWalletInfo`
* Проверить, из какого поля в БД берётся баланс и правильно ли он мапится

Если `transactions` не отображаются:

* Проверить `transactionController.ts` → `getUserTransactions`
* Проверить фильтры в запросе: не исключается ли тип `deposit`, `ton`, или статус `pending`

---

### 🧠 Возможные причины, почему **TON не отображается**

1. **Фронтенд не обрабатывает TON как тип баланса** (ошибка в `Wallet.tsx`)
2. **API не возвращает `tonBalance` в респонсе** (ошибка в контроллере)
3. **Сервис не обновляет баланс после получения транзакции** (ошибка в `tonWebhookHandler`)
4. **Запись в БД производится, но не связана с нужным `userId`**
5. **Hash транзакции сохраняется, но не триггерит пересчёт баланса**

---

### 🔒 Что сказать агенту

```plaintext
❗ Пожалуйста, не изменяй код, пока мы не закончим проверку.  
Сейчас проводится тотальный тест по маршруту пополнения TON, отображения баланса и транзакций.  
Если потребуется что-то добавить, удалить или поменять — обязательно опиши это в чате, зачем и почему, и только после обсуждения это можно внедрять.
```

---

Хочешь, я сделаю для тебя чеклист или карту проверки для агента/разработчика по этим пунктам в виде таск-листа?
