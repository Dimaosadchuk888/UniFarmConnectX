Вот промпт для агента:

---

🧪 **Задача: Полная техническая диагностика функционала ConnectWallet и прямой оплаты Boost-пакета**

Пожалуйста, проведи **глубокое исследование и проверку двух ключевых блоков логики**, связанных с ConnectWallet и покупкой Boost-пакета через внешнюю оплату. Нам нужна **абсолютно точная и структурированная информация** без допущений или догадок — только на основе реального поведения системы, кода и БД.

---

### 🔍 Часть 1: Подключение и пополнение через ConnectWallet

Исследуй полный цикл, начиная с подключения через ConnectWallet и заканчивая отображением баланса в приложении.

**Тебе нужно:**

1. **Подключение через ConnectWallet:**

   * Какие данные мы получаем после подключения?
   * Что сохраняется на нашей стороне (в БД или сессии)?
   * Есть ли валидация юзер-адреса? Проверка на дубликаты?

2. **Пополнение баланса:**

   * Что происходит после выбора опции пополнить?
   * Какие TON-адреса предлагаются и как формируются?
   * Как фиксируется входящий платёж:

     * где именно логика приёма транзакции? (уточни путь: web3 listener / backend / крон / webhook)
     * по какому адресу происходит слушание входящих TON?
     * как валидируется принадлежность транзакции?

3. **Отображение средств на балансе:**

   * Где и как обновляется пользовательский баланс?
   * Какая задержка между оплатой и отображением?
   * Какие статусы учитываются (pending, confirmed и т.п.)?

---

### 🔍 Часть 2: Покупка Boost-пакета через внешнюю оплату (минуя кошелёк)

Рассмотри сценарий, когда пользователь нажимает **"Купить" в Boost-пакете → выбирает способ оплаты через внешний кошелёк (не пополняет баланс)**, а платит напрямую.

**Тебе нужно:**

1. **Что происходит после нажатия "Купить" → "Оплата через внешний кошелёк":**

   * Куда направляется пользователь?
   * Как формируется TON-адрес и сумма?

2. **Что происходит после того, как пользователь отправляет TON напрямую:**

   * Как система определяет, что этот платёж — именно покупка Boost-пакета?
   * Где логика сопоставления транзакции с юзером и выбранным пакетом?
   * Где и как фиксируется факт покупки (в какой таблице, какие поля)?
   * Может ли произойти ошибка: TON получен, но Boost не активировался?

3. **Сравнение с внутренним кошельком:**

   * В чём отличие между оплатой Boost через внутренний баланс и оплатой напрямую?
   * Есть ли риск двойного учёта, потерянной транзакции или ошибки привязки?

---

💬 **Важно:**

* Укажи конкретные файлы и модули, где реализована логика (например: `modules/boost/logic/tonBoostCalculation.ts`, `transactions.ts`, `externalPaymentStatus.ts` и т.п.).
* Если где-то используется webhook или сторонний сервис — обязательно уточни.
* Опиши тип транзакции, структуру данных, которая используется для сопоставления (memo, комментарий, адрес и т.п.).

---

📌 **Формат отчета** — чёткий по блокам, с фактами, ссылками на код и таблицы, краткими выводами.
Без предположений. Всё должно быть проверено на **реальных сценариях**, **логике** и **данных**.
