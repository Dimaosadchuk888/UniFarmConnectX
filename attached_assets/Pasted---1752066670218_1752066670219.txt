Вот точное техническое задание для агента на **глобальное исследование системы на дубликаты, мусорные и потенциально опасные файлы**, **без изменения кода**, **в связке с `ROADMAP.md`**:

---

# 🧾 Техническое задание: Анализ системы на наличие дубликатов, мусорных и неучтённых файлов

## 📌 Цель:

Провести **полный аудит файлов проекта**, чтобы:

* 📁 Найти **файлы, которые отсутствуют в `ROADMAP.md`**
* 🗑️ Выявить **мусорные, неиспользуемые или устаревшие файлы**
* ⚠️ Отметить **потенциально опасные файлы**, которые могут повлиять на стабильность/безопасность
* ❗ Включить **все подозрительные файлы** (например, `routes_minimal.ts`, `test.ts`, `demo.js`, старые версии, неиспользуемые утилиты, временные сборки, альтернативные конфигурации и т.д.)

---

## 📦 Что агент должен сделать:

### 1. **Сравнение с ROADMAP.md**

* Пройтись по всем файлам проекта
* Сопоставить каждый файл/модуль/роут/компонент с тем, что **явно указано в `ROADMAP.md`**
* Если файл не упоминается в `ROADMAP.md` и выглядит как **альтернативная версия, тестовая сборка, или устаревший код** — пометить как **подозрительный**

### 2. **Проверка на дубликаты**

* Найти файлы с похожими или идентичными названиями (например, `routes.ts`, `routes_minimal.ts`, `routes_dev.ts`)
* Проверить: нет ли повторяющейся логики в разных файлах
* Обнаружить **дубли функций, компонентов, обработчиков**

### 3. **Анализ мусора**

* Найти файлы, которые:

  * Не подключены ни в один модуль
  * Не экспортируются/не используются
  * Не запускаются системой
  * Являются явно устаревшими или временными (например: `copy.ts`, `legacy.js`, `temp.ts`, `backup.ts`)
* Отдельно отследить файлы, которые:

  * Используют hardcoded данные
  * Подключаются к старым/неактуальным API
  * Содержат чувствительные данные

### 4. **Риски и потенциальная опасность**

* Выявить все файлы, которые:

  * Могут нарушить логику безопасности (например, дубли middleware без проверок)
  * Используют устаревшие методы или небезопасные конструкции
  * Могут быть **точкой входа для уязвимостей** (например, открытые debug роуты, старые API слои)

---

## 📌 Ограничения:

* ❌ **Не менять код**
* ✅ Только **анализ структуры и содержания файлов**
* ✅ Использовать только документацию, ROADMAP, и логику системы
* ❌ Никаких предположений — если файл не ясен, помечается как подозрительный

---

## 📤 Формат отчета:

```plaintext
📁 Найденные дубликаты:
- routes.ts и routes_minimal.ts — возможно, дублируют один и тот же модуль
- boostService.ts и boostService_old.ts — дублирование логики

🗑️ Мусорные/неучтённые файлы:
- routes_minimal.ts — не упоминается в ROADMAP.md, не подключается
- testController.ts — используется только для отладки
- mockData.ts — содержит захардкоженные данные, не используется

⚠️ Потенциально опасные файлы:
- debugRoutes.ts — содержит открытые методы без авторизации
- legacyAuth.ts — устаревшая авторизация, отключена в ROADMAP
- backupConfig.json — возможно содержит устаревшие токены

🟩 Безопасные, но не описанные в ROADMAP.md:
- monitor.ts — используется системой мониторинга
- healthCheck.ts — служебный файл, может быть сохранен
```

---

## 📌 Задача агента:

Провести анализ ВСЕЙ системы, чтобы понять:

* Что использовать безопасно ✅
* Что требует подтверждения и возможного удаления ❓
* Что потенциально угрожает проекту 🔥

Если нужно — могу сразу сделать шаблон-таблицу с блоками, в которые агент будет просто заносить каждый файл по категориям.
