## ✅ Техническое задание: Финальная проверка интеграции ConnectWallet и ToneBoost в UniFarm

Цель:
Провести глубокую финальную техническую проверку полной цепочки оплаты и отображения депозитов через ConnectWallet и модуль покупки TON Boost пакетов в рамках приложения UniFarm.

---

### 📌 Общая структура задания

> ❗️*Ниже представлен прототип чеклиста, на основе которого нужно провести полную диагностику работоспособности платёжной системы. После прохождения всех пунктов необходимо предоставить детальный технический отчёт о готовности системы и выявленных замечаниях, если таковые есть.*

---

### 🔐 1. Подключение кошелька (ConnectWallet)

* Проверить:

  * Корректность вызова компонента ConnectWallet
  * Связь с внешним кошельком (TON/Telegram/OpenMask и пр.)
  * Запись данных пользователя (адрес, userId) в контекст/хранилище (например, `userContext`)
  * Проверить fallback-сценарии, если пользователь отменил подключение

---

### 💳 2. Процесс оплаты

* Описание:

  * Пользователь инициирует оплату (TON Boost или Farming)
  * Отправка TON через выбранный метод
  * Получение транзакции на бэке
  * Подтверждение оплаты
  * Привязка транзакции к текущему пользователю

* Проверить:

  * Какие файлы/функции отвечают за приём оплаты (например, `modules/wallet/transactions.ts`)
  * Какие контроллеры принимают платёж (`POST /api/wallet/withdraw`, `POST /api/boosts/activate`)
  * Как система определяет пользователя, совершившего платёж (Telegram auth? Wallet?)
  * Проверить работу rewardDistribution.ts и deepReferral.ts при получении дохода

---

### 🧠 3. Сопоставление оплаты и пользователя

* Проверить:

  * Как идентифицируется отправитель средств
  * Как происходит поиск по базе (user ID, Telegram ID, wallet address)
  * Где хранится связка: кошелёк ↔ user
  * Файл: UserRepository.ts, telegramAuth.ts

---

### 📊 4. Фиксация транзакции в системе

* Проверить:

  * Какие модели и схемы участвуют (`shared/schema.ts`)
  * Логика записи TON транзакции в базу (`wallet/transactions.ts`)
  * Как фиксируется факт оплаты
  * Как вычисляется сумма
  * Проверка на дублирование транзакций

---

### 🧾 5. Отображение средств на кошельке

* Проверить:

  * Как часто обновляется баланс (вручную / авто через WebSocket)
  * Как работает Wallet.tsx → BalanceCard, TonTransactions.tsx
  * Проверка отображения депозита в UI после успешной транзакции

---

### 💥 6. Прямые покупки пакетов TON Boost

* Проверить:

  * Как работает модуль покупки (`modules/boost/`, `tonBoostCalculation.ts`)
  * Где в базе фиксируются Boost-пакеты
  * Какой маршрут отвечает за активацию: POST /api/boosts/activate
  * Правильно ли начисляется бонус UNI
  * Отображается ли Boost статус в UI (модуль BoostStatusCard или `TonFarmingStatusCard`)

---

### 📋 7. Общая готовность

Просим предоставить заключение по следующим пунктам:

* Насколько система готова к приёму реальных депозитов через кошельки
* Есть ли проблемы с фиксацией транзакций
* Корректно ли работает отображение баланса и Boost-пакетов в интерфейсе
* Необходимо ли что-то доработать для полной стабилизации
* Какие файлы, компоненты или endpoints потенциально требуют рефакторинга или тестов

---

Формат ответа:
Просьба оформить результат в виде списка или табличного отчёта с пояснениями по каждому блоку:
✅ Готово / ⚠️ Есть проблемы → + краткое описание, где и что.