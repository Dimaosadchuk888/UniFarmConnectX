✅ T72: Аудит и очистка неиспользуемых полей в базе данных Supabase

📌 Цель задачи:
Провести технический аудит всех таблиц Supabase и выявить поля, которые не используются в текущей архитектуре UniFarm (ни в TypeScript интерфейсах, ни в backend-логике, ни во frontend-отображении).

⸻

🔍 Этап 1 — Сопоставление схем
 1. Пройтись по каждой таблице Supabase в схеме public:
 • users, user_balances, transactions, wallet, referrals, referral_earnings, boost_purchases, farming_sessions, farming_deposits, airdrops, missions, user_sessions, user_mission_completions
 2. Для каждой таблицы:
 • 📁 Сравнить поля с актуальными интерфейсами TypeScript (например: User, UserBalance, Transaction, ReferralChain, BoostPurchaseData и т.д.)
 • 📦 Проверить: используются ли эти поля в сервисах (service.ts, model.ts, scheduler.ts, controller.ts)
 • 🎨 Проверить: отображаются ли эти поля на frontend (например, через React-компоненты, context, state, props)

⸻

🔍 Этап 2 — Выявить «мертвые» поля

Для каждого поля, которое не используется в коде, необходимо:
 • Отметить, в какой таблице оно находится
 • Приложить обоснование: почему оно считается устаревшим или неактивным
 • Указать, появлялось ли оно из прошлых версий/экспериментов/автогенерации Supabase

⸻

📄 Выходной результат:

Создать файл docs/T72_DB_UNUSED_FIELDS_REPORT.md, который будет содержать:
 • Полный список всех проанализированных таблиц
 • Таблицу: Таблица / Поле / Статус / Причина
 • Комментарии, какие поля точно подлежат удалению
 • Какие требуют уточнения/переиспользования
 • Общий уровень «чистоты» БД по результатам аудита (в %)

⸻

🧼 Этап 3 — Готовность к очистке

После завершения анализа:
 • Подготовить SQL-команды ALTER TABLE DROP COLUMN, но не выполнять — только сохранить в T73_DB_CLEANUP_COMMANDS.sql
 • Уточнить у владельца проекта (тебя), какие поля можно удалить сразу, а какие временно отключить (через SELECT EXCLUDE или supabase policy)

⸻

🟡 Важно:
❗ Никакие поля не удалять до ручного подтверждения
❗ Не трогать поля системного назначения (created_at, id, user_id, tx_hash и т.д.)

⸻

🧠 Цель:

Упростить архитектуру БД → Ускорить доступ к данным → Повысить читаемость и надёжность Supabase структуры
→ Навести полный порядок в базе данных перед продакшеном

Готов к выполнению — запускать с правами администратора Supabase + доступ к исходному коду всех модулей.