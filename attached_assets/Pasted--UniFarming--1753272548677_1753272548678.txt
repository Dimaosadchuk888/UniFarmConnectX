Спасибо, поняла! Тогда уточняю:
🔍 **Нам не нужно проверять запуск UniFarming.**
📌 Задача — **исследовать и проверить только работу с TON-платежами**, включая:

* Пополнение через `ConnectWallet`
* Отображение баланса после пополнения
* Покупку Boost-пакета через внешний кошелёк

Вот актуальный и точный **промпт-навигация** для агента 👇

---

### 🧭 Тех. задание: Проверка работы TON-платежей (ConnectWallet и Boost)

**Контекст:**
У нас настроена собственная система пополнения и покупки Boost-пакетов через TON. Сейчас требуется полная проверка текущей реализации **без изменения кода**. Эта задача — **аудит**: выяснить, как система **обрабатывает пополнения TON через ConnectWallet**, **отображает баланс** и **фиксирует покупку Boost через внешний кошелёк**.

---

## ✅ Что проверить:

### 1. 💳 Пополнение через ConnectWallet

* Что запускается на фронте при нажатии "Пополнить" через ConnectWallet?
* Как происходит вызов внешнего кошелька и отправка TON?
* Какой API-запрос обрабатывает факт пополнения?
* Где и каким методом фиксируется успешная транзакция в базе?
* Как обрабатываются статусы: `pending`, `confirmed`, `failed`?
* Как система связывает платеж с конкретным пользователем?
* Какие события (WebSocket/cron/webhook) обновляют статус транзакции?
* **Почему может возникнуть кейс, при котором деньги списаны, а баланс не обновлён?**

  * Проверить возможные точки обрыва или задержки
  * Логи, крон-задачи, ивенты

---

### 2. 👛 Отображение баланса после пополнения

* Как фронт получает актуальное значение баланса (API / WebSocket)?
* Есть ли кеш, который мешает отображению сразу?
* Какая логика отвечает за обновление баланса пользователя после пополнения?
* Проверить, что при успешном пополнении баланс корректно обновляется во всех частях интерфейса (главная / кошелёк).

---

### 3. 🚀 Покупка Boost-пакета через **внешний** TON-кошелёк

* Как оформляется покупка Boost не через внутренний баланс, а напрямую через ConnectWallet?
* Кто в бэкенде принимает и подтверждает этот платёж?
* Где сохраняется транзакция покупки Boost?
* Как система определяет, что Boost был активирован?
* Есть ли случаи, когда Boost был оплачен, но не активировался? Если да — где именно происходит обрыв?
* Как эта информация отражается в базе и интерфейсе?

---

### 4. 📂 Структура: какие модули участвуют

* Названия и расположение файлов:

  * Фронтенд компоненты для пополнения и Boost
  * API endpoints, через которые проходят операции
  * Контроллеры и сервисы (названия и роли)
  * Места записи в базу данных (таблицы/коллекции)

---

### ❗Важно

* **Ничего не менять в коде.**
* Цель — **понять и описать**, как именно всё работает **сейчас**.
* Нужно выявить:

  * Какие модули работают корректно
  * Где могут возникать сбои
  * Какие шаги происходят на каждом уровне (фронт → API → сервис → база)

---

Хочешь, я превращу это в PDF, Google Doc или в карточку задачи?
