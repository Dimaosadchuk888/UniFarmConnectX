Отлично. На основе твоего последнего аудита можно зафиксировать чёткую **задачу для агента**, которая завершит процесс: довести систему **до полного, подтверждённого 100% соответствия `roadmap.md`**.

Вот формулировка финального технического задания для агента:

---

### 📌 **ТЕХНИЧЕСКОЕ ЗАДАНИЕ: Завершение соответствия всей системы UniFarm с `ROADMAP.md`**

---

#### 🎯 Цель:

Привести всю систему **к 100% соответствию `roadmap.md`**, устранить все пробелы, дубли, недоработки, несостыковки между архитектурой проекта и документированной частью, включая недокументированные модули и API.

---

#### 🔍 Что нужно сделать:

1. **Реализовать отсутствующие endpoint’ы:**

   * `GET /rates` в модуле UNI Farming (`FarmingService.getRates()`)
   * `POST /activate` в TON Boost (`BoostService.activatePackage()`)
   * `GET /webapp-data` в Telegram (`TelegramService.getWebAppData()`)
   * `POST /set-commands` в Telegram (`TelegramService.setCommands()`)

2. **Документировать в `roadmap.md` следующие модули:**

   * `scheduler` — описать его задачи и связь с начислениями
   * `debug` — описать его функции как служебного инструмента

3. **Проверить и устранить архитектурные дубли:**

   * `/api/v2/farming/` и `/api/v2/uni-farming/` — должны быть объединены в единый модуль
   * `/api/v2/wallet/` и `/api/v2/wallet/data` — либо оставить один, либо пояснить назначение каждого
   * Исключить или переписать direct handler'ы (`directDeposit.ts`, `directBalanceHandler.ts`) в соответствии с основной архитектурой (`BaseController`)

4. **Проверить и привести к единому стилю:**

   * Все импорты, имена файлов и маршрутов
   * Переименовать или убрать файлы с дублирующими или противоречивыми названиями (`router.ts` vs `routers.ts`)
   * Проверить на уровне кода: нет ли несоответствий между названием роутов и названием используемого файла (особенно farming, wallet, user)

5. **Удалить тестовые/временные/захламляющие файлы:**

   * Убедиться, что в корне остались только `replit.md` и `roadmap.md`
   * Все прочие временные отчёты, включая `RoadMapDeposit`, `RedMap`, `Redmi`, перенесены в `archived_reports/` или удалены

---

#### 🧾 Что нужно выдать:

Создай файл: **`FINALIZATION_LOG.md`**, в котором:

1. Подтверди реализацию **всех ранее отсутствующих endpoint’ов** — с указанием файлов, где они добавлены
2. Укажи **фрагменты `roadmap.md`**, которые были изменены/дополнены
3. Опиши действия по устранению **дублирующих маршрутов и handler-файлов**
4. Приложи список **удалённых/перенесённых файлов**
5. Подтверди: «✅ Финальное соответствие достигнуто — 100% `roadmap.md` vs Реализация»

---

#### 🛡 Обязательные условия:

* Перед созданием **любого нового файла или функции** — ОБЯЗАТЕЛЬНО проверить, существует ли уже этот файл/метод/роут в системе
* Запрещается дублирование логики или дубли имен
* Все действия должны быть **выполнены строго в рамках архитектурного стиля системы**
* Категорически запрещена поверхностная проверка — **всё должно быть подтверждено на уровне кода**

---

Если хочешь, я могу составить шаблон `FINALIZATION_LOG.md` — скажи, и я его создам.
