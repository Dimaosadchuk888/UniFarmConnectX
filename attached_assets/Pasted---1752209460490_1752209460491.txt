Отлично, вот как можно **оформить финальное ТЗ на следующий этап работы**, учитывая твой отчет:

---

## ✅ Техническое задание: Устранение ошибок по результатам исследования стабильности UniFarm

**Дата:** 11 июля 2025
**Основание:** Отчет *«Исследование стабильности системы UniFarm после оптимизаций» от 11.01.2025*
**Цель:** Устранить выявленные ошибки и завершить интеграцию функционала, не доведенного до рабочего состояния после Фазы 1–3.

---

### 📌 ОБЯЗАТЕЛЬНО

* Вносить правки **аккуратно**, с **учетом всей архитектуры проекта**
* Перед каждым изменением — **проверка влияния на остальные модули (регрессионный анализ)**
* После каждого исправления — **тестовая проверка на staging/dev-среде**
* Все изменения фиксировать в отдельном **чеклисте с комментарием**

---

### 🧩 Задачи по исправлению

#### 🔹 1. Исправить `BatchBalanceProcessor`

* Проблема: используется `supabase.raw()`, который не поддерживается
* Решение: переписать batch-логику с использованием `supabase.rpc()` или обычных `.insert/update` в цикле
* Необходимо:

  * сохранить транзакционность
  * сохранить batch-логирование
  * устранить падения всей операции при ошибке одной записи

#### 🔹 2. Починить `TON Boost` планировщик

* Проблема: не начисляются доходы, ошибка в логах при попытке распределения наград
* Возможные причины:

  * неверный вызов `referralService.distributeReferralRewards()`
  * отсутствие валидных boost-данных
* Необходимо:

  * отловить реальные параметры, на которых возникает сбой
  * проверить логику `calculateIncome(boost)`
  * восстановить начисление доходов пользователю и referral-наград

#### 🔹 3. Восстановить сохранение TON-кошельков

* Проблема: нет логов о сохранении адресов, функционал не используется
* Проверить:

  * используется ли `saveTonWallet()` в `controller.connectTonWallet`
  * вызывается ли `/connect-ton` на фронте
* Необходимо:

  * реализовать/проверить post-запрос с сохранением адреса
  * добавить лог: `[TON] Кошелек сохранён для userId: ...`

#### 🔹 4. Восстановить работу `referral`-таблицы

* Проблема: таблица пуста, нет ни одной связи
* Проверить:

  * вызывается ли `referralService.createReferralLink` при регистрации
  * корректна ли логика вставки в таблицу `referrals`
* Необходимо:

  * обеспечить создание записи при использовании referral-кода
  * логировать `[Referral] Связь установлена: inviter → invited`

#### 🔹 5. Добавить метрики кеша (`BalanceCache`)

* Проблема: нет логов или подтверждений работы кэш-слоя
* Необходимо:

  * добавить `console.log`/лог в `getBalanceFromCache`
  * убедиться, что кеш отрабатывает и не запрашивает лишние данные из БД

---

### 🧪 После исправлений — Регрессионная проверка

После завершения каждой задачи, провести **ручную или автоматическую проверку всей цепочки**, чтобы убедиться, что:

* Балансы списываются/начисляются корректно
* Boost доходы идут и награды начисляются
* TON-кошельки сохраняются
* Referral-связи создаются
* WebSocket обновляет все в реальном времени
* Batch-обновления не падают

---

### 🕓 Сроки

* Начать немедленно
* Критические задачи (TON Boost + Batch) — в первую очередь
* Статус по каждой задаче — в виде отчета с лейблом ✅ / ⚠️ / ❌

---

Если хочешь, я могу отправить это агенту как следующее задание или сразу разбить это по подзадачам.
