Устранение критических проблем перед запуском UniFarm в продакшн (Т8)

🔧 Цель задачи:
Устранить все критические ошибки, зафиксированные в финальном аудите UniFarm (от 15.06.2025). Выйти на стабильную production-готовность: 100%.

⸻

❗ ПЕРЕД НАЧАЛОМ:
 • Работай только локально
 • Не меняй архитектуру проекта
 • Не создавай новые таблицы или endpoints
 • Все действия — строго по списку ниже

⸻

📌 СПИСОК ОБЯЗАТЕЛЬНЫХ ИСПРАВЛЕНИЙ:

1. Telegram initData
 • Добавить поддержку window.Telegram.WebApp.initData
 • Распарсить initData и передавать telegram_id, username
 • Использовать его для авторизации в каждом защищённом endpoint

2. API /auth/telegram и /register/telegram
 • Исправить ошибку 400/500
 • Убедиться, что:
 • Пользователь создаётся по telegram_id
 • ref_code создаётся корректно
 • Пользователь записывается в таблицу users
 • Возвращается JWT (если используется) или уникальный токен

3. Авторизация
 • Защитить все API /wallet/balance, /user/profile, /farming/start
 • Требование: авторизация через initData или JWT
 • В случае ошибки — возвращать 401 с пояснением

4. Manifest.json
 • Убедиться, что файл /client/manifest.json:
 • существует
 • подключён в HTML <head>
 • содержит корректные теги: name, theme_color, start_url, icons
 • Проверить отображение в Telegram WebApp

5. Очистка .env
 • Удалить переменные:

DATABASE_URL, PGHOST, PGUSER, PGPASSWORD,
PGDATABASE, PGPORT, DATABASE_PROVIDER, USE_NEON_DB


 • Оставить:

SUPABASE_URL, SUPABASE_KEY, TELEGRAM_BOT_TOKEN,
JWT_SECRET, SESSION_SECRET



⸻

⚙️ ОГРАНИЧЕНИЯ:
 • Не создавай новые endpoints
 • Не добавляй новые зависимости
 • Не модифицируй бизнес-логику рефералов или фарминга
 • Не трогай .sql схемы — адаптируй код, если что-то отсутствует

⸻

📄 ФОРМАТ ОТЧЁТА:
 • ✅ Что было сделано (по каждому пункту)
 • 🧪 Какие проверки выполнены (ручные/автоматические)
 • ⚠️ Что не удалось исправить (если есть)

⸻

🎯 ЦЕЛЬ:

Устранить блокирующие ошибки и подготовить UniFarm к 100% production-запуску в Telegram Mini App.

⸻

Хочешь, чтобы я сразу сформулировал следующую задачу Т9 после этого?