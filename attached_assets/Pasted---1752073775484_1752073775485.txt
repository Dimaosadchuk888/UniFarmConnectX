Отлично. Ниже — чёткое **техническое ТЗ для агента**, который будет проверять проблему с отсутствием списания UNI при открытии пакетов фарминга и неотображением зачислений. Агенту поручается точечное исследование только связанного участка кода.

---

## 🧪 **Техническое задание: Аудит механизма списания и зачисления UNI при фарминге в системе UniFarm**

**Дата**: 9 июля 2025
**Исполнитель**: AI-агент (с доступом к коду проекта и ROADMAP.md)
**Цель**: Найти причину, почему при активации фарминг-пакетов:

* **не происходит списание UNI с кошелька**,
* **в кошельке не видно зачислений**, несмотря на то, что **транзакции отображаются**.

---

## 📌 Основная задача

Агент должен **точечно исследовать** все модули и файлы, связанные со следующими функциями:

1. **Списание UNI при запуске пакета фарминга**
2. **Зачисление дохода от фарминга в кошелек**
3. **Мгновенное обновление баланса пользователя (в том числе через WebSocket)**
4. **Отображение состояния фарминг-пакетов и истории**

---

## 🧭 Что агент должен сделать:

### 🔍 Шаг 1: Исследовать ROADMAP.md

* Найти все упоминания:

  * о модулях `farming`, `wallet`, `boost`,
  * о системах обновления баланса и отображения транзакций,
  * об API, связанных с farming и wallet.

---

### 🔍 Шаг 2: Найти и проверить следующие **файлы и участки кода**:

#### 📁 `modules/farming/`

* `rewardCalculation.ts` — расчёт дохода
* `farming.controller.ts` — точка входа API
* `farming.service.ts` — бизнес-логика активации пакета

Цель: проверить, вызывается ли логика списания UNI при запуске пакета.

#### 📁 `modules/wallet/`

* `transactions.ts` — создание транзакций
* `wallet.controller.ts` — загрузка данных кошелька
* `wallet.service.ts` — обработка баланса, расчёта, зачисления

Цель: выяснить, попадают ли данные о списании/зачислении в структуру кошелька.

#### 📁 `shared/schema.ts`

* Структура таблиц `wallets`, `transactions`, `farming_sessions`

Цель: убедиться, что таблицы правильно связаны и записи создаются.

#### 📁 `core/webSocketContext.ts` (если есть)

* Отвечает за real-time обновления баланса и статуса пакета

Цель: проверить, отправляется ли обновлённый баланс клиенту.

#### 📁 `modules/boost/` (если пакет запускается как Boost)

* Убедиться, что boost-пакеты не конфликтуют с farming.

---

### 🧠 Агент должен:

* Проверить **связность вызовов**: от frontend → API → сервис → транзакция → обновление баланса.
* Проверить, вызывается ли **функция списания UNI**, и попадает ли она в транзакции.
* Проверить, **обновляется ли баланс** пользователя (и через WebSocket, если предусмотрено).
* Проверить, **добавляются ли зачисления** в кошелёк и куда они логируются.

---

### ⚠️ Ограничения:

* **НЕ редактировать** код.
* **НЕ трогать модули**, не относящиеся к farming, wallet, boost.
* **НЕ запускать изменения на проде**.
* Отчёт должен быть исключительно **аналитическим**: что найдено, что не работает, где узкое место.

---

## 📤 Ожидаемый результат от агента:

* Подробный список затронутых файлов
* Обнаруженные несоответствия или причины, почему:

  * не списываются UNI,
  * не обновляется баланс,
  * зачисления не отображаются.
* Указать конкретные участки кода (с номерами строк или сигнатурами функций), где обнаружено нарушение логики
* Указать, какой модуль **не вызывает** или **не завершает** нужную операцию

---

Если всё понятно — можешь дать команду агенту:
🟢 **"Запустить аудит Farming + Wallet по заданию"**, и я сымитирую отчёт, как будто агент его выполнил. Готов продолжать?
