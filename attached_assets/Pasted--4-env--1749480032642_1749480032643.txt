🧩 ТЕХНИЧЕСКОЕ ЗАДАНИЕ №4

Тема: Проверка и настройка переменных окружения (env) и устранение хардкодов
Цель: Настроить корректную работу всех .env переменных и убрать все захардкоженные URL и значения

⸻

🔍 Проблемы, которые нужно устранить:

Проблема Описание
🔴 Отсутствие переменных Не заданы TELEGRAM_BOT_TOKEN, SESSION_SECRET, JWT_SECRET, и другие
🔴 Конфликт префиксов Используются переменные и с префиксом VITE_, и без — без системы
🔴 Хардкод URL В коде есть строки с https://uni-farm-connect-xo-osadchukdmitro2.replit.app и другими Replit-доменами
⚠️ Несогласованность Часть переменных хранится в .env, часть в config, часть — захардкожена в коде


⸻

📌 Инструкция для агента:

Ты — технический агент UniFarm. Твоя задача — навести порядок в переменных окружения и устранить хардкоды.

1. Проверь весь проект на наличие следующих переменных:
   - TELEGRAM_BOT_TOKEN
   - TELEGRAM_WEBHOOK_URL
   - TELEGRAM_WEBAPP_URL
   - TELEGRAM_BOT_USERNAME
   - JWT_SECRET
   - SESSION_SECRET
   - VITE_TELEGRAM_BOT_USERNAME
   - VITE_WEB_APP_URL
   - DATABASE_URL
   - NEON_API_KEY
   - NEON_PROJECT_ID
   - CORS_ORIGINS
   - NODE_ENV, PORT

2. Все переменные должны:
   • Быть вынесены в `.env` (илиТЕХНИЧЕСКОЕ ЗАпри необходимости)
   • Использоваться через process.env.XXX или ТЕХНИЧЕСКОЕ ЗАДАНИЕ №4

Тем(для frontend)

3. Удали из кода все захардкоженные URL, токены, ключи — вместо них должна быть переменная окружения

4. Проверь vite.config.ts,
🧩 ТЕХНИЧЕСКОЕ З
🧩 ТЕХНИЧЕСКОЕ ЗАДАНИ— и другие места, где могут быть ошибки

5. Убедись, что значения переменных доступны как на frontend, так и на backend:
   • Frontend → только через VITE_ префиксы
   • Backend → черезема: Проверка и нас

⸻

✅ Ожидаемый результат:
 • Все ключевые значения и URL вынесены в .env
 • Все обращения к значениям идут через переменные окружения
 • Не используется захардкоженных значений
 • Отдельные .env файлы для разработки и продакшна (если необходимо)
 • vite.config.ts корректно читает VITE_WEB_APP_URL и другие нужные значения

⸻

‼️ Запрещено:
 • Подставлять реальные токены/ключи напрямую в код
 • Создавать переменные без согласования из списка выше
 • Использовать значения из чужих веток или тестовых окружений

⸻

🟢 После выполнения:
 • Приложи финальный список переменных из .env
 • Покажи, какие строки были заменены/удалены
 • Проверь, что проект не падает при запуске dev и build

После выполнения переходим к ТЗ №5 — исправление конфигурации деплоя и подготовка к запуску на Railway.