🛠 ЗАДАЧА: Исправление всех критических и средних ошибок, выявленных в итоговом аудите UniFarm
Вы — технический агент, ответственный за исправление всех системных, логических и архитектурных ошибок, которые зафиксированы в заключении системного аудита UniFarm.
Работа должна быть выполнена строго по приоритетам и без внесения изменений в логику RedMap.

⸻

🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ (высший приоритет):
 1. Ошибка 500 в API запросах пользователей:
 • Починить endpoint /api/v2/users/guest/, чтобы не происходил сбой при входе.
 • Обеспечить корректную обработку guest_id и user_id.
 2. Проблема с деплоем:
 • Наладить workflow Start Production Server, чтобы после build система запускалась стабильно.
 • Проверить все переменные окружения, конфигурации сервера и порты.
 3. Telegram WebApp нестабильна:
 • Устранить fallback в guest mode при наличии Telegram initData.
 • Обеспечить передачу и сохранение telegram_id.
 • Проверить, что не создаётся новый пользователь при каждом заходе.

⸻

🟡 СРЕДНИЕ ПРОБЛЕМЫ (второй приоритет):
4. Повторяющиеся API запросы:
 • Оптимизировать кеширование (особенно для /api/v2/farming/status, /api/v2/ton-farming/info).
 • Устранить множественные одинаковые вызовы при каждом рендере.

 5. Отсутствие обработки ошибок и состояния загрузки:
 • Добавить отображение loader’а при долгих API запросах.
 • Реализовать user-friendly отображение ошибок (через toasts / alerts / fallback components).
 6. WebSocket задержки:
 • Проверить реализацию WebSocket логики.
 • Устранить лаги при эхо-сообщениях (если требуется — оптимизировать ping/pong или reconnect).

⸻

✅ УСЛОВИЯ ВЫПОЛНЕНИЯ:
 • Каждый фикс должен сопровождаться кратким комментарием в коде.
 • После каждого этапа вы обязаны самостоятельно провести повторную проверку, чтобы убедиться, что ошибка устранена и не появилось новых.
 • Любые изменения должны строго соответствовать логике, описанной в RedMap. Изменение RedMap — ЗАПРЕЩЕНО без согласования.

⸻

📋 Формат отчёта после выполнения задачи:
 • ✅ Название задачи
 • 📍 Что было сделано (конкретные файлы, изменения)
 • 🔁 Проверка: как проверили, что всё работает
 • ❗ Остались ли проблемы?

⸻

🎯 Цель:
Полностью устранить блокеры перед запуском UniFarm в продакшн. После завершения задач система должна пройти повторный аудит и быть готова к публикации.