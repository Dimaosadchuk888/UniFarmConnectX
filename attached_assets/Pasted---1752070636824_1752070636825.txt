Вот корректно сформулированное **техническое задание (ТЗ)** для агента, основанное на вашей задаче:

---

### 🛠️ Техническое задание: Исследование системы хранения и генерации GVT (JWT) токена

#### 📌 Цель:

Провести **детальное исследование механизма генерации, валидации и хранения GVT/JWT токенов** в системе UniFarm **строго на основе информации, зафиксированной в `ROADMAP.md`**. Цепочка должна быть проверена **по файлам, официально задокументированным в ROADMAP**, без выхода за рамки.

---

#### 🔍 Этапы задачи:

1. **Прочитать `ROADMAP.md`** и найти **всю секцию, где упоминается работа с JWT токенами**, а также **сессии пользователя** и любые действия, связанные с авторизацией.

2. На основании навигации из `ROADMAP.md` определить **официальные файлы**, связанные с:

   * генерацией токена;
   * валидацией токена;
   * middleware проверки токена;
   * сохранением токена (или сессии) в базу данных;
   * обновлением и выходом (logout).

3. Пройтись по всем этим файлам, **НЕ изменяя код**, и **подтвердить или опровергнуть**:

   * где формируется токен;
   * что входит в payload токена;
   * сохраняется ли он в Supabase (и если да — в какую таблицу и в какие поля);
   * как происходит проверка токена при запросах;
   * как выполняется logout.

4. Проверить, **привязан ли токен к конкретному пользователю** в таблице `user_sessions` (или другой, указанной в ROADMAP).

5. Составить **итоговый отчет**, который должен включать:

   * список всех задействованных файлов (с путями);
   * описание всех найденных механизмов и логики;
   * название таблицы Supabase и поля, где сохраняется токен или связанная информация;
   * вывод о корректности работы цепочки GVT-токена;
   * замечания, если какие-либо из шагов не соответствуют документации или работают некорректно.

---

#### ❗ Ограничения:

* **Не модифицировать код!**
* **Не додумывать и не использовать недокументированные файлы** — только то, что описано в `ROADMAP.md`.
* Все результаты и путь проверки должны быть **максимально точными и обоснованными**.

---

Если хотите — могу сразу составить шаблон для итогового отчета агента.