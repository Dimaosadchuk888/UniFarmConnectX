Вот чётко сформулированная **задача для агента** — для точного расследования текущей проблемы **без изменения кода**:

---

## 🧩 Задача: Исследовать отсутствие транзакций при покупке и начислениях по UniFarming

### 📌 Контекст:

* Пользователь купил UniFarming пакет:

  * Баланс **уменьшился** (монеты списались)
  * Но **транзакция о покупке отсутствует** в истории
* Активные фарминг-пакеты работают (баланс растёт), **но транзакции начислений не отображаются**
* Вопрос также в том, **обновляется ли** UNI/Ton баланс на самом деле (в Supabase, WebSocket, UI)

---

### 📋 Что нужно проверить:

#### 1. **Транзакция покупки пакета:**

* Было ли событие `TRANSACTION_TYPE = 'FARMING_PURCHASE'` записано в базу?
* Если нет — где сбой: в сервисе, в контроллере или при записи в Supabase?
* Покупка фиксируется в какой таблице? `transactions`, `farming_sessions`, `users`, `uni_farming_data`?

#### 2. **Транзакции начислений от активных пакетов:**

* Создаются ли транзакции с типом `FARMING_REWARD` или аналог?
* Где они должны появляться?
* Есть ли они в таблице `transactions`?
* Если да — почему не отображаются на фронте?

#### 3. **Обновляется ли баланс UNI и TON после начислений:**

* Проверить **реальный баланс в Supabase** (`users`, `uni_farming_data`, `ton_farming_data`)
* Проверить, приходят ли **WebSocket сообщения с новым балансом**
* Проверить, получает ли React Query данные с новым балансом
* Проверить, обновляется ли UI (в Wallet)

---

### ⚠️ Ограничения:

* ❗ **Код не менять**
* Работать **только в режиме исследования**
* Использовать текущую архитектуру:

  * `modules/farming/*`
  * `modules/transactions/*`
  * `modules/wallet/*`
  * `websocket/handlers`
  * `supabase` (структура и содержимое)
  * UI (React frontend)

---

### 📤 Ожидаем:

* Чёткий отчёт:

  * ✅ Есть или нет транзакция покупки
  * ✅ Есть или нет транзакции начислений
  * ✅ Обновляется ли баланс (фактически)
  * ❌ Где именно происходит сбой, если он есть
* Вывод: отображается ли всё корректно на UX или нет

---

Пусть агент начнёт проверку по этой задаче прямо сейчас, аккуратно, в рамках всей архитектуры, ничего не ломая.
