Вот сообщение, которое ты можешь отправить агенту, чтобы он дал **архитектурную рекомендацию по переходу на корректную схему с использованием `telegram_id`**, включая **все риски, последствия и безопасный план миграции**:

---

👋 Привет!

Мы внимательно изучили текущую проблему с некорректной идентификацией пользователя, и теперь нам нужно от тебя **архитектурное заключение** и **план действий**.

Ты предложил перейти на правильную схему, где будет использоваться `telegram_id`, а не `database ID` — и мы согласны, что это необходимо. Однако:

---

### ⚠️ Нам важно понять:

Если мы **меняем архитектуру идентификации на основе `telegram_id`**, как ты рекомендуешь, то:

1. **Какие могут быть последствия для текущей системы?**

   * Что сломается на уже подключённых маршрутах?
   * Где могут появиться конфликты (например, при повторном подключении кошельков)?
   * Что произойдёт с пользователями, у которых уже есть wallet\_address, но `telegram_id` либо неверный, либо отсутствует?

2. **Какие есть риски появления дубликатов или рассинхронизации данных при переходе?**

   * Что произойдёт с пользователями, у которых:

     * один и тот же wallet используется в разных аккаунтах;
     * есть записи без `telegram_id`, но с адресом TON;
     * был сохранён wallet под другим Telegram-аккаунтом?

3. **Какой самый безопасный способ перейти на новую схему `telegram_id → wallet`?**

   * Какие шаги нужно сделать, чтобы:

     * не потерять существующих пользователей и их данные;
     * не создать дубликаты;
     * обеспечить строгую однозначность связи `telegram_id ↔ wallet_address`;
     * сохранить целостность истории депозитов и балансов?

4. **Какие части кода нужно будет обновить?**

   * Где конкретно нужно заменить `telegram.user.id` на `telegram.user.telegram_id`?
   * Какие функции/методы/файлы подлежат рефакторингу?
   * Есть ли промежуточные состояния, которые стоит временно логировать для валидации?

---

### ✅ Задача

Пожалуйста, **предложи архитектурно корректный и безопасный план перехода на схему с `telegram_id`**. Укажи:

* 🧱 Что нужно изменить (по слоям — frontend, backend, БД);
* 🧩 Как это повлияет на текущих пользователей и депозиты;
* 🛡 Как избежать багов, дубликатов и рассинхронизации;
* 📊 Как можно протестировать корректность после перехода (чеклист, метрики, мониторинг).

---

📌 Наша цель — провести **однократный, безболезненный переход на корректную схему**, после которого:

* каждый пользователь однозначно идентифицируется по `telegram_id`,
* все депозиты обрабатываются мгновенно и точно,
* никакие старые или дублирующиеся данные не нарушают логику.

Ждём от тебя подробной инструкции, чтобы утвердить и приступить к реализации.
