⸻

🧩 ТЕХНИЧЕСКОЕ ЗАДАНИЕ №2

Тема: Проверка и исправление структуры модулей
Цель: Убедиться, что каждый модуль имеет корректную архитектуру (controller, service, routes, model, types)

⸻

🔍 Проблемы, которые нужно устранить:

Проблема Описание
🔴 Отсутствует modules/user/model.ts Файл экспортируется в modules/index.ts, но фактически не существует
🔴 Циклические зависимости В экспортах models/types — особенно между модулями
⚠️ Неконсистентная структура Не все модули имеют одинаковую архитектуру: где-то нет model, где-то routes


⸻

📌 Инструкция для агента:

Ты — технический агент UniFarm. Твоя задача — проанализировать и привести в порядок все модули в папке `modules/`.

1. Проверь, что каждый модуль (auth, user, farming, wallet, boost, referral и т.д.) содержит:
   - controller.ts
   - service.ts
   - routes.ts
   - model.ts
   - types.ts или types/index.ts
2. Если какие-либо файлы отсутствуют — создай пустые заготовки с корректным экспортом.
   (например: export const UserModel = {}; — временно)
3. Исправь ошибку с отсутствующим modules/user/model.ts — он🧩 ТЕХНИЧЕСКОЕ ЗАДАНв⸻

🧩 ТЕХНИЧЕСКОЕ ЗА но файла нет.
4. Убедись, что все экспорты из modules/index.ts корректно работают и не вызывают циклических зависимостей.
5. Проверь на наличие циклических импортов между types,
⸻

🧩 ТЕХ
⸻

🧩 ТЕи исправь.
6. Не меняй содержимое логики модулей — цель этой задачиры модулей
Цель а не логика.

‼️ Запрещено:
- Редактировать функциональность модулей (например: менять логику boost или farming)
- Удалять существующие реализации, даже если они не завершены
- Переименовывать модули


⸻

✅ Ожидаемый результат:
 • Каждый модуль в modules/ содержит: controller, service, routes, model, types
 • Все файлы экспортируются без ошибок через modules/index.ts
 • Ошибка с отсутствующим user/model.ts устранена
 • Циклические зависимости в экспортах устранены

⸻

🟢 После выполнения:
 • Приложи список файлов в каждом модуле
 • Укажи, какие файлы были созданы/добавлены
 • Подтверди, что все импорты из modules/index.ts работают без ошибок

После этого перейдем к ТЗ №3 — устранение конфликтов зависимостей между frontend и backend.