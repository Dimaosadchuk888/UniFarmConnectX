Вот промт для задачи T2 — Удаление незашищённых API endpoints строго в формате «золотого промта»:

⸻

🛡️ T2. Удаление незашищённых endpoint’ов из server/index.ts

Ты — профессиональный AI-разработчик. Твоя задача — выполнить конкретное техническое задание, строго в рамках инструкции. Работай точно, локально и без побочных эффектов.

⸻

❗ ПЕРЕД НАЧАЛОМ РАБОТЫ:
 • Убедись, что работаешь в Replit.
 • Убедись, что находишься в файле server/index.ts.
 • Найди блок открытых endpoint’ов в строках 228–395 (или вблизи этой области).
 • Убедись, что это именно временные/демо endpoint’ы, не связанные с основной бизнес-логикой.

⸻

📌 ПРАВИЛА ВЫПОЛНЕНИЯ:
 1. Не изменяй другие участки кода, кроме указанного блока.
 2. Не трогай защищённые маршруты или модули (modules/, middlewares/, controllers/).
 3. Не перемещай логику в другие файлы — просто удали.
 4. Ничего не выдумывай — удаляй только демо-endpoint’ы без авторизации.

⸻

🧩 ТВОЯ ЗАДАЧА:
 1. Перейти в server/index.ts.
 2. Удалить все открытые маршруты (пример: app.get('/api/user/current', ...)) внутри строк 228–395, которые не используют авторизацию.
 3. Убедиться, что эти маршруты:
 • не проверяют req.user или verifyToken,
 • не валидируют telegram_id,
 • не обёрнуты в authMiddleware или аналогичную защиту.
 4. Убедиться, что после удаления проект компилируется и не вызывает ошибок.

⸻

⚙️ ДОПОЛНИТЕЛЬНЫЕ УКАЗАНИЯ:
 • Используй централизованную систему маршрутов через server/routes.ts и модули.
 • Все защищённые маршруты должны быть реализованы в modules/ и подключаться через app.use(...).

⸻

📄 ФОРМАТ ОТЧЁТА:
 • ✅ Что было удалено (список маршрутов или строк)
 • 🧪 Что было проверено (что проект запускается без ошибок)
 • ⚠️ Что оставлено без изменений (если что-то вызвало сомнения — укажи)

⸻

🎯 ЦЕЛЬ:

Очистить server/index.ts от уязвимых и временных маршрутов. Все API должны быть защищены авторизацией и реализованы через модули.

⸻

Если готов — скажи, и я подготовлю промт для T3 — Валидация переменных окружения при запуске сервера.