Вот финальное **техническое задание для агента** по диагностике `TonBoost`-пакетов **на продакшене**, **без изменений кода**:

---

### 🔍 **Техническое задание: Финальная диагностика TonBoost**

#### 🎯 Цель:

Провести **точечную проверку логики начислений и списаний** по `TonBoost`-пакетам у пользователей `ID 25` и `ID 287`. Обнаружить причину, почему:

1. У некоторых участников **деньги списываются**, Boost-пакет **открывается**, но **средства возвращаются обратно на баланс**.
2. У некоторых пользователей Boost-пакет **открывается**, но **начисления не приходят**.
3. При этом **в БД видны все записи** о депозите и активации пакета, **но фарминг/начисления не происходят**.

---

### 📌 Что нужно проверить:

#### 🔹 1. **Фиксация депозита**

* Убедиться, что при покупке Boost-пакета:

  * Списанные TON корректно фиксируются в БД (таблица транзакций).
  * Проверить поле `amount`, `userId`, `type`, `status`, `isConfirmed`.
  * Найти запись по `userId: 25` и `userId: 287`.

#### 🔹 2. **Срабатывание логики Boost-активации**

* Проверить, была ли создана запись в таблице активных Boost-пакетов:

  * Таблица `tonBoostPackages`, `userBoosts`, или аналогичная.
  * Убедиться, что Boost пакет был активирован.
  * Поля: `startDate`, `endDate`, `status`, `boostRate`, `isActive`.

#### 🔹 3. **Планировщик начислений**

* Проверить планировщик, который отвечает за начисления Boost-пакетов:

  * Логи или БД таблицу, где видны начисления.
  * Убедиться, что по `userId: 25` и `userId: 287` он:

    * Начал начислять UNI после покупки.
    * Продолжает начислять регулярно (если Boost активен).
  * Посмотреть, нет ли ошибок в логах/очередях задач/кронах (например, `cron/tonBoost.ts`).

#### 🔹 4. **Повторный возврат средств**

* Проверить, **почему списанные средства возвращаются обратно**:

  * Посмотреть все действия системы после списания: не запускается ли откат или rollback.
  * Проверить, нет ли в коде или триггерах логики, которая отменяет транзакцию или ошибочно считает Boost неуспешным.
  * Проверить, не запускается ли проверка "недостаточно средств", из-за которой отменяется операция.

#### 🔹 5. **Кэширование / отложенные записи**

* Проверить, возможно ли, что кэш или асинхронные процессы мешают отображению:

  * Баланса (он откатывается).
  * Активности Boost'а.
  * Начислений по Boost.
* Убедиться, что **все данные отображаются с актуального источника**, а не устаревшего кэша.

---

### 📦 Конкретные кейсы для анализа:

| User ID | Поведение                                                                                                                                                       |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `25`    | Открыл Boost, списание прошло, **пакет активировался**, но **TON вернулись обратно**. Однако система считает Boost активным. Начисления **иногда не приходят**. |
| `287`   | Boost открыт, **начислений вообще нет**, хотя пакет отображается активным. Баланс не меняется.                                                                  |

---

### ❗️Важно:

* **Никаких изменений кода.**
* Только проверка через **БД**, **логи**, **планировщик**, **активные Boost-пакеты** и **транзакции**.
* Цель — **найти источник рассинхронизации** между:

  * списанием → активацией → начислением → отображением баланса.

---

🔁 **После проверки** предоставить:

* Конкретные причины поведения по `userId: 25` и `287`.
* Список, какие Boost-пакеты активны, и идут ли по ним начисления.
* Если начисления не идут — почему (планировщик, статус, ошибки).
* Если деньги возвращаются — какой процесс это делает и почему.

---

Готов отправить это как сообщение агенту или подготовить его в другом формате — скажи, как будет удобнее.
