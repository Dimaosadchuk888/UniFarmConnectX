Ты — профессиональный AI-разработчик. Твоя задача — провести финальный аудит по списку выявленных несоответствий перед продакшн-деплоем. Работай строго по пунктам, ничего не пропускай и не изменяй без причины. Вся работа должна быть выполнена локально, без побочных эффектов.

---

## ❗ ПЕРЕД НАЧАЛОМ РАБОТЫ:
- Убедись, что работаешь в production-ветке.
- Не удаляй и не изменяй ничего без предварительной проверки.
- Все правки только по указанным пунктам — не выходи за пределы задачи.

---

## 🧩 ТВОЯ ЗАДАЧА:

Провести финальную проверку и при необходимости внести улучшения по следующим пунктам:

### ⚠️ АУДИТ ПРОЕКТА UNIFARM CONNECT:

1. CORS-настройки  
   • Проверить конфигурацию CORS.  
   • Убедиться, что не все источники (`*`) разрешены в production.  
   • Разрешены только нужные домены (Telegram Mini App / Vercel Frontend).

2. Валидация входных данных  
   • Проверить, используется ли zod, Joi или class-validator на всех критичных маршрутах (`POST`, `PATCH`).  
   • Отметить маршруты без валидации.

3. Логирование (Production)  
   • Убедиться, что используются уровни логирования (`info`, warn, `error`) корректно.  
   • Не должно быть console.log в production.  
   • Желательно использовать pino, winston или другой structured logger.

4. Защита от SQL-инъекций  
   • Проверить все запросы к базе данных.  
   • Убедиться, что используются parameterized queries, sql``, drizzle или knex с безопасной вставкой значений.

5. Обработка ошибок  
   • Все контроллеры должны использовать централизованную errorHandler.  
   • Ошибки должны логироваться и иметь корректный формат ответа (`status`, message, `code`).

6. Мониторинг серверов  
   • Проверить, есть ли /api/v2/health или /healthcheck.  
   • Убедиться, что сервер логирует uptime или интегрирован с внешним мониторингом (например, Railway, Vercel, UptimeRobot).

7. Конфигурация .env  
   • Проверить, чтобы .env не содержал секретов в открытом виде.  
   • Все переменные должны быть вынесены в Secrets.  
   • Не должно быть закоммиченных .env файлов.

8. Валидация кода рефералов  
   • Проверить, что код соответствует формату (например, 6 символов, `буквы + цифры`).  
   • Убедиться, что в базе нет дублей.  
   • Добавить проверку на существование ref_code у пользователя.

9. Документация переменных окружения  
   • Проверить наличие файла env.example или раздела в README.  
   • Все ключевые переменные (`JWT_SECRET`, DATABASE_URL, `TON_API_KEY`) должны быть описаны.

10. Безопасность API конечных точек  
   • Проверить, что все защищённые маршруты требуют авторизации (`authMiddleware`, `isAuthenticated`).  
   • Маршруты wallet, missions, boosts, farming, referrals — должны быть доступны только авторизованным пользователям.

---

## 📄 ОТЧЁТ О РЕЗУЛЬТАТАХ:

В конце напиши чёткий список:

- ✅ Что было исправлено (по пунктам 1–10)
- 🧪 Что было проверено, но не требовало правок
- ⚠️ Что осталось без изменений (если есть замечания, но они не критичны)
- ❗Что заблокировало работу (если что-то невозможно реализовать без доступа)

---

## 🎯 ЦЕЛЬ:

Полностью убедиться в безопасности, стабильности и готовности проекта UniFarm Connect к продакшн-деплою. Устраняем все уязвимости и несоответствия — финальная фаза перед запуском.