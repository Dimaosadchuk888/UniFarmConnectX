Отлично. Вот чёткое **Техническое задание для исследования**, которое можно передать агенту **до любых изменений**:

---

## 🔍 ТЗ: Исследование источника дохода UniFarming (без изменений кода)

**Цель:**
Понять, откуда берётся база для расчёта дохода по UNI — из `deposit_amount` или из `balance_uni`. Исследовать всю цепочку начисления дохода в UNI Farming. Проверить, как это устроено в TON Boost для сравнения.

---

### 📌 ЧТО НУЖНО ИССЛЕДОВАТЬ:

#### 1. **Расчёт дохода в UniFarming**

* Где формируется `farming income` (указать конкретные файлы и функции)
* Какой параметр используется для расчёта: `deposit_amount` или `balance_uni`
* Что происходит, если `deposit_amount` отсутствует — происходит ли fallback на `balance_uni`

#### 2. **Источники данных**

* Где именно в базе хранится `uni_deposit_amount`
* Какие пользователи имеют `null` или `0` в этом поле
* Есть ли связи между отсутствием данных и начислением от баланса

#### 3. **Механизм накопления**

* Почему могут накапливаться периоды (например, 889 периодов по 5 минут)
* Есть ли защита от дублирующих планировщиков

#### 4. **TON Boost (для сравнения)**

* Откуда берется база начисления в TON Boost
* Есть ли аналогичная проблема: начисление не от активного участия, а от баланса

---

### 🧠 ЗАДАЧА АГЕНТА:

* Проследить **всю архитектурную цепочку расчёта**: от планировщика (scheduler) до базы и формирования транзакции.
* Указать **все места, где участвует `balance_uni`** в расчётах.
* Указать, **используется ли fallback логика**, и если да — где именно.
* Ничего **не менять в коде** — только анализ и отчёт.
* Сравнить механизм расчёта дохода с TON Boost.

---

### 📤 ОФОРМЛЕНИЕ ОТЧЕТА:

* Структурированный .md или .json отчёт
* Явно указать: какие функции, файлы, поля используются
* Указать конкретные user case, в которых начисления идут от баланса
* Пример хотя бы 2–3 пользователей с разными сценариями
* Сравнение с TON Boost (отдельным блоком)

---

Хочешь, я оформлю этот документ как файл и отправлю в виде Markdown-структуры?
