✅ PROMPT ДЛЯ АГЕНТА:

РЕГРЕССИОННАЯ ПРОВЕРКА СИСТЕМЫ (БЕЗ ИЗМЕНЕНИЙ КОДА)

⸻

🎯 Цель:

Провести полную регрессионную проверку всей системы UniFarm после последних обновлений и централизации.
Важно: никаких фиксов, патчей, редактирования кода или базы — только аудит и наблюдение.

⸻

❗ Условия:

🔒 Никакие файлы, модули, переменные или базы не изменять
🔒 Не выполнять никаких фиксов — даже если ошибка очевидна
📝 Ваша задача — собрать точную, детальную картину происходящего

⸻

🔍 Что нужно проверить:

🔹 1. TON Boost / UNI Boost
 • Активируются ли
 • Начисляется ли прибыль
 • Сохраняются ли транзакции
 • Баланс в UI и Supabase синхронизированы

🔹 2. Баланс
 • Растёт ли он при активных депозитах
 • Совпадают ли значения в UI и БД
 • Видны ли актуальные цифры в Supabase по balances

🔹 3. Транзакции
 • Создаются ли после покупки пакета
 • Имеют ли корректный тип (BOOST_PURCHASE, FARMING_DEPOSIT, и т.п.)
 • Привязаны ли к правильному user_id

🔹 4. Daily Bonus
 • Кнопка работает
 • Запрос уходит
 • Баланс увеличивается
 • Транзакция фиксируется

🔹 5. Рефералы
 • Работает генерация ссылок
 • Сохраняется ref_user_id
 • В таблице referrals появляется запись
 • Дерево отображается корректно

🔹 6. Подключение кошелька
 • Подключается ли Tonkeeper
 • Видит ли правильный адрес
 • Запоминается ли сессия

🔹 7. Farming
 • Активные депозиты работают
 • Баланс растёт
 • Начисления происходят

⸻

📤 Что нужно предоставить:

📑 1. Таблицу проверок:

| Модуль        | Проверка               | Статус | Комментарий                   |
|---------------|------------------------|--------|-------------------------------|
| TON Boost     | Баланс начисляется     | ✅     | Всё синхронизировано          |
| Daily Bonus   | Кнопка работает        | ❌     | Запрос идёт, но баланс не растёт |
| Referrals     | Дерево строится        | ✅     | Связь с user_id работает       |

📄 2. Отчёт:
 • Где выявлены баги
 • Какие user_id использовать для проверки
 • Есть ли расхождения между UI и БД
 • Какие модули находятся под риском, но пока работают

⸻

⏱ Время: ~1 час

📌 Работать в режиме preview, сверять всё с Supabase вручную