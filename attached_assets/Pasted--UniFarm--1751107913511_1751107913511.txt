✅ ФИНАЛЬНЫЙ ПРОМПТ ДЛЯ АГЕНТА: ЧИСТКА И ИСПРАВЛЕНИЕ СИСТЕМЫ UniFarm (С БЕЗОПАСНОСТЬЮ)

⸻

Ты должен выполнить полную очистку системы UniFarm от временных, дублирующих и обходных решений, при этом:
 • Не повредив рабочий код
 • Не нарушив текущую архитектуру
 • Сохранив возможность отката при необходимости

⸻

🔐 ОБЯЗАТЕЛЬНЫЕ УСЛОВИЯ:
 1. Перед удалением любого файла или модуля — обязательно выполни анализ содержимого:
 • Сравни логику с оригинальным компонентом (если есть).
 • Убедись, что оригинал способен заменить Fixed/V2 без потери функционала.
 2. Удалять ничего сразу нельзя. Сначала:
 • Переместить файл во временную папку: deprecated/_archive_pending_review/
 • Убедиться, что система продолжает работать только на оригинальных компонентах.
 • Если всё стабильно — только тогда разрешено удалить навсегда (после одобрения).

⸻

📋 ЧЕК-ЛИСТ: ПОЛНОЕ БЕЗОПАСНОЕ ИСПРАВЛЕНИЕ

⸻

1. 🧱 Дублирующие и обходные файлы
 • Проанализировать DailyBonusCardFixed.tsx → сравнить с DailyBonusCard.tsx, убедиться, что основной компонент работает корректно.
 • Аналогично: MissionsListFixed.tsx → проверить соответствие с MissionsList.tsx
 • Анализ server/dailyBonusFixed.ts → сравнить с server/dailyBonus.ts
 • Удалить только после анализа и переноса логики (если она полезна) в оригинал.
 • Переместить все временные файлы в deprecated/_archive_pending_review/ перед удалением.

⸻

2. ⚙️ Архитектура
 • Проверить, что Dashboard.tsx и Missions.tsx используют только оригинальные компоненты.
 • Удалить (или переписать) все импорты Fixed-версий.
 • Обновить документацию (если ссылки ведут на устаревшие пути).

⸻

3. 🔗 Реферальная система
 • Исправить в UserService ошибку с id > 0 → заменить на id >= 0 или адаптировать валидацию корректно.
 • Проверить, как ref_code извлекается и передается через UserContext.
 • Убедиться, что отображение на интерфейсе работает без костылей.
 • Проверить копирование, обработку пустых значений и localStorage.

⸻

4. 🎯 Бонусы, миссии, farming
 • Убедиться, что используется только /daily-bonus, а не dailyBonusFixed.
 • Проверить, что missions.service.ts возвращает актуальные 12 миссий.
 • Проверить, что Farming работает через UniFarmingCardWithErrorBoundary без дополнительных прослоек.

⸻

5. 🧹 Очистка кэша и обновлений
 • Внедрить cache-busting механизм (например, main.js?v=1.1.2)
 • Убедиться, что в index.html установлены правильные заголовки: Cache-Control: no-store
 • Проверить, используется ли Service Worker. Если да — проанализировать и удалить только после полной проверки.
 • Убедиться, что при полной перезагрузке и в режиме инкогнито всегда загружается актуальная версия кода.

⸻

6. 🛡 Контроль и стабильность
 • Подтвердить, что:
 • Реферальная система работает корректно
 • Все миссии отображаются
 • Daily bonus доступен
 • Кэш не мешает работе
 • Все обходные компоненты удалены или перемещены
 • В коде нет ни одного подключения к *Fixed.tsx, *V2.ts, temp, patch, legacy и т.п.
 • Интерфейс работает чисто и стабильно.

⸻

📦 ПАПКА БЕЗОПАСНОГО УДАЛЕНИЯ

Все временные, устаревшие или дублирующие файлы необходимо переместить во временную директорию:

/deprecated/_archive_pending_review/

Удалять из проекта — только после финальной проверки, тестирования и явного подтверждения.

⸻

✅ Финальный отчет должен включать:

✔ Исправлено:
- Удалены Fixed компоненты (после переноса логики)
- Исправлена валидация в UserService
- Очищено кеширование, внедрен cache-busting
- Удалены все временные импорты

✖ Осталось:
- (если что-то не удалось исправить, указать почему)


⸻