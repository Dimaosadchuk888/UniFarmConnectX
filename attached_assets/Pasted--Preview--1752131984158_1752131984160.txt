Отлично, система авторизации и Preview подтверждена как стабильная. Теперь можно переходить к **следующему шагу — анализу проблемы со списанием баланса при открытии UniFarming пакета**.

Вот чёткое **техническое задание для агента**, строго в рамках твоих указаний:

---

### ✅ **Техническое задание: Исследование проблемы отсутствия списания баланса при открытии UniFarming пакета**

**Цель:**
Понять, **почему при открытии UniFarming пакета не происходит списания средств с баланса**, несмотря на корректную авторизацию, отображение баланса и подключение WebSocket.

---

### 🔍 Что нужно сделать:

#### 1. **Исследование, а не изменение**

* Ничего **не редактировать и не фиксить** на своё усмотрение.
* Провести **исследование** в рамках существующего кода и архитектуры — **исключительно на чтение**.

#### 2. **Навигация через RoadMap**

* Использовать **RoadMap проекта** как основную навигацию по модулям.
* Найти **конкретный файл / функцию / endpoint**, который:

  * обрабатывает **открытие UniFarming пакета**
  * инициирует **списание средств** с баланса (UNI или TON)
  * обновляет **данные пользователя** или вызывает `updateUserBalance` (или аналогичное)

#### 3. **Фокус на модуле Wallet**

* Особое внимание на:

  * `modules/wallet/` — как место хранения и списания баланса
  * `modules/farming/` — логика открытия пакета
  * `modules/user/` — обновление баланса, если оно там реализовано
* Проверить `WalletService`, `FarmingService`, `startFarming`, `rewardCalculation.ts`

#### 4. **Выявить причину**

* Найти, **почему не вызывается логика списания** или она не доходит до `wallet`-модуля
* Указать, **где именно в коде должно было быть списание**, и **что блокирует или пропускает** этот шаг

---

### 📋 Ожидаемый результат:

Агент должен предоставить:

1. **Название и путь к основному файлу**, в котором **должно происходить списание**
2. **Описание причины**, почему баланс не списывается
3. **Вывод, что нужно будет поменять** (в отдельной задаче, не сейчас)
4. Подтверждение, что проблема **точно воспроизводится** при попытке открыть пакет

---

🔒 **Важно:**

* Не писать временные фиксы
* Не подставлять значения вручную
* Не изменять структуру данных или контроллеров
* Только глубокий аудит причины и точка, в которой **система должна списывать, но не делает этого**

---

Когда агент закончит, можно будет формировать отдельное задание на исправление — строго по результату исследования.
