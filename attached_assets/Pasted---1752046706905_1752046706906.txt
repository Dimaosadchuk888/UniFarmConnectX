Вот чёткая и полная формулировка **технического задания для агента**, которая обеспечит полный аудит Superbase и его синхронизацию с `roadmap.md` и логикой всей системы:

---

### 📌 **ТЕХНИЧЕСКОЕ ЗАДАНИЕ: Полный аудит Supabase и синхронизация с системой UniFarm**

---

#### 🎯 Цель:

Полностью **исследовать все таблицы и поля Supabase**, отразить их в `roadmap.md` как активную структуру данных, и подготовить базу для синхронизации всей системы с этой структурой, чтобы исключить любые ошибки, несоответствия, пробелы или дубликаты в логике данных.

---

#### 🧩 Что нужно сделать:

1. 🔍 **Изучить все таблицы в Supabase:**

   * Получить полный список всех таблиц, включая временные, технические, служебные и системные
   * По каждой таблице извлечь:

     * Название таблицы
     * Полный список всех колонок (имя, тип, обязательность, значения по умолчанию, ограничения)
     * Все связи (foreign keys)
     * Индексы

2. 🗂 **Создать новый раздел в `roadmap.md`:**
   Название: `📚 Supabase Schema`
   В этом разделе по каждому модулю отразить:

   * Название таблицы
   * Назначение
   * Описание каждого поля
   * Какие endpoints или части бизнес-логики используют эти поля

3. ⚙️ **Сравнить Supabase поля с системой:**

   * Проверить, используются ли все эти поля в коде
   * Выявить:

     * Поля, которые есть в Supabase, но не используются в коде
     * Поля, которые используются в коде, но отсутствуют в Supabase
     * Несовпадения типов данных, названий, логики валидации
     * Критические поля (например, для вывода средств, транзакций, баланса), которых может не хватать

4. 🧾 **Составить отчет `SUPABASE_AUDIT_REPORT.md`:**

   * Список всех таблиц с полями
   * Статус использования каждого поля: ✅ используется, ❌ не используется, ⚠️ несовпадение
   * Список несоответствий, ошибок, дубликатов
   * Список рекомендаций (что добавить, переименовать, удалить)
   * План синхронизации всей системы с финальной схемой

---

#### 🛡 Обязательные условия:

* Категорически запрещена поверхностная проверка — необходимо проверять каждый столбец и его соответствие коду
* Перед фиксацией в `roadmap.md` — **обязательно убедиться, что поле действительно активно используется**
* Все поля, задействованные в бизнес-логике (например, `balance`, `referralCode`, `tonPackageId`, `missionReward`, `walletAddress`, `withdrawStatus` и т.д.) — должны быть выделены как **критические**
* Итоговая структура `Supabase` должна стать **единственным источником истины** для всей системы

---

#### 📤 Выходные файлы:

1. ✅ `roadmap.md` — с новым разделом `📚 Supabase Schema`, структурировано по модулям
2. ✅ `SUPABASE_AUDIT_REPORT.md` — технический аудит текущей структуры с рекомендациями
3. ✅ `TODO_SUPABASE_SYNC.md` — список задач по синхронизации логики с базой

---

Готов выдать шаблон `SUPABASE_AUDIT_REPORT.md` и формат раздела в `roadmap.md`, если нужно.
