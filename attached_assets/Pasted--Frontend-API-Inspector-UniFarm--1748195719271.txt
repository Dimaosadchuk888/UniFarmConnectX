🧠 РОЛЬ АГЕНТА:

Ты выступаешь как Frontend API Inspector для проекта UniFarm. Твоя задача — провести глубокий аудит всех API-запросов на frontend-части и убедиться, что:
 • Приложение использует только актуальные и поддерживаемые маршруты
 • Все вызовы синхронизированы с backend логикой
 • Нет обращения к временным или устаревшим endpoint’ам (например, /api/users/generate-refcode)
 • Отсутствует hardcoded логика, fallback без валидации или повторно дублированные функции

⸻

🎯 ТЕХНИЧЕСКОЕ ЗАДАНИЕ:

1. ПРОВЕРКА ВСЕХ API-ЗАПРОСОВ НА FRONTEND
 • Выполни поиск по ключевым шаблонам:
 • fetch('/api/
 • axios.get/post('/api/
 • correctApiRequest(, fetchWrapper, queryFn, queryKey
 • Включи проверку хуков и утилит: useQuery, useMutation, referralUtils, walletUtils, userUtils, boostUtils, telegramService, sessionRestoreService, userService, и других

2. СРАВНЕНИЕ С АКТУАЛЬНЫМИ МАРШРУТАМИ BACKEND
 • Использовать только маршруты из /api/v2/ (TON Farming, Wallet, Referrals, Boosts, Missions, Sessions)
 • Подтвердить соответствие каждому фронтенд-запросу его backend-контроллеру

3. ВЫЯВИТЬ ВСЕ НЕПРАВИЛЬНЫЕ ИЛИ УСТАРЕВШИЕ ВЫЗОВЫ
 • Примеры, которые нужно устранить:
 • /api/users/generate-refcode — ❌
 • /api/referral/code (v1) — ❌
 • /api/wallet/info — ❌
 • /api/boosts/list — ❌

4. ПРОВЕРКА ВСЕХ ЭКРАНОВ ПРИЛОЖЕНИЯ
 • Стартовая инициализация
 • Генерация партнёрской ссылки
 • TON Farming экран
 • UNI Farming экран
 • Boost Packages
 • Wallet / TON Connect
 • Missions / Tasks
 • Session Restore
 • Статистика и Referral Tree
 • Telegram Mini App интеграция

5. ЛОГИРОВАНИЕ

Добавь диагностические логи:
 • [FRONTEND API] Used route: ...
 • [FRONTEND FALLBACK] Generated temp_ref_code — NOT saved
 • [API MISMATCH] Called v1 endpoint while v2 available: ...

⸻

✅ ПОСЛЕ ПРОВЕРКИ ОБЯЗАТЕЛЬНО:
 • Подтвердить, что все маршруты соответствуют v2
 • Нет ни одного вызова временного API (например, /api/users/generate-refcode)
 • Все ref_code, wallet, boost, farming запросы обращаются к правильным v2 endpoint’ам
 • Ни один запрос не приводит к генерации временного ответа
 • Приложение не использует hardcoded логику обхода API

⸻

ОГРАНИЧЕНИЯ:
 • ❌ Не изменять backend-логику
 • ❌ Не переписывать структуру приложения
 • ✅ Только проверка и точечная замена вызовов API
 • ✅ Все изменения — только в рамках актуализации маршрутов
 • ✅ После исправлений — ОБЯЗАТЕЛЬНА ПОВТОРНАЯ САМООЦЕНКА

⸻

Цель: добиться 100% точности между frontend и backend, исключить временные обходы и гарантировать стабильную работу всех API.