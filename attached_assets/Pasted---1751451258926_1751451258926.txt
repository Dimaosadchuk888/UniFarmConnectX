Вот тебе готовый точный промпт для агента, чтобы он провёл исследование без догадок — и чётко выяснил, какая страница сейчас реально подключена через Replit Preview, и какой user_id используется системой.

⸻

✅ Промпт для агента: Диагностика подключения Replit Preview

🔧 Цель:
Определить, какая именно страница (какой user_id) подключена к базе данных через Replit Preview.
Убедиться, что это не user_id = 1, 50 или случайный ID. Мы готовим рабочую боевую среду, и нам нужно быть уверенными, что подключен правильный пользователь (в идеале — user_id = 48).

⸻

🔍 Что нужно проверить:
 1. Открой консоль разработчика в браузере (F12 → Console → Application → LocalStorage / Cookies):
 • Посмотри JWT токен:
 • Какой user_id?
 • Какой telegram_id?
 2. Проверь useUser() или userContext:
 • Какой user_id возвращается в системе?
 • Это совпадает с JWT?
 3. Открой WalletService, FarmingService, BoostService:
 • Какой user_id используется в запросах (на чтение и на запись)?
 4. Открой базу данных и найди пользователя по текущему user_id:
 • Существует ли такой пользователь?
 • Какие у него балансы (TON / UNI)?
 • Совпадают ли с тем, что ты видишь в приложении?
 5. Проверь, не используется ли fallback-режим (демо, telegram_id = 43 и т.д.)

⸻

📌 Ответить нужно строго по шаблону:

🔍 Результаты проверки:

• JWT токен:
   user_id: _______
   telegram_id:й точный • userContext → user_id:промпт дл
• Используемый ID в WalletService / Farming / Boost: ___

• ID из базы: существует / не существует
   - Баланс UNI: __
   - Баланс TON: точный 
• Используется fallback/demo режим? (да/нет): __

🟩 Подключена страница user_id =я агент(точно)


⸻

⚠️ Важно:
 • Не додумывай — проверь всё по коду и логам
 • Не нужно менять ничего — только проверить и доложить
 • Нам нужно зафиксировать одного работающего юзера и отключить всех остальных

⸻

📦 После этого мы дадим инструкции, какой user_id подключать финально и как его очистить, чтобы всё работало в боевом режиме.

Хорошо? Отправь ему — он должен будет просто заполнить ответ по шаблону.