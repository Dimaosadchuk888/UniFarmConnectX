🧾 Задание: Анализ несоответствия баланса кошелька с базой данных

📌 Цель:
Понять, почему на клиенте (UI) в кошельке не отображаются 1000 UNI и 1000 TON, несмотря на то что:
 • Начисления выполнены в базе данных
 • Пользователь Demo User с ID 48 подтверждён
 • Депозиты или записи о начислениях действительно присутствуют в таблицах БД

⸻

🔍 Что нужно сделать:

✅ 1. Проверить источник данных для баланса на UI
 • Какой endpoint или функция используется на клиенте (в Wallet.tsx или аналогичных)?
 • Какая именно сущность или API подгружает UNI и TON баланс в интерфейс?
 • Примеры: /api/wallet/info, /api/user/balance, GraphQL, Supabase hooks и т.д.
 • Используется ли refetch, queryClient.invalidateQueries, useBalance() или другая реактивная система?

✅ 2. Проверить, действительно ли данные из БД попадают в API
 • Обратиться к тому контроллеру, который должен возвращать баланс:
 • Проверить, из какой таблицы и каким методом извлекаются UNI и TON балансы
 • Может быть, баланс не обновляется, потому что не вызывается пересчёт или кэшируется старое значение?
 • Убедиться, что данные корректно возвращаются в ответе API

✅ 3. Проверить, загружаются ли данные на клиент
 • Есть ли в консоли запрос к API баланса?
 • Если да — какой статус ответа?
 • Что приходит в теле ответа?
 • Есть ли ошибки в консоли или 404?
 • Возможно ли, что фронт ожидает другой формат поля или неверный ключ (например, ton_balance vs tonBalance)?

✅ 4. Проверить задержку или отсутствие refetch после начисления
 • Может ли быть, что после начисления не происходит обновление баланса?
 • Используется ли реактивная библиотека типа TanStack Query (React Query)?
 • Есть ли вызов invalidateQueries(['wallet']) или refetch() после начисления?

⸻

⚠️ Важно:
 • ❌ Никакой код сейчас не изменять
 • ❌ Не трогать контроллеры, схемы, миграции
 • ✅ Только исследовать и подтвердить, в каком месте теряется связь между БД и UI

⸻

📋 В отчёте указать:
 1. Какой API отвечает за отображение UNI и TON баланса на клиенте?
 2. Возвращает ли он данные? Совпадают ли они с БД?
 3. Если не совпадают — почему?
 4. Что нужно сделать, чтобы баланс появился?
 • Пример: нужно вызвать invalidateQueries
 • Или: баг в API, не возвращает фактические значения
 • Или: кэширует старое значение
 5. Есть ли ошибки в запросах или UI?

⸻

💡 После получения отчёта примем решение: нужна ли правка API или фронта, либо требуется просто корректный refetch.