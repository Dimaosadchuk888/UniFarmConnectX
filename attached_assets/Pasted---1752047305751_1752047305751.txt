Отлично, ты проделал огромную и важную работу. Теперь следующий шаг — сформулировать **техническое задание** для агента, чтобы он **довёл базу данных Supabase до 100% готовности**, согласно выявленным проблемам.

Вот точное и конкретное ТЗ, которое ты можешь выдать агенту:

---

### 📌 ТЕХНИЧЕСКОЕ ЗАДАНИЕ: Завершение настройки Supabase для UniFarm

---

#### 🎯 Цель:

Привести структуру базы данных Supabase **в полное соответствие с бизнес-логикой UniFarm** и `roadmap.md`. Обеспечить наличие всех необходимых таблиц, полей и связей для функционирования всех модулей системы.

---

### ✅ ЧТО НУЖНО СДЕЛАТЬ:

#### 1. 🏗 СОЗДАНИЕ НЕДОСТАЮЩИХ ТАБЛИЦ (по TODO\_SUPABASE\_SYNC.md):

Создать следующие таблицы:

* `user_sessions`
* `user_missions`
* `daily_bonus_logs`
* `airdrops`

> Использовать SQL-скрипты, зафиксированные в `TODO_SUPABASE_SYNC.md`. Убедиться, что каждая таблица имеет все нужные поля, типы данных и связи (foreign keys), соответствующие `roadmap.md` и текущей бизнес-логике.

---

#### 2. 📥 ЗАПОЛНЕНИЕ ПУСТЫХ ТАБЛИЦ:

Проверить и заполнить базовыми данными:

* `referrals`
* `farming_sessions`
* `boost_purchases`
* `missions`

> Примеры и шаблоны содержатся в `SUPABASE_AUDIT_REPORT.md` или можно сгенерировать миграции через систему. Обязательно проверить: поля `referrer_id`, `mission_type`, `boost_id`, `farming_start`.

---

#### 3. 🧪 ПРОВЕРКА СООТВЕТСТВИЯ ПОЛЕЙ:

* Исправить **некорректные поля**:

  * `referred_by` → `referrer_id` (в таблице `users`)
* Проверить:

  * Типы данных (например, `float`, `timestamp`, `boolean`)
  * Обязательность (`NOT NULL`)
  * Значения по умолчанию
  * Все foreign keys и связи между таблицами

---

#### 4. 🔄 СИНХРОНИЗАЦИЯ СИСТЕМЫ С SUPABASE:

* Убедиться, что **все используемые в коде поля присутствуют в Supabase**
* Если какие-либо поля используются в коде, но их нет в БД — срочно добавить
* Проверить, что все поля для:

  * вывода средств (`withdraw_requests`)
  * начислений (`farming_sessions`)
  * партнёрки (`referrals`)
  * бонусов (`daily_bonus_logs`)
  * миссий (`user_missions`)
    — **присутствуют, активны и валидны**

---

#### 📂 ОБНОВЛЕНИЕ ДОКУМЕНТАЦИИ:

После всех изменений:

* Обновить раздел `📚 Supabase Schema` в `ROADMAP.md`
* Обновить `SUPABASE_AUDIT_REPORT.md` и выделить, что достигнуто 100%
* Закрыть задачи в `TODO_SUPABASE_SYNC.md`

---

#### 🔒 ОБЯЗАТЕЛЬНЫЕ УСЛОВИЯ:

* ❗ Нельзя вносить поля без проверки — всё должно соответствовать бизнес-логике
* ❗ Никаких дубликатов, временных таблиц или неиспользуемых колонок
* ❗ Все foreign key связи должны быть проверены и установлены
* ❗ Любые SQL-операции должны логироваться в Git (или фиксироваться как миграции)

---

### 📈 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:

* Supabase содержит **100% таблиц и полей**, нужных для системы UniFarm
* Нет пустых или лишних таблиц
* Все данные синхронизированы с кодом, API и логикой
* Документация в `ROADMAP.md` актуальна и проверена

---

Хочешь — могу сформировать шаблон финального `Supabase Schema` блока в `ROADMAP.md`, если нужно наглядно отразить.
