Отлично, вот промпт/техническое задание для агента с акцентом на пошаговую диагностику и восстановление отображения заданий на странице missions.nav (Задания):

⸻

🧾 Техническое задание: Анализ и восстановление отображения заданий на странице missions.nav

📌 Цель:
Понять, почему на странице не отображаются карточки с заданиями, и корректно восстановить вывод реальных задач, если они есть в системе.

⸻

🔍 🔧 Этап 1: Диагностика

Перед любыми правками — провести анализ текущего состояния:
 1. Проверить компонент, отвечающий за отображение заданий (например: MissionsPage, MissionsList, TasksCard, Missions.tsx или аналог).
 2. Определить источник данных:
 • Из какого API загружаются задания? (например: /api/v2/missions/active, /api/missions, /api/tasks)
 • Какая бизнес-логика фильтрует или ограничивает задания?
 3. Проверить ответ API:
 • ⚠️ Есть ли реальные задания в ответе?
 • ⚠️ Возвращается ли [ ] (пустой массив) или null?
 • ⚠️ Нет ли ошибок 404, 500 или Unauthorized?

⸻

🔧 Этап 2: Восстановление карточек с заданиями

Если данные существуют, но не отображаются — восстановить отображение:
 1. Настроить корректную интеграцию с API:
 • Убедиться, что компоненты получают реальные задания (а не фиктивное “Заданий пока нет”)
 2. Отобразить задания карточками:
 • Название задания
 • Награда (например: UNI, XP и т.п.)
 • Кнопка или статус (“Выполнить”, “Получено”, “Недоступно”)
 3. Проверить фильтры и условия:
 • Задания не должны скрываться из-за isCompleted, isAvailable, isVisible, и т.п., если они активны
 • Убедиться, что условие tasks.length === 0 отрабатывает корректно

⸻

⚠️ Важно:
 • ❌ Никаких временных карточек или тестовых заданий вручную
 • ❌ Не менять API, если он работает
 • ✅ Только отобразить то, что уже есть в системе
 • ✅ Всё должно работать на реальных данных

⸻

📝 После выполнения:

Попросить агента подтвердить:
 • Какой API используется и какие ответы он возвращает
 • Есть ли реальные задания в базе
 • Почему изначально не отображались карточки
 • Какие изменения были внесены
 • Отображаются ли карточки с заданиями сейчас ✅