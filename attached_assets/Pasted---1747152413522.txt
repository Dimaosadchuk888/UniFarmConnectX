Вот чёткое ТЗ с жёсткими ограничениями и рамками задачи:

---

### 📌 **Техническое задание: Настройка корректного отображения реферальных вознаграждений в истории транзакций кошелька**

1. **Цель:**
   Обеспечить корректное отображение реферальных вознаграждений в компоненте истории транзакций кошелька, аналогично тому, как сейчас отображаются фарминг-награды.

2. **Важно!**
   🚨 **Категорически запрещается:**

* Вносить какие-либо изменения в бизнес-логику начисления вознаграждений.
* Изменять или удалять существующие данные в базе данных.
* Создавать новые API или изменять логику других компонентов проекта.
* Работать с неоригинальными файлами или создавать альтернативные версии компонентов.

**Разрешается работать ТОЛЬКО:**

* С существующими API-запросами и контроллерами.
* С компонентом `TransactionHistory` и связанными с ним оригинальными файлами.
* С существующими полями и типами данных в базе данных.

---

3. **Требования к выполнению задачи:**

* ✅ Убедиться, что API-запрос `/api/transactions` корректно возвращает данные о транзакциях типа **"referral\_reward"** или аналогичных, если они есть в базе.
* ✅ Если транзакции реферальных вознаграждений существуют в БД, но не отображаются — доработать фильтрацию и отображение в компоненте `TransactionHistory`.
* ✅ В случае отсутствия нужных типов транзакций в БД — НЕ ВНОСИТЬ данные вручную и НЕ менять логику начисления, а сообщить об этом в итоговом отчете.
* ✅ Реализовать визуальное отображение реферальных транзакций с понятной меткой (например, "Реферальное вознаграждение") и соответствующей валютой (UNI или TON).
* ✅ В случае, если в данных API отсутствует поле, необходимое для корректного отображения типа транзакции, оформить это в отдельном предложении и не вносить правки самостоятельно.

---

4. **Отчет по выполнению задачи должен включать:**

* Перечень выполненных действий.
* Список типов транзакций, которые успешно отображаются после изменений.
* Подтверждение, что в коде и данных не было внесено ничего сверх текущей задачи.
* При необходимости — список предложений, что требуется для полного решения проблемы (например, если данные не приходят из API).

---

Нужно добавить шаблон для отчёта или оставить на усмотрение исполнителя? 😊
