(роль: Ведущий системный разработчик/рефактор UniFarm)

⸻

🎯 Твоя задача: провести полную стабилизацию проекта UniFarm. Вступи в роль ведущего разработчика уровня CTO.

Проект содержит критические ошибки и не может быть выведен в продакшн в текущем виде. Твоя миссия — поэтапно устранить все критичные проблемы и привести проект к стабильному продакшену.

⸻

❌ КРИТИЧЕСКИЕ ПРОБЛЕМЫ, КОТОРЫЕ НУЖНО ИСПРАВИТЬ

1. Telegram интеграция не работает:
 • initData от Telegram WebApp не обрабатывается
 • Пользователи не инициализируются через Telegram
 • guest_id используется как fallback — нужно устранить
 • Не создаётся ref_code и не привязывается к telegram_id

2. Hardcoded данные в API:
 • /api/v2/farming, /api/v2/missions, /wallet и другие отдают фейковые данные
 • Надо заменить все мок-данные на реальные запросы из Neon DB

3. Дублированные файлы:
 • В проекте много дубликатов сервисов, контроллеров и маршрутов
 • Особенно дубли routes, controllers, boost, farming, missions
 • Нужно провести консолидацию: оставить только рабочие версии и удалить лишнее

4. Отсутствуют ключевые endpoints:
 • /api/v2/missions/complete, /api/v2/boost-packages/:id/buy, /api/v2/user/info — отсутствуют или заглушки
 • Не реализована логика отправки миссий, покупки бустов, выдачи бонусов

5. Фейковые данные:
 • Баланс всегда 0.000
 • Фарминг начисляется по рандому, не по настоящей логике
 • Миссии не проверяются, а просто возвращают готовый список

⸻

✅ ПЛАН СТАБИЛИЗАЦИИ

📍 Приоритет 1:
📌 Реализовать корректную обработку initData Telegram WebApp
📌 Привязать telegram_id, username, ref_code к пользователю
📌 Проверить регистрацию, авторизацию, генерацию и отображение ref_code
📌 Убрать fallback guest_id

📍 Приоритет 2:
📌 Заменить все hardcoded данные на реальные SQL-запросы к Neon DB
📌 Проверить /missions, /wallet, /farming, /boost
📌 Сделать базовые SELECT-запросы из таблиц missions, users, wallet, boost_purchases

📍 Приоритет 3:
📌 Найти и удалить все дублированные контроллеры, сервисы и роуты
📌 Оставить только одну рабочую версию каждой сущности
📌 Проверить routes.ts и routes-new.ts — объединить без потерь

📍 Приоритет 4:
📌 Добавить недостающие endpoint’ы:
 • /api/v2/missions/complete
 • /api/v2/boost-packages/:id/buy
 • /api/v2/user/info
📌 Реализовать всю недостающую логику по существующим таблицам

📍 Приоритет 5:
📌 Провести end-to-end тестирование всех маршрутов и бизнес-логики
📌 Проверить, что пользователь:
 • может зайти через Telegram
 • получает ref_code
 • видит баланс
 • запускает фарминг
 • выполняет миссии
📌 Проверить admin_bot и связи с базой

⸻

🧠 ПРАВИЛА И ВАЖНЫЕ УСЛОВИЯ:
 • Не создавай новые дубли. Всё должно быть централизовано.
 • Не пиши мок-данные. Всё должно быть из базы данных.
 • Каждое исправление — отдельно. Делай отчёт после каждого.
 • Проверяй результат самостоятельно: не переходи к следующему шагу, пока текущий не работает.

⸻

💡 Если готов — начинай с Приоритета 1: Telegram интеграция + initData + ref_code.