Диагностика продакшн-сборки UniFarm и Telegram Mini App

Цель:
Провести полную техническую диагностику проекта UniFarm, чтобы установить, почему приложение работает в режиме разработки, но не работает в продакшн, выдавая пустой или тёмно-синий экран.
Также необходимо выяснить, почему Telegram Mini App не загружается, несмотря на наличие корректного домена, секретов и конфигураций.

⸻

🔍 ЧТО НУЖНО СДЕЛАТЬ (ПОДРОБНО):

🔹 1. Проверить логи и поведение в продакшн:
 • Открыть проект по адресу https://uni-farm-connect-x-alinabndrnk99.replit.app/
 • Посмотреть ошибки в консоли браузера
 • Посмотреть сетевые ошибки (Network → Status 404/500/ERR)
 • Зафиксировать любые JS-ошибки, ошибки загрузки ресурсов, зависаний

⸻

🔹 2. Сравнить dev и prod-сборку:
 • Убедиться, что npm run dev отображает корректный интерфейс
 • Выполнить npm run build → npm run preview и проверить результат
 • Найти отличия в:
 • логике инициализации
 • работе Supabase / auth
 • путях до статики или index.html
 • env-переменных (особенно VITE_)

⸻

🔹 3. Проверить Mini App в Telegram:
 • Зайти через Telegram по ссылке вида:

https://t.me/UniFarming_Bot/UniFarm


 • Посмотреть:
 • запускается ли WebApp?
 • есть ли ошибки в консоли Telegram (через tg://webview)
 • есть ли ошибки CORS, CSP, window.Telegram.WebApp инициализации

⸻

🔹 4. Проверить переменные окружения:
 • Убедиться, что в продакшн-среде корректно читаются переменные:
 • APP_DOMAIN
 • VITE_TELEGRAM_WEBAPP_NAME
 • VITE_TELEGRAM_BOT_USERNAME
 • SUPABASE_KEY, SUPABASE_URL
 • Убедиться, что они присутствуют во время сборки
 • Проверить, что import.meta.env.VITE_... доступен в продакшне

⸻

🔹 5. Проверить vite.config.ts и base path:
 • Проверь, есть ли настройка base: '/' или иная
 • Убедись, что Vite не срезает пути при сборке (например, /assets)

⸻

🔹 6. Проверить Telegram WebApp requirements:
 • Убедись, что:
 • сайт открывается по HTTPS
 • Content-Security-Policy не блокирует Telegram
 • CORS разрешает домен https://t.me
 • WebApp запускается в <html> без X-Frame-Options: DENY

⸻

🔹 7. Дополнительно:
 • Проверить наличие 404.html, если SPA
 • Проверить, не происходит ли сбой на hydration стадии (React/Vue)
 • Убедиться, что после npm run build в dist/ создаются все нужные файлы

⸻

❌ НЕ ДОПУСКАЕТСЯ:
 • Нельзя вносить изменения в код, пока причины не зафиксированы
 • Нельзя пересобирать проект до завершения диагностики
 • Нельзя менять конфиги (vite.config.ts, package.json) до отчёта

⸻

📋 ОТЧЁТ ПОСЛЕ ДИАГНОСТИКИ:
 • Таблица: Ошибка / Модуль / Причина
 • Список обнаруженных проблем
 • Явные расхождения между dev и prod
 • Выводы: одна ли проблема или их несколько