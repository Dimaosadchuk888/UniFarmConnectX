🟥 Техзадание T8 — Добавить отсутствующие маршруты API

Формат: золотой промпт | Только реализация указанных маршрутов

⸻

🎯 Цель:

Добавить три отсутствующих API-маршрута в систему UniFarm согласно финальному аудиту. Эти маршруты обязательны для production.

⸻

📌 Контекст:

Во время финальной проверки были выявлены отсутствующие, но заявленные маршруты:
 1. GET /api/v2/me
 2. GET /api/v2/farming/history
 3. POST /api/v2/airdrop/register

⸻

🔧 Описание каждого маршрута:

1. GET /api/v2/me
 • Описание: Возвращает краткую информацию о текущем пользователе
 • Ответ: { user_id, telegram_id, username, ref_code, balance, level }
 • Требуется Telegram авторизация
 • Источник данных: Таблица users (Neon DB)

2. GET /api/v2/farming/history
 • Описание: Возвращает историю начислений от фарминга (TON и UNI)
 • Ответ: Массив объектов: { amount, source, timestamp }
 • Требуется Telegram авторизация
 • Источник данных: Таблица transactions, фильтр по type = 'farming_reward'

3. POST /api/v2/airdrop/register
 • Описание: Регистрирует пользователя в airdrop-программе
 • Параметры: { telegram_id }
 • Защита: JWT или Telegram initData (по текущей логике)
 • Повторная регистрация запрещена
 • Источник данных: Таблица airdrop_participants

⸻

🧩 Разрешено:
 • Работать только с этими тремя маршрутами
 • Использовать существующие слои: controller → service → model
 • Добавлять недостающие файлы в рамках текущего модуля, если они отсутствуют

⸻

🛑 Запрещено:
 • Изменять другие маршруты
 • Рефакторить связанные модули
 • Вносить правки в архитектуру проекта
 • Использовать заглушки (все данные должны идти из БД)

⸻

✅ Ожидаемый результат:
 • Все 3 маршрута работают на /api/v2/...
 • Возвращают реальные данные из базы Neon
 • Защищены авторизацией
 • Обработаны ошибки (напр. пользователь не найден, повторная регистрация и т.д.)

⸻

🧪 Финальная проверка:
 • GET /api/v2/me возвращает корректный профайл
 • GET /api/v2/farming/history показывает реальную историю наград
 • POST /api/v2/airdrop/register записывает участника и блокирует повтор

⸻