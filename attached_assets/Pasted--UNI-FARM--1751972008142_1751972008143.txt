🔬 ЦЕЛЬ: СИНХРОНИЗАЦИЯ БАЗЫ ДАННЫХ UNI FARM С СИСТЕМОЙ

📌 Почему это важно:
 • Сейчас система вызывает поля, которых нет → ошибки в рантайме.
 • В базе лежат поля, которые нигде не используются → мусор, усложнение поддержки.
 • Без ясной структуры будет невозможно гарантировать стабильность и масштабируемость.

⸻

✅ ЧТО МЫ СЕЙЧАС ДОЛЖНЫ СДЕЛАТЬ:

ШАГ 1: ПОЛНЫЙ СПИСОК ВСЕХ ТАБЛИЦ В БД

Получаем список всех таблиц, например:
 • users
 • transactions
 • uni_farming_deposits
 • daily_bonus
 • missions
 • referrals
 • ton_boosts
 • и т.д.

ШАГ 2: АНАЛИЗ СТРУКТУРЫ КАЖДОЙ ТАБЛИЦЫ

Для каждой таблицы — выписать все поля + краткое описание, что они означают (если есть документация — хорошо, если нет — делаем reverse-инжиниринг из кода).

Пример:

Таблица users:
- id (PK) ✅ используется
- telegram_id ✅ используется
- username ✅ используется
- balance_uni ✅ используется
- balance_ton ✅ используется
- referral_code ✅ используется
- ton_boost_package ❌ не используется (возможно устарело)
- referral_income_total ❌ нигде не подсчитывается
- farming_total ❌ нигде не отображается

→ Так для всех таблиц, включая поля Supabase (created_at, updated_at).

⸻

ШАГ 3: СОПОСТАВЛЕНИЕ С ФРОНТЕНД/БЭКЕНД КОДОМ
 • Пройти по каждому service.ts, controller.ts, utils, model, hook, context и выявить:
 • Какие поля используются
 • Какие поля ожидаются от API
 • Какие поля из базы реально дергаются

📎 Если где-то запрашивается user.farming_balance, а такого поля нет в Supabase — фиксируем как ошибку.

⸻

ШАГ 4: КЛАССИФИКАЦИЯ ПОЛЕЙ

Статус Значение
✅ Обязательное Поле используется в коде (frontend или backend) и важно для логики
⚠️ Условно полезное Поле нигде не используется, но может использоваться в будущем
❌ Лишнее/мертвое Поле не используется нигде и дублирует другие


⸻

📋 ИТОГОВЫЙ ОТЧЕТ ДОЛЖЕН СОДЕРЖАТЬ:

Пример формата:

🧩 Таблица: users

| Поле                  | Используется? | Где используется                            | Комментарий                                  |
|-----------------------|---------------|---------------------------------------------|----------------------------------------------|
| id                    | ✅            | auth, context, middleware                    | Primary Key                                  |
| telegram_id           | ✅            | /auth/telegram                              | используется при создании и авторизации      |
| username              | ✅            | profile.tsx, /profile                       | отображается на UI                           |
| balance_uni           | ✅            | BalanceManager, farming, bonus              | основная валюта                              |
| balance_ton           | ✅            | boost.ts                                    | списание при Boost                           |
| ton_boost_package     | ❌            | НЕ используется                             | возможно устарело                            |
| referral_code         | ✅            | /referrals/stats, /referral/link            | отображается и используется                  |
| total_income_uni      | ❌            | НЕ используется                             | не ведется подсчет                           |
| farming_total_deposit | ⚠️            | потенциально для аналитики                  | не задействовано на UI                       |

...

Такой же разбор — по всем таблицам, в том числе:
 • transactions
 • uni_farming_deposits
 • daily_bonus
 • missions
 • referrals
 • ton_boosts
 • wallet_history (если есть)
 • logs (если есть)

⸻

🔧 ДАЛЬНЕЙШИЕ ДЕЙСТВИЯ ПОСЛЕ ОТЧЕТА:
 1. Удалить или архивировать ❌ лишние поля
 2. Добавить недостающие поля, если они вызываются в коде (и вызывают ошибки)
 3. Прописать строгую схему:
 • Какие поля обязательны
 • Какие — опциональны
 • Какие — служебные
 4. Внедрить механизм валидации DTO / schema (например, через Zod или Yup, если используется TS/React)
 5. Добавить автоматическую миграцию (если используется Supabase CLI / SQL миграции)

⸻

🎯 ЦЕЛЬ: ПОЛНАЯ СИНХРОНИЗАЦИЯ СИСТЕМЫ С БАЗОЙ