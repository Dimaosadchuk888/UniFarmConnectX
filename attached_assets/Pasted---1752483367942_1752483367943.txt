Вот чётко оформленное **техническое задание для агента**, с учётом твоих требований и того, что это **этап исследования**, без изменений кода:

---

### 🧪 ЗАДАЧА: Исследование отображения данных в карточке **TON Farming**

**Цель:** выяснить, почему карточка **TON Farming** на вкладке "Фарминг" не отображает корректные значения, в частности:

---

### 📋 ЧТО НУЖНО ПРОВЕРИТЬ:

1. **Общая сумма инвестированных TON**

   * 🔍 Проверь, подключено ли к компоненту поле `farming_balance` или аналогичное поле из таблицы `ton_farming_data`
   * 📌 Это значение должно суммировать все активные депозиты пользователя
   * ⚠️ Сейчас отображается `0` — нужно выяснить **почему**: данные не приходят из API, неправильно передаются в компонент, либо вовсе не запрашиваются

2. **Доход в сутки и в секунду**

   * 🧮 Проверь соответствие между:

     * `farming_balance` пользователя
     * `farming_rate` (дневной процент доходности)
     * расчетами UI: доход в сутки = `farming_balance * rate`, в секунду = `в сутки / 86400`
   * ✅ Убедись, что формула работает, и данные действительно приходят из API, а не захардкожены
   * 🔁 Сравни с **фактическими начислениями в транзакциях** за последние 5 минут

3. **Источник данных**

   * 📦 Определи, из какого **API** или контекста берутся данные (`/api/boosts/user/:userId`, `tonFarmingData`, контекст `TonFarmingContext`, др.)
   * 🔗 Проверь, правильно ли они **передаются на фронт** (в компонент карточки)
   * 📊 Убедись, что данные в Supabase есть, но **не теряются по пути в UI**

4. **Что отображается / не отображается**

   * Составь список:

     * ✅ какие поля отображаются корректно
     * ❌ какие поля не отображаются
     * ❌ какие значения отображаются, но **не соответствуют данным в базе**

---

### ⛔️ НЕЛЬЗЯ:

* Вносить изменения в код
* Устранять баги или фиксировать отображение
* Удалять или затирать данные

---

### 📌 Цель отчета:

* Назвать **точную причину**, почему данные не отображаются или отображаются некорректно
* Указать **конкретные поля**, которые не подключены или неправильно используются
* Подготовить основу для следующего этапа — **точечного исправления** отображения (после утверждения)

---

Если хочешь, я могу подготовить шаблон отчета, который агент должен использовать.
