Вот сообщение для агента — можно отправить напрямую:

---

🔔 **Аудит проекта UniFarm Connect: Проверка фактического статуса**

Привет! Пожалуйста, проверь и отработай **следующий чек-лист** по проекту. Нам нужно:

1. Убедиться, что **всё, что отмечено как "не сделано" — действительно не сделано**
2. Если **что-то уже реализовано — сразу обнови документацию** (`ROADMAP.md`, `SUPABASE_AUDIT_REPORT.md`)
3. Все **нереализованные пункты** собери в отдельный список `TODO_BACKEND_FIXES.md` или комментарием к roadmap
4. Отметь любые **несовпадения между Supabase / API / логикой / документацией**

---

### 📌 Что нужно проверить:

#### 🔒 Authentication Module (71%)

* [ ] `POST /logout` — **реализован ли?**
* [ ] `GET /session` — **реализован ли?**

#### 🌱 UNI Farming (71%)

* [ ] `GET /rates` — **есть ли?**
* [ ] `POST /stop` — **есть ли?**

#### 💎 TON Boost (80%)

* [ ] `POST /activate` — **реализован ли?** Или логика встроена в `purchase`?

#### 📲 Telegram Integration (33%)

* [ ] `GET /webapp-data` — реализовано?
* [ ] `POST /set-commands` — реализовано?

---

### ❌ Supabase: отсутствующие таблицы

* [ ] `user_sessions`
* [ ] `user_missions` — **обязательно**, иначе не работает отслеживание миссий
* [ ] `daily_bonus_logs`
* [ ] `airdrops`

### ⚠️ Supabase: пустые таблицы

* [ ] `referrals`
* [ ] `farming_sessions`
* [ ] `boost_purchases`
* [ ] `missions` — **уточни**, почему 5 миссий работают, но таблица пуста (JSON? hardcoded?)

---

### 🟡 Недокументированные модули (есть в коде, нет в roadmap)

* [ ] `modules/scheduler/tonBoostIncomeScheduler.ts`
* [ ] `core/metrics.ts`, `core/alerting.ts`, `core/envValidator.ts`
* [ ] `core/middleware/emergencyBypass.ts`
* [ ] Прочие из `components/`, `core/`, `utils/` — нужны краткие описания в roadmap

---

### 📦 Что нужно сделать:

* Обнови все соответствующие файлы, если что-то **уже реализовано**
* Пометь явным списком то, чего **не хватает**
* Если какие-то таблицы намеренно пустые — поясни почему

Спасибо! После этого мы сможем финализировать архитектуру и начинать строгую синхронизацию всех слоёв системы.
