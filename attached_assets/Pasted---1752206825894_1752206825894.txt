Вот чётко сформулированная задача для агента, основанная на его собственном плане. Включена обязанность учитывать текущую архитектуру и **проводить регрессионную проверку** после каждого изменения:

---

### 🛠️ **ЗАДАЧА ДЛЯ АГЕНТА**

**Цель:** Начать реализацию улучшений по плану оптимизации UniFarm от 11.01.2025, строго в рамках утверждённых этапов и с полной регрессионной проверкой после каждого изменения.

---

### 📌 **ФАЗА 1 — КРИТИЧЕСКИЕ И БЕЗОПАСНЫЕ ИЗМЕНЕНИЯ (реализация немедленно)**

#### 1. **Централизация всех операций с балансами через `BalanceManager`**

* Перевести *все существующие* операции пополнения, списания и начисления баланса (UNI и TON) на `BalanceManager`.
* Удалить прямые SQL-операции из `modules/farming`, `modules/boost`, `modules/dailyBonus`, `modules/referral`, `modules/missions`.
* Обеспечить **журналирование** всех операций.
* Провести **тестирование на реальных сценариях** (например, старт фарминга, получение награды, выполнение миссии).
* Сделать **регрессионную проверку**: не должно сломаться начисление, отображение и снятие баланса.

#### 2. **Исправление логики Referral-наград**

* Удалить начисление referral-наград при покупке Boost.
* Начислять referral только от дохода (farming/boost income).
* Обновить `modules/scheduler/tonBoostIncomeScheduler.ts` и аналогичные места.
* Проверить через лог, что:

  * Boost покупается, но награда не начисляется.
  * Доход приходит — награда начисляется корректно.
* Регрессионно проверить расчёт реферальных уровней, отображение баланса, графы "Referral earnings".

#### 3. **Стандартизация типов транзакций**

* Обновить `TransactionType` enum.
* Проставить корректные типы для всех новых транзакций: `FARMING_DEPOSIT`, `BOOST_PURCHASE`, `BOOST_INCOME` и т.д.
* Не выполнять миграцию исторических транзакций — **только новые записи**.
* Провести регрессионную проверку отображения истории транзакций, фильтрации и аналитики.

---

### 📎 ОБЯЗАТЕЛЬНЫЕ УСЛОВИЯ

* **Соблюдать текущую архитектуру проекта.**
  Нельзя вносить хаотичные или прямые SQL-операции вне `BalanceManager`.

* **Покрытие всех изменений ручным регрессионным тестированием:**

  * Проверить старые функциональные потоки (фарминг, бонус, миссии, бусты, вывод).
  * Проверить, что пользователь видит правильный баланс, история работает, ничего не сломано.

* **Каждый этап — через pull request с отдельным описанием.**
  Названия PR: `feature/balance-manager-integration`, `fix/referral-income-logic`, `feat/transaction-types-standardization`

* **После успешной проверки — приступить к Фазе 2 (по согласованию).**

---

Если хочешь, я могу сразу:

* Разбить эти пункты на **GitHub issues**
* Написать **чек-лист регрессионного тестирования**
* Или подготовить шаблон **технической документации по изменениям**

Хочешь, чтобы я оформил это и для второй фазы тоже?
