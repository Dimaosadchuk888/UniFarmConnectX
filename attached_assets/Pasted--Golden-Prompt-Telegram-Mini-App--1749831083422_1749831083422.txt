Вот золотой промт (Golden Prompt) для агента, строго по проблеме с авторизацией Telegram Mini App:

⸻

🟣 ЗАДАЧА: ПРОВЕРИТЬ И ИСПРАВИТЬ АВТОРИЗАЦИЮ ЧЕРЕЗ TELEGRAM MINI APP

Контекст:
UniFarm — Telegram Mini App, подключён к боту @UniFarming_Bot. Проект задеплоен в Replit:
https://uni-farm-connect-x-osadchukdmitro2.replit.app
Проблема: при запуске Mini App из Telegram появляется сообщение «Не удалось авторизоваться», несмотря на то, что пользователь реально запускает Mini App из Telegram. В базе данных пользователь не создаётся.

⸻

🎯 ЦЕЛЬ:

Проверить и гарантировать корректную авторизацию пользователей через Telegram. После захода через Mini App, пользователь должен:
 1. Проходить HMAC-подпись Telegram (initData)
 2. Попадать в базу данных (таблица users)
 3. Получать свой ref_code и отображать партнерскую ссылку
 4. Не получать ошибку “Требуется авторизация через Telegram Mini App”

⸻

🔒 ОГРАНИЧЕНИЯ:
 • Работаем ТОЛЬКО в Replit
 • Нельзя трогать другие модули и бизнес-логику
 • Нельзя переименовывать переменные, endpoints, модели
 • Никаких удалений логики, только исправление авторизации

⸻

✅ ПЛАН ПРОВЕРКИ:
 1. Проверь, загружается ли window.Telegram.WebApp.initDataUnsafe на клиенте
 • Убедись, что telegram_id, username, hash и auth_date действительно передаются.
 2. Проверь middleware или AuthService
 • Найди точку входа, где происходит валидация Telegram initData
 • Убедись, что бот использует верный BOT_TOKEN
 • Убедись, что проверяется подпись от Telegram (checkTelegramInitData())
 3. Проверь наличие токена в .env

BOT_TOKEN=7980427501:AAHdia3LusU9dk2aRvhXgmj9Ozo08nR0Gug

 • Убедись, что он загружается из process.env
 • Убедись, что токен не хардкодится напрямую в middleware

 4. Проверь регистрацию пользователя
 • Убедись, что после авторизации пользователь попадает в Neon DB → users
 • Проверь, что создаётся Telegram user с telegram_id, username, ref_code
 5. Проверь, что Telegram WebApp работает в Telegram, а не в браузере
 • Логика должна различать, если пользователь НЕ зашёл из Telegram
 • И в этом случае блокировать доступ
 6. Проверь, зарегистрирован ли webhook в BotFather
 • Миниаппка должна быть связана с ботом @UniFarming_Bot
 • Команда /setdomain в BotFather должна быть выставлена на:

https://uni-farm-connect-x-osadchukdmitro2.replit.app


 7. Логгируй и исправь ошибки
 • Добавь логи в auth.controller.ts и auth.middleware.ts, чтобы отследить поток авторизации
 • Исправь все ошибки авторизации, не трогая бизнес-логику

⸻

🧪 ПОСЛЕ ИСПРАВЛЕНИЯ:
 • Протестируй авторизацию в Telegram (на iOS и Android)
 • Убедись, что при первом заходе пользователь создаётся в базе
 • Убедись, что ref_code работает и реферальная ссылка отображается

⸻

🟩 Ожидаемый результат:
 1. Telegram initData считывается
 2. Авторизация проходит
 3. Пользователь попадает в базу данных
 4. Ошибка Не удалось авторизоваться больше не появляется
 5. Система полностью функциональна для всех новых пользователей через Telegram

⸻

Сообщи результат, как только всё будет проверено ✅