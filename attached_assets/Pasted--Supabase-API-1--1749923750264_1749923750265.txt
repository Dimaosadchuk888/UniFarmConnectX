⸻

🟡 Техническое задание: Проверка полного перехода на Supabase API (Этап 1)

🎯 Цель:

Провести аудит всех изменений и убедиться, что весь backend переведён на Supabase API, без следов pg, drizzle-orm, core/db.ts, и что SQL-скрипт создания таблиц соответствует бизнес-логике проекта.

⸻

🔍 Задачи:
 1. Структура и логика:
 • Проверить, что все актуальные модули (Users, Wallet, Auth, Scheduler, Referral, Airdrop, UserRepository) подключаются через core/supabaseClient.ts, а не через pg или drizzle.
 • В этих модулях не должно быть:
 • импорта pg, drizzle-orm
 • подключения к core/db.ts
 2. Конфигурация:
 • Файл .env должен содержать только переменные:
 • SUPABASE_URL
 • SUPABASE_KEY
 • Не должно быть переменных DATABASE_URL, PGHOST, NEON и других, связанных с PostgreSQL напрямую.
 3. SQL-скрипт миграции:
 • Проверить наличие и содержимое create-supabase-schema.sql
 • Убедиться, что в нём чётко создаются следующие таблицы:
 • users, wallet, transactions, referrals, missions, airdrops, boosts, farming_sessions, user_sessions, daily_bonus
 • Названия таблиц и их структура должны на 100% совпадать с бизнес-логикой (RedMap) и использованием в коде.
 4. Отчётность:
 • Если всё корректно — создать или обновить файл SUPABASE_MIGRATION_REPORT.md, в котором описать:
 • какие модули подключены к Supabase
 • какие таблицы созданы
 • какие методы используются (insert, select и т.д.)

⸻

🧪 Проверка подключения:
 • Добавить тестовую проверку: supabase.from('users').select('*').limit(1)
 • Вывести в консоль лог console.info("Supabase connection OK"), но только в режиме разработки

⸻

⛔ Ограничения:
 • Нельзя менять бизнес-логику или структуру данных
 • Нельзя возвращать pg/drizzle
 • Нельзя создавать новые таблицы или переменные без одобрения

⸻

🟩 Ожидаемый результат:
 • Все модули используют @supabase/supabase-js
 • Полностью удалены pg, drizzle, core/db.ts
 • SQL-скрипт соответствует бизнес-логике
 • Подключение к Supabase проверено
 • Создан файл SUPABASE_MIGRATION_REPORT.md

⸻