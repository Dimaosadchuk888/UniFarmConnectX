Вот точный и структурированный **Prompt / Техническое задание для агента** по новому окну — **миссии**:

---

# 🧾 ТЕХНИЧЕСКОЕ ЗАДАНИЕ: Исследование проблемы с переходом по ссылке в миссии

### 🎯 Цель

Выяснить, **почему кнопка «Перейти» в миссии (например, «Подписка на соцсеть») не ведёт на внешний ресурс**, а сразу отображает кнопку «Забрать бонус», несмотря на то, что **ссылка ранее работала** и миссия была корректно настроена в БД.

---

### 🔍 Что необходимо проверить:

#### 1. **Фронтенд**

* Найти компонент, отвечающий за отображение **миссии с кнопкой «Перейти»**
* Проверить:

  * Откуда берётся URL социальной сети (например, `mission.link`, `mission.meta.url` и т.п.)
  * Вызывается ли редирект/переход при клике?
  * Есть ли условие, по которому кнопка «Перейти» **скрывается** или заменяется на «Забрать бонус»?

#### 2. **API / Backend**

* Проверить, какой **endpoint** отдаёт миссии (`GET /api/missions/active` или `GET /missions`)
* Выяснить:

  * Приходит ли поле с **ссылкой на соцсеть** в ответе API?
  * Есть ли логика в контроллере/сервисе, которая может **убирать** или **не отдавать** ссылку, если миссия уже засчитана?
  * Убедиться, что API действительно передаёт миссии с корректной структурой (включая ссылку)

#### 3. **База данных**

* Найти таблицу, в которой хранятся миссии (например, `missions`, `mission_templates`, `user_missions`)
* Проверить:

  * Есть ли в БД ссылка (`url`, `link`, `action_url` и т.п.) у этой конкретной миссии?
  * Присутствует ли она у той миссии, которую пользователь прошёл?
  * Были ли изменения в записях в последние дни?

#### 4. **Цепочка отображения**

* Полностью пройти путь:

  * База → API → Сервис → Контроллер → Фронтенд → UI
* Определить, **на каком этапе теряется ссылка**, или **заменяется кнопка «Перейти» на «Забрать бонус»**

---

### ✅ Что нельзя делать

* ❌ **НЕ менять код**
* ❌ НЕ вносить миграции
* ❌ НЕ «догадываться» — только исследование и точные выводы

---

### 📌 Ожидаемый результат

1. ✅ Указать **точную причину**, почему кнопка «Перейти» не отображается (например, ссылка не приходит из БД / API)
2. ✅ Указать, **в каком месте она теряется**: на фронте, в API, в сервисе или в базе
3. ✅ Подтвердить, была ли ссылка сохранена в БД и **передаётся ли она в ответ**
4. ✅ Убедиться, что система не удаляет ссылку после прохождения миссии **(если такое поведение зашито)**

---

⏳ Срок: как можно скорее
🔒 Без вмешательства в продакшен или кодовую базу

---

Хочешь, чтобы я сейчас передал это агенту или оформил ещё и как SQL-запросы/cheatsheet, чтобы он быстрее проверил?
