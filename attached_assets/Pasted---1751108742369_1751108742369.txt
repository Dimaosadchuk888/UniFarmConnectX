✅ Расширенный промт: Проверка подключения и чистоты системы (без правки кода)

⸻

🔒 ОГРАНИЧЕНИЯ (напоминаю):

Ни один файл нельзя изменять или комментировать. Цель — проверка и аудит, без правки.
Не добавлять новые импорты, не трогать стили, не редактировать компоненты.
Только исследовать, задокументировать, и показать, где не работает или не подключено.

⸻

🎯 Цель:
 • Выявить мёртвые/неподключённые файлы и компоненты.
 • Проверить, почему изменения по дизайну и функциям не отражаются на интерфейсе.
 • Собрать полный список элементов, которые есть в коде, но не работают на экране.
 • Подготовить систему к финальной чистке перед продакшеном.

⸻

🧭 План проверки:

⸻

🔍 1. Проверка новых функций и UI-изменений
 • Найти все новые функции и UI-изменения, которые были заявлены.
 • Указать:
 • где находятся эти изменения (в каком файле);
 • вызываются ли они где-либо;
 • импортированы ли компоненты, функции и стили;
 • рендерятся ли они в реальном DOM.

📌 Пример: “Функция ‘generateReferralLink()’ есть в utils/ref.js, но она не импортирована ни в один компонент.”

⸻

🔍 2. Проверка подключений компонентов и файлов
 • Пройтись по всем новым/изменённым папкам:
 • Pages
 • Components
 • Styles
 • Utils
 • Проверить:
 • все ли .jsx, .ts, .css файлы действительно подключены в основной поток;
 • есть ли “мертвые” модули, не вызываемые нигде;
 • не дублируются ли файлы (например, Button.jsx и Buttons.jsx, но один не используется);
 • Сделать список всех неподключённых или незадействованных элементов.

⸻

🔍 3. Проверка отображения стилей
 • Проверить:
 • подключены ли новые CSS/SCSS/JSX-стили?
 • отображаются ли они на UI?
 • не перекрываются ли они другими стилями?
 • не отключены ли из-за приоритетов (!important, z-index, opacity, display)?

📌 Проверь в DevTools → Elements → Computed.

⸻

🔍 4. Проверка логики компонентов
 • Визуально подтвердить, что новые компоненты действительно видны на странице.
 • Проверить, вызывается ли функция при событии (нажатие, загрузка и т.п.).
 • Если функция не вызывается — указать причину (например: не вставлена в onClick или useEffect).

⸻

📋 Формат отчёта:

Элемент Найден в файле Импортирован? Используется в UI? Работает? Комментарий
PromoCard.jsx src/components/PromoCard.jsx ❌ ❌ ❌ Лежит в проекте, но никуда не подключён
generateReferral() utils/ref.js ✅ ✅ ❌ Ошибка в логике, не срабатывает
ref-link-block (CSS) styles/ref.css ✅ ✅ ✅ Работает


⸻

📎 В конце:

Собери выводы по категориям:
 • ❌ Элементы, которые есть в коде, но не подключены
 • ❌ Элементы, которые подключены, но не отображаются
 • ✅ Элементы, которые работают корректно

⸻