Синхронизация логики под текущие поля Supabase

🎯 ЦЕЛЬ:

Адаптировать код UniFarm под уже существующую структуру таблиц в Supabase. Избежать конфликтов и не добавлять новые поля — использовать только то, что уже существует в базе. Обеспечить 100% работоспособность всех модулей на Supabase API без изменения схемы базы данных.

⸻

❗ ПЕРЕД НАЧАЛОМ:
 • Убедись, что работаешь в production-версии UniFarm.
 • Не добавляй поля в базу данных.
 • Не создавай новые таблицы и не удаляй существующие.
 • Не вноси изменения в архитектуру, бизнес-логику или структуру проекта.

⸻

✅ ЧТО НУЖНО СДЕЛАТЬ:
 1. Проанализировать текущие поля в таблицах:
• users
• referrals
• farming_sessions
• user_sessions
• transactions
 2. Найти альтернативы недостающим полям:
• Вместо referrer_id использовать, например, user_ref или существующее поле, которое логически связано.
• Вместо last_active, updated_at — использовать modified, last_seen и т.д., если такие есть.
• Вместо amount в farming_sessions — использовать существующие поля, где хранится сумма.
 3. Обновить модули, которые используют старые или отсутствующие поля:
• modules/farming/service.ts
• modules/referral/service.ts
• modules/user/model.ts
• modules/dailyBonus/service.ts
 4. Провести полную замену логики:
• Все обращения должны идти через supabase.from(...)
• Подключение — только через core/supabase.ts
• Ни одного drizzle, pg, pool, db.select и прочего быть не должно
 5. Провести ручную проверку всех обновлённых запросов на соответствие:
• Запросы работают
• Ошибок в runtime нет
• Supabase не возвращает column does not exist

⸻

🧪 ПРОВЕРКИ:
 • Проверка каждого модуля в отдельности
 • Проверка insert, update, select на существующих таблицах
 • Проверка авторизации, фарминга, кошелька, бонусов, рефералок

⸻

📄 ФОРМАТ ОТЧЁТА:

В конце напиши:
 • ✅ Какие модули были обновлены
 • 🔄 Какие поля были заменены (старое → новое)
 • 🧪 Что было протестировано
 • ⚠️ Какие поля отсутствуют, но заменены
 • ❌ Что невозможно реализовать с текущей схемой

⸻

🚫 ЗАПРЕЩЕНО:
 • Добавлять, удалять или переименовывать таблицы или поля
 • Использовать старые подключения (pg, drizzle, pool)
 • Менять бизнес-логику или добавлять временные заглушки
 • Использовать @ts-ignore, any, eslint-disable

⸻

💡 ПРИМЕЧАНИЕ:

Если какой-либо функционал невозможно реализовать без определённого поля — зафиксируй это в отчёте, но не создавай поле самостоятельно.

⸻