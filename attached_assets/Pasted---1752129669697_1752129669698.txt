Вот чёткое и поэтапное **техническое задание для агента** на создание новой полноценной тестовой страницы с рабочей интеграцией.

---

### ✅ **Техническое задание: Создание боевой тестовой страницы для реального тестирования (1000 TON / 1000 UNI)**

**Цель:**
Создать полностью валидную, боевую страницу в базе данных проекта, которая позволит начать полноценное тестирование всех бизнес-процессов в реальном времени — с депонированием средств, токеном авторизации и подключением к интерфейсу Replit Preview.

---

### 🔧 Что нужно сделать — по шагам:

#### 1. **Создать новую страницу (entity) в базе данных**

* Создать новую запись (или связанный объект), которая будет представлять **реальную страницу с депозитом**.
* Установить значения:

  * Депозит **1000 TON**
  * Депозит **1000 UNI**

#### 2. **Подключить страницу ко всем необходимым частям системы**

* Обеспечить, чтобы страница:

  * Имела **валидный токен/ключ доступа** (если используется auth-token / session-token)
  * Была связана с актуальными модулями (farming, boost, wallet и т.д.)
  * Корректно отображалась на **Replit Preview** с полным рендером

#### 3. **Проверить боевое поведение**

* Убедиться, что:

  * Система воспринимает страницу как боевую (не тестовую)
  * Вся логика (начисление, списание, отображение балансов, фарминг, миссии и т.д.) работает в рамках этой страницы
  * Не возникает ошибок при действиях пользователя

#### 4. **Подключение на Replit**

* Подключить страницу к текущему **Replit Preview**, чтобы её можно было открыть и взаимодействовать с ней в веб-интерфейсе.
* Проверить, что страница доступна по URL, токен работает, все действия выполняются.

#### 5. **Результат:**

* Готовая **боевая тестовая страница**, которую можно использовать для финального ручного тестирования.
* Все модули должны быть подключены и функционировать.
* Можно тестировать как обычный пользователь (через UI) в реальном времени.

---

Если нужна вторая такая страница с другим балансом — сообщи, сделаем отдельным заданием.
