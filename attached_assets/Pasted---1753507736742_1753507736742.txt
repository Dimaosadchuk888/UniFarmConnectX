Вот уточнённое техническое задание (ТЗ) с добавленным контекстом, поправками, ограничениями и пометкой, что это пример. Формулировка написана для агента или разработчика, который будет проводить диагностику **по коду**, не внося изменений:

---

## 🧾 Техническое задание (пример)

### 🔍 Диагностика системы пополнений, транзакций и активации пакетов в ToneFarming

**Важно: это ТЗ — **пример**, который необходимо выполнять строго на основе **текущей архитектуры проекта**, согласно нашей существующей файловой структуре и логике. Никаких внешних предположений или догадок.**

---

### 📌 **Контекст:**

В системе недавно были зафиксированы следующие **аномалии в поведении**, которые могли быть вызваны ошибками в логике обработки пополнений и активации пакетов. Предположительно, часть проблем уже устранена, но необходимо **подтвердить это по коду**, без анализа истории коммитов или внешнего поведения приложения.

---

### ⚠️ **Важные условия:**

1. **Никаких изменений в код вносить нельзя.**
   Это **диагностическая проверка**, не разработка или исправление.

2. **Работать строго на основе существующего кода** проекта.
   Не добавлять "возможные улучшения" и не менять текущую логику без запроса.

3. **Фокус только на логике и внутренних связях между модулями и базой данных.**
   Важно убедиться, что всё работает корректно и нет рисков повторения ошибок.

---

### ✅ Задачи для диагностики (по коду):

#### 🔹 1. **Дублирование транзакций при пополнении через ConnectWallet**

* Проверь, где и как создаётся запись транзакции при пополнении.
* Есть ли защита от дублирования (`idempotency` или проверка по `txHash`, `externalId` и т.д.).
* Исключена ли ситуация, при которой при одном пополнении записывается **две транзакции и два раза начисляется TON**.

#### 🔹 2. **Списание средств после пополнения**

* Выясни, в каком месте кода происходит **уменьшение баланса** после пополнения.
* Убедись, что **баланс не уменьшается автоматически** сразу после увеличения.
* Проверь, чтобы **все списания происходили только при явной покупке пакета**, а не после самого пополнения.

#### 🔹 3. **Дублирование Boost-пакетов в ToneFarming**

* При активации Boost через пользовательский интерфейс, система иногда записывала **два одинаковых Boost-пакета**.
* Необходимо убедиться, что сейчас **Boost записывается один раз**.
* Проверь, нет ли повторных вызовов записи или асинхронной гонки, из-за которой возникает дубликат.

#### 🔹 4. **Отсутствие записи в таблицу ToneFarmingData → не запускается начисление**

* После покупки Boost-пакета, система должна передавать данные в таблицу `ToneFarmingData`.
* Убедись, что эта логика **не пропущена**, что запись производится, и что после неё корректно запускается **планировщик начислений**.
* Это критично, так как без записи в `ToneFarmingData` пользователи не получают доход по пакетам.

---

### 📂 Ожидаемые файлы и области анализа (ориентировочно):

| Область                 | Файлы                                                                                |
| ----------------------- | ------------------------------------------------------------------------------------ |
| Пополнения и транзакции | `modules/wallet/transactions.ts`, `modules/wallet/service.ts`, `connectWallet.ts`    |
| Boost-пакеты            | `modules/boost/service.ts`, `modules/boost/controller.ts`                            |
| ToneFarming начисления  | `modules/farming/toneFarmingData.ts`, `modules/farming/logic/tonBoostCalculation.ts` |
| Работа с балансом       | `modules/user/service.ts`, `wallet.utils.ts` (если есть)                             |

---

### 📌 Цель проверки

Подтвердить, что:

* Транзакция создаётся **один раз**, нет дубликатов.
* Баланс пользователя не списывается после пополнения.
* Boost-пакет создаётся **однократно** при активации.
* Данные передаются в `ToneFarmingData`, и планировщик запуска начислений работает.

---

Если нужно — могу подготовить чек-лист по строчным проверкам или сопроводить диагностическую сессию.

Готово. Нужно что-то адаптировать под конкретного исполнителя или систему тикетов (Jira, Notion и т.п.)?
