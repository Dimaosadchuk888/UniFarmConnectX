🔧 PROMPT ДЛЯ АГЕНТА (ЗОЛОТОЙ ФОРМАТ)

Ты — профессиональный AI-разработчик. Твоя задача — выполнить конкретное техническое задание, строго в рамках инструкции. Работай точно, локально и без побочных эффектов.

⸻

❗ ПЕРЕД НАЧАЛОМ РАБОТЫ:
 • Убедись, что работаешь в Replit и с production-ссылкой проекта:
https://uni-farm-connect-x-osadchukdmitro2.replit.app
 • Работай только с backend модулями, которые отвечают за авторизацию, регистрацию и токены.
 • Frontend НЕ изменяй, если это не требуется для тестов или логов.
 • Базу данных НЕ модифицируй, только проверка структуры и логики записи.

⸻

📌 ПРАВИЛА ВЫПОЛНЕНИЯ:
 1. Не выходи за рамки задачи.
 2. Не изменяй архитектуру и бизнес-логику.
 3. Работай строго с Telegram авторизацией.
 4. Не добавляй временных заглушек или тестов вне scope задачи.
 5. Включи логирование, чтобы показать этапы регистрации (можно временно).

⸻

🧩 ТВОЯ ЗАДАЧА:

Проверить и устранить проблему отсутствия пользователя в базе данных после захода через Telegram Mini App. На текущий момент:
 • Пользователь открывает Mini App, но не попадает в таблицу users
 • Не создается ref_code
 • Все API возвращают 401 или «Session not found»

Требуется:
 1. Проверить, вызывается ли /api/v2/auth/telegram после захода пользователя.
 2. Убедиться, что в AuthController и AuthService реализован механизм:
 • Валидации Telegram initData
 • Создания записи users с полями: telegram_id, username, ref_code
 • Возврата JWT токена
 3. Убедиться, что JWT сохраняется на frontend (проверяется не тобой, но ты должен отдать его в ответ).
 4. Проверить, передается ли токен в API-запросах после авторизации.
 5. Проверить, создается ли ref_code в ReferralService.

⸻

⚙️ ДОПОЛНИТЕЛЬНЫЕ УКАЗАНИЯ:
 • Код, связанный с AuthService, TelegramService, ReferralService — в приоритете.
 • Добавь лог console.log('✅ User created:', user) временно, чтобы подтвердить регистрацию.
 • Не создавай новых таблиц.
 • Не дублируй существующие методы.

⸻

📄 ФОРМАТ ОТЧЁТА:
 • ✅ Что было сделано (в виде списка)
 • 🧪 Что было проверено (ручные/программные тесты)
 • ⚠️ Что осталось без изменений (если заметил, но не трогал — запиши)

⸻

🎯 ЦЕЛЬ:

Пользователь, заходящий в Telegram Mini App, должен:
 • Быть автоматически зарегистрирован
 • Получить JWT токен
 • Иметь доступ к API (/me, /referrals, /wallet, и др.)

⸻