📋 Промт для агента: Проверка конфигурационных файлов UniFarm (без удаления)

🛑 Внимание: Ничего не удалять, не изменять. Только проанализировать текущие файлы, выдать отчёт по каждому, объяснить назначение и необходимость, а также указать, содержат ли они критические данные или дублирующуюся/устаревшую информацию.

⸻

📁 ЗАДАЧА 1 — Анализ .env и связанных файлов

🔍 Проверь следующие файлы в корне проекта:
 • .env
 • .env.example
 • .env.explain (или похожие)
 • .env.local, .env.preview, .env.production (если есть)

📌 Для каждого:
 1. Указать, какие переменные в нём хранятся
 2. Есть ли реальные токены (SUPABASE_KEY, TELEGRAM_BOT_TOKEN и др.)
 3. Используется ли этот файл приложением
 4. Является ли этот файл шаблоном/образцом или реально используемым
 5. Закомментированы ли переменные или содержат значения

⸻

📁 ЗАДАЧА 2 — Анализ всех .replit файлов

🔍 Проверь следующие файлы:
 • .replit
 • .replit.unified
 • .replit.workflows
 • .replit.workflow
 • любые другие .replit.*

📌 Для каждого:
 1. Объясни его содержимое и назначение
 2. Указывается ли в нем нестандартный путь запуска (run, entrypoint, port, env)
 3. Есть ли потенциально ценные настройки (автозапуск, переменные среды, алиасы)
 4. Содержит ли ошибки, ссылки на несуществующие файлы, устаревшие команды
 5. Используется ли реально системой (или загружен в Git случайно)

⚠️ Не удалять даже дублирующие или устаревшие .replit-файлы, пока не будет финального аудита и подтверждения необходимости удаления. Сначала выдать отчёт.

⸻

📁 ЗАДАЧА 3 — Связь с переменными окружения
 1. Сравнить переменные из .env и .replit файлов с тем, что установлено в Replit Secrets
 2. Выявить дублирующиеся или отсутствующие переменные
 3. Отметить, какие переменные можно безопасно удалить из .env, потому что они уже есть в Replit Secrets

⸻

🔍 ЗАДАЧА 4 — Вывод и рекомендации (только в текстовом виде)

В результате:
 • Список всех .env и .replit.* файлов
 • Таблица: имя файла → назначение → содержит токены? → используется?
 • Какие файлы точно нужны
 • Какие возможно устарели (но не удалять!)
 • Какие переменные дублируются между .env и Replit Secrets

⸻

Если агент выполнит это исследование правильно — на основе его отчёта можно будет принять осознанное решение, какие файлы очистить, какие переменные обновить, а какие оставить как есть.