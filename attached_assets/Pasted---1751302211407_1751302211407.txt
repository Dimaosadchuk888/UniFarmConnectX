Вот переписанный точный и безопасный промпт для агента, без упоминания конкретных имён файлов, только по визуальной логике и назначению блоков:

⸻

🧩 ЗАДАЧА: Восстановление оригинального визуального дизайна (UX)

🔧 Важно:
 • Работай только с визуальной частью (UI/UX), не трогай функциональность, API или бизнес-логику.
 • Не добавляй новые файлы в папку client/
 • Не исправляй ошибки, если они связаны с данными или API — твоя задача только визуальное восстановление
 • Не создавай временных, тестовых, альтернативных версий компонентов — всё должно быть точно и по оригиналу
 • Даже если ты видишь “ошибки” — не исправляй их, если они не касаются визуала

⸻

📋 Что нужно сделать:

🔍 Сравни полностью весь текущий дизайн приложения с ZIP-версией (UniFarmConnect-XO.zip)
Работай по страницам, по порядку:

1. Главная (Dashboard):
 • Блок приветствия
 • Блок “Доход от фарминга”
 • График
 • Статус бустов
 • Ежедневный бонус
 • UNI Farming карточка

Проверь, чтобы все визуальные стили (градиенты, иконки, шрифты, тени, анимации, порядок блоков) соответствовали версии из ZIP. Если есть отличия — восстанови исходный вид.

2. Farming (Фарминг):
 • Вкладка UNI Farming
 • Вкладка TON Farming
Убедись, что карточки отображаются как в ZIP, с теми же стилями и визуальной структурой.

3. Missions (Задания):

Проверь карточки заданий:
 • Градиенты, иконки, прогресс-бары, анимации награды и завершения
 • Если сейчас применён упрощённый дизайн (один цвет, простая сетка), нужно вернуть анимированный, как в оригинале

Даже если данные не отображаются — ты работаешь только с визуалом

5. TON Boost:
 • Отображаются ли Boost-пакеты?
 • Есть ли отдельная страница TON Boost?
 • Все элементы визуального оформления должны быть возвращены в соответствии с ZIP

⸻

📌 Фокус:

👉 Работай точечно, по одному блоку за раз.
👉 Не вноси изменений вне указанного контекста.
👉 Не исправляй API или логику — только визуальная проверка и восстановление.

Как только восстановишь один блок — остановись и жди следующей задачи. Мы работаем поэтапно.

⸻