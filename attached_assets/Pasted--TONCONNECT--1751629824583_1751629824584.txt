📋 ТЕХНИЧЕСКОЕ ЗАДАНИЕ: ДИАГНОСТИКА ПРОБЛЕМЫ СЕРВЕРА И TONCONNECT

🔒 Ограничения
 • ❌ Категорически запрещено вносить изменения в код (frontend, backend, context, WebSocket, TonConnect, и пр.) без вашего прямого одобрения.
 • ✅ Задача — только провести исследование и выдать подробный отчёт по каждому из пунктов.

⸻

✅ ЗАДАЧА 1: Ошибка соединения с сервером

Цель:

Выяснить, почему приложение пишет “Ошибка соединения с сервером”.

Что проверить:
 1. Проверить, какой тип соединения используется (обычный fetch/Axios запрос или WebSocket).
 2. Выяснить, в какой момент возникает ошибка (при загрузке, при запросе данных, при авторизации и т.п.).
 3. Определить, есть ли WebSocket-подключение, и если да —:
 • Активно ли оно?
 • Устанавливается ли соединение?
 • Возникает ли ошибка при инициализации или при подписке?
 4. Проверить логи:
 • В консоли браузера (Console, Network, WebSocket)
 • На сервере (server.log или консоль server/index.ts)
 5. Убедиться, что все критичные API endpoints работают (status 200):
 • /api/v2/users/profile
 • /api/v2/wallet/balance
 • /api/v2/farming/status
 • и т.д.

⸻

✅ ЗАДАЧА 2: Проверка работоспособности TonConnect / ConnectWallet

Цель:

Получить детальный отчёт по состоянию TonConnect в системе.

Что нужно выяснить:
 1. Подключён ли провайдер TonConnectUI:
 • Используется ли useTonConnectUI() в userContext.tsx?
 • Возвращается ли объект tonConnectUI без ошибок?
 2. Корректно ли работают следующие функции:
 • connectWallet(tonConnectUI)
 • disconnectWallet(tonConnectUI)
 • getWalletAddress(tonConnectUI)
 • isWalletConnected(tonConnectUI)
 3. Протестировать:
 • Подключение кошелька (имитация реального соединения с TonKeeper / TonHub)
 • Получение адреса
 • Проверка статуса подключения
 • Отключение кошелька
 4. Проверить в интерфейсе:
 • Отображается ли состояние кошелька
 • Есть ли ошибки в DevTools (Console)
 5. Проверить консоль на ошибки вида:
 • Cannot read property 'connectWallet' of undefined
 • useTonConnectUI is not a function
 • null is not an object (evaluating 'tonConnectUI.connect')

⸻

🧠 Формат отчёта:

Для каждой задачи агент должен предоставить:
 • 📍 Диагноз (что конкретно не работает)
 • 🕵️ Где конкретно возникает ошибка (файл, строка, компонент)
 • 📂 Список зависимостей и связанных участков кода
 • 🧩 Причина (что именно нарушено)
 • 📌 Варианты решения (без внедрения — только предложения)

⸻

🔁 Повтор:

После составления отчёта — ожидается утверждение вами, и только после этого можно переходить к правкам, если они будут нужны.