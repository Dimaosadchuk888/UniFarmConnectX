Отлично! Вот ТЗ T57 на русском языке:

⸻

✅ ТЕХНИЧЕСКОЕ ЗАДАНИЕ: T57 — Финальное тестирование партнёрской программы

📌 Цель:

Провести полнейшее production-тестирование всей логики партнёрської программы после исправлений в T56. Проверка должна подтвердить, что:
 • награды идут только от реального дохода,
 • логика работает для TON Boost и UNI Farming,
 • создаются транзакции REFERRAL_REWARD в Supabase,
 • начисления приходят по цепочке до 20 уровней.

⸻

🧩 Проверяемые компоненты:

1. TON Boost доходы:
 • Проверить, что каждые 5 минут начисляется доход пользователям, у кого есть активные Boost-пакеты.
 • Проверить, что их рефереры получают награды от этого дохода, а не от покупки.
 • Убедиться, что транзакции в transactions создаются с типом REFERRAL_REWARD.

2. UNI Farming доходы:
 • Проверить, что партнёрские начисления формируются строго после фарминга UNI.
 • Убедиться, что сумма и уровни распределения соответствуют маркетингу.

3. Суперважные таблицы в Supabase:
 • transactions: есть REFERRAL_REWARD записи с валидными user_id, amount, source.
 • users: балансы корректно увеличиваются на сумму наград.
 • referral: цепочка до 20 уровней работает без обрывов.

4. Логирование (logger.ts):
 • Проверить, что в логе присутствуют события:
 • Referral income distributed for user #id
 • Boost income referral reward sent to referrer #id
 • UNI farming reward shared to referrer #id

5. Отсутствие ошибок:
 • Убедиться, что нигде не происходит fallback на temp_user.
 • Нет начислений “в никуда”.
 • Нет ошибок 500 или 401 в /api/v2/boost или /api/v2/farming

⸻

❗ Ограничения:
 • НЕ менять никакие алгоритмы.
 • НЕ перезапускать планировщик вручную.
 • НЕ тестировать через Dev-ссылки — только Production.
 • Все проверки — только read-only + визуальная верификация данных в Supabase и логе.

⸻

📤 Формат отчёта:

Ответ должен содержать:
 1. Таблицу проверки по каждому пункту (✅ / ❌)
 2. Конкретные user_id, для которых были подтверждены начисления
 3. Примеры логов REFERRAL_REWARD и данных в Supabase

⸻

🔚 Цель: Убедиться, что реферальная система работает(доход от дохода TON Boost и UNI Фарминг , а не от пополнения).