📋 Техническое задание: Аудит дублирующихся полей и структуры подключения

🧩 Цель:

Провести полный аудит базы данных (в частности, таблиц users, transactions, wallets, farming и др.), чтобы:
 1. Выявить все дублирующиеся поля, которые пересекаются по смыслу и содержанию.
 2. Определить, к каким из них подключены бизнес-логика, фронт, агенты и другие части системы.
 3. Построить чёткий план миграции: какие поля можно удалить, какие оставить, и как безопасно переключить систему.

⸻

🧪 Этап 1: Диагностика дубликатов

Для всех таблиц системы:
 • Найти пары или группы полей, которые дублируют одни и те же значения. Примеры:
 • amount, amount_uni, value_uni, deposit_amount
 • uni_farming_start_timestamp, start_ts, farming_start
 • ton_farming_rate, farming_rate, boost_rate, и т.д.
 • Проверить, содержат ли они одинаковые или отличающиеся значения у реальных пользователей (ID 191–303).
 • Выявить, какие поля реально используются, а какие просто лежат без дела (могут быть удалены).

⸻

🔌 Этап 2: Аудит подключений к этим полям
 1. Проверить все модули (бэкенд, агенты, cron-скрипты, фронт), которые используют поля из выше найденных дубликатов.
 2. Построить список:
 • “Поле → используется в: [модуль, скрипт, компонент]”
 3. Выделить, где логика ещё подключена к старым полям.

⸻

🔁 Этап 3: План действий
 1. На основе анализа — предложить единый набор полей, которые нужно оставить как основные.
 2. Предложить план миграции:
 • Куда скопировать данные (если нужно)
 • Как переключить код на нужные поля
 • Какие поля удалить
 • В каком порядке всё это сделать безопасно

⸻

🛡 Безопасность
 • Никаких изменений не вносить.
 • Только собрать данные, построить таблицу зависимости и подготовить рекомендации.
 • Проверка производится только по пользователям с ID от 191 до 303 — остальные считать тестовыми и не учитывать.

⸻

💬 Пример результата, который мы ожидаем:

Таблица Дублирующее поле Основное поле Используется в логике Можно удалить?
users amount_uni amount Нет (устарело) ✅ Да
users farming_start uni_farming_start_timestamp Да (cron job, frontend) ❌ Нет
transactions value_uni amount Только в старом скрипте ⚠ Возможно