⸻

✅ ПОВТОРНЫЙ АНАЛИЗ ПАРТНЁРСКОЙ СИСТЕМЫ UNI 🧩

🔍 1. Проверка отображения партнёрской ссылки
 • Отображается в интерфейсе?
 • Формат кода соответствует REF_{user_id}_{short_hash}?
 • Нет значения missing, undefined или null?
 • Кнопка “Скопировать” работает?

🔍 2. Проверка API и backend логики
 • Метод generateReferralCode реализован?
 • Запрос /api/v2/referrals/code возвращает код?
 • Запрос /api/v2/referrals/stats показывает реальных рефералов и доход?
 • Запрос /api/v2/referrals/levels работает и возвращает массив до 20 уровней?

🔍 3. Проверка отображения уровней
 • Таблица динамическая, данные приходят с backend?
 • Уровень 1 отображает 100% от дохода реферала?
 • Уровень 2 — 2%?
 • Уровни 3–20 имеют соответствующие значения (3%–20%)?

🔍 4. Проверка транзакций и начислений
 • После покупки Boost пакета новым пользователем по ссылке реферала:
 • Рефералу 1 уровня начисляется 100% от дохода?
 • 2 уровню — 2%?
 • Ниже — по модели?
 • Записи создаются в transactions с типом referral_reward?
 • Баланс пользователей обновляется?

🔍 5. Проверка интерфейса
 • Компонент ReferralLevelsTable не содержит хардкода (0/0/0)?
 • Все значения — реальные, из базы?
 • Нет заглушек и временных решений?
 • Нет лишних файлов или версий вроде ReferralTableTest.tsx, DummyTable.tsx?

📦 6. Проверка принятых обновлений
 • Все коммиты и изменения агентом реально отражены в продакшн-версии?
 • Обновления прошли деплой и доступны по текущей ссылке https://uni-farm-connect-x-alinabndrnk99.replit.app/?
 • Компоненты, которые заменялись (например ReferralLinkFixed.tsx), действительно используются?
 • Нет конфликтов старых и новых версий компонентов?

⸻

🧪 Финальное задание агенту (промпт)

Проведи финальную повторную проверку системы партнёрки. Убедись, что:
 1. Все исправления и обновления, внесённые ранее, успешно применены.
 2. Система работает на production без демо-версий.
 3. Все компоненты отображаются корректно, данные приходят из API, логика начислений работает строго по модели:

 • Уровень 1: 100% от дохода реферала
 • Уровень 2: 2%
 • Уровни 3–20: от 3% до 20%

 4. Проверь, используются ли новые файлы, а старые заглушки не подключены.
 5. Все отображаемые значения и балансы реальны, а не статичны.

⚠️ Важно: Никаких временных решений и UI-заглушек. Проверка должна быть произведена только по действующим компонентам, подключённым в рабочую версию интерфейса.

⸻