Вот чётко сформулированное **техническое задание для агента**, которое заставит его **перечитать весь `roadmap.md`, выявить все недореализованные файлы, указать % готовности каждого, обосновать, что именно не сделано, и сравнить с текущей архитектурой проекта**. Также он обязан проверить **дубликаты, пробои и несостыковки**.

---

📌 **ЗАДАЧА: Полный аудит реализации `roadmap.md` и архитектуры проекта**

---

### 🎯 Цель:

Ты должен **прочитать весь `roadmap.md`**, и на его основе:

1. 📁 Составить **полный список всех файлов, компонентов, маршрутов, модулей и логики**, которые:

   * Не реализованы
   * Реализованы частично (например, 60%, 71%, 80%)
   * Реализованы полностью (100%)

2. 🧾 Указать **процент готовности по каждому** элементу (файл, модуль, endpoint, блок логики).

3. 🧠 По каждому **частично реализованному** элементу:

   * Объясни, **что именно не сделано**
   * Укажи, **в каких файлах, модулях или маршрутах отсутствует логика**
   * Опиши, **что ты планируешь сделать**, чтобы довести его до 100%

4. 🔁 Сравни с **фактической архитектурой проекта**:

   * Найди и отрази **дубликаты** (одинаковые или похожие файлы с разной реализацией)
   * Укажи, **где структура проекта расходится с `roadmap.md`**
   * Отрази **избыточные, устаревшие или конфликтующие файлы**, которые нужно удалить

---

### 📋 Что ты должен выдать:

Создай файл **`ROADMAP_COMPLETION_REPORT.md`**, в котором:

#### 🔹 Структура документа:

1. **Раздел 1: Таблица соответствия**

   * Модуль / Файл / Endpoint
   * Процент готовности
   * Статус: `✅ Реализовано`, `⚠️ Частично`, `❌ Отсутствует`
   * Комментарий: Что не сделано, план исправлений

2. **Раздел 2: Список дубликатов и несостыковок**

   * Дублирующие файлы (например, `wallet.ts` и `walletRouter.ts`)
   * Несовпадения в именах маршрутов / методов / путей
   * Устаревшие или неиспользуемые файлы

3. **Раздел 3: План доработок**

   * Перечень всех задач, которые нужно сделать для достижения 100% соответствия
   * Оценка времени на выполнение (по модулю)

4. **Раздел 4: Финальное резюме**

   * Общий % выполнения по `roadmap.md`
   * Список "чистых" модулей
   * Список приоритетных зон, требующих доработки

---

### 🛡 Обязательные условия:

* Запрещена проверка "по диагонали" — всё должно быть проверено по **реальному коду**
* Используй только **актуальную реализацию проекта** (то, что реально есть в системе)
* Не создавай новых файлов — только анализ и отчёт
* Все данные должны быть достоверны, с привязкой к существующему коду

---

🛠 При необходимости можешь использовать инструменты кросс-анализа и grep-поиск по проекту для обнаружения дубликатов.

Если нужно — могу выдать шаблон Markdown-документа, как должен выглядеть `ROADMAP_COMPLETION_REPORT.md`.
