🧩 ЗАДАЧА T56 — Исправление логики партнёрских начислений от TON Boost

📌 Контекст:

В ходе аудита T55 выявлено, что партнёрские награды за TON Boost начисляются неправильно — сразу при покупке Boost-пакета (вручную или через TON Connect), что нарушает бизнес-логику проекта UniFarm. Согласно правилам, награды должны начисляться только от дохода, который пользователь получает от активного Boost-пакета TON.

⸻

✅ Цель задачи:

Перенести логику distributeReferralRewards() из BoostService в специальный планировщик дохода от Boost, аналогичный уже реализованному для UNI Farming.

⸻

📂 Зоны работы:

Разрешены изменения только в следующих зонах:
 • modules/boost/
 • modules/referral/
 • modules/scheduler/ (создание нового Boost Scheduler)
 • core/logger.ts (для логирования)

Запрещено:
 • Изменять UNI Farming
 • Удалять текущие функции без согласования
 • Создавать дубликаты логики или новых модулей

⸻

⚙️ Технические требования:

1. Создать Boost-планировщик (TON):
 • Файл: modules/scheduler/tonBoostIncomeScheduler.ts
 • Запускается каждые 5 минут (использовать cron в scheduler/index.ts)
 • Ищет всех пользователей с активными Boost-пакетами (статус confirmed)
 • Расчитывает доход каждого Boost за 5 минут (по boost.power)
 • Начисляет доход TON пользователю (wallet.balance_ton)
 • Вызывает distributeReferralRewards() с типом boost_income

2. Удалить неправильные вызовы:
 • В modules/boost/service.ts удалить вызовы distributeReferralRewards():
 • строка ~195 (внутренняя оплата)
 • строка ~559 (внешняя активация)
 • Заменить их логом logger.warn("Referral reward отключён: перенесено в Boost-планировщик")

3. Обновить referral лог:
 • В modules/referral/service.ts поддержать новый source: 'boost_income'
 • Запись транзакции с типом REFERRAL_REWARD, как уже реализовано для UNI

4. Логирование:
 • Внедрить логирование каждого действия:
 • Начисление дохода
 • Вызов distributeReferralRewards
 • Обнаружение ошибок

⸻

📁 Итоговые файлы:
 • modules/scheduler/tonBoostIncomeScheduler.ts ✅
 • modules/scheduler/index.ts — запуск по cron ✅
 • modules/boost/service.ts — удалены вызовы distributeReferralRewards ✅
 • modules/referral/service.ts — добавлен boost_income ✅
 • core/logger.ts — логирование процесса ✅

⸻

🧪 Проверка:

После завершения:
 • Покупка Boost НЕ вызывает реферальных начислений
 • Через 5 минут после покупки Boost активируется доход + идут партнёрские выплаты
 • В Supabase создаются transactions типа REFERRAL_REWARD с source = boost_income
 • Баланс партнёров обновляется

⸻

🎯 Цель:

Достичь 100% соответствия партнёрской логики:
 • 🔄 Только от реального дохода (фарминг + бусты)
 • ❌ Не с покупки, не вручную
 • ⚙️ Через автоматический планировщик

⸻