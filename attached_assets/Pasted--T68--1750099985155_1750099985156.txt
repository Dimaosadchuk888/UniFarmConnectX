⸻

✅ ТЗ: T68 — Аудит базы данных и подтверждение активности всех таблиц

🧩 Цель задачи:

Проверить, что все ключевые таблицы базы данных Supabase используются корректно: записи создаются, данные обновляются, транзакции отражаются, партнёрские цепочки фиксируются. Убедиться в целостности всей системы UniFarm.

⸻

📌 Что конкретно нужно сделать:

1. Проверка активности транзакций
 • Убедиться, что типы транзакций корректно сохраняются в таблице transactions:
 • FARMING_REWARD — фиксируются ли начисления UNI
 • REFERRAL_REWARD — начисляются ли реферальные проценты
 • TON_BOOST_INCOME — появились ли записи после запуска планировщика
 • Проверить, что данные в поле amount, user_id, type и created_at соответствуют логике работы системы.

2. Проверка пользовательских балансов
 • Убедиться, что таблица users и/или user_balances содержит:
 • Актуальные балансы в полях balance_uni и balance_ton
 • Балансы увеличиваются после начислений от фарминга и бустов
 • Провести сверку с транзакциями

3. Анализ реферальных связей
 • Проверить таблицу users:
 • Поля referred_by, ref_code заполнены
 • Убедиться, что глубина цепочки достигает до 20 уровней
 • Проверить таблицу transactions:
 • Начислены ли проценты согласно логике:
• Уровень 1 — 100%
• Уровень 2 — 2%
• Уровень 3 — 3%
• …
• Уровень 20 — 20%

4. Анализ ключевых таблиц Supabase

Проверить, какие таблицы реально используются, а какие — остаются пустыми:
 • farming_deposits — должны отображаться активные фарминги, если не используются — зафиксировать
 • boost_purchases — должны отражать покупки бустов. Если таблица всё ещё пустая — причина: RLS?
 • referral_earnings — должна фиксировать награды по партнёрке, если не заполняется — указать причину
 • farming_sessions — если используется только users, нужно указать это
 • missions, airdrops, mission_progress — зафиксировать статус (используются/не используются)

5. Дополнительно: логика начислений
 • Проверить:
 • Начисляется ли доход по TON Boost пакетам
 • Начисляется ли бонус в UNI при активных бустах
 • Имеется ли связь между boost и farming модулями

⸻

📂 Отчёт по задаче должен включать:
 • Список всех таблиц Supabase с пометкой: ✅ активно / ⚠️ частично / ❌ не используется
 • Количество строк в каждой ключевой таблице
 • Список подтвержденных типов транзакций
 • Фактические уровни в реферальной цепочке
 • Указание на любые RLS-политики или причины, блокирующие запись

⸻

⏱️ Приоритет:

🔥 Высокий (priority #1)
Необходимо подтвердить, что система корректно сохраняет всю информацию о доходах, пользователях, транзакциях и партнёрских связях. Без этой проверки невозможно двигаться дальше к публичному запуску.

⸻