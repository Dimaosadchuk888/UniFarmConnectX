Вот как можно оформить письмо-ответ агенту — чётко, по делу, с техническими задачами, без допуска к изменениям кода на этом этапе:

---

**Тема:** 🔄 Запрос на решение проблемы с TON Boost + сравнение с UniFarming

Привет!

Спасибо за подробное исследование. Теперь, пожалуйста, перейди ко **второму этапу** — предложи **чёткое решение проблемы с TON Boost**, но **пока не вноси никаких изменений в код**.

---

### ✅ Что нужно сделать сейчас:

#### 1. **Предложить решение проблемы**

* Учитывая, что ты уже нашёл архитектурный конфликт в `tonBoostIncomeScheduler`,
* Напиши, **что конкретно нужно поправить**, чтобы доходы начали начисляться корректно и `balance_ton` обновлялся,
* Укажи конкретные интерфейсы и поля, которые нужно синхронизировать.

---

#### 2. **Провести архитектурное сравнение с UniFarming**

Сравни подробно:

| Компонент             | UniFarming                              | TON Boost                          |
| --------------------- | --------------------------------------- | ---------------------------------- |
| Источник баланса      | `balance_uni`                           | `balance_ton`                      |
| Где хранятся данные   | (таблица users / farming / ...)         | (где именно, укажи)                |
| Планировщик           | `farmingScheduler.ts`                   | `tonBoostIncomeScheduler.ts`       |
| Кто обновляет баланс  | `BalanceManager.updateBalance()`        | ❓ (уточни, вызывается ли он здесь) |
| Механизм начисления   | `processFarmingIncome()` → batch update | ❌ `userDeposit = 0`                |
| WebSocket уведомления | ✅ отправляются                          | ❌ нет транзакций и нет обновлений  |
| Транзакции            | `FARMING_REWARD`                        | ❌ `TON_REWARD` отсутствуют         |

---

#### 3. **Вывести точную причину, почему `balance_ton` не обновляется**

* Какие **модули не отрабатывают**
* Где именно **цепочка обрывается** (от планировщика → до фронта)
* Почему не создаются **TON транзакции**
* Проверить: вызывается ли **`BalanceManager`** или всё обходит его стороной

---

🔒 **Важно:**
👉 Пока ничего не менять. Никаких исправлений.
👉 Только чёткое, структурное описание решения и полной цепочки (от базы → до UI), как ты сделал с UniFarming.

Жду полный технический отчёт и предложение по решению. Спасибо!
— Ann

---

Если хочешь, могу дополнительно оформить шаблон сравнительной таблицы в Markdown, чтобы агент вставил туда факты.
