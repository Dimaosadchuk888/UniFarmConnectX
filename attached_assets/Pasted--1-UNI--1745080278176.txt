Ответ по результатам визуальной проверки интерфейса:
 1. Раздел “Фарминг > UNI Фарминг”
• Отображается только одна транзакция, хотя было открыто много фарминг-пакетов.
• Блок “Доход в сутки / Доход в секунду” выглядит как заглушка, цифры не соответствуют фактическим значениям в базе или ожидаемому доходу.
• История транзакций UNI не отображается, хотя в базе логируются farming_reward.
 2. Раздел “Фарминг > TON Boost”
• Последние TON Boost транзакции не отображаются в интерфейсе.
• Все блоки TON Boost показывают нулевой доход, хотя активные бусты были куплены и должны начислять доход.
• История транзакций по boost_farming не отображается, несмотря на наличие логов в БД.
 3. Раздел “Кошелёк”
• Полностью исчез блок транзакций, который ранее отображался корректно для всех монет (UNI, TON).
• Сейчас интерфейс отображает только сообщение “Кошелек не подключен”, даже если Tonkeeper уже авторизован.

⸻

Вывод:
• Проблема не в логике начислений или базе данных — фарминг начисляется и логируется.
• Проблема — в отображении транзакций и расчётах дохода на frontend:
 • Возможно, frontend не получает данные из новой структуры базы
 • Или неправильно обрабатывает входящие транзакции (формат, тип, привязку к пользователю)
 • Блок кошелька также, вероятно, потерял отображение после последней миграции логики.

Провести аудит frontend-компонентов, отвечающих за отображение истории транзакций, и выявить причины, по которым данные не отображаются, несмотря на успешную запись в базу данных.
⸻

Области проверки:
 • Компоненты:
 • FarmingHistory.tsx
 • TonBoostHistory.tsx
 • WalletTransactions.tsx
 • API:
 • /api/transactions/history
 • /api/farming/deposits
 • /api/boosts/active

⸻

Важно:

Запрещено:
 • Вносить любые изменения в код
 • Исправлять баги до утверждения аудита
 • Переписывать работающие участки (кошелёк, фарминг, boost)

Разрешено:
 • Добавить временное логирование
 • Подготовить отчёт о причинах отсутствия отображения
 • Указать, какие данные приходят/не приходят в компоненты