📱 Техническое задание: Адаптивность интерфейса (Wallet + Скроллы)

🎯 Цель:

Обеспечить полную мобильную адаптивность интерфейса UniFarm, особенно:
 • Раздела «Кошелек» → История транзакций
 • Всех скроллов и блоков, где возможен выход контента за рамки

⸻

📌 Задачи:

1. 🧾 Проверка и исправление блока История транзакций
 • Проверить как выглядит история транзакций на:
 • 📱 Мобильных устройствах (ширина до 480px)
 • 📏 Средних экранах (планшеты)
 • 🖥 Десктопах
 • Убедиться, что:
 • Контент не выходит за пределы карточки
 • Нет горизонтального скролла на странице
 • Длинные строки (например, адреса, ID транзакций) переносятся корректно
 • При необходимости:
 • Добавить word-wrap: break-word;
 • Использовать overflow-x: auto для оборачивания таблиц в мобильной версии
 • Убедиться в правильной высоте карточек на разных экранах

2. 🖼 Проверка скроллов на всех экранах
 • Проверить главную страницу и все компоненты:
 • Wallet
 • Boost
 • Farming
 • BalanceCard
 • Убедиться, что scroll работает стабильно:
 • На всех браузерах
 • Нет перекрытия нижней навигации
 • Нет двойных скроллов (внутренний и внешний одновременно)
 • Убедиться, что на iPhone и Android:
 • Фиксируются UI-ошибки (например, залипание футера, прыжки при скролле)

3. 🎨 Общие UI-детали
 • Все карточки должны иметь минимум отступов, но без сжатия контента
 • Проверить кнопки и шрифты на адаптивность — они должны масштабироваться
 • Проверить работу React Query UI состояний (загрузка, пусто, ошибка) — на всех экранах

⸻

🔐 Ограничения:
 • ❗ Не выходить за пределы текущей задачи
 • ❗ НЕ менять бизнес-логику или функционал
 • ❗ Работаем только с UI / адаптивностью / scroll / отображением

⸻

📤 На выходе предоставить:
 1. ✅ Отчет с перечнем исправленных компонентов
 2. 🧪 Список протестированных разрешений (моб, планшет, десктоп)
 3. 📸 Скриншоты ДО / ПОСЛЕ (если возможно)
 4. 🛠 Рекомендации по поддержанию адаптивности в будущем

⸻

📣 Комментарий:

Фокусируемся на качестве отображения. Контент не должен обрезаться или выходить за блок. Поведение должно быть одинаково стабильным во всех средах — мобильных, планшетах, десктопах. Вся работа строго внутри UI-задачи.