🔍 Никаких изменений — только исследование: нужно убедиться, что в Replit Preview сейчас подключен именно кабинет user_id = 48, и что UI (в частности, кошелёк) отображает реальные боевые данные.

⸻

🧪 Техническое задание для агента: Исследование GPT-Preview

Цель: Убедиться, что Replit Preview работает на боевом user_id = 48, и отображает настоящие данные, особенно в Wallet.

⸻

🔍 Этап 1: Проверка активного пользователя в Preview

Задача:
 • Проверить, какой user_id действительно подключён к GPT-Preview.
 • Сделать это через:
 • консоль (console.log)
 • UserContext
 • содержимое JWT токена (unifarm_jwt_token)
 • network-запросы (/api/v2/users/profile, /auth/me, и т.п.)

Ожидаем:
 • user_id = 48
 • telegram_id, username соответствуют базе
 • Токен валиден, без подмен

⸻

🔍 Этап 2: Проверка, отображаются ли реальные данные в Wallet

Задача:
 • Зайти на Wallet-страницу
 • Проверить в UI:
 • Баланс UNI
 • Баланс TON
 • История транзакций
 • Проверить в DevTools → Network:
 • Какие данные приходят из API
 • Соответствуют ли они данным из БД users, transactions

Если UI пустой:
 • Проверить, вызываются ли нужные endpoints (например, /wallet/balance, /transactions)
 • Что именно приходит в ответах
 • Какие ошибки (если есть) пишутся в консоль (DevTools → Console)

⸻

🔍 Этап 3: Сопоставление данных с БД

Задача:
 • Сравнить данные из Preview-интерфейса с тем, что реально лежит в базе для user_id=48

Таблицы:
 • users (UNI, TON, boost_package)
 • transactions (последние операции)
 • wallet (если есть отдельная)

⸻

📋 Вывод исследования

Агент должен предоставить:

Компонент Факт Подключен Комментарий
JWT токен payload с user_id = ? ✅ / ❌ user_id = 48 или другой
/profile возвращает user_id=? ✅ / ❌ user_id = 48?
Wallet API баланс приходит? ✅ / ❌ что вернул API
UI Wallet отображает баланс? ✅ / ❌ если нет — почему
Ошибки в консоли есть? ✅ / ❌ детали
Сопоставление с БД совпадают ли цифры? ✅ / ❌ различия?


⸻

📌 Важно:
🔒 Запрещается менять код, переменные, токены или окружение
🔎 Только исследование и сбор фактов