Вот точная задача для агента — сформулировано чётко и готово к передаче:

---

🔍 **Новая задача: Полный аудит системы по `ROADMAP.md`**

Привет! Нужно **поэтапно проверить ВСЮ систему** согласно последней версии `ROADMAP.md`.

---

### 📌 Что именно требуется:

**1. Проверка каждого модуля**

* Пройди по **всем разделам roadmap**
* На каждый модуль: **найди соответствующий код** (в `modules/`, `core/`, `config/`, `client/`)
* Убедись, что **всё описанное действительно реализовано**
* Если чего-то **нет — зафиксируй** это отдельно

**2. Проверка бизнес-логики**

* Каждая заявленная логика (фарминг, бусты, рефералы, бонусы, миссии, кошелёк и т.п.) должна быть:

  * [ ] В коде (модули, функции, схемы)
  * [ ] На уровне API (endpoints)
  * [ ] В Supabase (таблицы + поля)

**3. Проверка API**

* Пройтись по **всем 104 endpoint'ам**, сверить с:

  * Реальными маршрутами в `routes/` или `controller/`
  * Сервисами, которые они используют
  * Middleware и auth, если применимо

**4. Проверка структуры Supabase**

* Таблицы
* Поля
* Типы данных
* Связи между таблицами
* Убедиться, что все поля **из roadmap отражены в Supabase**

**5. Проверка фронтенда**

* Найти в `components/`, `pages/`, `services/`, `hooks/`
* Убедиться, что фронтенд действительно реализует всё описанное (особенно Telegram Mini App части, WebSocket, отображение доходов, миссии, бонусы и т.п.)

**6. Проверка инфраструктуры**

* Все процессы: планировщики, алерты, логгеры, валидаторы, monitoring
* Проверить папки: `core/`, `modules/scheduler/`, `utils/`, `config/`, `middleware/`

---

### 📄 Формат отчета:

Создай файл `ROADMAP_AUDIT_REPORT.md` с такой структурой:

```md
# ROADMAP AUDIT REPORT

## Модуль: User Management
✅ Реализовано полностью

- /profile — есть
- /create — есть
- ...
- Комментарии: всё работает корректно, покрытие 100%

---

## Модуль: Authentication
⚠️ Не реализовано:
- /logout отсутствует
- /session отсутствует
- ...

---

## Supabase Таблицы
- ✅ users — поля совпадают
- ⚠️ referrals — есть таблица, но нет данных
- ❌ user_missions — таблицы нет

---

## Заключение:
- Готовность: 92%
- Требуется доработать: logout, session, таблица user_missions, endpoint /stop
- Дополнительно: нужно задокументировать 4 внутренних файла
```

---

После завершения мы зафиксируем **финальный технический статус системы** и приступим к финальной QA-синхронизации.
