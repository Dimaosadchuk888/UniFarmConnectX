🟨 Техзадание T10 — Оптимизировать отображение страниц и скроллинг

Тип задачи: визуальная отладка и корректировка интерфейса
Среда: Replit
Модуль: client/
Цель: Устранить проблему усечённых страниц, настроить нормальную прокрутку на всех экранах Mini App

⸻

🎯 Цель задачи

На некоторых страницах интерфейс обрезается по высоте, пользователь не может доскроллить до конца. Нужно реализовать корректную прокрутку (scroll) и устранить визуальные ограничения, из-за которых скрывается контент.

⸻

📍 Где возникает проблема:
 • /wallet — не видно историю транзакций
 • /missions-nav — часть миссий обрезана
 • /farming — не отображаются бусты полностью
 • Общая проблема затрагивает большинство страниц Mini App

⸻

✅ Что нужно сделать:
 1. Убедиться, что контейнеры страниц поддерживают вертикальный скролл (overflow-y: auto)
 2. Проверить обёртки <main>, <div className="page">, layout — должны иметь height: 100% и не блокировать прокрутку
 3. Удалить или заменить все overflow: hidden, мешающие доскроллить
 4. Проверить, чтобы body, html, #root имели height: 100% или min-height
 5. На мобильных устройствах Telegram WebApp scroll должен работать на всех страницах

⸻

⚠️ Обязательные условия:
 • Нельзя ухудшать UX (всё должно остаться удобно и аккуратно)
 • Не трогать структуру компонентов и маршрутов
 • Не менять бизнес-логику (только CSS / style / layout)
 • Минимально касаться HTML — только при необходимости

⸻

📂 Где работать:
 • client/src/layouts/MainLayout.tsx
 • client/src/pages/*.tsx
 • client/src/styles/global.css (или аналогичный глобальный файл)
 • Компоненты, отвечающие за scrollable-контент: FarmingPage, MissionsPage, WalletPage

⸻

✅ Ожидаемый результат:
 • Все страницы корректно прокручиваются до конца
 • Контент больше не обрезается
 • Появляется scroll где необходимо
 • Работает и в Telegram, и в браузере
 • Компоненты адаптивны на мобильных

⸻

🧪 Финальная проверка:
 • На всех ключевых страницах виден полный контент
 • Удалённые стили overflow: hidden не мешают логике
 • Scroll доступен даже при маленьком балансе или пустых миссиях
 • Визуальные баги не появились