Вот срочный промт для агента, чтобы он **немедленно провёл полное исследование всех файлов** — **от фронта до БД**, по всей цепочке:

---

**🔧 Задача (СРОЧНО): Полный трек от фронта до базы — TON Boost / TON Farming**

Привет! Мы обнаружили критическую архитектурную проблему в логике TON Boost.

---

### 🔥 Критическая проблема:

Сейчас **доход от TON Boost начисляется не на `ton_farming_balance`, а на `balance_ton`**, то есть на весь основной баланс пользователя. Это приводит к **массовому завышению дохода**.

---

### 📌 Твоя задача:

**Провести полное исследование всей цепочки — от фронта до базы — для TON Boost:**

1. **Фронт (UI / отображение баланса):**

   * Где отображается `ton_farming_balance`?
   * Какие поля запрашиваются из API?
   * Откуда берётся значение при рендере Boost экрана?

2. **API и Backend:**

   * Какие контроллеры, сервисы, репозитории и модули участвуют в:

     * Покупке пакета
     * Начислении дохода
     * Отображении баланса (UI → API → DB)
   * Что именно используется планировщиком `tonBoostIncomeScheduler`:

     * Какие поля он запрашивает?
     * Какой баланс берёт в расчёт (`balance_ton` vs `ton_farming_balance`)?

3. **База данных:**

   * Какие поля и таблицы реально обновляются при покупке пакета?
   * Почему поле `ton_farming_balance` остаётся равным 0?
   * Где и как должен происходить апдейт этого поля?

4. **Сравнение с UNI Farming:**

   * Как реализована цепочка `deposit → farming_balance → начисления` в UNI farming?
   * Где хранятся и как обновляются farming балансы?
   * Какие отличия в архитектуре между UNI и TON farming?

---

### ⚠️ Ограничения:

* **Нельзя вносить никаких изменений в код.**
* Твоя задача — **провести полное трассирование**: какие файлы отвечают за какие этапы, какие поля используются, что идет не так.
* Все выводы должны быть подкреплены **фактами и конкретными файлами**, без догадок и предположений.

---

### 📈 Ожидаемый результат:

* **Карточка по каждому этапу цепочки**: фронт → API → контроллер → сервис → репозиторий → база данных
* Указание, где именно происходит ошибка (или архитектурная недоработка)
* Сравнение с UNI farming как эталонной схемой
* Чёткое понимание: **где и почему доход начисляется неправильно**

---

Жду срочный полный отчёт.
— Ann

---

Если нужно, могу помочь с шаблоном для документации каждого этапа.
