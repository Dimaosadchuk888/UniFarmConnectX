Ты — профессиональный AI-разработчик. Твоя задача — выполнить конкретное техническое задание, строго в рамках инструкции. Работай точно, локально и без побочных эффектов.

---

## ❗ ПЕРЕД НАЧАЛОМ РАБОТЫ:
- Убедись, что работаешь в актуальной ветке проекта (production/dev).
- Проверь, что UniFarmingCard действительно вызывает ошибку HTTP 500 при запросе данных.
- Логику frontend не изменять, задача — устранить ошибку на стороне backend API.

---

## 📌 ПРАВИЛА ВЫПОЛНЕНИЯ:
1. Не изменяй другие модули, кроме farming API.
2. Не удаляй и не сокращай существующую бизнес-логику.
3. Не заменяй контроллеры-заглушки, если они работают корректно.
4. Не добавляй заглушки, тестовые значения, @ts-ignore, any.

---

## 🧩 ТВОЯ ЗАДАЧА:

🎯 Цель: Устранить ошибку HTTP 500, которая возникает при запросе /api/v2/farming/data

### Конкретные действия:
1. Найти соответствующий контроллер и сервисный метод, обслуживающий endpoint /api/v2/farming/data или аналогичный (например, `FarmingController.getFarmingStatus()`).
2. Проверить логику получения информации о фарминге (TON и UNI).
3. Убедиться, что не происходит:
   - Ошибки при запросе из базы данных (например, undefined или null поля),
   - Ошибки из-за отсутствующих пользователей, токенов или некорректной авторизации,
   - Исключений без try/catch.
4. Добавить try/catch блок, если его нет, и логирование ошибки (через `logger.error`).
5. Убедиться, что возвращаемый ответ соответствует frontend ожиданиям (`farmingActive`, totalEarned, lastClaim, и т.д.).

---

## ⚙️ ДОПОЛНИТЕЛЬНО:
- Проверь корректность userId / telegramId, передаваемого в query или headers.
- Проверь, не вызывает ли frontend лишние запросы в цикл (если используется useEffect).
- Не изменяй frontend, только backend!

---

## 📄 ФОРМАТ ОТЧЁТА:
- ✅ Что было сделано (в каких файлах, какая логика исправлена)
- 🧪 Что было проверено (ручной вызов API, frontend карточка, отсутствие 500)
- ⚠️ Что осталось без изменений (если есть подозрения на баг — зафиксируй)

---

## 🎯 ЦЕЛЬ:
Убедиться, что /api/v2/farming/data корректно возвращает JSON-ответ, без HTTP 500 ошибок, и отображается в компоненте UniFarmingCard.