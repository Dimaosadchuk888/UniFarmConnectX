Вот как можно чётко и строго оформить **техническое задание для агента**, чтобы он без догадок и домыслов **нашёл точную причину**, почему *после внедрения накопительной логики TON Boost* в системе всё ещё **активно только 71 TON вместо 330 TON**:

---

# 🧾 Техническое задание: Исследование причины несрабатывания накопления TON Boost

**Роль:** Технический аудитор / разработчик backend
**Тип задачи:** Детальное расследование архитектурного конфликта
**Метод:** Анализ кода и структуры БД без внесения изменений

---

## 🎯 Цель

Проверить, **почему после внедрения логики накопления в TON Boost**, несмотря на 36 депозитов на сумму **330 TON**, в базе активным значится только **71 TON**.

---

## 📌 Факт проблемы

* Внесено 330 TON через 36 BOOST\_PURCHASE транзакций
* Активный farming\_balance: **71 TON**
* Потери: **259 TON** (≈78%)
* Ожидание: после фикса — все депозиты суммируются, как в UNI Farming
* Реальность: всё ещё работает замещение вместо накопления

---

## 🧠 Что нужно сделать

Ты должен провести **точное расследование**, *почему изменения, внесённые в activateBoost(), не работают как ожидалось*.

---

## 🔍 План действий (пошагово)

### Шаг 1: Проверка метода `activateBoost`

* Файл: `modules/boost/TonFarmingRepository.ts`
* Найти метод `activateBoost(userId, depositAmount, boostPackageId)`
* Убедиться, что логика **добавления** (`existing.farming_balance + depositAmount`) действительно используется в продакшене
* Проверить, не перезаписывается ли значение `farming_balance` другим кодом после этого

### Шаг 2: Отследить вызов метода `activateBoost`

* Файл: `modules/boost/service.ts`
* Метод: `purchaseWithInternalWallet()`
  — Убедись, что **вызов `activateBoost()` содержит правильную сумму** (в параметре `depositAmount`)
* Проверь, нет ли других путей записи в `ton_farming_data`, которые могли бы **обнулять или перезаписывать farming\_balance**

### Шаг 3: Проверка структуры БД

* Таблица: `ton_farming_data`
* Поля:

  * `farming_balance`: актуальное значение должно быть = сумма всех депозитов
  * `boost_package_id`
  * `farming_rate`

Убедись:

* что `farming_balance` обновляется через накопление, а не замену
* что ставка (`farming_rate`) тоже обновляется правильно при новых покупках

### Шаг 4: Проверка факта обновления farming\_rate

* Найти, откуда берётся `this.getPackageRate(boostPackageId)`
* Проверить, что она вызывается **на каждый вызов `activateBoost()`** и **сохраняется в БД**
* Протестировать сценарий:

  1. Купили Premium Boost → ставка 2.5%
  2. Купили Standard Boost → ставка должна стать 1.5%
  3. Проверить: какое значение `farming_rate` в БД

---

## ⚠️ Обязательно

* Без догадок. Привести **точную причину**, по которой накопление не происходит (например: ошибка в логике, дублирование, конфигурация Supabase, стороннее перезаписывание, некорректный импорт и т.д.)
* Указать **конкретный файл, метод, строку**, где происходит сбой
* Не касаться UI, не предлагать дизайн-решения

---

## 📋 Ожидаемый результат

1. Чётко указать:

   * Почему **не происходит накопление**
   * Почему **farming\_balance = 71 TON**, а не 330 TON
   * Почему **ставка `farming_rate` не меняется** корректно
2. Предоставить:

   * Все SQL-запросы, подтверждающие данные
   * Логику из кода, где происходит сбой
   * Выводы: что именно работает неверно и почему

---

Если нужна — могу сразу подготовить шаблон итогового отчёта, куда агент просто вставит результаты.
