✅ Техническое задание (ТЗ №7):

🔍 Исследование режима Replit Preview — авторизация, подключение к БД, отображение Boost-пакетов

⸻

🎯 Цель:

Проверить, работает ли Replit Preview кабинет как полноценный кабинет, и подключён ли он к реальной базе данных (Supabase), включая:
 • авторизацию (JWT),
 • корректную генерацию и сохранение user_id,
 • корректную загрузку Boost-пакетов.

Ничего не изменять. Только провести техническое исследование и предоставить отчёт.

⸻

📌 Что нужно проверить:

1. JWT авторизация
 • ✅ Сохраняется ли JWT токен в localStorage?
 • ✅ Есть ли запрос к /api/v2/auth/direct_registration?
 • ✅ Какой user_id присвоен пользователю?
 • ✅ Есть ли действующая сессия (можно ли вызывать приватные API)?

2. Подключение к Supabase
 • ✅ Делает ли frontend запросы к API, которые взаимодействуют с БД?
 • ✅ Отображаются ли актуальные значения (например, баланс, пакеты)?
 • ✅ Есть ли пользователь с этим user_id в Supabase таблице users?

3. Boost-пакеты (TON Boost)
 • ✅ Загружаются ли данные с API /api/v2/boost/packages?
 • ✅ Есть ли JWT в заголовке?
 • ✅ Возвращаются ли данные (массив с 5+ пакетов)?
 • ✅ Отображаются ли карточки на UI?
 • ✅ Пропадает ли toast “Не удалось загрузить Boost пакеты”?

4. Визуальная часть (UI)
 • ✅ Появляются ли карточки Boost-пакетов?
 • ✅ Отображаются ли:
 • цена в TON,
 • доход в сутки,
 • срок действия,
 • кнопка покупки?

⸻

📁 Дополнительно:
 • Проверить консоль браузера (F12) на наличие ошибок (401, 500, undefined, NaN)
 • Проверить вкладку Network:
 • Видно ли Authorization: Bearer <jwt>?
 • Какой ответ приходит от /api/v2/boost/packages?

⸻

📋 Формат отчёта от агента:

# Отчёт по исследованию Replit Preview кабинета

## 1. Авторизация
- JWT токен присутствует: ✅
- Содержимое токена: user_id=48, exp=..., ...
- Авторизация работает через direct_registration: ✅

## 2. Подключение к Supabase
- API /auth и /boost работают: ✅
- Пользователь найден в таблице `users`: ✅ (id=48)
- Загрузка данных корректна: ✅

## 3. Boost-пакеты
- Запрос: /api/v2/boost/packages
- Ответ: 200 OK
- Получено 5 пакетов
- Отображение: Все карточки отображаются, ошибок нет

## 4. UI
- Карточки отрисованы корректно
- Все поля заполняются (TON, % дохода, кнопки)
- Ошибок в консоли нет

## Вывод:
Replit Preview кабинет работает как полноценный кабинет.
Авторизация, подключение к БД и отображение Boost пакетов — ✅


⸻

🔁 После отчёта можно будет переходить к доработкам, если будут обнаружены проблемы.