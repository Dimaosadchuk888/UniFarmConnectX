⸻

🧾 ТЕХНИЧЕСКОЕ ЗАДАНИЕ: T41

📌 Роль:

Вы — AI-инженер в рамках проекта UniFarm. Ваша задача — выполнить точечную проверку недоступности Telegram Mini App после Redeploy. Запрещено придумывать или чинить другие ошибки. Проверка строго по описанному сценарию.

⸻

✅ Контекст:

После Redeploy UniFarm Mini App отображается ошибка:

Произошла ошибка
Что-то пошло не так при загрузке приложения

Ошибка наблюдается:
 • В Telegram Mini App
 • В браузерной версии по production-ссылке

⸻

🧪 Цель:

Проверить, почему Mini App не загружается:
 • Проблема в frontend (vite, index.html, Telegram.initData)
 • Проблема в backend (health, initData JWT, /me)
 • Ошибка конфигурации (vite.config.ts, routes.ts, CORS)
 • Ошибка манифеста Telegram Mini App

⸻

🔍 Этапы проверки:
 1. Проверка frontend-доступа
 • Перейти по production-ссылке Mini App напрямую в браузере
 • Убедиться, что frontend открывается (не 404, не 500)
 • Проверить, загружается ли index.html
 • Проверить, есть ли ошибки в консоли (window.Telegram.*)
 2. Проверка Telegram initData
 • Найти код, где вызывается Telegram.WebApp.initDataUnsafe
 • Убедиться, что он вызывается корректно
 • Проверить, используется ли он в App.tsx или index.tsx
 3. Проверка backend
 • Проверить endpoint /health (должен вернуть 200 OK)
 • Проверить /api/me или /api/v2/users (не должен быть 500)
 • Убедиться, что backend инициализирует Telegram JWT и не падает
 4. Проверка vite.config.ts
 • Есть ли base: '/'
 • Прописан ли fallback для SPA (например, rewrite на index.html)
 • Проверить настройки CORS

⸻

🚫 Запрещено:
 • Удалять любые файлы
 • Переписывать frontend
 • Менять структуру приложения
 • Подключать другие endpoints без согласования
 • Переносить проект на другую платформу

⸻

📤 Формат отчета:

🧾 ОТЧЕТ ПО T41

1. Frontend доступен: ✅/❌
2. Ошибки в браузерной консоли: ...
3. initData получен: ✅/❌ (описать)
4. Backend health (/health): ✅/❌
5. Ответ /api/me: ...
6. Проблема найдена: ...
7. Рекомендация: ...


⸻

🎯 Цель задачи:

Выяснить почему Mini App не запускается, не меняя архитектуру проекта. После получения отчета будет принято решение о следующем шаге.

⸻