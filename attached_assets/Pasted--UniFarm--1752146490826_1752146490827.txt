Отлично, принимаю роль главного аудитора UniFarm. Вот чёткое **техническое задание** для агента, которое сфокусировано **исключительно на поиске и анализе** — **никакого изменения кода, никакого "фиксинга" пока. Только расследование.**

---

### 🛠️ **Техническое задание: Аудит несрабатывающего списания UNI при открытии farming-пакета**

**Цель:**
Найти точку обрыва в цепочке списания UNI токенов при открытии farming пакета, **без вмешательства в код**, только анализ.

---

### 📍 Контекст:

* В разделе **"Кошелёк / Unitoken"**:

  * ✅ TON баланс корректно списывается при покупке Boost пакетов (через внутренний баланс)
  * ❌ UNI **не списывается** при открытии farming пакетов
  * ❌ Баланс UNI **не обновляется** в интерфейсе (UI), хотя должен
* Ранее **всё работало стабильно**: списания происходили, пакеты открывались, балансы обновлялись.

---

### 🔍 Что нужно сделать:

#### 1. **Проверить фронтенд цепочку отображения и обновления UNI-баланса:**

* Найти и проследить, какие компоненты на фронте отвечают за отображение UNI:

  * `BalanceCard`
  * `UserContext` (если оттуда берётся текущий баланс)
  * `WebSocket` и/или `queryClient` — получают ли новые значения?
* Проверить, действительно ли на клиенте **не происходит обновление баланса** после открытия farming пакета.

  * Срабатывает ли API обновление баланса?
  * Летит ли WebSocket update с новым значением?

#### 2. **Проверить backend-логику списания UNI при открытии farming пакета:**

* Исследовать **файл, отвечающий за открытие farming пакета**, например:

  * `modules/farming/controller.ts`
  * `modules/farming/service.ts`
  * `modules/farming/logic/rewardCalculation.ts`
* Определить:

  * Где происходит попытка списания UNI?
  * Есть ли вызов функции списания с `wallet` или `transactions` модуля?
  * Возвращает ли backend успешный ответ, и летит ли транзакция?

#### 3. **Проверить модуль `wallet` и `transactions`:**

* Убедиться, что транзакция **со списанием UNI**:

  * создаётся
  * попадает в базу (`transactions` или аналогичная таблица)
  * фиксируется логами

---

### 🔒 Ограничения:

* ❌ Не редактировать код
* ❌ Не чинить, не создавать временные заглушки
* ✅ Только **исследование цепочки**: UI → API → service → DB
* Отчёт должен быть полным, поэтапным: где начинается вызов, где обрывается

---

### 📋 Ожидаемый результат:

* Указана точка обрыва: где именно UNI **перестал списываться**
* Подтверждено, вызывается ли API, попадает ли запрос в контроллер
* Определено, отправляется ли транзакция в базу или нет
* Проверено, летит ли WebSocket с обновлением баланса

---

🧠 После получения отчёта будет принято решение: править в UI, API или логике farming.
