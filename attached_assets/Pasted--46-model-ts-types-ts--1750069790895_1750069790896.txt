🧩 Техническое задание Т46: Проверка структуры и состояния всех моделей model.ts и типов types.ts во всех модулях

⸻

📌 Контекст:

Проект UniFarm содержит множество модулей (auth, user, wallet, airdrop, referral, farming, boost, dailyBonus, tonFarming, admin, monitor, transactions и др.), каждый из которых должен включать корректно оформленные и подключённые:
 • model.ts — для описания структуры данных
 • types.ts — для TypeScript-типов

В некоторых модулях:
 • Эти файлы отсутствуют или не используются
 • Есть дублирование или устаревшие поля
 • Присутствует частичное подключение без использования

⸻

🚫 Запрещено:
 • Удалять существующие файлы без проверки
 • Создавать новые файлы без подтверждения их отсутствия
 • Менять архитектуру без согласования
 • Вносить изменения в другие модули

⸻

✅ Задача:
 1. Пройтись по каждому модулю в modules/ и:
 • Проверить наличие файлов model.ts и types.ts
 • Проверить, подключаются ли они в controller.ts, service.ts, logic.ts или index.ts
 • Если файл есть, но не используется — указать это
 • Если файл отсутствует — зафиксировать
 • Если файл присутствует, но содержит пустую или ошибочную структуру — указать
 2. Проверить, используются ли типы и модели в сервисной логике (а не просто существуют)
 3. Проверить на дублирование и повторяющиеся поля
 4. Сформировать таблицу по каждому модулю:

Модуль model.ts types.ts Используется в логике? Комментарий
auth ✅ ✅ ✅ Всё подключено корректно
airdrop ❌ ✅ ⚠️ model.ts отсутствует
tonFarming ✅ ❌ ⚠️ types.ts отсутствует


⸻

📁 Зоны поиска:
 • modules/*/model.ts
 • modules/*/types.ts
 • modules/*/*.ts на предмет использования этих моделей/типов

⸻

📤 Формат отчёта:

Создать файл MODEL_TYPES_REPORT.md в корне. В нём:
 • Таблица с результатами
 • Комментарии к каждому модулю, где есть проблемы
 • Общий вывод: какие модули требуют доработки, какие можно оставить

⸻

🎯 Цель:

Завершить аудит всех моделей и типов, чтобы избежать хаоса, багов и утечек валидации. Это важна частина перехода в стабильный production.

⸻