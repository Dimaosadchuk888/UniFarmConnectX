Отлично! Ниже — обновлённый и жёсткий промт с обязательным требованием: проверить каждый файл проекта по отдельности, без исключений. Это ключевое указание подчёркнуто специально.

⸻

✅ Финальный Промт для агента: Тотальный аудит проекта UniFarmConnectX

⸻

🎯 ЦЕЛЬ:

Провести полный технический аудит проекта UniFarmConnectX.

⸻

🔒 ЖЁСТКИЕ УСЛОВИЯ:
 • ❌ НЕЛЬЗЯ редактировать, удалять, перемещать или дописывать код;
 • ❌ НЕЛЬЗЯ выполнять “поверхностную проверку” или “на глаз”;
 • ✅ ОБЯЗАТЕЛЬНО ПРОВЕРИТЬ КАЖДЫЙ ФАЙЛ ОТДЕЛЬНО:
Каждый файл — отдельная строка в чеклисте. Никаких группировок. Ничего не пропускать. Никаких “это не важно”.

⸻

🔍 ЧТО НУЖНО ДЕЛАТЬ:
 1. Пройтись по всему проекту, по всем папкам и подпапкам (включая root, client/, modules/, shared/, utils/, и любые другие).
 2. Взять каждый файл отдельно, и по нему:
 • определить, подключён ли он;
 • используется ли он;
 • является ли он дубликатом;
 • является ли устаревшим, временным или мусорным;
 • правильно ли подключён (импорты, зависимости, конфиги);
 3. Проверять:
 • .js, .ts, .jsx, .tsx, .json, .md, .sql, .env, .config.js, .cjs, .sh, .log, и любые другие форматы;
 • временные / старые / backup / old / copy файлы;
 • тесты, mock-данные, заглушки;
 • дубликаты с похожими названиями (Card.tsx и CardOld.tsx и т.п.)

⸻

📋 ЧЕК-ЛИСТ ДОЛЖЕН ВЫГЛЯДЕТЬ ТАК:

Файл Папка Подключён? ✅/❌ Используется? ✅/❌ Дубликат? ⚠️ Комментарий
ReferralCard.tsx components ✅ ✅ ❌ Рендерится на главной странице
ReferralCardOld.tsx components ❌ ❌ ⚠️ Старый, не подключён
vite.config.ts root ✅ ✅ ❌ Используется в сборке
server.log root ❌ ❌ ❌ Мусор, удалить после аудита


⸻

❗ ОЧЕНЬ ВАЖНО:
 • Каждый файл проекта — должен быть проверен отдельно;
 • Нельзя писать “эта папка в порядке” — только пофайловая проверка;
 • Даже если файл кажется мусорным — он должен быть указан в списке и прокомментирован;
 • Если файл не подключён, не используется или является копией — это должно быть зафиксировано в таблице.

⸻

📝 ФИНАЛЬНЫЙ ОТЧЁТ ДОЛЖЕН СОДЕРЖАТЬ:
 • Полный список файлов проекта (все, без исключения);
 • По каждому файлу:
 • Подключён ли он;
 • Используется ли;
 • Дубликат или нет;
 • Подозрение на мусор или временный;
 • Комментарий, где он используется (или почему нет);
 • Разметка в виде чек-листа ✅ ❌ ⚠️;
 • Желательно: выделить цветом строки в отчёте (зелёный = ОК, жёлтый = частично, красный = ошибка/мусор).

⸻

📣 Никакие файлы проекта не должны быть проигнорированы. Каждый файл = отдельный пункт отчёта. Только так будет возможна чистка, рефакторинг и деплой.

⸻

Если хочешь, я сделаю Google Sheets шаблон с галочками и цветами — готов загрузить. Скажи только «да».