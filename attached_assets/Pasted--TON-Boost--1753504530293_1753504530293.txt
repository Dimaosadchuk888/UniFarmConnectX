🔧 ЭЗАДАНИЕ (ПРОТОТИП): Анализ работы TON Boost — Сопоставление полей, подключений и причин багов

⚠️ Это экспертное задание (ЭЗ) является прототипом. По этой структуре агент должен провести полный анализ подключения системы 
TON Boost к базе данных. Главная цель — выяснить, какие поля реально используются системой, какие поля существуют в базе данных, 
и есть ли между ними несоответствия. 

📌 Цель:
Выяснить, как реализована логика TON Boost в системе и почему после покупки Boost-пакета не создаётся депозит. 
Также установить, какие поля система пытается использовать, какие реально заполнены, и в какие поля она пишет.

🧩 Что проверить:

1. Какие таблицы и поля задействованы в логике TON Boost:
   - Определить, какие именно поля таблиц запрашиваются системой в момент:
     - Покупки Boost
     - Начисления процентов
     - Отображения на фронтенде
   - Какие поля система ожидает (в коде запроса или логике)
   - Какие поля реально существуют и используются в базе

2. Фактическое подключение:
   - К каким таблицам и полям система реально подключена
   - Где происходит расхождение между ожиданием и фактическими записями

3. Анализ таблиц (по модели):

| Таблица              | Проверить поля                                             | Что делать                          |
|----------------------|------------------------------------------------------------|-------------------------------------|
| users                | boost_package_level, ton_balance                           | Проверить, обновляется ли Boost     |
| transactions         | type, description, tx_hash, user_id, amount                | Проверить, фиксируется ли покупка   |
| user_boost_package   | user_id, level, timestamp, source                          | Проверить, создаётся ли лог         |
| ton_farming_data     | user_id, amount, source, is_active, start_time, package_level | Проверить, создаётся ли депозит     |

4. Проверка логики создания депозита:
   - Есть ли вызов создания депозита внутри функции Boost
   - Если есть — проверить, не блокируется ли он ошибкой/условием
   - Если нет — это основная причина отсутствия farming

5. Проверка планировщика:
   - Какие условия фильтрации депозита (например: `source = 'TON_BOOST'`)
   - Как он выбирает активные депозиты
   - Может ли быть пропуск из-за пустого поля или неправильного значения

🧠 Задачи для агента:

1. Провести полное сопоставление:
   - Какие поля и таблицы ожидаются системой (по коду)
   - Какие реально используются
   - Где происходят несостыковки (например: поле запрашивается, но в таблице всегда NULL)

2. Вывести список всех точек подключения и сравнить с моделью данных

3. Отметить:
   - Где отсутствуют вызовы создания записей
   - Где система не может найти нужные поля
   - Какие расхождения могли вызвать:
     - отсутствие начислений
     - отсутствие депозитов
     - дублирование

📎 Примечание:

Это задание — ориентир. Агент должен использовать его как чеклист и в ходе анализа дополнить реальными фактами: 
где что подключено, где есть проблемы, и почему Boost не создаёт депозит.