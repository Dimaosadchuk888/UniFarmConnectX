Роль: Ти — технічний інтегратор-фіналізатор UniFarm.
Твоя задача — виправити критичні помилки, виявлені в останньому аудиторському звіті, без створення нових файлів, дублювання, тестових копій або тимчасових рішень.
Працюй самостійно. Не став уточнень. Не змінюй структуру системи.

Основні принципи:
1. Працюй тільки з існуючими файлами
2. Кожне виправлення — окремий звіт (файл, рядок, що виправлено і чому)
3. Після кожної дії — оновлюй чеклист нижче (☑️ / ⬜️)
4. Після всього — фінальний висновок про готовність до продакшну

Чеклист критичних завдань:

1. Виправити критичну помилку імпорту
⬜️ Виправити ConnectionStatus в server/db.ts (рядки 41–43)
• Наслідок: неможливість запуску серверу
• Дія: скоригувати імпорт і тип, без створення нового enum або константи

2. Telegram-авторизація
⬜️ Уніфікувати збереження guest_id / telegram_id
⬜️ Забезпечити стабільне створення профілю користувача
⬜️ Перевірити логіку створення/використання реферального коду
⬜️ Виправити дублювання або конфлікти при повторному вході

3. Консолідація контролерів і сервісів
⬜️ Об’єднати:
   • *Controller.ts та *ControllerFallback.ts
   • *Service.ts та *ServiceInstance.ts
⬜️ Видалити дублікати:
   • routes.ts, routes-fixed.ts, routes-new.ts
⬜️ Залишити лише 1 версію на кожну логіку

4. Стандартизація API-відповідей
⬜️ Уніфікувати всі відповіді API під формат:
{ success: true/false, data: ..., error?: { message: ..., code?: ... } }
⬜️ Виправити HTTP-коди (200/400/401/500)
⬜️ Забезпечити відповідність усіх endpoint'ів цьому формату

Додатково (не обов'язково, але бажано виконати):
⬜️ Видалити дублікати SQL-запитів (де ще є — наприклад, storage-adapter.ts)
⬜️ Провести м'який рефакторинг LSP/SRP у сервісах, якщо помітно критичні порушення
⬜️ Видалити *original.ts, якщо вони не використовуються
⬜️ Видалити зайві файли db-adapter.ts, db-standard.ts, якщо вони вже неактуальні

Звітність:
Після кожного пункту — надай:
• ☑️ Статус
• Назву файлу
• Рядок (якщо можливо)
• Що саме виправлено і чому

Фінально — висновок про готовність до деплою. Якщо ще не готово — напиши, чому.