Вот чёткий и строгий промпт для агента, который отражает твою задачу и требования:

---

### 🔍 **Исследование проблемы: Баланс UNI не обновляется автоматически после начислений через UniFarming**

#### 📌 Цель:

Выяснить, **почему баланс пользователя не обновляется автоматически** после выполнения farming-операций, несмотря на то, что **транзакции `UNI_FARMING_REWARD` видны в истории**. Баланс остаётся прежним даже после **перезагрузки сервера**, что нарушает целостность системы.

---

### 📋 Условия и ограничения:

1. **🚫 Запрещено вносить какие-либо изменения в код или БД** — только аналитика и отчёт.
2. **⛔ Без предположений и догадок.** Все выводы должны основываться **только на коде и текущих данных**.
3. Важно найти **точные места разрыва цепочки** или несостыковки, даже если их несколько.
4. Цель — не «починить», а **доказательно исследовать и локализовать проблему**, чтобы мы приняли решение.

---

### 📂 Область исследования (всё в рамках продакшн-кода и текущей базы данных):

1. **База данных:**

   * Проверить, создаются ли корректно записи о `UNI_FARMING_REWARD` в таблице транзакций.
   * Проверить, изменяется ли физически баланс в таблице пользователя после этих транзакций.

2. **Backend:**

   * Проверить, вызывается ли функция обновления баланса после создания транзакции `UNI_FARMING_REWARD`.
   * Логика расчёта farming-награды (`rewardCalculation.ts`)
   * Логика применения транзакций к кошельку пользователя (`transactions.ts`, `wallet.service.ts`, `wallet.controller.ts` и т.д.)

3. **Frontend:**

   * Проверить, слушает ли фронт WebSocket или другие события по изменению баланса.
   * Убедиться, что компонент баланса запрашивает актуальные данные или подписан на обновления после операций farming.

---

### 🧾 Что ты должен предоставить в ответ:

1. **Подробный отчёт по результатам проверки** всех звеньев, связанных с автоматическим обновлением баланса.
2. Указать **где конкретно происходит обрыв** (или несколько обрывов), по коду, по логике, или на уровне данных.
3. Приложить ссылки или участки кода, где наблюдаются:

   * отсутствие вызова,
   * некорректная логика,
   * неработающее событие/обновление/триггер.
4. Указать, **в каких файлах и строчках** находятся критические участки.

---

### ✅ Цель этого этапа:

Ты **не исправляешь проблему**, а **докладываешь по факту**, где именно в текущей системе происходит сбой, **с максимально возможной технической точностью**, чтобы мы приняли обоснованное решение о способе починки без риска сломать продакшн.

---

Хочешь — я могу помочь составить шаблон для отчёта, чтобы он сразу писал по нужной структуре.
