✅ Техническое задание T55 – Проверка и активация партнёрской программы

📌 Контекст:

В проекте UniFarm партнёрская программа уже реализована, но:
 • Она не начисляет доход от TON Boost и UNI Farming
 • Не создаются транзакции типа referral_income в базе данных Supabase
 • В коде может существовать логика, но она не подключена или не вызывается
 • Возможны дубликаты старой логики (Replit, Neon) — их использовать нельзя

⸻

🛑 Жёсткие ограничения:
 1. ❌ Запрещено создавать дубликаты referralService.ts, controller.ts, referral.types.ts, моделей и логики.
 2. ❌ Запрещено переписывать архитектуру, если это не согласовано.
 3. ❌ Нельзя использовать устаревшие или неиспользуемые модули (например, из Neon).
 4. ❌ Нельзя создавать новый endpoint, если он уже есть.

⸻

✅ Задача:

Твоя задача — найти, проверить и активировать логику партнёрской программы без дублирования:

🔍 1. Найди существующую бизнес-логику:
 • В каких модулях реализована передача реферального дохода (возможно в referralService.ts, transactions, wallet)
 • Есть ли логика обхода 20 уровней по ref_code и процентных начислений

🔗 2. Проверь вызов логики:
 • Начисляется ли referral_income при TON Boost (через /boost/purchase или wallet)
 • Начисляется ли referral_income при UNI Farming (через /farming/start или farming reward)

🧾 3. Проверь базу данных Supabase:
 • Создаётся ли транзакция типа referral_income в таблице transactions
 • Записываются ли поля: type, from_user, to_user, amount, source

🛠 4. Исправь:
 • Если логика не вызывается — подключи её в нужные места (farming, boost)
 • Если нет записи в БД — добавь создание referral_income
 • Если обход не до 20 уровней — реализуй обход по маркетинговой логике
 • Если используются console.log — замени на logger

⸻

📌 Ожидаемый результат:
 • ✅ Партнёрская программа работает при доходе от TON Boost и UNI Farming
 • ✅ В Supabase создаются транзакции referral_income для всех уровней
 • ✅ Нет дубликатов логики — используется централизованный referralService
 • ✅ Логика строго соответствует маркетингу: 1 уровень — 100%, далее от 2% до 20%
 • ✅ Все действия логируются через core/logger.ts

⸻

📁 Файлы, которые можно использовать:
 • modules/referral/referralService.ts
 • modules/referral/controller.ts
 • modules/farming/service.ts
 • modules/boost/service.ts
 • modules/transactions/service.ts
 • core/logger.ts
 • shared/transactions.types.ts, referral.types.ts

⸻

📝 Отчёт:

Укажи в отчёте:
 1. Где именно была найдена логика
 2. Что было подключено / изменено
 3. Как вызывается логика для TON и UNI
 4. Примеры записей в базе данных (тип referral_income)
 5. Вывод: готовность партнёрской программы в процентах

⸻