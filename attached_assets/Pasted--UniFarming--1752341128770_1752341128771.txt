Вот точное и структурированное техническое задание под финальное **исследование сектора UniFarming**, строго по коду и фактам, **без опоры на старые отчёты агента**:

---

# 🔬 Глубокое исследование сектора UniFarming (ФИНАЛ)

## 🎯 Цель

Провести **чистовое техническое исследование** текущей реализации модуля **UniFarming** — на основе **реальных файлов проекта**, без использования ранее составленных отчётов.

---

## ✅ Основные условия:

* ❗ **Код не править**
* ❗ Не опираться на старые документы
* ✅ Изучить **текущую архитектуру**: frontend, backend, БД
* ✅ Исследовать **по фактам** — на основе живых данных и логики
* ✅ Проверить **открытие новых депозитов**, которые были только что созданы вручную
* ✅ Убедиться, что **вся цепочка работает стабильно и корректно**

---

## 🔍 Что проверить:

### 1. **Backend-логика**

* Открытие фарминг-пакета:

  * Где принимается запрос (controller/route)
  * Как отрабатывает метод депозита (service/repository)
  * Как списывается сумма (BalanceManager или аналог)
  * Где и как сохраняется информация (таблицы/поля БД)

* Начисление процентов:

  * Где запускается начисление (cron, scheduler и т.д.)
  * Какая формула используется
  * Где фиксируются начисления (таблицы, поля)

* Проверка, что проценты добавляются к балансу пользователя

---

### 2. **База данных**

* Проверить, что вручную открытые депозиты:

  * Отражаются в `farming_sessions` (или аналогичной таблице)
  * Имеют корректную сумму, user\_id, дату старта
  * Не дублируются
  * Помечены как `active` или аналогично

* Начисления:

  * Записи FARMING\_REWARD или подобные
  * Привязаны к тем же user\_id
  * Суммы начислений логичны (по ставке)
  * Баланс пользователя увеличивается

* Обязательно: проверить записи по **пользователю с ID 74 / telegram\_id: 999489**

---

### 3. **Frontend**

* Где и как:

  * Отправляется запрос на открытие пакета
  * Отображается активный фарминг
  * Показываются или не показываются транзакции

* Проверить компонент или страницу:

  * Есть ли отображение FARMING\_DEPOSIT
  * Есть ли отображение FARMING\_REWARD
  * Правильно ли обновляется баланс (через WebSocket или polling)
  * Используется ли `/api/v2/transactions`, `/uni-farming/status`, `/wallet` и т.п.

---

### 4. **Интеграция**

* Проверить **реальный живой поток**:

  * Депозит → фиксируется → начисляется → отображается
  * Без разрывов и расхождений
  * Без багов, задержек, ошибок в логах

---

## 📌 Особенности:

* ✅ **Файлы и названия могут отличаться** от прежних версий — агент должен найти актуальные и использовать их
* ✅ Исследование должно быть **строго по фактам**: выводы на основе кода и БД
* ✅ Исследование покрывает **весь сектор UniFarming**
* ❌ Не добавлять UI-улучшений
* ❌ Не исправлять код
* ❌ Не предлагать новые функции

---

## 🧾 Вывод:

После завершения этого исследования мы должны:

* ✅ Быть уверены, что UniFarming работает «на 10 из 10»
* ✅ Подтвердить стабильную работу всех процессов и потоков
* ✅ Не иметь скрытых архитектурных проблем или потерь данных

---

Хочешь, я сразу оформлю это в `UNIFARMING_FINAL_TECH_AUDIT.md`?
