🚀 Финальный чек-лист перед deploy проекта с Replit Secrets

1. Конфигурация и секреты
 • Все секреты (API ключи, токены, адреса) вынесены в Replit Secrets
 • В проекте отсутствует файл .env и его бэкапы (.env_backup удалён)
 • .gitignore корректно настроен и не допускает попадания секретов в git
 • Файлы .replit.unified, .replit.workflows, .replit.workflow обновлены и не содержат устаревших настроек

2. Сервер и backend
 • Сервер запускается корректно без ошибок без .env файла
 • Все API эндпоинты (GET, POST, PATCH, DELETE и др.) работают и возвращают ожидаемые данные
 • JWT аутентификация и авторизация работают корректно
 • Логика работы с базой данных (Supabase) отрабатывает без ошибок
 • Обработка ошибок настроена и возвращает понятные сообщения
 • WebSocket соединение успешно устанавливается и поддерживается

3. Frontend
 • Frontend успешно получает все VITE_* переменные из окружения
 • Telegram Mini App корректно инициализируется, передаёт данные (initData)
 • UI отображает актуальные данные: балансы, транзакции, статус пользователя и т.п.
 • Все взаимодействия с API работают без ошибок и задержек
 • Нет ошибок в консоли браузера (DevTools)
 • UI адаптивен и корректно отображается на целевых устройствах

4. Интеграции
 • Telegram боты работают с правильными токенами из Secrets
 • Webhook’и Telegram и TON платежей настроены и активны
 • Платёжный модуль TON Boost принимает платежи, фиксирует транзакции и обновляет баланс
 • Все внешние зависимости и библиотеки загружены и не вызывают ошибок

5. Безопасность
 • Нет никаких секретов в открытом виде в коде и репозитории
 • Replit Secrets доступны только нужным сервисам
 • Включён .gitignore для исключения секретных файлов
 • JWT секрет используется корректно и не раскрыт
 • Проверена и включена rate limiting или защита от спама (если применимо)

6. Документация и workflow
 • .replit.workflow настроен на production режим, без тестовых или dev-команд
 • Документация по запуску и настройке обновлена и соответствует актуальному состоянию проекта
 • Вся команда в курсе новых настроек и порядка работы с Replit Secrets

7. Финальные проверки
 • Проверить, что в продакшен среде нет старых или временных данных, которые могут влиять на работу
 • Сделать бэкап базы данных перед деплоем (если возможно)
 • Провести smoke-тесты основных функций проекта после деплоя
 • Проверить мониторинг и логи на наличие ошибок
 • Убедиться, что все модули проекта работают согласованно

⸻

Если всё из чек-листа ✅ — можно запускать redeploy и спокойно идти в production! 🎉