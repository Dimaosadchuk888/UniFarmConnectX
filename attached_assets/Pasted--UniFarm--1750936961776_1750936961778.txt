Отлично, приступим к финальной пошаговой проверке проекта UniFarm по всем папкам. Цель — убедиться, что:
 • Все папки содержат только нужные продакшн-файлы.
 • Нет заглушек, мусора или временных скриптов.
 • Все функции и модули работают как часть продакшн-системы.
 • Проверка идет строго по папкам, одна за одной, в стиле “контроль качества перед запуском”.

⸻

🔍 СТРАТЕГИЯ ПРОВЕРКИ — ЧАСТЬ 1

📁 1. client/
 • Проверить: src/pages/, components/, layouts/, App.tsx, main.tsx
 • Цель: Нет ли больше заглушек, подмен, упрощённых компонентов.
 • Убедиться: Запускается React, работает маршрутизация, не используется Simple*, Mock*, Placeholder*

📁 2. server/
 • Проверить: controllers/, routes/, middleware/, index.ts, db.ts
 • Цель: Всё ли подключено, нет ли неиспользуемых файлов.
 • Убедиться: Express поднимается, API отвечает, есть подключение к Supabase.

📁 3. core/
 • Проверить: содержит ли бизнес-логику, утилиты, хуки.
 • Убедиться: используется во всех нужных местах — особенно core/farming, core/missions.

📁 4. modules/
 • Проверить: Каждому модулю соответствует отдельная зона приложения.
 • Убедиться: Есть индексные файлы, модули целостны, нет незавершённых папок.

📁 5. shared/
 • Проверить: Компоненты, используемые в разных частях приложения.
 • Убедиться: Ничего не дублируется с components/, стили общие.

📁 6. config/
 • Проверить: vite.config.ts, tsconfig*.json, .env*, .replit, tailwind.config.js
 • Убедиться: Нет конфигурационных конфликтов, все пути резолвятся корректно.

📁 7. types/
 • Проверить: TypeScript типы для API, DTO, интерфейсов пользователя.
 • Убедиться: Все типы используются, нет неактуальных.

📁 8. utils/
 • Проверить: Утилиты и хелперы. Например, конвертация валют, форматы даты.
 • Убедиться: Нет дубликатов и устаревших функций.

📁 9. dist/
 • Проверить: Собран ли проект в эту папку (dist/public, dist/server)
 • Убедиться: Используется ли в production, нет ли случайного dev-содержимого.

📁 10. logs/ и docs/
 • Проверить: Только нужные лог-файлы, нет ли мусора.
 • Убедиться: Документация адекватна и относится к текущей архитектуре.

⸻

✅ ПРОМПТ ДЛЯ АГЕНТА (используй как есть):

Ты должен провести поэтапную финальную проверку всего проекта UniFarm. Раздели аудит по всем основным папкам в корне:

- client/
- server/
- core/
- modules/
- shared/
- config/
- types/
- utils/
- dist/
- logs/
- docs/

Для каждой папки:
1. Проверь, что все файлы и подпапки актуальны и относятся к production версии.
2. Обозначь подозрительные или неиспользуемые файлы.
3. Убедись, что структура соответствует модульной архитектуре.
4. Никакие временные файлы, заглушки или "Simple*", "Mock*", "Placeholder*" использовать нельзя.
5. Никаких обходных решений, костылей, упрощенных компонентов — работаем только с оригинальной системой.
6. Проверяй работоспособность всех файлов — если используется компонент, должен быть его вызов.
7. Сделай вывод по каждой папке: ✅ чисто, ⚠️ подозрительно, ❌ требует очистки.

Отчет должен быть максимально честный и основан только на содержимом проекта, а не на предположениях.


⸻

Когда агент завершит проверку по всей структуре, мы сможем спокойно переходить к деплою или устранению последних ошибок.