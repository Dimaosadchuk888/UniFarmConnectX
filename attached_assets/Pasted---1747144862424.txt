Задача: Удалить все карточки с миссиями которые есть на странице и разместить новые задания: 

⸻

✅ Список миссий:

№ Социальная сеть Задание Ссылка Награда
1 Telegram канал Подпишись на Telegram-канал Universe Games https://t.me/UniverseGamesChannel +500 монет
2 Telegram чат Вступи в Telegram-чат Universe Games https://t.me/UniverseGamesChat +500 монет
3 YouTube Подпишись на YouTube-канал Universe Games https://youtube.com/@universegamesyoutube?si=XHebHkmEcGpADUAE +500 монет
4 TikTok Подпишись на TikTok Universe Games https://www.tiktok.com/@universegames.io +500 монет.      Ограничения при работе с проектом:

1. Работать строго с существующими файлами и компонентами:

   * Использовать только те файлы, которые уже присутствуют в проекте и связаны через импорты, роуты или конфигурации.
   * Не создавать новые файлы, модули, контроллеры или сервисы без явного запроса на это.

2. Изменять только при необходимости:

   * Вносить минимальные и точечные правки для решения конкретной задачи или исправления ошибки.
   * Сохранять текущую архитектуру и логику работы проекта без добавления новых сущностей или концепций.

3. Запрещено создавать альтернативные реализации:

   * Не создавать файлы с похожими именами (например, Controller, ControllerNew, ControllerV2).
   * Не дублировать маршруты API и не создавать новые endpoints, если это не предусмотрено изначальным проектом.

4. При необходимости новой сущности — обязательно запрашивать разрешение:

   * Любая попытка создать новый файл, компонент или API должна предварительно согласовываться вручную.

5. Соблюдать существующую структуру и соглашения по наименованию:

   * Следовать уже используемым паттернам проектирования, архитектурным принципам и стилям кода.
   * Не менять структуру проекта без отдельного запроса и одобрения.


6. Запрещено выполнять автоматический рефакторинг кода без явного одобрения:

   * Не изменять архитектуру, названия классов, функций, переменных и файлов в рамках «оптимизации» или «улучшения читаемости» без предварительного запроса.
   * Любые предложения по рефакторингу должны предоставляться в виде рекомендаций и обсуждаться до внесения изменений.Работать строго в рамках поставленной задачи:       
Выполнять только те действия, которые прямо указаны в задаче.
Не анализировать и не изменять другие части кода без явного запроса.
Любые инициативные правки, улучшения или предложения по неотносящимся областям проекта необходимо оформлять в виде рекомендаций, без внесения изменений в код.