🔍 Ответ и уточнения по текущим задачам (важно: никаких правок в коде без согласования!)

🧩 1. Возможные риски при переименовании referral_reward → referral_reward_ton

Ты предлагаешь внести изменения в схему, добавив referral_reward_ton. Прежде чем мы это допустим, просим ответить:
 • ❓ Как это повлияет на отображение TON-транзакций в истории?
У нас есть подозрение, что отображение транзакций может быть завязано на значение type, и любое изменение приведёт к их исчезновению в интерфейсе.
 • ❓ Есть ли в базе перемешка TON и UNI транзакций?
Нужно точно установить: не использует ли история транзакций одну таблицу с фильтрацией по type, и не отвалится ли отображение или логика анализа (в том числе в кошельке).
 • ✅ Просьба: Проведи диагностику и опиши последствия, если мы внедрим referral_reward_ton как отдельный тип. Только после анализа можно принимать решение.

⸻

⏱️ 2. Планировщик начислений (Dev vs Prod)

Ты пишешь, что уже решал проблему с таймингом, и всё работало, но после redeploy всё откатилось назад.
 • ❓ Где именно хранится конфигурация запуска крон-задачи?
 • Есть ли вероятность, что используется кешированная версия крона или cron.ts?
 • Возможно, в pm2, Docker, Fly.io, Railway или другом оркестраторе остался старый планировщик?
 • ❓ Почему на девелопере все транзакции идут строго: 12:35, 12:40 и т.д., а на проде — с разбросом?
Надо убедиться, что не работает второй крон параллельно, или нет “зацикленных” перезапусков.
 • ✅ Просьба: только диагностика, без внесения изменений в код или таймер.

⸻

💸 3. Проблема с отображением пополнения TON через connect-wallet

Ты написал, что баланс “растёт”, но:
 • ❗Вопрос был не в росте баланса.
 • ❗Проблема в том, что после отправки TON через TonConnect пополнение не отображается как транзакция, хотя TON действительно отправлен и баланс технически изменяется.

Просим ответить:
 • ❓ Какая цепочка компонентов участвует в отображении входящих транзакций?
 • Wallet Service?
 • Transaction Service?
 • External listener или парсер транзакций?
 • Возможно, getWalletTransactions() фильтрует только исходящие?
 • ❓ Как можно отследить входящие транзакции TON через Tonkeeper, если они не отображаются, но попали в базу?
 • ❗ Особенно важно: если транзакции уже есть в базе, почему они не показываются в UI?

⸻

🔒 Обязательные условия
 • 🚫 Категорически запрещается вносить правки в код без отдельного письменного разрешения.
 • ✅ Ваша задача: максимально точная диагностика, анализ и предложения по исправлению, но не вмешательство.