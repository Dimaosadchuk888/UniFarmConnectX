⸻

🔬 Промпт для агента (только исследование, без исправлений):

Проведи детальное исследование всей системы на соответствие ранее выданному техническому заданию. Твоя задача — не вносить никаких изменений, а точно выяснить текущее состояние проекта, включая риски, несоответствия и подтверждение работоспособности всех компонентов.

⸻

📌 Исследуй следующие ключевые направления:

1. Дублирование и обходные пути
 • Есть ли в проекте дублирующие файлы, такие как UserServiceV2.ts, ReferralCodeDisplay, и подобные?
 • Используются ли в интерфейсе или логике временные/обходные версии вместо оригинальных компонентов?
 • Подключен ли где-то код, который больше не нужен или конфликтует с текущей архитектурой?

2. Архитектурная целостность
 • Используются ли в проекте только оригинальные компоненты и сервисы?
 • Все ли подключения и зависимости построены на стабильной основной структуре?
 • Присутствует ли “раздвоение логики” или конфликт между версией V1 и V2 в любом месте?

3. Работа реферальной системы
 • Возвращается ли корректный ref_code из API?
 • Отображается ли ref_code на интерфейсе (раздел “Друзья”) корректно?
 • Обрабатывается ли он в UserContext согласно спецификации (поддержка ref_code и refCode)?
 • Сохраняется ли правильно в localStorage и извлекается без ошибок?
 • Работает ли копирование реферальной ссылки?
 • Что происходит, если у пользователя отсутствует ref_code — как реагирует интерфейс?

4. Миссии, farming, бонусы
 • Сколько активных миссий реально отображается на интерфейсе?
 • Совпадает ли это с данными API?
 • Функции bonus и farming — отображаются ли, активны ли?

5. Кэш и устаревший код
 • Загружается ли на фронтенде устаревший JS-код?
 • Есть ли признаки, что браузер кэширует удаленные или устаревшие версии скриптов?
 • Используется ли Cache-Control, no-store, etag или подобные механизмы?
 • Есть ли Service Worker, localStorage или другие участки, где может “залипать” старый код?
 • Есть ли неиспользуемые зависимости или старые версии библиотек?

⸻

📋 Формат ответа

Ответ должен быть чётким, по пунктам:

✔ Компонент: [что работает правильно]
✖ Компонент: [что не соответствует] → [в чём ошибка / чем вызвано / где находится]
❓ Подозрения: [не подтвержденные, но возможные проблемы]

Никаких исправлений выполнять не нужно — только диагностика и вывод текущего состояния проекта.

⸻