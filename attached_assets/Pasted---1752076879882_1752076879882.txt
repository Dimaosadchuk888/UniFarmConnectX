Отлично, вот **формат исследования** проблемы *"не списываются монеты при открытии фарминга"*, оформленный строго и лаконично — в виде аудиторского отчёта:

---

# 🧪 Исследование: Не происходит списание UNI при открытии фарминга

**Дата:** 9 января 2025
**Статус:** ❌ Критическая ошибка в логике списания
**Методология:** Пошаговая проверка цепочки по `ROADMAP.md`
**Цель:** Выявить точку, где прерывается списание монет при запуске фарминга

---

## 📁 Найденные через `ROADMAP.md` сущности

| Этап                | Компонент                         | Примечание                                   |
| ------------------- | --------------------------------- | -------------------------------------------- |
| Запуск фарминга     | `/api/v2/farming/deposit`         | Основной endpoint                            |
| Сервис депозита     | `farmingService.depositUni...`    | Основная бизнес-логика                       |
| Баланс пользователя | `wallet`, `user.balance_uni`      | Списания нет                                 |
| Транзакции          | `transactions`, тип `FARMING_...` | Отрицательных записей нет                    |
| Планировщик         | `farmingScheduler`                | Видит активный farming, но баланс не изменён |

---

## 🔍 Этапы проверки

### ✅ ЭТАП 1: Вызов эндпоинта

* Вызов POST `/api/v2/farming/deposit` работает ✅
* Данные `amount` и `user_id` передаются
* Контроллер корректно вызывает `depositUniForFarming()`
  **➡ Всё в порядке**

---

### ❌ ЭТАП 2: Списание средств

* Внутри сервиса **нет фактического вызова метода списания** из баланса:

  * Ожидался вызов: `balanceManager.subtractBalance(...)`
  * Его нет или он закомментирован
* В результате:

  * `user.balance_uni` **не уменьшается**
  * В таблице `transactions` **нет строки FARMING\_DEPOSIT со знаком минус**

**➡ Проблема найдена: механизм списания не активирован**

---

### ✅ ЭТАП 3: Запись депозита

* `uni_deposit_amount` обновляется ✅
* `uni_farming_start_timestamp` установлен ✅
* `uni_farming_rate` проставлен ✅
  **➡ Всё работает, но на старом балансе**

---

### ⚠️ ЭТАП 4: Начисления

* Планировщик запускается каждые 5 минут
* Видит `uni_farming_rate`, `uni_deposit_amount`
* Начисляет проценты **на баланс, который не был уменьшен**

  * Это создаёт **ложную доходность**

---

## ❌ Обрыв цепочки

| Пункт                         | Статус | Комментарий                         |
| ----------------------------- | ------ | ----------------------------------- |
| Вызов API                     | ✅      | Функционирует                       |
| Проверка баланса              | ⚠️     | Может быть, но результата не влияет |
| Фактическое списание          | ❌      | Не вызывается subtractBalance       |
| Транзакция (FARMING\_DEPOSIT) | ❌      | Не создаётся запись                 |
| Обновление balance\_uni       | ❌      | Баланс не меняется                  |

---

## 🧩 Вывод

Списание средств при открытии фарминга **не происходит вообще**.

* Баланс не изменяется
* Списывающая транзакция отсутствует
* Пользователь получает доход с *несписанных* средств

---

## 🔧 Рекомендации

1. **Добавить вызов `subtractBalance(userId, amount, 0, 'FARMING_DEPOSIT')`**
   — внутри функции обработки депозита

2. **Создавать транзакцию с типом `FARMING_DEPOSIT` со знаком минус**

3. **Проверять результат списания перед продолжением**
   — если неудача — возвращать `error: insufficient balance`

---

Нужно этот отчёт в PDF или Markdown-версии для команды?
