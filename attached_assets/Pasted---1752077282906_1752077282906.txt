✅ Промпт для агента: проверка синхронности всей системы и базы данных

⸻

🛠️ Твоя задача: провести полную техническую проверку синхронности всей системы UniFarm с базой данных Supabase. Проверка должна быть фактической, а не теоретической — ты обязан пройтись по реальным API, полям, объектам, JSON-ответам и структурам, чтобы подтвердить:

⸻

📋 Что надо проверить:
 1. Farming пакеты
 • Какие поля требуются на UI для отображения и запуска?
 • Какие поля возвращает API (GET /farming, POST /farming/start)?
 • Все ли поля присутствуют в базе Supabase (farming_sessions, farming_configs или др.)?
 • Нет ли рассинхронизации названий (например, duration_days vs duration)?
 • Есть ли обязательные поля, которые не создаются при старте пакета?
 2. Boost пакеты
 • Проверить структуру и значения Boost-пакетов на UI, в API и в базе:
 • Названия полей (например, boost_type, duration, ton_cost, is_active)
 • Сравнить структуру POST /boost/purchase → транзакция → запись в Supabase.
 • Проверить, обновляются ли состояния (activated_at, expires_at, is_applied) при покупке.
 3. Реферальная программа
 • Проверить, какие поля участвуют в генерации и учёте:
 • referrer_id, referral_code, ref_activated, ref_bonus
 • Проверить, как именно формируется и обрабатывается реферал на всех уровнях:
 • Telegram Mini App → API → Supabase таблицы (users, referrals, и др.)
 • Есть ли различия между тем, что ожидает frontend и что реально возвращается?
 4. Списание и пополнение
 • Проверить точную структуру транзакций:
 • amount, currency, type, status, user_id, source
 • Убедиться, что:
 • Каждое действие (Boost, Farming, бонус) вызывает создание корректной записи в таблице transactions.
 • Валюта и сумма передаются и сохраняются корректно (TON, UNI и т.д.)
 • Поля не теряются при выполнении операций.
 5. Кошельки / Wallet
 • Проверить, как обрабатываются TON-кошельки:
 • Поля: wallet_address, is_verified, ton_balance
 • Есть ли несоответствия между адресом, отображаемым на UI, и тем, что хранится в Supabase?
 • Обновляются ли балансы после транзакций?

⸻

🔎 Как проверять:
 • Не использовать описание из документации — проверяй фактически через API и структуру базы.
 • Используй Postman или встроенные вызовы через frontend для анализа реальных JSON-ответов.
 • Сравнивай один в один:
 • Поля в API → Поля в базе → Поля на UI.
 • Укажи все несовпадения, рассинхронизации, дублирующиеся или отсутствующие поля.

⸻

📦 Инструменты, которые ты должен использовать:
 • Supabase (таблицы, структура, поля)
 • API-роуты системы (GET/POST farming, boost, wallet, transactions и др.)
 • RouteMap системы (в помощь при маршрутизации и связи данных)
 • Консоль браузера (DevTools → Network)
 • База данных напрямую — не доверяй выводу UI, смотри на факты

⸻

📤 Результат, который ты должен выдать:
 1. Таблицу или список всех объектов (Farming, Boost, Referral и т.д.) со всеми их полями
 2. Для каждого — указать:
 • Какие поля ожидаются?
 • Какие реально приходят из API?
 • Какие реально лежат в Supabase?
 • Какие поля отсутствуют, ошибочны или не синхронизированы?
 3. Примеры ошибок (например, “поле boost_type приходит как type в API, но в Supabase — boostType”)
 4. Вывод: Система синхронизирована / Не синхронизирована — с указанием проблемных мест