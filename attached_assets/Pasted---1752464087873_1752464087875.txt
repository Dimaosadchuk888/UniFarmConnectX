Вот точный и формализованный **промпт для агента** в формате **Технического задания (ТЗ)**. Он задаёт чёткую роль, цель и границы допустимых действий:

---

# 📌 Техническое задание: Аудит множественных депозитов в UniFarm (без вмешательства в код)

## 🎯 Цель

Провести **полный аудит работы системы UniFarm** в двух направлениях:

1. **UNI Farming**
2. **TON Boost**

Сфокусироваться на **наличии и обработке множественных депозитов**:
– Можно ли открыть несколько пакетов одновременно?
– Как система их обрабатывает?
– Как начисляется доход?
– Как это отображается в истории транзакций?

## 👤 Ваша роль:

Вы выступаете как **технический аналитик**, задача которого — **провести глубокое исследование системы и базы данных** *без изменения кода и без вмешательства в текущую логику*.
Вы должны дать **только подтверждённые выводы** и подготовить **доказательную базу на основе SQL-запросов и существующей структуры данных**.

## 📌 Объекты исследования

### 1. UNI Farming

* ✅ Можно ли иметь одновременно несколько farming депозитов?

* ✅ Где и как они хранятся (в какой таблице)?

* ✅ Если я открыл 10 и более депозитов, как это отображается:

  * в базе данных?
  * в начислениях (транзакции)?
  * в пользовательском балансе?

* ✅ Создаётся ли по каждому депозиту отдельная транзакция?

* ✅ Фиксируются ли все начисления (FARMING\_REWARD)?

---

### 2. TON Boost

* ✅ Можно ли открыть несколько пакетов TON Boost?
* ✅ Как они хранятся в базе (одна строка или много)?
* ✅ Как начисляется доход по каждому из них?
* ✅ Создаётся ли отдельная транзакция на каждый активный boost?
* ✅ Как система фиксирует эти операции:

  * в таблице `ton_farming_data`?
  * в таблице `transactions`?
  * в `metadata.original_type`?

---

## ⛔ Ограничения

* ❌ Не вносить никаких изменений в код проекта
* ❌ Не запускать миграции или обновления данных
* ✅ Только **SQL-запросы, анализ данных и документация логики**

---

## 📄 Ожидаемый результат

* Подробный отчёт с таблицами, выборками и скриншотами (если применимо)
* Ответы на все вышеуказанные вопросы
* Выводы о текущем состоянии системы (работает ли корректно?)
* Выявление архитектурных ограничений (если есть)

---

💡 *Если вы обнаружите, что множественные депозиты технически невозможны или обрабатываются некорректно — укажите это с объяснением причин.*

Готовы? Начинайте анализ и возвращайтесь с отчётом.
