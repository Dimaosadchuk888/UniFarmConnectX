Вот корректный **Prompt (Техническое задание)** для агента, который должен провести повторную верификацию и предоставить обоснованный отчет:

---

# 📋 ТЕХНИЧЕСКОЕ ЗАДАНИЕ

### 🎯 Цель

Провести **финальную верификацию работоспособности модуля TON Boost** после изменений, касающихся логики накопления депозитов.

---

### 🧩 Твоя роль

Ты — **технический аудитор системы**, задача которого — убедиться, что:

* Система **реально работает корректно** (а не только выглядит как рабочая)
* **Все цепочки** — от фронта до базы данных — функционируют стабильно
* **Проблемы в исторических данных не маскируются**, а исключены

---

### ✅ Что нужно проверить

1. **Накопление депозитов**

   * Суммируется ли `farming_balance` при новых покупках
   * Отображается ли накопленный баланс корректно в БД (`ton_farming_data`)
   * Создаются ли транзакции `BOOST_PURCHASE` на каждую покупку
   * Отражается ли верная сумма в `metadata` этих транзакций

2. **Начисления дохода**

   * Начисляется ли доход с **полного `farming_balance`**
   * Правильна ли ставка (`farming_rate`) в расчётах
   * Привязаны ли транзакции `TON_BOOST_INCOME` к нужному пользователю и сумме

3. **SQL и ошибки**

   * Есть ли ошибки типа `invalid input syntax for type json`
   * Почему эти ошибки появляются
   * Какие поля metadata вызывают сбой

4. **Несоответствие между транзакциями и балансом**

   * Почему транзакции показывают меньшую сумму, чем фактический `farming_balance`
   * Нужно точно выяснить: это из-за истории или из-за текущей логики?

5. **Проблемы у других пользователей**

   * Проверь всех пользователей с активными Boost'ами
   * Почему у некоторых `farming_balance = 0` при наличии BOOST\_PURCHASE
   * Составь список таких ID и объясни причину по каждому

---

### 📌 Условия

* **НЕ меняй код**
* Только **аудит, SQL и логический анализ**
* Покажи **чёткие факты и выводы** — что работает, что нет
* Если есть ошибки — обязательно **укажи, где конкретно**: в логике, в транзакциях, в обновлении данных, в расчетах и т.д.
* **Если найдена одна ошибка — продолжай исследовать дальше**, не останавливайся

---

### 📤 Что ты должен выдать:

* **Финальный отчёт**: текстовый, с блоками «что работает», «что не работает», «что вызывает ошибки»
* **SQL-запросы**, которыми ты всё проверил
* **Список user\_id**, у которых выявлены отклонения
* Подтверждение, что система готова или не готова к использованию

---

⏳ Срок выполнения: как можно скорее, но с полным покрытием всех пунктов
📌 Отчёт должен быть на 100% основан на реальных проверках, а не на предположениях

---

Хочешь, я его отправлю за тебя, как сообщение агенту?
