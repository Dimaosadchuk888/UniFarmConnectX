🎯 [ИНСТРУКЦИЯ ДЛЯ АГЕНТА]

Тема: Диагностика необработанного TON-депозита User #25 (Production)
Условие: ❗ Категорически запрещено вносить изменения в код или базу данных без моего личного одобрения. Только диагностика.

⸻

📌 ОБЩАЯ СИТУАЦИЯ:
 • Депозит в TON был подтвержден пользователем через TON Connect.
 • Транзакция успешно прошла в блокчейне. Админ получил средства.
 • Однако депозит не был зафиксирован в базе данных, и баланс пользователя не обновился в интерфейсе.
 • User #25 подтвержден как валидный Production-пользователь.

⸻

🔍 ЧТО УЖЕ НАЙДЕНО:
 1. TON транзакция подтверждена на блокчейне.
 2. User #25 существует в продакшн-БД.
 3. Баланс в UI не отображается.
 4. Транзакция НЕ была записана в таблицу transactions.
 5. Метод BalanceManager.addBalance() не вызывался.
 6. WebSocket-обновление не инициировалось.

⸻

📁 ТВОЯ ЗАДАЧА:
 1. Провести полную диагностику на Production (НЕ в Preview).
Убедись, что работа ведется в контексте User ID = 25 (Telegram ID: 425855744).
 2. Подтвердить, в каком месте цепочка обработки TON-депозита была прервана:
 • Обработчик события после TON Connect
 • Верификация транзакции
 • Создание записи в transactions
 • Вызов BalanceManager.addBalance()
 • Отправка WebSocket-уведомления
 • Обновление frontend-данных
 3. Подтвердить все пути вывода информации на UI:
 • client/src/services/balanceService.ts
 • client/src/contexts/userContext.tsx
 • API /api/v2/wallet/balance?user_id=25 — JWT невалиден?
 4. Уточни, есть ли недостающий webhook или отключенный listener для депозита TON через Connect.

⸻

🛑 ОБЯЗАТЕЛЬНЫЕ ОГРАНИЧЕНИЯ:
 • Не изменяй код.
 • Не запускай write-запросы в базу.
 • Никаких автоматических исправлений.
 • Работа должна быть строго аналитической, основанной на фактах, логах и трассировке вызовов.
 • Все выводы должны быть подтверждены — никаких предположений.

⸻

🧠 ПОСЛЕДУЮЩИЕ ДЕЙСТВИЯ:

После завершения анализа — подготовь четкий технический отчет:
 • Где именно разрывается логика
 • Какие конкретно файлы, методы, endpoints задействованы
 • Что именно нужно будет сделать (после подтверждения), чтобы депозит User #25 попал в базу и отобразился