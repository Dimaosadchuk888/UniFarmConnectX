Вот финальная версия ТЗ с учётом того, что **названия и структура файлов могут отличаться**, и агенту нужно следовать **реальной логике проекта**, а не только шаблону:

---

# 🧪 Исследование сектора UniFarming

## 🎯 Цель

Провести **детальное техническое исследование всего сектора UniFarming** — от открытия фарминг-пакетов до начислений и фиксации транзакций в системе.

---

## 🔍 Объект исследования:

### 1. **Модули и внутренняя логика**

* Изучить и зафиксировать **реальную архитектуру и логику**, которая лежит в основе UniFarming.
* Обратить внимание, что **названия файлов и модулей могут отличаться** от примеров — задача агента следовать реальной структуре и коду проекта.
* Исследовать все используемые функции, в том числе:

  * логика открытия пакета,
  * расчёт доходности,
  * использование cron-задачи,
  * обработка фарминг-сессий и транзакций.

---

### 2. **Фиксация депозитов**

* Проверить, что при открытии пакета:

  * Проходит авторизация пользователя по JWT
  * Вызывается соответствующий метод для депозита
  * Баланс корректно уменьшается
  * В базе создаются корректные записи:

    * `farming_sessions`
    * флаг `uni_farming_active`
    * поле `uni_deposit_amount`
    * таймстамп начала фарминга

---

### 3. **Начисление дохода с пакетов**

* Проверить реализацию периодических начислений (например, через cron или иные планировщики)
* Убедиться, что:

  * Доход начисляется согласно формуле `депозит × ставка × дни`
  * Записи о начислениях фиксируются в базе (например, в `transactions`, `farming_sessions`)
  * Баланс пользователя увеличивается
  * Начисления происходят **автоматически, без участия пользователя**

---

### 4. **Отображение начислений**

* Проверить, отображаются ли FARMING\_REWARD транзакции:

  * Через API `/api/v2/transactions` или соответствующие endpoint'ы
  * В истории транзакций в кошельке
* Убедиться, что:

  * Привязка к пользователю сохраняется
  * Валюта указана корректно (UNI)
  * Суммы совпадают с расчётами

---

### 5. **Работа с базой данных**

* Проверить и зафиксировать:

  * Записи в `transactions`, `farming_sessions`, `users`
  * Фактическое обновление баланса пользователя
  * Отсутствие дублирования или потерь данных
  * Все изменения корректно связываются с `user_id` пользователя (в частности — ID 74, `telegram_id: 999489`)

---

## 📌 Важно:

* Это **исследование**, а не реализация фичи.
* Агент должен **ориентироваться на фактическую реализацию**, а не просто следовать шаблону из документа.
* Названия файлов, путей и структура модулей **могут отличаться** — агент должен адаптироваться к архитектуре текущего проекта.
* Цель — **подтвердить надёжность и корректность текущей системы UniFarming** по всей цепочке:

  1. Открытие пакета
  2. Фиксация в БД
  3. Начисление
  4. Отображение в транзакциях
  5. Обновление баланса

---

## 🛑 Вне рамок задачи:

* ❌ Любые визуальные изменения
* ❌ UI-улучшения или редизайн
* ❌ Добавление новых компонентов, полей, данных
* ❌ Изменение бизнес-логики или cron-задач

---

Если нужно, могу сразу оформить этот документ в `UNIFARMING_INVESTIGATION_PLAN.md`.
