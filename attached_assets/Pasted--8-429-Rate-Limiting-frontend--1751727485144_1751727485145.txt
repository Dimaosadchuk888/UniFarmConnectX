✅ Техническое задание №8: Централизованная обработка ошибок 429 (Rate Limiting) на frontend

🎯 Цель:

Добавить обработку ошибок типа 429 Too Many Requests в пользовательском интерфейсе, чтобы при превышении лимита запросов (rate limit) приложение не ломалось, а отображало понятное уведомление пользователю.

⸻

🛑 Проблема:

В отчёте:

❌ Нет обработки ошибок 429 (rate limiting) в UI



Симптомы:
 • При превышении количества запросов к API (например, частый переход по страницам, нажатие кнопок, запросы от бота) — сервер возвращает 429.
 • Frontend не перехватывает это, пользователь получает “ошибку в консоли” или “ничего не происходит”.
 • UX портится: непонятно, почему всё зависло или не работает.

⸻

🔍 1. Где возникает ошибка?

Часто затрагиваемые зоны:
 • client/src/services/api.ts
 • client/src/hooks/useQuery*
 • client/src/pages/Farming.tsx
 • client/src/pages/Wallet.tsx
 • client/src/components/common/ErrorBoundary.tsx

⸻

📁 2. Что нужно сделать:

🔧 A. Добавить глобальный перехват 429 ошибок в API-сервис

Файл:

client/src/services/api.ts

Что сделать:
 • Обновить общий обработчик ошибок (axios.interceptors.response или fetch wrapper)
 • Проверять: если status === 429, показать уведомление:

toast.error("Слишком много запросов. Попробуйте позже.");



⸻

🧩 B. UI уведомление

Библиотека:
 • Использовать уже установленную react-toastify или shadcn/ui (если подключен toast)

Требование:
 • Уведомление должно отображаться максимум 1 раз в 10 секунд, чтобы не заспамить пользователя

⸻

🧪 C. Обновить ErrorBoundary

Файл:

client/src/components/common/ErrorBoundary.tsx

Добавить:
 • Поясняющий текст при ошибке 429, если она не поймана выше
 • Опционально — кнопка “Повторить запрос”

⸻

🔁 D. Протестировать сценарии:
 1. Быстрый скролл → список транзакций (wallet)
 2. Частое открытие farming карточек
 3. Быстрое переключение между вкладками

⸻

✅ Критерии завершения:
 • Ошибка 429 перехватывается во всех случаях
 • Пользователь видит уведомление
 • Нет “белого экрана”, неработающего UI или падения приложения
 • В консоли нет необработанных 429 ошибок
 • Компоненты не зацикливаются в повторных запросах

⸻

📄 Финальный отчёт должен включать:
 • ✔️ Пример кода перехвата 429 в api.ts
 • ✔️ Скриншот уведомления на UI
 • ✔️ Подтверждение обработки ошибки на Farming/Wallet страницах
 • ✔️ Проверка повторных запросов (UI стабилен)

⸻

🧩 Ожидаемый результат
 • Поведение системы при 429 становится дружелюбным
 • Ошибки не скрываются, но и не пугают пользователя
 • Приложение устойчиво при перегрузке

⸻