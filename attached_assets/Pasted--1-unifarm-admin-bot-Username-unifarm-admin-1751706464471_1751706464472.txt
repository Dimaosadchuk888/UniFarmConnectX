✅ ТЗ №1: Настройка нового админ-бота @unifarm_admin_bot

🔐 Данные бота:
 • Username: @unifarm_admin_bot
 • API Token: 7662298323:AAFLgX05fWtgNYJfT_VeZ_kRZhIBixoseIY

⸻

🎯 Цель:

Перенести весь админский функционал (выплаты, заявки, статистика, управление пользователями) из основного бота @UniFarming_Bot в отдельного админ-бота @unifarm_admin_bot.

⸻

📌 Что нужно сделать:

1. Подключить нового админ-бота
 • Настроить webhook или polling
 • Использовать текущую архитектуру (встроенный fetch, без внешних SDK)
 • Протестировать, что бот стабильно работает и получает команды

2. Добавить админ-проверку по Telegram username
 • ✅ Авторизовать только двух пользователей:
 • @a888bnd
 • @DimaOsadchuk
 • Проверка по username в дополнение к is_admin из базы

3. Перенести весь админский функционал из @UniFarming_Bot
 • Команды:
 • /admin — главное меню
 • /stats — общая статистика
 • /users, /user <id> — пользователи и детали
 • /withdrawals — заявки на вывод (если уже реализовано)
 • /approve <id>, /reject <id> — подтверждение/отклонение заявок
 • /missions, /mission_complete, /mission_reward
 • /ban <telegram_id>
 • Логика должна работать только внутри админ-бота

4. Добавить блок “Заявки на вывод” (если не реализован)
 • Бот должен получать заявки на вывод TON
 • Показывать:
 • Username
 • Wallet address
 • Сумма
 • Дата
 • Кнопки: ✅ “Подтвердить”, ❌ “Отклонить”
 • При подтверждении: бот отправляет выплату (если возможно) или фиксирует подтверждение в базе

⸻

❗️Важно: все вызовы, связанные с админкой, теперь идут только через @unifarm_admin_bot.

⸻

🚫 ТЗ №2: Очистка основного бота @UniFarming_Bot от админ-функций

🎯 Цель:

Оставить в боте только основную пользовательскую логику:
 • Авторизация через Telegram
 • Работа с Mini App
 • Стартовое приветствие

⸻

📌 Что нужно сделать:

1. Удалить из @UniFarming_Bot все админские команды
 • /admin, /stats, /users, /missions и т.д.
 • Убедиться, что ни одна из админ-функций не реагирует

2. Проверить код
 • Убедиться, что в коде основного бота не осталось упоминаний:
 • is_admin
 • /ban, /withdrawals
 • логика отправки заявок в чат

3. Тестирование
 • Пройти полный UX Mini App через Telegram
 • Убедиться, что ничего не сломано в пользовательской части

⸻

🧪 Режим тестирования:

После выполнения всех пунктов:
 1. Мы вручную протестируем:
 • Подключение @unifarm_admin_bot
 • Все команды и права
 • Получение заявок
 • Ответы на команды
 2. Отдельно протестируем @UniFarming_Bot с обычного аккаунта — чтобы он не выдавал админ-меню и работал строго как клиентский.

⸻

✅ Формат финального отчета, который должен предоставить агент:
 • Подключение бота @unifarm_admin_bot: [Да/Нет]
 • Реализованные команды:
 • /admin
 • /stats
 • …
 • Админ-доступ проверен: [Да/Нет]
 • Вызовы из @UniFarming_Bot удалены: [Да/Нет]
 • Тест UX @UniFarming_Bot: [Ок/Ошибка]