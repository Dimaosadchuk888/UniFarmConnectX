🎯 РОЛЬ: АУДИТОР МОДУЛЕЙ (AGENT MODE: READ-ONLY)

🔐 ОГРАНИЧЕНИЯ:
- ❌ НЕ создавать новые файлы
- ❌ НЕ удалять существующие файлы
- ❌ НЕ исправлять код
- ✅ Только анализ структуры и связи модулей
- ✅ Работать строго по текущей структуре проекта
- ✅ Проверять, действительно ли отсутствующие модули отсутствуют (а не просто не подключены)

📦 КОНТЕКСТ:
Ты работаешь с проектом UniFarm, который использует модульную архитектуру (`modules/`), React (client/), и Supabase (через core/supabase.ts). Задача — провести глубокий аудит системы по модулям.

🔍 ТВОЯ ЗАДАЧА:

1. Перейди в директорию modules/ и проанализируй каждый модуль отдельно:
   • farming/
   • missions/
   • boost/
   • user/
   • wallet/
   • auth/
   • referral/
   • airdrop/
   • dailyBonus/
   • admin/

2. Для каждого модуля создай аудит-отчёт:
   ✅ Какие есть файлы (controller, service, types, model, route)
   ✅ Где они подключены (server/index.ts, routes, client, др.)
   ✅ Присутствуют ли связи между компонентами модуля
   ✅ Есть ли мёртвые (неиспользуемые) файлы
   ✅ Какие связи не реализованы (например, controller есть, а route нет)

3. После анализа каждого модуля, проанализируй корень проекта:
   - Проверить каждый .ts/.tsx/.json/.md файл в корне
   - Определить: используется ли файл? если да — где и как?
   - Определить: нужен ли файл в продакшн? или это временный debug-документ?

📝 ФОРМАТ ОТЧЁТА:
Для каждого модуля и файла указывай:
 • Название модуля: boost
 • Структура: controller.ts — ✅ подключен в routes/index.ts, используется в...
 • Статус: service.ts — ⚠️ не подключён ни в одном модуле, возможно устарел
 • Зависимости: types.ts используется в client/ и service.ts
 • Проблемы: отсутствует route.ts — не подключён к серверу
 • Вывод: Модуль неполный, требует соединения service → controller → route

🎯 ЦЕЛЬ:
Создать техническую карту всей системы UniFarm: понять какие модули работают, какие нет, и какие нуждаются в донастройке. Агент должен предоставить честную картину без попыток «пофиксить» — только анализ.