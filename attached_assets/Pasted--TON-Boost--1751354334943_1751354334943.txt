🧾 Обновлённое Техническое Задание: Финальная правка TON Boost оплаты

⸻

📌 Цель:

Исправить два критических недочёта в системе покупки Boost-пакетов:
 1. Неправильный API endpoint при оплате с внутреннего баланса
 2. Неверный адрес приёма TON-платежей при оплате через внешний кошелёк (ConnectWallet)

⚠️ Работать только с текущей системой, без дублирования логики и без добавления новых маршрутов или структур.

⸻

🔧 Что нужно сделать:

⸻

1. ✅ Исправить API endpoint для покупки с внутреннего баланса
 • Проблема:
Сейчас запрос идёт по неправильному маршруту ❌ /api/v2/ton-farming/purchase
 • Что делать:
 • Найти вызов этого endpoint на фронте
 • Заменить на правильный маршрут:

✅ /api/v2/boost/purchase


 • Убедиться, что передаётся payment_method='wallet'
 • Проверить, что после покупки данные фиксируются в базе (boost_purchases)

⸻

2. ✅ Обновить адрес получателя TON-платежей (через ConnectWallet)
 • Проблема:
Сейчас система использует адрес кошелька пользователя в качестве получателя, что критически неправильно.
 • Правильно должно быть:
При оплате через внешний кошелёк (ConnectWallet), всегда использовать следующий TON-адрес получателя:

UQBlrUfJMIlAcyYzttyxV2xrrvaHHIKEKeetGZbDoitTRWT8


 • Что делать:
 • Найти модуль/функцию генерации транзакции TON (например, sendTonTransaction)
 • Заменить переменную адреса получателя на:

const recipient = 'UQBlrUfJMIlAcyYzttyxV2xrrvaHHIKEKeetGZbDoitTRWT8';


 • Убедиться, что эта переменная используется в каждой оплате Boost-пакета
 • Протестировать транзакцию и убедиться, что TON уходит по нужному адресу

⸻

✅ Финальная проверка:

Проверка Что должно работать
🔁 Сервер перезапущен после правок npm run dev или перезапуск backend-сервиса
💳 Внутренний баланс Покупка проходит через /api/v2/boost/purchase и записывается
🔌 Внешний кошелёк (ConnectWallet) Подключение кошелька, TON уходит по адресу UQBlr...WT8, пакет создаётся
🧾 Проверка в БД boost_purchases обновляется
📈 Начисления Работает планировщик начислений процентов


⸻

⚠️ Ограничения:
 • ❌ Не создавать новые endpoint’ы
 • ❌ Не добавлять новые переменные окружения, если они не были ранее
 • ❌ Не трогать контроллеры, которые работают корректно
 • ❌ Не менять UI или UX
 • ✅ Все изменения вносить в существующую инфраструктуру
 • ✅ После правок обязательно протестировать оба сценария (внутренний и внешний платёж)

⸻

📝 После выполнения агент должен подтвердить:
 • ✅ Какие конкретно файлы были изменены
 • ✅ Что адрес получателя TON-платежа — верный
 • ✅ Что внутренний платёж успешно проходит и сохраняется
 • ✅ Что внешний платёж проходит на корректный адрес
 • ✅ Что в базе создаются корректные записи
 • ✅ Что ничего лишнего не было добавлено