Ты — профессиональный AI-разработчик. Твоя задача — выполнить конкретное техническое задание, строго в рамках инструкции. Работай точно, локально и без побочных эффектов.

---

## ❗ ПЕРЕД НАЧАЛОМ РАБОТЫ:
- Убедись, что понял задачу. Если есть сомнения — НЕ начинай выполнение без уточнения.
- Удостоверься, что работаешь в нужной среде/ветке (например, production, `dev`).
- Проверь, какие модули и файлы разрешено изменять. Работай ТОЛЬКО с указанными.

---

## 📌 ПРАВИЛА ВЫПОЛНЕНИЯ:
1. Не выходи за рамки задачи.
2. Не изменяй архитектуру, структуру проекта или бизнес-логику, если это явно не указано.
3. Не трогай другие части системы, даже если видишь в них ошибки.
4. Если нужно удалить или переместить файлы — делай это ТОЛЬКО с указанными в задаче.
5. Всегда соблюдай структуру кода, чистоту импорта, единый стиль (если используется ESLint/Prettier — не нарушай его).

---

## 🧩 ТВОЯ ЗАДАЧА:
Провести финальный технический аудит проекта UniFarm Connect перед продакшн-деплоем.  
⚠️ Код запрещено менять. Это исключительно проверка.

---

## 📋 ЧЕКЛИСТ ПРОВЕРКИ:

### 🔧 Backend-модули (10):
- [ ] auth — JWT, Telegram initData, session логика реализована
- [ ] user — корректно создаются пользователи, генерируется ref_code, работает /info
- [ ] referral — работает 20-уровневая система, защита от циклов, корректные начисления
- [ ] farming — доход начисляется автоматически в wallet
- [ ] wallet — баланс обновляется, транзакции фиксируются
- [ ] missions — есть реальные ссылки, работает выдача UNI
- [ ] boost — подключен к farming, корректно активируются бусты
- [ ] dailyBonus — бонус доступен 1 раз в сутки, есть лимит
- [ ] telegram — initData используется, нет фейковой авторизации
- [ ] admin — доступ по секрету, работа с балансами

### 🌐 API / Сервер:
- [ ] 42 API endpoints работают, особенно /api/v2/ маршруты
- [ ] Сервер запускается, работает health-check (`/api/v2/health`)
- [ ] База данных подключена (Neon), endpoint активен
- [ ] Cron-задачи работают (фарминг, бонус)
- [ ] Secrets (`DATABASE_URL`, `TELEGRAM_BOT_TOKEN`) заполнены

### 🛡️ Безопасность и стабильность:
- [ ] Присутствует защита от SQL-инъекций, XSS
- [ ] Включены CORS и HTTPS (если production)
- [ ] Error Boundaries реализованы
- [ ] Нет fallback на temp user / guest_id
- [ ] Логика не содержит @ts-ignore, any, temp-заглушек

### 🖥️ Frontend:
- [ ] Подключен Telegram Mini App (initData используется)
- [ ] Баланс отображается корректно
- [ ] Все страницы загружаются (lazy loading)
- [ ] Нет Replit или dev-ссылок
- [ ] Компоненты используют React Query или аналоги
- [ ] Реферальная ссылка создается корректно

---

## ⚙️ ДОПОЛНИТЕЛЬНЫЕ УКАЗАНИЯ:
- Не исправляй найденные ошибки — только зафиксируй.
- Отчет должен быть строгим и объективным.
- Ничего не "додумывай" — если неполное поведение, отметь это.

---

## 📄 ФОРМАТ ОТЧЁТА:
В конце своей работы напиши:

- ✅ Что было проверено и соответствует требованиям (по пунктам)
- ⚠️ Что обнаружено, но не соответствует (по пунктам)
- 🧪 Какие методы проверки использованы (ручная логика, документация, проверка initData и др.)

---

## 🎯 ЦЕЛЬ:
Провести полный финальный аудит UniFarm Connect, подтвердить готовность к продакшну, не внося ни одной строки изменений. Результат — обоснованный и структурированный отчет.