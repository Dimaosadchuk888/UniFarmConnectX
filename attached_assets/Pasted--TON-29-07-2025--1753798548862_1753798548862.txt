🚨 Срочное техническое задание: диагностика повреждений в системе пополнений TON (29.07.2025)
Контекст:

Система пополнений TON работала стабильно, но после изменений, которые мы зафиксировали в Replit.md (29.07.2025), произошёл откат/повреждение логики:

Последние 2 депозита TON не были зачислены на кабинет (user ID 25).
Примеры:
14:08: tx_hash: te6cckECAgEAAKoAAeGI...FZ8mrv
14:06: tx_hash: te6cckECAgEAAKoAAeGI...CdUc8k
Логи обрабатывают их как TON deposit from blockchain, но баланс не обновляется и транзакции не отображаются в истории.
🔧 Задача:
Срочно восстановить информацию о том:
Какие изменения были внесены 29.07.2025, особенно в логике обработки tx_hash, дедупликации, записи транзакций и баланса;
Какие из этих изменений могли повредить механизм пополнения;
Почему транзакции от 14:06 и 14:08 не зачислились — хотя пришли с блокчейна и были зафиксированы в логах;
Какую логику мы раньше использовали, когда депозиты работали стабильно.
🔍 Что нужно сделать:
📁 1. Найти файл Replit.md или аналог, где зафиксированы изменения 29.07.2025

Найти все коммиты, записи, патчи, в которых фиксировались изменения:
логики дедупликации tx_hash;
фильтрации по description, metadata, comment;
условий isReady, simulate, bounce, payload;
UnifiedTransactionService, WalletService, TransactionMapper.
🔬 2. Сравнить текущую логику с предыдущей

До изменений всё работало корректно.
После изменений — депозиты не записываются в базу / не доходят до кошелька.
Сравнить:
что именно изменилось в дедупликации (description ilike %hash% и metadata->tx_hash);
был ли заменён способ валидации транзакции перед записью;
была ли отключена проверка isReady или simulateTransfer — это критично.
📊 3. Проверить, как система обработала эти 2 транзакции (без правок в коде!)

Проверить по tx_hash, как они обрабатывались:
попали ли в transactions;
был ли вызван saveTransaction(...);
был ли откат транзакции (rollback, delete);
Посмотреть, было ли начисление в wallet_balance;
Проверить, вызывался ли wallet.balance -= после депозита.
🧷 Итог:
Провести диагностику только на чтение: никакие части кода или базы не менять.
Цель — точно выяснить, что именно было изменено, какие строчки могли нарушить обработку, и почему 2 последние депозита не дошли до кабинета.