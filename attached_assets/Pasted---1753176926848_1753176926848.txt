Вот готовое техническое задание (ТЗ) для агента:

---

### 🧾 Техническое задание: Проверка корректной работы планировщика UniFarming

**Цель:** Убедиться, что планировщик начислений по UniFarming работает строго в соответствии с бизнес-логикой, без ошибок и отклонений.

---

#### ✅ Что необходимо проверить:

1. **Работа планировщика:**

   * Планировщик должен начислять доход каждые **5 минут**.
   * Суточный процент должен составлять **ровно 1% в сутки**, то есть дробно начисляться 1/288 (0.003472%) каждые 5 минут.
   * Расчет должен идти:

     * либо от **суммы всех активных депозитов пользователя** (если начисление агрегированное),
     * либо от **каждого отдельного депозита**, если логика распределяет начисление по ним.

2. **Отображение начислений:**

   * В течение последних **5 минут** должно появиться новое **начисление от UniFarming** в таблице транзакций (`transactions`).
   * Если **нет начисления**, необходимо выяснить:

     * работает ли сам планировщик,
     * были ли вызваны функции начисления,
     * не произошло ли ошибок в процессе.

3. **Реферальные награды:**

   * Начисления по реферальной системе **приходят корректно** — это подтверждено.
   * Но **начисления по UniFarming** не отображаются — это необходимо выяснить.

4. **Отсутствие дубликатов или конфликтов:**

   * Проверить, нет ли **дублирующих планировщиков**, которые могли бы вмешиваться в процесс и **накручивать лишние проценты**.
   * Проследить **все активные планировщики**, связанные с UniFarming, и собрать **полный список**:

     * какие из них активны,
     * с какой частотой они работают,
     * как считают процент,
     * какие функции вызывают.

---

#### 🔒 Важно:

* **Никаких изменений в коде.** Только проверка и анализ текущей работы.
* **Никаких ручных перерасчетов или исправлений логики.**
* Ответ должен содержать **чёткое описание текущей логики**, а также **подтверждение, что начисления происходят по правилам**.

---

Если нужно, могу дополнительно добавить шаблон ответа агента или разбить по подпунктам.
