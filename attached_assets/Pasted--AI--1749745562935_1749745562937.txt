Ты — профессиональный AI-разработчик. Твоя задача — выполнить конкретное техническое задание, строго в рамках инструкции. Работай точно, локально и без побочных эффектов.

---

## ❗ ПЕРЕД НАЧАЛОМ РАБОТЫ:
- Убедись, что работаешь в production-версии кода.
- Проверь, где в коде используются guest_id, guestUser, temp_user, temp_id или аналогичные временные пользователи.
- Не трогай Telegram авторизацию — она должна остаться основной моделью.

---

## 📌 ПРАВИЛА ВЫПОЛНЕНИЯ:
1. Работай ТОЛЬКО с авторизацией, моделью users и контроллерами, связанными с fallback.
2. Не удаляй Telegram ID авторизацию и логику, связанную с real users.
3. Не создавай моковые/временные переменные и не отключай типизацию.
4. Не изменяй другие части проекта, кроме указанных в задаче.

---

## 🧩 ТВОЯ ЗАДАЧА:

🎯 Цель: Удалить временную логику fallback на guest_id в системе авторизации и регистрации пользователей.

### Конкретные действия:
1. Найти и удалить все участки кода, где:
   - Пользователи создаются с guest_id, temp_user, isGuest или аналогичными полями.
   - Есть fallback, если telegram_id отсутствует → используется временный guest_id.
2. Удалить из users поля, связанные с гостевыми пользователями:
   - guest_id, temp_user_id, is_guest, если есть.
3. Проверить:
   - Контроллеры авторизации (`/api/register/telegram`, `/api/users`)
   - Сервисы (`AuthService`, `UserService`)
   - Middleware, где используется логика fallback.
4. Убедиться, что теперь регистрация возможна ТОЛЬКО через Telegram и telegram_id.

---

## ⚙️ ДОПОЛНИТЕЛЬНО:
- Если таблица users содержит записи temp_user_... — игнорируй их, не трогай в рамках задачи.
- Если после удаления полей в БД (в schema.ts или Prisma schema) возникает ошибка — оставь комментарий в отчёте.

---

## 📄 ФОРМАТ ОТЧЁТА:
- ✅ Что было сделано (какие поля/участки кода удалены)
- 🧪 Что было проверено (ручная регистрация через Telegram, отсутствие fallback)
- ⚠️ Что оставлено без изменений (если что-то не удалено — укажи причину)

---

## 🎯 ЦЕЛЬ:
Полностью исключить логику гостевой регистрации через guest_id. Система должна работать ТОЛЬКО с Telegram-пользователями (telegram_id, username, ref_code).