Вот готовое **техническое задание (ТЗ)** для агента на **исследование стабильности системы по всем фазам оптимизаций** без внесения изменений в код:

---

## ✅ ТЕХНИЧЕСКОЕ ЗАДАНИЕ

**Название:** Исследование корректности работы системы после оптимизаций (Фаза 1–3)
**Цель:** Проверить, работает ли система стабильно **после всех изменений**, выполненных в рамках Фазы 1, 2 и 3. Важно: **не вносить никаких изменений в код**, а только провести **исследование** и **предоставить отчёт**.

---

### 🔍 ЗАДАЧА

Тебе необходимо поочерёдно пройтись по **всем задачам трёх фаз оптимизации** и проверить, корректно ли работает система на каждом этапе. Отчёт должен включать:

* статус работоспособности каждого блока (✅ работает / ❌ ошибка)
* описание ошибки (если есть)
* краткое заключение по фазе в целом

---

### 📦 ОБЛАСТЬ ПРОВЕРКИ

#### 🔹 **Фаза 1 — Критические изменения**

1. Централизация управления балансами (`BalanceManager`)
2. Обновлённая логика реферальных наград
3. Стандартизация типов транзакций (`TransactionType`)

#### 🔹 **Фаза 2 — Важные улучшения**

4. WebSocket: реальное время обновления баланса
5. Интеграция TON кошельков
6. Поддержка `farming_sessions` для отслеживания доходности

#### 🔹 **Фаза 3 — Архитектура и оптимизация**

7. Оптимизация планировщиков (batch начисления)
8. Архитектурные паттерны: EventBus, Repository, DI (без проверки кода — только последствий)

---

### 🛠️ КАК ПРОВОДИТЬ ИССЛЕДОВАНИЕ

* Используй staging/dev среду
* Проверь поведение вручную или через запросы к базе
* Протестируй:

  * стандартного пользователя (без рефералов)
  * пользователя с 1 уровнем рефералов
  * пользователя с boost-пакетом
* Никаких правок в коде — **только наблюдение и выводы**

---

### 📎 ФОРМАТ ОТЧЁТА

Для **каждого пункта** напиши:

* 🔸 Название пункта
* ✅ Всё работает **или** ❌ Есть проблема
* Краткое объяснение (если есть ошибка)
* Скриншот/запрос в Supabase (если нужно подтвердить)

---

### 📌 ПРИМЕР ФОРМАТА

```
🔸 1. Централизация управления балансами  
✅ Всё работает корректно  
→ Баланс списывается через BalanceManager, прямых SQL-обновлений не найдено.

🔸 2. Обновлённая логика реферальных наград  
❌ Ошибка: referral награда всё ещё начисляется при покупке Boost  
→ Проверено на userId=123, транзакция BOOST_PURCHASE — с начислением referral.
```

---

⛔️ **Важно:** любые найденные ошибки нужно задокументировать, но **не устранять самостоятельно** без отдельного согласования.

🕒 **Срок:** как можно скорее, но с полной проверкой всей цепочки.

---

Готов отправить это как задачу агенту?
