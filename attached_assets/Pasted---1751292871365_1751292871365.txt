📍 Проблема: Сборка прошла некорректно, интерфейс сломан

После выполнения последнего ТЗ:
• Приложение запускается, но визуально видно, что страницы и компоненты не на своих местах
• Интерфейс деформирован, часть элементов отображается неправильно или отсутствует
• Есть подозрение, что build прошёл частично, и часть модулей была либо не собрана, либо не подключена

---

🔎 Что происходит сейчас:
1. Интерфейс выглядит как поврежденная или частично восстановленная версия
2. Некоторые стили или компоненты явно не из оригинального дизайна
3. Есть ощущение, что проект восстановлен не полностью, и присутствуют остатки промежуточных файлов

---

🎯 Задача: Полностью пересобрать проект и восстановить оригинальное поведение приложения

Пошагово:
1. Провести глубокую проверку всех файлов из папки client/:
   • Сравнить с эталонным ZIP (UniFarmConnectX.zip)
   • Выявить отличия, дубликаты, временные или альтернативные компоненты (например, AppV2.tsx, LayoutTest и т.д.)
   • Удалить все дублирующие и неиспользуемые файлы

2. Выполнить полную очистку и пересборку:
   • Очистить кеш, временные файлы, старые сборки (`dist/`, .vite/, .next/, node_modules если нужно)
   • Провести корректный production build через npm run build и убедиться, что:
     ◦ В dist/ созданы JS бандлы
     ◦ index.html подключает скомпилированный файл, а не /src/main.tsx

3. Проверить vite.config.ts и build-пути:
   • Убедиться, что root указывает на client
   • build.outDir правильно настроен на dist/public

4. Проверить результат вручную:
   • Приложение запускается без ошибок
   • Все страницы и маршруты работают
   • Все компоненты находятся на своих местах
   • Стили, навигация, функциональность — в соответствии с оригиналом

---

🚫 Что нельзя:
• Не использовать временные заглушки
• Не подключать напрямую TypeScript файлы в HTML
• Не вносить изменения в бизнес-логику
• Не добавлять новые компоненты или менять структуру приложения

---

✅ Ожидаемый результат:
Приложение должно запускаться в своём оригинальном виде, с полным интерфейсом, в финальной сборке, без багов и без временных обходов. Всё должно соответствовать эталонной версии из архива.

🛠 Если будет нужно, можно сравнивать с ZIP, присланным ранее (`UniFarmConnectX.zip`) как эталоном.