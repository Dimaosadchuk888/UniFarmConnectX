Отлично, вот переработанный и уточнённый вариант сообщения — строго по делу, **без предложений временных решений**, с акцентом на **единственно корректное и безопасное архитектурное решение**, которое устраняет **главную проблему: пользователь пополнил — деньги списались — они должны быть отображены сразу**:

---

👋 Привет!

Спасибо за подробный отчёт — он подтверждает критичность проблемы, и особенно важно, что она связана **не с дублями, а с нарушением аутентификации в момент обработки TON депозита**.

🔧 **Просим тебя сейчас предложить архитектурное решение**, которое:

* **Не нарушит текущую систему** и **не вызовет побочных эффектов** (все ключевые модули уже подключены и завязаны друг на друга);
* **Устранит точку отказа**, где сейчас запрос блокируется на уровне JWT и не доходит до `processTonDeposit()`.

🎯 **Цель**:
Когда пользователь совершает TON-депозит:

1. Деньги списываются с кошелька;
2. Транзакция фиксируется в блокчейне и система получает хэш;
3. После этого депозит **должен немедленно отобразиться на балансе** пользователя.

Сейчас система **принимает хэш**, но **не привязывает транзакцию к пользователю** из-за JWT mismatch. Это ключевая архитектурная ошибка: в момент, когда приходит callback с TON-хэшем, система **ищет не того пользователя**, либо **не находит его вовсе**, из-за чего `processTonDeposit()` не вызывается.

🚫 Нас не интересуют временные фиксы (fallback-и, патчи и пр.). Нам нужно **единое стабильное архитектурное решение**, которое:

* Гарантирует корректную обработку хэша TON-транзакции;
* Исключает привязку исключительно к JWT, если он может быть невалидным или принадлежать другому пользователю;
* Позволяет **на 100% точно связать TON-перевод с конкретным аккаунтом**, и **отобразить средства в балансе немедленно** после подтверждения перевода.

💬 Просим тебя:

1. Предложить финальное архитектурное решение, которое безопасно устранит текущую точку отказа;
2. Указать, **где именно и что нужно поменять** в логике, чтобы `WalletService.processTonDeposit()` гарантированно вызывался после получения TON-хэша;
3. Учитывать, что изменение JWT-механизма требует осторожности — система используется в продакшене и уже активно обменивается данными между модулями и маршрутами.

Спасибо, ждём твоего чёткого архитектурного плана.

---

Если хочешь, я могу дублировать это как задачу в трекере с чек-листом.
