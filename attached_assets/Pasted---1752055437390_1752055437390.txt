Понял тебя точно. Ниже — **структурированное техническое задание для агента**, чтобы он **провел полное аналитическое исследование** блока **Telegram Bot**, **не лез в код**, а **исследовал файлы, интерфейсы, конфигурации и документацию**, и **дал подробный отчёт по всем аспектам**, включая **то, что отсутствует в Roadmap.md**, но реализовано.

---

# 🧾 Техническое задание для исследования блока Telegram Bot (админ и пользовательский)

## 🎯 Цель:

Провести **полный аудит Telegram Bot системы UniFarm**, в том числе:

* Telegram WebApp-бота для пользователей
* Telegram-бота для администраторов
* Сопутствующих интеграций и вспомогательных механизмов (например, WebApp API, webhook, сообщения)
* Проверку наличия и полноты описания в `ROADMAP.md`

## 🔍 Формат работы:

* Не открывать и не анализировать код напрямую
* Использовать готовые названия, которые указаны в файлах, но **не делать предположений по логике**
* Изучить структуру проекта, где какие боты указаны, какие есть конфигурации
* Сделать **аналитический обзор по блокам функционала**, а не по файлам
* Все найденные команды, функции, обработчики — **записать детально**, если они **не указаны в Roadmap.md**, — отметить как "не описано"

---

## 📦 Что входит в исследование

### 1. **Telegram WebApp Bot (Пользовательский бот)**

#### 1.1 Подключение и инициализация

* Где в проекте указан WebApp
* Как именно происходит вход пользователя
* Используется ли `initData` и как он принимается
* Валидация пользователя: подпись, токены, создание сессии
* Фиксируются ли данные в базе
* Используется ли кнопка закрытия `Telegram.WebApp.close()` — если нет, отметить

#### 1.2 Функциональность WebApp

* Какие функции доступны пользователю через WebApp
* Какие блоки отображаются (доска, фарминг, миссии, бонусы и т.д.)
* Какие взаимодействия происходят с Telegram API (например, отправка сообщений, пуш-уведомления)
* Есть ли какие-либо команды или интерактивные кнопки вне WebApp (например, через чат бота)
* Есть ли какие-либо ограничения, недоработки (например, нет кнопки выхода, нет смены языка)

#### 1.3 Документация

* Есть ли в `ROADMAP.md` раздел про этот бот
* Если нет, указать, что отсутствует, и выписать, что реализовано на самом деле
* Указать, что нужно было бы задокументировать (авторизацию, initData, роли, разделы WebApp)

---

### 2. **Admin Bot (бот для администрирования)**

#### 2.1 Подключение

* Есть ли webhook, куда приходит запрос
* Как определяются администраторы (по username? роли? конфигурации?)
* Где зарегистрирован (название бота, конфигурация)

#### 2.2 Команды и логика

Составить таблицу всех команд, которые:

* Упомянуты в `ROADMAP.md`
* Реализованы, но не задокументированы

Для каждой команды:

| Команда | Что делает | Есть ли в ROADMAP.md | Комментарий |
| ------- | ---------- | -------------------- | ----------- |

Например:
\| `/stats` | Выдает статистику по пользователям | ✅ | Работает |
\| `/withdrawals` | Список заявок на вывод | ❌ | Реализовано, но не описано |
\| `/ban` | Блокирует пользователя | ❌ | Есть в боте, но не указано |

#### 2.3 Интерфейс и формат взаимодействия

* Бот работает через Telegram чат (не WebApp)
* Есть ли кнопки/inline меню
* Каков механизм подтверждения действий (например, `/approve <id>`, есть ли подтверждение или всё выполняется сразу)

#### 2.4 Документация

* Упоминается ли в `ROADMAP.md` как отдельный блок
* Если частично — что именно указано, а что пропущено
* Отметить, какие команды требуют добавления в документацию

---

### 3. **Интеграция и безопасность**

#### 3.1 Безопасность и ограничения

* Как бот ограничивает доступ (по username? token?)
* Фиксируются ли действия бота в логах
* Есть ли защита от злоупотреблений (rate limit, подтверждение)

#### 3.2 WebApp API

* Есть ли использование `Telegram.WebApp.initDataUnsafe`, `Telegram.WebApp.expand()`, `Telegram.WebApp.close()` и т.д.
* Отмечено ли это в Roadmap.md
* Какие методы Telegram WebApp используются фактически

---

### 4. **Выводы и рекомендации**

* Соответствует ли реализованный функционал заявленному в Roadmap.md
* Если нет — какие разделы отсутствуют, что требует добавления
* Полный список команд бота с объяснением, какие не документированы
* Перечислить, чего **не хватает** для полной прозрачности и расширения возможностей (например: нет inline-кнопок, нет кастомной панели администратора и т.п.)
* Предложить обновить `ROADMAP.md`, включая:

  * Раздел "Telegram Bots"
  * Подразделы:

    * Пользовательский WebApp бот
    * Админ-бот (по командам)
    * API взаимодействие и ограничения
    * Интеграции

---

## 📌 Ожидаемый результат:

Агент должен предоставить структурированный **аналитический отчёт** в формате:

* Блок **Telegram WebApp Bot** — подключение, функции, проблемы
* Блок **Admin Bot** — команды, доступ, подключение, описание
* Блок **Интеграция и API Telegram** — какие методы используются
* Таблицы с командами и состоянием (описано/реализовано/отсутствует)
* Финальный список, **что отсутствует в Roadmap.md**, и **что нужно документировать**
* **Заключение** о готовности Telegram-части проекта к production

---

Если нужно, могу сразу сгенерировать шаблон такого отчета, чтобы агент мог в него вписать всё.
