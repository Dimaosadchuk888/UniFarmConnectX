🧾 Техническое задание: Анализ блока партнёрской программы (Партнёрка)

📌 Цель:
Провести только аналитическую проверку корректности работы блока партнёрской программы на странице Партнёрка.
Не вносить никаких изменений в код или структуру проекта.

⸻

🔍 Что необходимо проверить:

1. Генерация партнёрской ссылки
 • 🧩 Проверить, генерируется ли партнёрская ссылка автоматически при первом входе пользователя
 • 🔗 Формат ссылки: https://app.unifarm.io?ref=<ref_code> (или подобный)
 • 📤 Откуда берётся ref_code:
 • Хранится ли в users таблице?
 • Генерируется ли при создании нового пользователя?
 • 🛡 Есть ли защита от дублирующихся ref_code?

⸻

2. Отображение партнёрской ссылки на UX
 • 🖥 Проверить, отображается ли ссылка в соответствующем блоке интерфейса
 • 📋 Есть ли кнопка «Скопировать» и работает ли она?
 • ✅ Правильно ли подставляется текущий ref_code пользователя?

⸻

3. Таблица приглашённых пользователей (партнёров)
 • 📊 Отображается ли список всех пользователей, пришедших по реферальной ссылке?
 • 📅 Показываются ли:
 • Telegram-имя (или ID) приглашённого
 • Дата регистрации
 • Доход от него (если предусмотрено)
 • 🧮 Проверить, как рассчитывается партнёрский доход — отображается ли он корректно?

⸻

4. Backend-проверка (анализ логики)
 • 📂 Где обрабатывается логика генерации ссылки?
 • 🛠 Есть ли привязка ref_code к telegram_id или user_id?
 • 🔄 Как происходит подсчёт и отображение количества приглашённых?

⸻

🛑 Ограничения:
 • ❌ Не менять ни одну строку кода
 • ❌ Не исправлять баги и не оптимизировать поведение
 • ✅ Только технический анализ текущего состояния блока

⸻

📝 В отчёте указать:
 • Где и как генерируется ref_code
 • Работает ли отображение ссылки на клиенте
 • Корректно ли отображаются приглашённые пользователи и доход от них
 • Есть ли несоответствия между логикой и интерфейсом
 • Возможные ошибки или сбои (например, белый экран, 404, ошибки в консоли)

⸻

📌 После успешного анализа будет выдано следующее задание — оптимизация и исправления, если потребуется.