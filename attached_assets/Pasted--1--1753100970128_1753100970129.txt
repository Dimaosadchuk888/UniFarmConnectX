📌 Новые замечания и задачи для агента (важно — без правок в коде)

🔵 1. Не обновляются цвета транзакций (TON)
 • По задаче сменить цвет входящих TON-транзакций на синий (как ты обещал) — ничего не поменялось.
 • Даже после перезагрузки сервера, транзакции отображаются как раньше.
 • Возможно, где-то стоит жесткий frontend или backend кеш, который не позволяет увидеть обновлённые стили/метки.

👉 Что надо сделать:
 • Провести диагностику, где может кешироваться цвет транзакции.
 • Проверить: как формируется поле color / typeStyle / transactionStyle (или аналог) на backend и frontend.
 • Убедиться, что стили или классы не захардкожены в кэше или компоненте.
 • Уточнить: есть ли механизм сброса кеша для frontend-рендера (Next.js, Nuxt, React, Vite?).
 • Без внесения изменений в код! Сначала только полная проверка логики отображения.

⸻

💰 2. Повторное начисление по депозиту не отображается в кошельке
 • Пополнение TON-кошелька прошло успешно, как ты подтверждаешь.
 • Агент говорит, что в базе запись есть, но в интерфейсе кошелька баланс не обновился.
 • Есть сильное подозрение на кеширование, в том числе:
 • Redis
 • кеш на уровне walletService.getBalance()
 • кеш CDN или frontend

👉 Что нужно сделать:
 • Проверить: данные в таблице wallets (или где хранится баланс) действительно обновились?
 • Проверить: возвращает ли API /wallet корректный баланс?
 • Если да — значит UI не рендерит (кеш на клиенте).
 • Если нет — значит кеширован ответ API или вообще не вызывался addBalance.

Важно: снова — никаких изменений в код! Только глубокая проверка цепочки отображения и кешей.

⸻

🔁 3. Планировщик на продакшене работает не как на деве
 • Агент настроил планировщик TON Boost начислений раз в 5 минут, и на девелопере работает правильно.
 • Но на продакшене начисления идут каждую минуту, что приводит к аномально большим начислениям и удару по бюджету.

👉 Проверить:
 • Вероятно, в прод-среде закеширован старый крон, или работает второй крон параллельно.
 • Возможно, работает несколько экземпляров, и один из них запускает “минутный” скрипт.
 • Агент должен:
 • сравнить крон-файлы и интервалы на проде и деве.
 • убедиться, что на проде только один запуск, и он идёт строго каждые 5 минут.

❗ Запрещается менять таймер или код — сначала полная диагностика и сравнение.