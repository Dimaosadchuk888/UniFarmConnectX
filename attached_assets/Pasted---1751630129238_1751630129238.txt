🛠️ ТЕХНИЧЕСКОЕ ЗАДАНИЕ: УСТРАНИТЬ ПРОБЛЕМУ С СОЕДИНЕНИЕМ

🔒 Общие правила:
 • ❗ Запрещено вносить правки в код без твоего согласия.
 • ✅ Разрешается только провести диагностику и предложить варианты точечных решений.
 • ❗ Нельзя удалять или отключать TonConnect, WebSocket или другие ключевые модули.

⸻

✅ ЦЕЛЬ: ИСПРАВИТЬ ОШИБКУ «СОЕДИНЕНИЕ С СЕРВЕРОМ»

⸻

📍 ЧТО УЖЕ ЯСНО:
 • Сообщение «Ошибка соединения с сервером» вызвано нестабильной работой WebSocket.
 • Ошибка исходит из: client/src/contexts/webSocketContext.tsx
 • Есть конфликт между Vite Dev WebSocket и основным AppSocket соединением.
 • Переподключения происходят, но пользователь видит ошибку, что ломает UX.

⸻

✅ ЗАДАЧА 1: ТОЧНАЯ ДИАГНОСТИКА WebSocket

Что агент должен сделать:
 1. Пройтись по коду webSocketContext.tsx и:
 • Обнаружить механизм инициализации WebSocket (в каком месте запускается).
 • Проверить, где именно обрабатываются ошибки (onError, onClose).
 • Проверить, откуда приходит message: "Ошибка соединения с сервером".
 2. Проверить:
 • Какие компоненты зависят от WebSocket (например, BalanceCard, App.tsx).
 • Не происходит ли дублирующее подключение, которое мешает работе.
 3. Выяснить:
 • Почему reconnect срабатывает, но ошибка не исчезает.
 • Может ли быть race condition (реакция интерфейса на isConnected === false, даже если соединение восстановилось).

⸻

✅ ЗАДАЧА 2: ПРОВЕРКА UI-ПОВЕДЕНИЯ

Агент должен:
 1. Найти компонент, в котором выводится сообщение “Ошибка соединения с сервером”.
 2. Проверить:
 • Что именно вызывает этот вывод (например, глобальное состояние isSocketConnected, status, error).
 • Удаляется ли сообщение при восстановлении соединения?
 3. Протестировать сценарий:
 • Стабильное подключение
 • Ручной дисконнект (например, offline mode)
 • Автоматическое восстановление

⸻

✅ ЗАДАЧА 3: ПОДГОТОВИТЬ АККУРАТНЫЕ ПРЕДЛОЖЕНИЯ ПО ИСПРАВЛЕНИЮ

Никаких правок без твоего одобрения.

Агент должен выдать один из следующих вариантов решений, не меняя код:

Вариант Суть
✅ 1. Heartbeat Реализовать ping/pong каждые 30 секунд
✅ 2. State-синхронизация Проверить флаг connected, сбрасывать ошибку после успешного reconnect
✅ 3. UI delay Добавить небольшую задержку перед выводом ошибки, чтобы дать WebSocket переподключиться
✅ 4. Exponential backoff Увеличивать время между попытками повторного подключения
✅ 5. Разделение потоков Использовать отдельный WebSocket для логики приложения и для Devtools


⸻

📋 ФОРМАТ ОТЧЕТА

Агент должен предоставить тебе:
 1. 🔍 Где в коде возникает ошибка
 2. 🧠 Почему она возникает
 3. 📂 Какие компоненты/файлы связаны
 4. 📌 Что можно сделать
 5. 🚫 Чего точно не делать (например, отключать TonConnect)

⸻

✅ ТОНКОННЕКТ — ОСТАЕТСЯ БЕЗ ИЗМЕНЕНИЙ

Из отчета видно, что:
 • TonConnectUIProvider стоит правильно
 • Хуки useTonConnectUI работают
 • Функции connectWallet, disconnectWallet, isWalletConnected действуют как нужно
 • Ошибок больше нет

Поэтому:
 • Никаких “заглушек”
 • Никакого отключения
 • Только проверка: если всё работает — ничего не трогать