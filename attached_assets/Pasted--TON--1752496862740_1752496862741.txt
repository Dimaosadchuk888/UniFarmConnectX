Понял. Вот полностью переписанное и **точное техническое задание**, без упоминания TON, с чётким акцентом на **проблему балласта UNI** и нарушенную бизнес-логику:

---

### 🛠️ ТЕХНИЧЕСКОЕ ЗАДАНИЕ

**Задача:** Исследование некорректной логики расчёта доходности UniFarming

---

### 🧩 СУТЬ ПРОБЛЕМЫ

На текущий момент система UniFarming **рассчитывает процент дохода не от суммы монет, вложенных в активные фарминг-пакеты**, а от общего количества UNI **на балансе пользователя (`balance_uni`)**.

Это грубое нарушение бизнес-логики:
**Доходность должна начисляться только на те UNI, которые были вложены пользователем в фарминг, а не просто находятся у него на счету.**

---

### 🔍 ЦЕЛЬ ЗАДАНИЯ

Провести техническое исследование и точно выяснить:

1. **Откуда система берет значение для расчета дохода?**

   * Использует ли она `balance_uni`, или другие поля?
   * Игнорируются ли данные по активным фарминг-депозитам?

2. **Где в коде заложено текущее поведение?**

   * Названия файлов, функций, модулей
   * Какие поля участвуют в логике расчёта?

3. **Почему доход начисляется не на сумму, вложенную в фарминг?**

---

### 📁 НАПРАВЛЕНИЯ ДЛЯ ПРОВЕРКИ

Агент должен исследовать следующие элементы системы:

#### 1. Логика расчёта доходности

* `modules/farming/logic/rewardCalculation.ts`
* `modules/farming/service.ts`
* Проверить, какие поля используются в расчёте дохода (например: `balance_uni`, `farming_deposits.amount`, `user.active_farming_amount`, и т.д.)

#### 2. Хранилище фарминг-депозитов

* Таблица `farming_deposits`:

  * поля: `user_id`, `amount`, `active`, `start_time`, `package_id`
* Проверить, фиксируется ли отдельная сумма, вложенная в фарминг, и используется ли она в расчёте

#### 3. Модули обновления баланса

* `BalanceManager` (если участвует в начислении дохода)
* Любые упоминания `balance_uni` внутри farming-модулей

---

### ⚠️ ОГРАНИЧЕНИЯ

* **Запрещено вносить любые изменения в код**
* На этом этапе — только исследование и документирование

---

### 📄 ОТЧЕТ

В результате агент должен предоставить:

1. ✅ **Подтверждение или опровержение**, используется ли `balance_uni` для расчёта дохода
2. 🧩 **Путь логики**, откуда берутся значения, участвующие в начислении дохода
3. ⚠️ **Технические выводы**, в чём именно ошибка, почему это нарушение логики
4. 🛠️ **План корректировки (словами)** — как безопасно перейти на расчёт от суммы активных farming-депозитов, если сейчас используется баланс

---

Если нужно, могу сразу превратить это в текст для отправки агенту.
