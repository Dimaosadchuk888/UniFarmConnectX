Вот точный **Prompt / Техническое задание для агента** по новому разделу:

---

# 🧾 ТЕХНИЧЕСКОЕ ЗАДАНИЕ: Исследование нулевых значений в блоке «Тонфарминг»

### 📍 Раздел интерфейса:

* Страница: **Фарминг**
* Подраздел: **Тонфарминг**
* Карточка: **«Тонфарминг стейкинг ТОН токенов»** (синяя)
* Проблема: Все поля (доход в сутки, доход в секунду, общая сумма депозитов) отображаются как `0`, хотя ранее данные подтягивались из базы данных.

---

### 🎯 Цель

Выяснить, **почему карточка «Тонфарминг» отображает нули**, несмотря на то, что:

* Система начислений TON Boost работает корректно
* В базе есть активные депозиты и начисления
* Ранее блок работал и отображал живые данные

---

### 🔍 Что нужно проверить:

#### 1. **Фронтенд**

* Найти компонент, отвечающий за карточку Тонфарминга (например, `TonFarmingStatusCard.tsx`)
* Проверить:

  * Откуда приходят данные (`useEffect`, `useQuery`, `api.tonFarming.getStats()` и т.п.)
  * Есть ли условие, при котором компонент может показать `0`, даже если данные есть?
  * Есть ли fallback/stale кэш, который мог сбрасывать данные?

#### 2. **API / Сервис**

* Найти API endpoint, отдающий данные по Тонфармингу:

  * Вероятно `GET /api/boosts/stats/:userId` или `/api/v2/ton-farming/info`
* Проверить:

  * Что именно возвращает endpoint: `daily_income`, `total_deposit`, `per_second_rate` и т.д.
  * Есть ли расхождение между расчётной логикой и выводом?
  * Проверить, есть ли баги, при которых в API может отдаваться `0`, даже если в БД всё есть

#### 3. **Backend Logic**

* Проверить файл: `modules/boost/service.ts` или `modules/ton-farming/service.ts`
* Найти:

  * Где рассчитывается доход в сутки и в секунду
  * Логику агрегации суммы активных TON-депозитов
* Убедиться, что:

  * Все расчёты основаны на **актуальной версии структуры депозитов**
  * В логике используется **суммарный farming\_balance**, а не, например, последний

#### 4. **База данных**

* Проверить таблицу `ton_farming_data` и другие связанные таблицы
* Убедиться:

  * Что у пользователя/пользователей действительно есть farming\_balance > 0
  * Что cron начисляет доходы каждые 5 минут (есть `ton_income` транзакции в истории)
* Сравнить:

  * Расчёт по формуле: `balance × daily_rate / 86400` (секунд в сутках)
  * Со значением, возвращаемым API (пересечь)

---

### 🛠️ Дополнительно:

* Если расчёты идут каждые 5 минут (cron), то **в UI тоже можно показывать доход за 5 минут**, а не в секунду — но **только если это уже реализовано в бэке**.
* **Если данные приходят, но UI показывает 0**, возможно, проблема в типах, неправильной структуре или баге кэширования.

---

### ✅ Что НЕ делать:

* ❌ Не менять код
* ❌ Не перезаписывать данные вручную
* ❌ Не делать догадок — только факты

---

### 📌 Ожидаемый результат:

1. Указать **точную причину**, почему в UI отображаются нули.
2. Указать, **на каком участке цепочки** данные теряются или не рассчитываются.
3. Подтвердить, есть ли данные в БД и приходят ли они через API.
4. Если расчёт происходит каждые 5 минут — предложить отображать «доход за 5 минут» и убрать "в секунду", **если так проще и безопаснее для архитектуры**.
5. Подтвердить, что система стабильна и можно запускать карточку Тонфарминга в прод.

---

Готов передать агенту. Если хочешь, могу дополнительно подготовить SQL для проверки дохода и общей суммы TON-депозитов.
