🚨 СРОЧНАЯ ДИАГНОСТИКА: приложение не загружается, отображается только заглушка

🔍 ЗАДАЧА:
Провести глубокий аудит, чтобы точно установить, почему не запускается оригинальный UniFarm, а отображается только бесконечная заглушка. ❗ Работа строго в режиме анализа. Никаких правок, временных решений, обходов, костылей.

⛔ Не допускается:
 • Создание временных файлов
 • Использование обходных точек входа (SimpleFile, test-index.html и т.п.)
 • Подмена путей, запуск через старые/временные точки входа

🧪 ЧТО НУЖНО ПРОВЕРИТЬ:

1. 📦 Сравнение текущей папки client/ с оригинальной из ZIP
 • Вывести отличия: добавленные, удалённые, изменённые файлы
 • В приоритете: App.tsx, main.tsx, vite.config.ts, index.html, components/, routes/, layouts/, providers/
 • Проверить, не были ли внедрены временные версии компонентов или подмены (`AppV2.tsx`, AppCopy.tsx, FallbackUI, и т.п.)

2. 🧱 Проверка всех корневых директорий
 • Убедиться, что реальное приложение запускается строго из client/
 • Проверить, не происходит ли запуск из других директорий: client-new/, client-copy/, simple/, test/, replit-neon/, и т.п.
 • Отследить откуда реально подключается index.html, какой vite.config.ts используется, какой main.tsx
 • Вывести путь к ТЕКУЩЕЙ рабочей директории: process.cwd()
 • Проверить, нет ли временных путей или подмен через symlink, alias или serve-static

3. 🔍 Поиск причины бесконечной загрузки
 • Проверить, что index.html не указывает напрямую на TypeScript: src="/src/main.tsx" ❌
 • Проверить, что dist содержит корректный JS-бандл и index.html подключает его
 • Проверить, что App.tsx не застревает на isLoading === true без выхода
 • Проверить, нет ли блокирующих хуков или пустых useEffect, которые мешают рендеру

4. 🧹 Кеш и остаточные файлы
 • Проверить: .vite, .next, dist/, build/, .cache, .turbo
 • Проверить, нет ли старых .html, .js, .tsx файлов, подключённых вручную
 • Проверить localStorage и Service Worker — не остался ли старый JS-код

📌 ОТЧЕТ:
 • Только ФАКТЫ: либо точная причина с файлом и строкой, либо «не обнаружено, требуется ручная верификация»
 • Никаких предположений и временных решений
 • Если обнаружено подключение к неправильной директории или нестандартной точке входа — указать точно путь и где это происходит
 • Убедиться, что никакие временные/ненастоящие точки входа (SimpleFile, index-test.html и т.д.) НЕ используются

🎯 Цель: найти и чётко зафиксировать ПРИЧИНУ, по которой UniFarm не запускается в оригинальном виде. Без поломок, без замены файлов, только точный аудит. Работать строго в production-логике.