⸻

🧩 ТЕХНИЧЕСКОЕ ЗАДАНИЕ — Т23: Завершение модуля airdrop — добавление types.ts и model.ts

📌 Роль:

Ты — AI-агент, отвечающий за доведение проекта UniFarm до стабильного продакшн-состояния. В этом задании ты должен точечно завершить неполный модуль airdrop, строго по выявленным недостаткам.

⸻

🛑 Жёсткие ограничения:
 1. Нельзя изменять другие модули, только modules/airdrop.
 2. Нельзя удалять или переписывать существующие файлы (controller.ts, routes.ts, service.ts).
 3. Нельзя выдумывать бизнес-логику — структура должна соответствовать архитектуре других модулей UniFarm.
 4. Обязательно использовать подход Supabase (через API), как во всех остальных модулях.
 5. Не пиши никакой тестовой логики — только структурно верные types.ts и model.ts.

⸻

🧠 Контекст задачи:

В ходе аудита выявлено, что модуль airdrop работает, но неполный:
 • ❌ Отсутствует файл types.ts
 • ❌ Отсутствует файл model.ts

Остальная структура (controller.ts, routes.ts, service.ts) — присутствует.

⸻

✅ Что нужно сделать:
 1. 📄 Создать файл modules/airdrop/types.ts:
 • Тип для AirdropUser (id, telegram_id, username, ref_code, reward, status, created_at).
 • Типы для RegisterAirdropPayload (при регистрации).
 • Типы ответов API (AirdropResponse, AirdropStatus, и т.п.).
 2. 📄 Создать файл modules/airdrop/model.ts:
 • Экспортировать AIRDROP_TABLE = 'airdrop_users' (или актуальное название таблицы в Supabase).
 • Экспортировать AIRDROP_FIELDS = ['id', 'telegram_id', 'username', 'reward', 'status', 'created_at'] — для select.
 3. ✅ Проверить service.ts:
 • При необходимости — заменить хардкод строки таблицы на AIRDROP_TABLE.
 • Не переписывай логику — только импортируй model.ts.

⸻

📤 Формат отчета:
 1. Какие файлы были созданы?
 2. Какие типы и поля были добавлены?
 3. Был ли обновлён service.ts?
 4. Подтверждение, что структура модуля завершена и приведена к общему виду.

⸻

🎯 Цель:

Сделать модуль airdrop структурно завершённым — чтобы он имел полную архитектуру: controller, routes, service, types, model. Готовность к продакшн без предупреждений.

⸻