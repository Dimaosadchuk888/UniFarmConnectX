Вот как должно быть оформлено **профессиональное техническое задание** на **глубокое исследование проблемы отсутствия FARMING\_REWARD начислений в UniFarming**, с учётом текущей ситуации и запрета на изменение кода:

---

# 🧠 ТЕХНИЧЕСКОЕ ЗАДАНИЕ

## Глубокое исследование неработающего планировщика начислений FARMING\_REWARD в UniFarming

**Формат**: Только исследование, без правок кода
**Цель**: Выявить, **почему система UniFarming перестала создавать транзакции FARMING\_REWARD**, несмотря на активные депозиты
**Объект исследования**: Вся внутренняя цепочка UniFarming от планировщика до отображения в кошельке

---

## 📍 Контекст

### Текущая ситуация:

* Последняя FARMING\_REWARD транзакция: **11.07.2025, 09:25**
* Сейчас: **12.07.2025, 17:59** — **прошло 32.5 часа**
* Новые депозиты:

  * 5000 UNI @ 17:57:05
  * 10000 UNI @ 17:57:26
* Планировщик не начислил ни одного FARMING\_REWARD за последние сутки+
* Баланс в системе корректный: **1,420,100 UNI**
* Общий депозит: **508,589 UNI**
* Ручной запуск `farmingScheduler` — **не дал результата**
* Некоторые транзакции TON Boost ошибочно помечены как `FARMING_REWARD`
* Пагинация истории транзакций исправлена

---

## 🧩 Что исследовать (по этапам)

### 1. **FarmingScheduler** (скорее всего `core/scheduler/farmingScheduler.ts`)

* Запускается ли по cron (`*/5 * * * *`)
* Фактически ли вызывается `processUniFarmingIncome()`
* Используется ли логирование при запуске?
* Что делает `getActiveFarmers()` — возвращает ли что-либо?
* Применяется ли фильтр по `uni_farming_active = true`?
* Какие условия должны быть выполнены, чтобы начать начисление?
* Есть ли проверки времени последнего обновления? (например, `last_updated_at > X`)
* Есть ли ограничения по количеству обработанных пользователей?
* Есть ли блокировки, которые могли "заморозить" планировщик?

---

### 2. **FarmingService** (например, `modules/farming/service.ts`)

* Метод начисления процентов: `processIncome()` или аналог
* Проверяется ли `last_update_timestamp`?
* Как рассчитывается процент (формула: `deposit × rate × days`)?
* Где формируется транзакция типа `FARMING_REWARD`?

  * Используется ли `TransactionRepository.createTransaction()`?
* Где создается запись в `farming_sessions` со статусом `completed`?
* Проверить: возможно ли, что планировщик "думает", что все начислено, но не записал транзакцию?

---

### 3. **Формирование транзакции FARMING\_REWARD**

* Где именно в коде создается эта транзакция?
* Передается ли туда правильный `user_id`, `amount`, `type = FARMING_REWARD`, `currency = UNI`?
* Не отрабатывает ли условие `if (!income || income === 0)`?
* Сохраняется ли транзакция в таблицу `transactions`?
* Есть ли ошибки в логах (если доступны)?

---

### 4. **Проверка базы данных**

* Есть ли новые транзакции `type = FARMING_REWARD` за последние 32.5 часа?
* Есть ли записи в таблице `farming_sessions` с `status = completed` и свежими датами?
* Есть ли записи в `transactions` для `user_id = 74`, `currency = UNI`, `type = FARMING_REWARD`?
* Сохранился ли `uni_farming_last_update` после депозита?

---

### 5. **Wallet UI → Почему FARMING\_REWARD не видно в истории**

* Проверить компонент истории транзакций (например, `TransactionHistory.tsx`)
* Проверить: не фильтруются ли транзакции по типу
* Проверить, корректно ли отображаются `FARMING_REWARD` (название, иконка, перевод)
* Проверить: получает ли frontend эти транзакции от API
* Проверить: как обрабатываются pagination + lazy load

---

## 🔒 Ограничения:

* ❌ **Нельзя** менять код (ни frontend, ни backend)
* ✅ **Можно** читать и анализировать любой код/модули
* ✅ **Можно** читать и проверять данные в базе (SELECT-запросы)
* ✅ **Можно** вручную запускать планировщик (если это часть CI/CD)
* ❌ **Нельзя** вносить правки в базу напрямую (только анализ)

---

## 📊 Ожидаемый отчет:

Формат: `UNIFARMING_REWARD_ISSUE_REPORT.md`

Содержимое:

1. ✅ Планировщик: вызывается / не вызывается
2. ✅ Какие модули он задействует
3. ✅ Как он вычисляет начисление
4. ✅ Почему транзакция не создается
5. ✅ Почему запись не попадает в базу
6. ✅ Есть ли баг в `getActiveFarmers()`
7. ✅ Есть ли транзакции, но не отображаются (UI/API проблема)
8. ✅ Точные места в коде, где "рвется" цепочка
9. ✅ Рекомендации (в каком именно месте нужно вмешательство — если потребуется)

---

## 📌 Напоминание:

> Некоторые имена файлов и структур **могут отличаться** от отчётов или документации.
> Агент должен **ориентироваться на реальные имена и структуру проекта**, а не только на предложенные шаблоны.

---

## ✅ Цель задания:

Выдать **только фактический отчёт** по всей цепочке `UniFarming → Scheduler → Transactions → Wallet UI`
Без предположений. Без теории. Только то, что **на самом деле работает или не работает**.

Готов оформить это в `UNIFARMING_REWARD_ISSUE_REPORT.md`, как только подтвердишь.
