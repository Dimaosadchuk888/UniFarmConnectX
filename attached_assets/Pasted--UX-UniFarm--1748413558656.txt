🧠 ЗАДАЧА ДЛЯ АГЕНТА:
🔍 Полный UX-аудит системы UniFarm

⸻

📌 Цель:
Проанализировать, как визуальные элементы (фронтенд, дизайн, кнопки, интерфейс) взаимодействуют с данными и логикой проекта.

❗ Важно:
Ты не имеешь права менять или исправлять код. Только исследование и отчёт о том, что именно не работает, какие участки не связаны с данными, и где визуал не отображает то, что должен.

⸻

🔧 Проведи анализ по следующим критериям:
 1. Отображение балансов в кошельке
 • Проверить, откуда берутся данные.
 • Проверить, обновляются ли данные с сервера.
 • Обнаружить, где теряется связка: API → state → визуал.
 2. Кнопки и действия
 • Работают ли интерактивные элементы?
 • Есть ли реакция на нажатие кнопок?
 • Откуда поступают данные и что происходит после клика?
 3. Реальное подключение данных
 • Где визуальные блоки (например, Boost-пакеты, миссии, фарминг, баланс) не получают актуальные данные?
 • Есть ли в коде моки (заглушки), фейковые данные, которые используются вместо настоящих?
 4. Проблемы с кэшем
 • Есть ли признаки, что данные берутся из устаревшего состояния?
 • Используются ли некорректно localStorage, sessionStorage, react-query cache, redux и т.п.?
 5. Ошибки UX/React-логики
 • Где компоненты не ререндерятся при получении данных?
 • Есть ли некорректное использование useEffect, useState, useContext, useQuery?
 6. Функциональность, которая заявлена, но не работает
 • Какие блоки отображаются, но ничего не делают?
 • Какие обещанные функции не реализованы визуально?

⸻

📤 Выходной результат:

Предоставь структурированный текстовый отчёт со следующими пунктами:
 • 🔴 Критические UX-разрывы
 • 🟠 Средние UX-проблемы
 • 🟡 Незначительные UX-несовпадения
 • ✅ Модули, работающие корректно
 • 📍 Файлы, в которых находятся ошибки
 • 📂 Компоненты, которые не получают актуальные данные
 • 💬 Твоя гипотеза: почему это не работает

⸻

💡 Пример вывода:

1. Кошелёк отображает 0.000 UNI — причина: компонент WalletInfo.tsx подключается к useUserBalance(), но хук возвращает фиксированное значение.
2. Кнопка “Начать фарминг” не реагирует — в компоненте отсутствует onClick.
...


⸻

✅ Твоя задача — только анализ. Без правок.
После отчёта будет отдельная задача на исправление.