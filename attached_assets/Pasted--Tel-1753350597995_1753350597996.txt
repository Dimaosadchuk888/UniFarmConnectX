Вот точное техническое задание для агента:

---

🔧 **Техническое задание: Диагностика доступа в Telegram-бот (админка)**

---

### 📌 Проблема:

После **redeploy приложения** перестал работать доступ в админ-панель через Telegram-бот.
Хотя **мой Telegram-логин прописан как администратор в системе**, бот выдает:
❌ *"Доступ запрещен. Вы не являетесь администратором."*

---

### 📋 Что нужно проверить:

1. **Почему Telegram-бот не обновился при redeploy:**

   * Убедиться, что админский бот был перезапущен вместе с приложением.
   * Проверить, что он корректно инициализировался (в логах инициализации).

2. **Проверка Telegram-авторизации:**

   * Бот корректно парсит `initData` и извлекает Telegram ID/username?
   * Не нарушена ли связка между Telegram ID и текущим пользователем в системе?

3. **Проверить логику проверки прав администратора:**

   * Где в коде происходит проверка `isAdmin` / `roles.includes('admin')` или аналог?
   * Откуда и как бот получает список админов?
   * Убедиться, что мой Telegram ID / username точно находится в этой базе или в переменной окружения.

4. **Проверить конфигурацию и окружение после redeploy:**

   * Не слетели ли `.env` переменные, особенно те, которые касаются:

     * `ADMIN_USER_IDS`, `TELEGRAM_ADMINS`, `ADMIN_USERNAME` и т.д.
   * Подключены ли актуальные `webhooks` у админ-бота?
   * Используется ли корректный токен Telegram-бота, и не поменялся ли он случайно при сборке?

---

### ❗️Важно:

* **Не менять код**, **не трогать роли**, **не делать фикс** — только **исследование и отчёт**, почему бот отказывает в доступе.
* Мой пользователь точно находится в списке админов — нужно понять, почему бот этого не видит.

---

### ✅ Ожидаемый результат:

* Обоснованное объяснение, **почему бот не пускает администратора** в панель.
* Перечень всех возможных причин (от неправильной auth до неверной конфигурации или кэша).
* Предложение, **какой компонент нужно перезапустить / почистить / перепроверить**, чтобы восстановить доступ.

---

🕵️ Жду технический отчёт — без правок в коде — по итогам диагностики.
