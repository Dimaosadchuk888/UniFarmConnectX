Отлично, вот **техническое ТЗ для диагностики проблемы с заявкой на вывод TON**, **без изменения кода** — только проверка и локализация ошибки:

---

### 🔧 **Техническое задание: Диагностика проблемы с созданием заявки на вывод TON**

**Цель:** выяснить, почему при создании заявки на вывод в интерфейсе Replit Preview отображается ошибка, что **"нет подключения к интернету"**, при этом интернет точно работает.

#### 🧩 Контекст:

* Пользователь работает в **Replit Preview** (Telegram WebApp)
* Создаёт заявку на вывод TON через Telegram WebApp
* Видит **ошибку**, связанную с якобы отсутствием подключения
* В прошлом всё работало, сейчас — **ошибка воспроизводится стабильно**
* Важно: **не менять код**, только проверить, что мешает корректному выполнению запроса

---

### 🔍 **Что нужно проверить пошагово:**

#### 1. **Frontend (Replit Preview Telegram WebApp)**

* Убедиться, что:

  * Telegram WebApp SDK инициализирован корректно (`TelegramWebApp.initData` доступен)
  * Нет ошибок в консоли при создании заявки
  * Проверить, корректно ли формируется запрос: `POST /api/wallet/withdraw` или соответствующий endpoint
  * Убедиться, что запрос действительно отправляется (можно отследить в Network или логах)
  * Обратить внимание, **в какой момент возникает ошибка**: при нажатии на кнопку, при ответе от сервера или сразу

#### 2. **Frontend → Backend**

* Пройти по цепочке:

  * Компонент → хук → `fetch` или `axios` → `/api/wallet/withdraw`
  * Проверить, **какие параметры отправляются**, особенно:

    * `amount`
    * `tonAddress`
    * `userId`
    * `initDataRaw` (если Telegram WebApp)
  * Проверить, корректно ли **обрабатывается ошибка** — возможно, отображается некорректное сообщение (“нет интернета”), хотя ошибка другая (например, 401, 403 или 422)

#### 3. **Middleware / Server (Backend)**

* Проверить, доходит ли запрос до сервера (Express.js):

  * Middleware `requireTelegramAuth` — может ли отклонять запрос из-за неподтверждённого `initData`
  * Логировать входящие запросы, убедиться, что **запрос доходит**
* Проверить, срабатывает ли:

  * `wallet/withdrawals.ts`: создание заявки
  * Ошибка может быть брошена здесь — важно её залогировать

#### 4. **База данных (Mongo/PostgreSQL)**

* Проверить таблицы (или коллекции):

  * `Users` — есть ли у пользователя `wallet.tonAddress`
  * `ton_farming_data` — убедиться, что у пользователя не активен Boost, если он влияет на вывод
  * `Withdrawals` — формируется ли новая заявка или даже не доходит до записи

#### 5. **Админ-бот / логика отправки в Telegram**

* Проверить, доходит ли заявка до админ-бота
* Убедиться, что цепочка:

  * `POST /api/wallet/withdraw` → создание заявки → запись в БД → отправка в админ-бот
  * **не обрывается** на одном из шагов
* Если обрывается — залогировать место

---

### ✅ **Результат задачи**

* Найти точку, где рвётся цепочка создания заявки:

  * UI (ошибка отображается неверно?)
  * Network (запрос не уходит?)
  * Backend (авторизация, валидация, логика)
  * БД (данные не соответствуют?)
  * Интеграция с админ-ботом
* Никаких изменений в коде вносить **не нужно**
* После локализации проблемы — оформить отчёт с логами или скриншотами

---

Хочешь, чтобы я сформировал это как PDF для команды или оформил в Notion/Jira-формате?
