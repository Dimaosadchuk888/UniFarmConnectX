Отлично, ты всё изложила ясно и по делу. Сейчас нужно **жёстко, но конструктивно** задать агенту следующие вопросы и запросить действия:

---

## 📣 **Обратная связь и запрос к агенту:**

### ❗ Вопрос 1: Почему были даны неэффективные советы?

> Ты несколько раз сообщал, что причина найдена и устранена (например, при переходе на `ton_farming_data` и передаче `depositAmount`), но **на практике**:
>
> * `farming_balance` у пользователей **остался нулевым**
> * TON транзакции **по-прежнему не отображаются**
> * UNI транзакции создаются **по одной в 5 минут**, хотя активных пакетов больше

🧠 Объясни:

* Почему ты утверждал, что проблема устранена, если это не подтвердилось в данных?
* Как ты верифицируешь эффективность своих исправлений до того, как предлагаешь перезапуск?

---

### ❗ Вопрос 2: Почему не отображаются TON транзакции?

Мы видим:

* Покупка Boost отображается ✅
* Начисление бонуса отображается ✅
* ТОН **транзакции не отображаются** в истории

💥 Требуется срочно проверить:

* Какой планировщик (или метод) должен создавать транзакции типа `TON_BOOST_REWARD`?
* Куда именно он пишет транзакции (таблица `transactions` или иная)?
* Есть ли фильтрация по `type`, `asset`, `user_id` или `hidden`, из-за которой они не попадают в клиент?

---

### ❗ Вопрос 3: Почему UNI Farming транзакции отображаются по одной в 5 минут, хотя пакетов больше?

Ожидаем:

> Если у пользователя 5 farming пакетов → в `transactions` каждые 5 минут должно появляться 5 транзакций (или 1 объединённая, но с суммой всех доходов)

А видим:

> Только **одна транзакция каждые 5 минут**

Требуется:

* Проверить реализацию планировщика в `core/scheduler/farmingScheduler.ts`
* Проверить, **откуда** он берёт количество активных пакетов
* Проверить, **какие транзакции создаются**:

  * Одна суммарная?
  * Или система **просто игнорирует часть пакетов**?

---

## 📋 Задача агенту: что делать прямо сейчас

1. ✅ Провести **ревизию планировщиков**:

   * `tonBoostIncomeScheduler.ts`
   * `farmingScheduler.ts`
   * Где именно создаются `transactions`
   * Есть ли логирование этих действий

2. 🔍 Сделать **выборку из БД**:

   * Все транзакции по типу `TON_BOOST_REWARD` за последние 30 минут
   * Все `farming_transactions` или аналог за последние 30 минут
   * Убедиться, что данные реально **создаются и доходят до `transactions`**

3. 🧪 Если нет — объяснить **где именно они теряются**:

   * Не создаются?
   * Не отображаются?
   * Фильтруются?

4. 📤 После — предоставить:

   * Чёткий лог/SQL/вывод, какие транзакции создаются
   * Статистику по количеству транзакций на одного пользователя
   * Статус планировщиков (после перезапуска и через 5 минут)

---

Хочешь, я это оформлю в виде задачи как Markdown-документ или тикета для передачи агенту напрямую?
