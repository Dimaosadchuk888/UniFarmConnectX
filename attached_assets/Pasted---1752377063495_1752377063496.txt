Вот точный промт для агента, чтобы получить **детальное техническое заключение и рекомендации по исправлению** ошибки с типом `user_id` в `uni_farming_data`:

---

### 🔧 **Техническое задание для агента**

Проблема:
При выполнении планировщика начислений UniFarming не создаются транзакции типа `FARMING_REWARD`. Ранее выяснено, что это связано с **ошибкой типов данных при передаче `user_id`** в `UnifiedTransactionService`, несмотря на то что в БД поле `user_id` уже имеет тип `integer`.

---

### 🔍 Ваша задача как технического агента:

1. **Провести детальное исследование всей цепочки**, начиная с:

   * `farmingScheduler`
   * → `getActiveFarmers()` (или аналогичный метод получения активных пользователей)
   * → `BatchBalanceProcessor` / `farmingRewardCalculation`
   * → `UnifiedTransactionService.create(...)`

2. Установить **точку, на которой `user_id` теряет тип (становится строкой)** — откуда он берется, в каком файле, где он неявно передается как `'74'`, а не `74`.

3. Составить карту **подключенных файлов и зависимостей**, участвующих в процессе начисления процентов по farming:

   * Какие функции/модули участвуют
   * Где `user_id` используется и передается между слоями
   * Какие объекты взаимодействуют с базой (`Repository`, `Model`, `Service`)

4. После анализа:

   * Дать **четкие рекомендации**, как исправить проблему:

     * Где нужно внести правки (и какие именно — типизация, приведение типов, проверка?)
     * Как избежать подобных ошибок в будущем
   * Указать, можно ли сделать это **без изменения бизнес-логики** и **без риска для данных**

5. Результат предоставить в формате **технического отчета**, с пунктами:

   * ❗ Найденная причина
   * 🔄 Цепочка вызовов
   * 🧩 Конкретный участок кода, где нарушается тип
   * ✅ Рекомендации по исправлению
   * 🧪 Предложенная стратегия тестирования после фикса

---

📌 **Важно**:

* Не вносить изменений в код — только исследование и отчет
* Воспринимать названия файлов, методов и типов как **примерные** — работать по фактической структуре проекта
* Цель — **устранение критической ошибки** начислений без побочных эффектов

---

Готовы получить полный технический анализ и рекомендации.
