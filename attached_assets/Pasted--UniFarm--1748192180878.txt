✅ Техническое задание: Финальное устранение критических багов перед запуском UniFarm

Цель: устранить баги, мешающие полноценной работе Telegram Mini App, и обеспечить стабильность на продакшн-уровне.

⸻

1. Webhook сбрасывается на dev-ссылку

Проблема:
Webhook Telegram бота автоматически сбрасывается на https://9283...replit.dev/api/telegram/webhook, несмотря на то, что была установлена продакшн-ссылка https://uni-farm-connect-x-lukyanenkolawfa.replit.app/api/telegram/webhook.

Задача:
 • Найти и удалить любые автоматические скрипты/логики, которые регистрируют dev-ссылку при старте.
 • Убедиться, что только продакшн-ссылка задаётся при старте сервера.
 • Проверить, что setWebhook() вызывается 1 раз и правильно.

⸻

2. Лишняя кнопка “Начать фарминг”

Проблема:
На всех экранах отображается кнопка Начать фарминг внизу, даже если пользователь не на странице фарминга.

Задача:
 • Удалить глобальное отображение этой кнопки.
 • Сделать отображение этой кнопки только на нужной странице, если она действительно нужна.
 • Протестировать, чтобы она не накладывалась на основной UI.

⸻

3. Кнопка “Назад” (BackButton) не работает

Проблема:
Кнопка “Назад” в левом верхнем углу приложения визуально отображается, но не реагирует на нажатие.

Задача:
 • Проверить реализацию Telegram.WebApp.BackButton.
 • Убедиться, что подписка на .onClick() работает и обрабатывает переход на предыдущий экран.
 • Логировать [BACK BUTTON CLICKED] при активации.

⸻

4. Реферальная ссылка не генерируется

Проблема:
На экране “Партнёрка” отображается “Ошибка загрузки реферальной ссылки”.

Задача:
 • Проверить, срабатывает ли generateReferralCode при регистрации пользователя.
 • Убедиться, что пользователь записывается в БД и имеет поле ref_code.
 • Проверить API: /api/v2/referral/code?user_id=X должен возвращать success: true и ссылку.
 • Исправить возможные ошибки связи с БД или сервисом.

⸻

5. Ошибка подключения к серверу

Проблема:
На всех экранах висит сообщение “Ошибка соединения с сервером / Попытка переподключения”.

Задача:
 • Убедиться, что все API возвращают success: true и корректные данные.
 • Проверить корректность correctApiRequest(), возможно, запросы ловят ошибку по timeout или CORS.
 • Проверить серверные логи: что падает?

⸻

6. Кнопка Start не работает

Проблема:
После нажатия на кнопку /start в Telegram нет ответа от бота.

Задача:
 • Проверить, зарегистрирован ли обработчик /start в handleTelegramWebhook.
 • Убедиться, что он отправляет приветственное сообщение и кнопку открытия Mini App.
 • Проверить, что не мешает старое состояние webhook или ошибка парсинга initData.

⸻

7. Админка в Telegram-боте не работает

Проблема:
При нажатии на админ-функции в боте приходит ответ “Функция в разработке”.

Задача:
 • Проверить подключение admin-команд в adminBotRouter.
 • Убедиться, что команды get_wallets, issue_deposit, approve_withdraw и др. зарегистрированы.
 • Подключить обработчики и протестировать 1–2 команды на test-админке.

⸻

8. Пользователь не регистрируется в БД

Проблема:
Пользователь, по всей видимости, не сохраняется в таблице users, из-за чего не запускается логика рефералки и wallet.

Задача:
 • Проверить validateTelegramInitData → registerUser() → usersService.createUser().
 • Убедиться, что не падает логика из-за дублей, ошибок или плохого подключения к БД.
 • Протестировать через лог: [USER REGISTERED] TelegramID=... RefCode=...

⸻

Дополнительно:
 • Убедиться, что логи: [TG INIT], [USER REGISTERED], [REF CODE CREATED], [WALLET BALANCE REQUEST] отображаются в Replit Console.
 • Протестировать полный путь нового пользователя: вход → регистрация → реф ссылка → кошелек → фарминг.

⸻

Формат выполнения:

После выполнения каждого пункта — приложить:
 • ✅ Статус (что сделано)
 • 🔍 Что было найдено
 • 📁 Код, который был изменён (файл + строка)
 • 🧪 Как протестировано

⸻