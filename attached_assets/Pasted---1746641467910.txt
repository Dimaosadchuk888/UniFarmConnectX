Отлично! Вот **финальный чеклист регрессии** после полной миграции сервисов, стандартизации и настройки ESLint + CI. Он поможет убедиться, что всё работает стабильно:

---

## ✅ **Функциональный чеклист регрессии**

### 🔌 **1. Сервер и база данных**

* [ ] Сервер успешно запускается (`npm run dev` / `start`)
* [ ] Подключение к базе данных проходит без ошибок
* [ ] Все таблицы/модели корректно мигрированы
* [ ] Нет ошибок при первой инициализации (`init`, `seed`, `sync`)

### 🌐 **2. API и маршруты**

* [ ] Все основные API-эндпоинты работают (GET, POST, PATCH и т.д.)
* [ ] Ошибки валидации обрабатываются корректно (400, 422 и т.д.)
* [ ] Ошибки сервера отдаются в формате `json` с читаемым `error.message`

### 💬 **3. WebSocket (если есть)**

* [ ] Подключение к WebSocket устанавливается
* [ ] Сервер не падает при отключении клиента
* [ ] Сообщения корректно обрабатываются и транслируются

### 📦 **4. Сервисы после миграции**

* [ ] `referralService` работает как ожидается
* [ ] `referralBonusService` начисляет бонусы
* [ ] `transactionService` записывает операции
* [ ] `farmingService` корректно обрабатывает фарминг-логику
* [ ] `tonBoostService` работает через инстанс + прокси (если включен)

### 🤖 **5. Telegram-инициализация**

* [ ] `initData` корректно передаётся в заголовках
* [ ] В режиме без Telegram приложение работает без краха
* [ ] `hasTelegramData` и `hasTelegramUserId` логируются правильно

### 📁 **6. Стандарты кода и CI**

* [ ] ESLint проходит (`npm run lint`)
* [ ] Husky/lint-staged не допускает коммитов с неправильными именами файлов
* [ ] CI пайплайн проходит без ошибок (если используется GitHub Actions, GitLab CI и т.д.)

---

## 🧪 (Опционально) UI-проверка

Если у тебя есть frontend:

* [ ] Приложение собирается (`npm run build`)
* [ ] Отображается главная страница / WebApp
* [ ] Не возникает ошибок `w.map is not a function` или `undefined`

---

Хочешь, я сделаю из этого чеклиста `REGRESSION_CHECKLIST.md` файл, который можно добавить в проект?
