1. Кошелек (Wallet): перевірити таблиці wallet, wallet_transactions (або user_wallet), створити реальні дані, зробити простий тестовий endpoint /api/v2/wallet/balance та /api/v2/wallet/withdraw.
 2. Фармінг (Farming): перевірити таблиці uni_farming_deposits, ton_farming_boosts, реалізувати /api/v2/ton-farming/info, /api/v2/ton-farming/purchase.
 3. Бусти (Boosts): таблиця boost_packages, /api/v2/boosts/active, /api/v2/boosts/purchase.
 4. Реферальна система (Referral): перевірити referral_links, referral_stats, /api/v2/referral/info, /api/v2/referrals/apply.

⸻

Ось готовий промт для агента:

⸻

Промт для агента (на русском языке):

⸻

Привет. У нас 2 задачи:

ЗАДАЧА 1 — Исправить ошибки с миссиями:
 1. Карточки миссий не открывают ссылки (на чат, канал, YouTube, TikTok) — кнопка кликается, но ничего не происходит.
 2. После прохождения миссии не зачисляются монеты на баланс пользователя.

Требуется:
 • Проверить frontend: корректно ли срабатывает window.open(mission.link) или другой механизм открытия ссылки.
 • Проверить backend: вызывается ли /api/missions/complete, и идет ли начисление награды на balance_uni пользователя.
 • Включить логирование при клике на кнопку “Выполнить”, чтобы отслеживать действия.

⸻

ЗАДАЧА 2 — Активировать остальные модули, как было с /missions:

Нужно активировать и протестировать следующие API-модули в продакшн базе ep-lucky-boat-a463bggt:

A. Кошелек (Wallet)
 • /api/v2/wallet/balance
 • /api/v2/wallet/withdraw
 • /api/v2/wallet/connect
 • /api/v2/wallet/transactions

B. Фарминг (Farming)
 • /api/v2/ton-farming/info
 • /api/v2/ton-farming/boosts
 • /api/v2/ton-farming/purchase
 • /api/v2/ton-farming/confirm-payment

C. Бусты (Boosts)
 • /api/v2/boosts/active
 • /api/v2/boosts/purchase

D. Рефералы (Referral)
 • /api/v2/referral/info
 • /api/v2/referrals/apply
 • /api/v2/referrals/tree
 • /api/v2/referrals/stats

Цель: Убедиться, что все работает с правильной базой (9 пользователей), как это было протестировано с /missions.

После этого можно будет перейти к фронтенд-интеграции этих модулей.