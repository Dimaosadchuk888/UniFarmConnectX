Вот готовое Техническое Задание T50: Аудит системы оплаты TON Boost-пакетов — на русском языке, без внесения изменений в код, только диагностика.

⸻

🧾 Техническое Задание T50: Аудит системы оплаты Boost-пакетов (TON)

🔧 Роль агента:

Вы — технический аудитор. Ваша задача — провести полную проверку механизма покупки Boost-пакетов в UniFarm, как с внутреннего баланса, так и через внешний кошелёк (TON Connect / Tonkeeper).
Код не трогать, ничего не изменять. Только анализ.

⸻

📌 Правила и ограничения:
 • ❌ Запрещено вносить изменения в код
 • ❌ Запрещено добавлять/удалять файлы
 • ❌ Запрещено “улучшать” реализацию
 • ✅ Разрешено читать логику, config, логи
 • ✅ Нужно составить точный отчёт по системе

⸻

🧩 Проверить 2 варианта оплаты Boost-пакетов:

1. ✅ Внутренняя оплата через wallet.balance.ton

Проверить:
 • Есть ли проверка баланса перед покупкой?
 • Списываются ли средства с баланса при покупке Boost?
 • Создаётся ли запись в таблице boost_purchases?
 • Возвращается ли успешный статус клиенту?

2. 🌐 Внешняя оплата через Tonkeeper / TON Connect

Проверить:
 • Корректно ли инициируется внешняя транзакция?
 • Используется ли актуальный TON Connect manifest (production)?
 • Отображается ли название приложения (а не dev-домен/email)?
 • Отслеживается ли транзакция на backend (polling, listener)?
 • Boost-пакет активируется только после подтверждения оплаты?

⸻

🗃️ Проверить базы данных Supabase:
 • Таблица boost_purchases:
 • Есть ли поля source, tx_hash, confirmed?
 • Записывается ли источник оплаты: internal/external?
 • Таблица wallets:
 • Обновляется ли баланс пользователя при оплате Boost?

⸻

🔍 Проверить API /api/v2/boost/purchase
 • Принимает ли два источника оплаты (внутренний / внешний)?
 • Проверяет ли баланс при внутренней оплате?
 • Обрабатывает ли подтверждение при внешней оплате?

⸻

🔁 Проверить frontend-логику:
 • Есть ли выбор между внутренней и внешней оплатой?
 • Корректно ли вызывается TonConnect при внешней оплате?
 • Есть ли логика ожидания подтверждения?
 • Отображается ли сообщение об успехе/ошибке?

⸻

📈 Ожидаемый формат отчёта:

# ОТЧЁТ ПО T49 — АУДИТ СИСТЕМЫ ОПЛАТЫ BOOST-ПАКЕТОВ

1. Внутренняя оплата: ✅/⚠️/❌
 - ...
2. Внешняя оплата Tonkeeper: ✅/⚠️/❌
 - ...
3. Supabase таблицы:
 - ...
4. API: ...
5. frontend: ...
6. Вывод: ...

Цель — подтвердить, что:
 • Покупка работает и с баланса, и через кошелёк
 • Данные записываются корректно
 • Boost активируется только после оплаты

⸻