1. ОБЩИЕ ПРАВИЛА РАБОТЫ (ОБЯЗАТЕЛЬНЫ К ИСПОЛНЕНИЮ!)

НЕЛЬЗЯ:
 • ❌ Создавать дубликаты существующих файлов, контроллеров, сервисов, маршрутов.
 • ❌ Менять структуру архитектуры, импортов, интерфейсов без согласования.
 • ❌ Использовать @ts-ignore, отключать eslint, strict или any без явной причины.
 • ❌ Добавлять временные костыли, логику с флагами if (DEV) и др.
 • ❌ Вносить изменения в dev-режим, если задача относится к продакшн.

ОБЯЗАТЕЛЬНО:
 • ✅ Все исправления делать строго в существующих модулях (controller/service/router).
 • ✅ Следовать правилам чистой архитектуры: контроллер → сервис → слой БД.
 • ✅ Каждое изменение сопровождается комментарием: // [FIX: WEBHOOK ISSUE #1]
 • ✅ Вести лог в консоли по шаблону: [MODULE] → [ACTION]: подробности

⸻

2. КРИТИЧЕСКИЕ ЗАДАЧИ (ОБЯЗАТЕЛЬНО К ВЫПОЛНЕНИЮ):

1. Webhook постоянно сбрасывается на dev
 • Удалить все участки кода, где явно прописана dev-ссылка.
 • Установить и зафиксировать webhook только на:

https://uni-farm-connect-xo-osadchukdmitro2.replit.app/api/telegram/webhook



2. Удалить кнопку “Начать фарминг” с нижней панели
 • Убедиться, что она не отображается глобально.
 • Разрешить отображение только при входе в farming-экран (при необходимости).

3. BackButton не работает
 • Проверить подписку на .onClick().
 • Обновить страницу или маршрут на navigate('/') или аналогичный.

4. Реферальная ссылка не генерируется
 • Проверить: есть ли ref_code у пользователя в БД.
 • Протестировать ручками /api/v2/referral/code?user_id=X.

5. Ошибка подключения к серверу
 • Проверить каждый запрос в network.
 • Ловить ошибки через try/catch, логировать [CONNECTION FAIL].
 • Убедиться, что фронт и бэкенд используют один и тот же /api.

6. Кнопка /start не реагирует
 • Проверить регистрацию webhook.
 • Проверить handleTelegramWebhook на /start-обработчик.
 • Убедиться, что приветствие и кнопка “Открыть UniFarm” отправляются.

7. Админка Telegram не работает
 • Все команды, кроме “в разработке”, должны быть подключены.
 • Пример команды: get_wallets, issue_deposit, get_users, approve_withdraw.

8. Пользователь не регистрируется в БД
 • Проверить регистрацию: validateInitData() → createUser().
 • Убедиться, что срабатывает ref_code + telegram_id.

⸻

3. ДОПОЛНИТЕЛЬНЫЕ ЗАДАЧИ (РЕКОМЕНДОВАНЫ):
 • Проверить, что логика connectTonWallet() и isTonWalletConnected() работает без багов.
 • Все критические логи должны отображаться в Replit-консоли:
 • [WEBHOOK OK], [REF CODE CREATED], [WALLET CONNECTED], [START RECEIVED].

⸻

ОТЧЕТНОСТЬ (ОБЯЗАТЕЛЬНО):

После выполнения каждой задачи агент должен прислать:
 • ✅ Статус (что сделано)
 • 🔍 Что было найдено
 • 🛠️ Файл + строка (где исправлено)
 • 🧪 Как протестировано

⸻