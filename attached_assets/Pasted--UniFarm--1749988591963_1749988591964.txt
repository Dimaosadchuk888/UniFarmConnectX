🟩 Роль: Системный аудитор UniFarm

Ты выполняешь задачу в роли профессионального аудитора. Твоя задача — провести полный и глубокий анализ системы UniFarm перед релизом, чтобы гарантировать её 100% готовность к продакшену.

⸻

🛑 Ограничения:
 1. ❌ Категорически запрещено вносить любые изменения в код.
 2. ❌ Нельзя перезаписывать .env, Secrets или выполнять Redeploy.
 3. ❌ Нельзя использовать фиксы “на ходу”, заглушки, мок-данные.
 4. ❌ Нельзя переписывать логику Supabase, Telegram, API.
 5. ✅ Разрешено только анализировать, использовать console.log, DevTools, Postman, Supabase Dashboard и другие инструменты наблюдения.

⸻

📋 ЗАДАЧА:

Выполни глубокую технико-функциональную проверку всех ключевых компонентов UniFarm и подготовь структурированный отчёт.

⸻

🔍 ЧТО НУЖНО ПРОВЕРИТЬ:

✅ 1. Авторизация через Telegram:
 • Используется ли window.Telegram.WebApp.initData
 • Проходит ли создание пользователя и выдача JWT
 • Присутствует ли токен в localStorage (или в cookies)

✅ 2. API-эндпоинты (через Supabase):

Проверь каждый вручную (Postman или fetch):
 • /auth/telegram — выдаёт JWT (200 OK)
 • /me — возвращает профиль пользователя
 • /wallet/balance — возвращает UNI и TON
 • /farming/start — запускает фарминг
 • /referral/tree — строит структуру
 • /airdrop/missions — возвращает список миссий

✅ Каждый должен работать без ошибок (❌ 401, ❌ 500)

✅ 3. Подключение к базе:
 • Используется только core/supabase.ts
 • Прямое подключение к PostgreSQL, drizzle и Neon должно быть полностью исключено
 • CRUD-операции проходят в Supabase Dashboard

✅ 4. Telegram WebApp:
 • Приложение открывается через Telegram без ошибок
 • Инициализация WebApp проходит
 • Контекст Telegram (user, ref) передаётся в initData

✅ 5. UI и интерфейс:
 • Интерфейс отображается корректно, нет “чёрного экрана”
 • Все кнопки работают: “Старт”, “Фарминг”, “Баланс”, “Бонус”, “Рефералы”
 • Нет ошибок в DevTools (❌ fetch error, ❌ console.error)

✅ 6. Секреты .env и Replit Secrets:
 • Удалены: DATABASE_URL, PG*, USE_NEON_DB
 • Присутствуют: SUPABASE_URL, SUPABASE_KEY, JWT_SECRET, BOT_TOKEN
 • Убедись, что код реально использует Supabase API, а не старые подключения

✅ 7. Webhook:
 • URL: должен быть production, не Dev
 • Проверь вручную или через Telegram API

✅ 8. Health-эндпоинты:
 • /health и /api/v2/health возвращают 200 OK

✅ 9. Manifest:
 • Проверить наличие manifest.json
 • Проверить регистрацию в BotFather
 • WebApp URL должен быть указан

✅ 10. Supabase Dashboard (тест с реальными действиями):
 • Claim бонус → создаётся транзакция
 • Старт фарминга → записывается сессия
 • Приглашение пользователя → отображается в дереве
 • Миссии → появляются записи в user_mission_completed

⸻

📦 Формат отчёта:

📌 UniFarm Аудит (дата/время)

✅ Что работает:
- …
- …

❌ Что не работает:
- …
- …

⚠️ Подозрительные места:
- …

🟢 Общий статус: Готов / Требует доработки / Критические ошибки


⸻

🎯 Цель:

Ты должен подтвердить, что система UniFarm действительно работает на 10 из 10, без фейковых логов или поверхностных заявлений. Каждый блок должен быть проверен вручную и подтверждён реальным тестом.

⸻