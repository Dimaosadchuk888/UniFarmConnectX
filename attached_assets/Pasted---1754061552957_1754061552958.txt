Вот готовое **техническое задание** для агента, максимально чёткое и структурированное — с акцентом на полный аудит базы данных и поведения системы, включая тестовые сценарии и симуляции:

---

## 📋 Техническое задание: Полный аудит Базы Данных и Системы

**Цель:**
Провести **глубокий аудит** текущей архитектуры базы данных и поведения системы UniFarm, включая анализ всех запросов, полей, таблиц, бизнес-цепочек и симуляцию пользовательских сценариев.

---

### 🔍 Задачи:

#### 1. **Анализ структуры базы данных**

* Выгрузить и описать **всю актуальную схему БД** (таблицы, поля, связи, индексы).
* Отдельно указать:

  * Какие таблицы активно используются
  * Какие поля используются системой, а какие — нет (пустые, устаревшие, дублирующие)
  * Какие таблицы системно важные (farming, wallet, user и т.п.)
  * Где возможно возникновение конфликтов, утечек, дублирований

#### 2. **Анализ системных запросов**

* Зафиксировать все **основные SQL-запросы и взаимодействия с БД**, исходящие от системы (frontend, backend, WebSocket, Telegram-бот).
* Какие таблицы и поля:

  * читаются (`SELECT`)
  * изменяются (`UPDATE`, `INSERT`)
  * удаляются (`DELETE`)
* Отметить частоту и триггеры этих операций (по какому событию/действию происходят).

#### 3. **Проверка на расхождения**

* Проверить соответствие между:

  * фактической структурой БД
  * используемой бизнес-логикой
* Найти поля или таблицы, которые:

  * задекларированы, но не используются
  * используются, но не задокументированы
  * создаются, но не заполняются
  * дублируют друг друга

---

### 🧪 Тестовые сценарии (симуляции)

Создать **отдельные тестовые аккаунты** и вручную пройти следующие сценарии:

1. **Регистрация нового пользователя**

   * Проверить, что создаётся в БД, в каких таблицах и как связаны записи.

2. **Присвоение баланса**

   * Начислить тестово 10 TON и 100 UNI
   * Проверить: как и где это отразилось в БД (таблицы, поля, логи)

3. **Фарминг / бусты / бонусы / рефералка**

   * Запустить фарминг, активировать буст, получить ежедневный бонус
   * Пригласить тестового реферала, посмотреть распределение
   * Проверить все связанные таблицы и расчёты

4. **Вывод средств**

   * Подать заявку на вывод, пройти все шаги до выплаты
   * Проверить создание и изменение записи в таблице заявок, статус, связанные поля

5. **Все возможные пути пользователя**

   * Пройтись через все разделы: farming, missions, wallet, friends, boosts и т.д.
   * Зафиксировать, какие запросы и таблицы участвуют на каждом этапе

---

### 📤 Ожидаемый результат

1. **Отчёт по базе данных**:

   * Актуальная структура БД (схема)
   * Список реально используемых таблиц/полей
   * Найденные несоответствия, потенциальные ошибки

2. **Отчёт по логике работы системы**:

   * Цепочки запросов от действий пользователя до изменений в БД
   * Все триггеры, автоматические операции
   * Где и как хранятся ключевые данные

3. **Скриншоты и логи из тестовых симуляций**

   * Что создаётся, куда пишется, что обновляется при каждом шаге

---

### ⚠️ Важно:

* Никаких изменений в проде — только чтение и симуляции на тестовых данных
* Обязательно сохранять все логи, скриншоты и записи тестов
* Не использовать реальные данные пользователей

---

Если хочешь, я могу помочь потом сверить отчёт агента или сформировать SQL-запросы для проверки.
