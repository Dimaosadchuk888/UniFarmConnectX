🧪 Техническое задание №1 (в формате исследования)

📍 Тема:

Проблема с Telegram-авторизацией: при входе в Mini App любой пользователь попадает в тестовый аккаунт user_id=48.

⸻

🎯 Цель исследования:

Определить:
 1. В каком окружении сейчас развернуто приложение (продакшн / дев / preview).
 2. Почему при входе через Telegram Mini App подгружается user_id=48 независимо от реального аккаунта пользователя.
 3. Где именно происходит жесткая привязка (hardcode) к user_id=48 или аналогичная подмена данных.

Важно:
🔒 Агенту категорически запрещено принимать какие-либо архитектурные или технические решения до завершения исследования и согласования отчёта.

⸻

📋 Обязательные этапы исследования

🔍 1. Определение окружения
 • Установить, в каком режиме развернута текущая версия сайта:
 • Продакшн (main, release, live)
 • Preview / Replit (временный или dev-режим)
 • Staging / Sandbox (если есть)
 • Указать, на каком хостинге или платформе запущена Mini App: Vercel, Replit, Railway, Render, Heroku, и т.п.
 • Уточнить, используется ли переменная окружения NODE_ENV, и какое значение у неё на активной сборке (development, production, test, и т.д.).

🧠 2. Анализ механизма авторизации через Telegram
 • Проверить, откуда берётся user_id при запуске Mini App:
 • Используется ли Telegram.WebApp.initDataUnsafe.user.id
 • Как формируется initData и передаётся ли оно на сервер (бэкенд)
 • Убедиться, не используется ли какой-либо мок (mock user), defaultUser, testData или hardcoded value в следующих частях:
 • auth.service.ts, user.service.ts, telegram.ts, preview.ts, session.ts
 • Указать, какие именно данные приходят при запуске Mini App, и отображаются ли они в DevTools / Network.

📎 3. Поиск привязки к user_id = 48
 • Найти все участки кода, где явно или косвенно подставляется user_id=48.
 • Проверить, не возвращается ли user_id=48 в случае:
 • Отсутствия Telegram initData
 • Тестового режима или ошибки авторизации
 • Выявить, как формируется текущий пользователь при запросе данных (вызовы к /me, /wallet, /transactions и т.д.).

⸻

📌 Ожидаемый результат исследования (в формате отчёта):
 1. Среда запуска приложения:
 • Продакшн или тест
 • Подтверждено по URL / переменным окружения / платформе хостинга
 2. Источник user_id:
 • Как именно подгружается текущий user_id
 • Откуда берется user_id = 48
 3. Факты по hardcode или фиктивной авторизации:
 • Какие файлы/модули используют захардкоженный user_id=48
 • Почему Telegram initData не применяется (если не применяется)
 4. Список всех мест в коде, где происходит подмена или фиксация пользователя.

⸻

🛑 Важное ограничение:
 • НИ ОДНОГО изменения в коде, конфиге, БД, логике, даже если оно очевидное — вносить нельзя.
 • Только исследование и отчёт.
 • После завершения отчёта — передаём его на согласование и только потом приступаем к исправлениям.

⸻