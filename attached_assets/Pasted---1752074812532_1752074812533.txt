Отлично, вот полностью уточнённое и жёстко ограниченное **техническое задание для агента**, с учётом всех твоих пожеланий:

---

# 🛠️ **Техническое задание для агента UniFarm: Аудит перехода на `user_id` и проверка баланса**

**Дата**: 9 июля 2025
**Цель**: Проверить, не нарушил ли переход от `telegram_id` к `user_id` текущую работу системы. А также — проверить, почему отображается некорректный баланс UNI (100 UNI вместо 448), хотя транзакции идут.

---

## 🧷 Ограничения

🔴 **Категорически запрещено:**

* Вносить какие-либо изменения в код
* Переопределять логику
* Модифицировать существующие файлы
* Перезапускать сервер
* Переопределять переменные среды

🟢 **Разрешено только:**

* Читать код
* Извлекать информацию из ROADMAP.md
* Анализировать конкретные связанные файлы
* Делать выводы, не затрагивая сторонние модули

---

## 🧭 Методика

Агент должен **начать с анализа `ROADMAP.md`** и использовать его как основу для поиска всех модулей и файлов, относящихся к:

* пользовательской идентификации (`user_id`, `telegram_id`)
* отображению баланса UNI
* запуску фарминга
* записи и отображению транзакций
* realtime-обновлению (если используется)

---

## 🧩 Проверочные блоки

### ✅ **1. Анализ перехода от `telegram_id` к `user_id`**

* Найти все участки кода, в которых:

  * `telegram_id` используется в качестве ключевого идентификатора пользователя
  * API или логика обращается к `getUserByTelegramId`
  * `req.telegramUser.telegram_id` участвует в логике
* Сравнить их с местами, где уже используется `user_id`
* Выявить:

  * где остались старые вызовы по `telegram_id`
  * не нарушилось ли что-то после перехода на `getFarmingInfoByUserId`

**Пример вопросов:**

* Все ли текущие farming-операции используют `user_id`?
* Не происходит ли в кошельке отображение по `telegram_id`, в то время как farming обновляет по `user_id`?

---

### ✅ **2. Проверка отображаемого баланса пользователя с `user_id = 62`**

Анализ:

* Что возвращает `GET /api/v2/wallet?user_id=62`?
* Что отображается в `localStorage`, `DevTools`, `JWT payload`?
* Какой `telegram_id` связан с этим пользователем? (Ожидается: `88210042`)
* В какой структуре (или таблице) числится баланс 448 UNI?

**Важно:** сверить `wallets.balance` или `wallets.available_balance` в базе с отображением в интерфейсе.

---

### ✅ **3. Аудит endpoint `/api/v2/uni-farming/status?user_id=62`**

* Проверить, какие данные возвращаются
* Сравнить с тем, что видит пользователь в UI
* Убедиться, что farming статус и кошелёк **синхронизированы** по одному и тому же ID

---

### ✅ **4. Сопоставление транзакций**

* Проверить, в каких таблицах и по какому ID хранятся транзакции
* Убедиться, что они не дублируются на разных ID (например, telegram vs user)
* Проверить, отображаются ли эти транзакции в UI по текущему ID

---

## 📤 Ожидаемый результат от агента:

* 📑 Таблица всех мест, где ещё используется `telegram_id`
* ✅ Список модулей, где `user_id` используется корректно
* ⚠️ Места потенциальной рассинхронизации между farming и wallet
* 📊 Фактические данные:

  * `user_id` → `62`
  * `telegram_id` → `88210042`
  * Откуда отображается `100 UNI`, откуда `448 UNI`
* ❓ Ответ: может ли быть, что фарминг и кошелёк смотрят на **разные идентификаторы**
* 💡 Вывод: требует ли ситуация отката изменений или полного перехода системы на `user_id`

---

Если ты согласна, я могу «запустить» этот анализ как будто агент его выполняет — и дать тебе **финальный симулированный отчёт** по результатам всех четырёх проверок.

Дать агенту команду?
