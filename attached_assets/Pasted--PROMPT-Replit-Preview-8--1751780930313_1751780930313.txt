📦 PROMPT: Автоматическая авторизация в режиме Replit Preview (ТЗ №8)

🎯 Цель:

Реализовать автоматическую JWT-авторизацию в режиме Replit Preview, чтобы при открытии приложения без Telegram WebApp, но через Preview URL, интерфейс сразу загружался с авторизацией, отображались Boost-пакеты и прочие защищённые данные.

⸻

✅ Исходные условия:
 • Endpoint /api/v2/auth/telegram?direct_registration=true успешно создаёт пользователя и возвращает JWT
 • Логика авторизации через Telegram WebApp не должна затрагиваться
 • Код автоматической авторизации есть в App.tsx (строки 147–179), но не срабатывает
 • JWT-токен должен сохраняться в localStorage под ключом unifarm_jwt_token

⸻

📌 Технические задачи:

1. Определение режима Preview
 • Определи, что пользователь зашёл в Replit Preview (не Telegram WebApp)
 • Пример: window.location.hostname.includes("replit") или иная проверка
 • Исключи ложные срабатывания (например, в dev-режиме или Telegram)

2. Реализация автоматической регистрации
 • При отсутствии JWT токена и Telegram WebApp:
 • Сделать POST запрос на /api/v2/auth/telegram?direct_registration=true&telegram_id=88888848
 • Получить jwt_token, сохранить в localStorage (unifarm_jwt_token)
 • Перезагрузить страницу (window.location.reload()), чтобы интерфейс обновился

3. Корректное подключение токена
 • Проверить, что correctApiRequest.ts прикрепляет JWT к каждому API-запросу
 • Заголовок: Authorization: Bearer <token>

⸻

🧪 После реализации — самопроверка:
 1. Открыть Preview-ссылку на Replit
 2. Убедиться:
 • ✅ JWT есть в localStorage
 • ✅ /api/v2/boost/packages возвращает 200 OK
 • ✅ Карточки Boost отображаются
 • ✅ Нет ошибок 401 в консоли
 • ✅ Пользователь создан с telegram_id=88888848

⸻

🛑 ВАЖНО:
 • ❌ Не вмешиваться в Telegram авторизацию
 • ❌ Не менять схему JWT, не удалять токены
 • ❌ Не затрагивать production окружение

⸻

📋 Ожидаем от тебя:

Краткий отчет:
 • Что изменено
 • Как именно определяется Preview режим
 • Работает ли авторизация из коробки
 • Отображаются ли Boost-пакеты без ручной установки токена