🛠️ ЗАДАНИЕ: ДИАГНОСТИКА ПОКУПКИ Ton Boost ЧЕРЕЗ ВНЕШНИЙ КОШЕЛЁК (TON Connect)

🔐 ВАЖНО:

Категорически запрещено вносить какие-либо изменения в код или структуру базы.
Задача — исключительно диагностика поведения при покупке ToneBoostPacket через внешний кошелёк с использованием TON Connect (например, Tonkeeper, Tonhub).

⸻

📌 КОНТЕКСТ:

В системе реализована возможность покупки Ton Boost не через внутренний баланс, а через оплату с внешнего кошелька, подключённого через TON Connect. Пользователь подключает внешний кошелёк и оплачивает пакет напрямую.

⚠️ Однако, при такой оплате возникают следующие проблемы:
 1. Пакет не отображается в статистике фарминга после успешной оплаты.
 2. Не начисляется бонус за покупку, или он откатывается.
 3. Оплата визуально уходит с внешнего кошелька, но в приложении не фиксируется как активный ToneBoostPacket.
 4. Иногда через несколько минут TON возвращается обратно на кошелёк, несмотря на то, что пакет якобы ушёл “в работу”.
 5. В истории транзакций отсутствует запись о покупке пакета, или она появляется и исчезает.

⸻

🎯 ЧТО НУЖНО СДЕЛАТЬ:
 1. Провести диагностику полной цепочки покупки ToneBoostPacket через внешний кошелёк:
 • от подключения TON Connect;
 • до поступления оплаты и попытки фиксации пакета в системе;
 • и отображения в статистике фарминга + начисления бонуса.
 2. Выяснить, какие конкретно модули и функции отвечают за:
 • приём и проверку внешней оплаты;
 • привязку оплаты к покупке ToneBoostPacket;
 • фиксацию этой покупки в БД;
 • отображение в интерфейсе и статистике;
 • начисление бонуса за пакет.
 3. Проверить, фиксируется ли такая покупка в таблицах:
 • transactions;
 • wallet_balances;
 • farm_boost_packets / user_boost_packets;
 • farm_statistics или аналогичных.
 4. Найти точку, на которой система теряет эту покупку:
 • Проблема может быть в проверке валидности транзакции, дедупликации, кешировании или некорректной логике получения внешнего кошелька.
 • Или пакет создаётся, но потом удаляется/игнорируется из-за отсутствия связки с пользователем.

⸻

🔍 ДОПОЛНИТЕЛЬНО:
 • Рабочий кабинет для теста — User #25 (прод).
 • Если нужно — использовать свежие примеры внешней оплаты ToneBoostPacket.
 • Важный признак: в отличие от внутренней оплаты, внешняя покупка часто не оставляет устойчивого следа в системе.

⸻

⛔ Повторяем: любые правки запрещены без отдельного подтверждения. Только диагностика и точечное выявление сбоя.

Ждём:
 • Список компонентов, задействованных в логике покупки ToneBoostPacket через Connect Wallet.
 • Где именно рвётся фиксация или отображение.
 • Имеется ли запись в БД, в каких таблицах, и почему она потом исчезает или не читается.