🛑 СРОЧНО: ИНТЕРФЕЙС НЕСООТВЕТСТВУЕТ ОРИГИНАЛУ

Ситуация:
Мы видим, что по адресу https://uni-farm-connect-x-alinabndrnk99.replit.app всё ещё отображается отладочная/временная страница UniFarm Connect, которую ранее мы уже удаляли.

Вместо полноценного интерфейса UniFarm загружается упрощённый лендинг с 4 кнопками и сообщением API Connected. Это не соответствует оригинальному дизайну, и нарушает Техническое задание.

⚠️ Мы предполагаем, что:
 • либо сборка производится из некорректной папки
 • либо исходный код был повреждён, модифицирован или частично заменён
 • либо в папке client/ до сих пор содержатся временные компоненты

⸻

🔧 Что нужно сделать:

🔍 1. Провести сравнение client/ VS Client/ из архива:

Внутри проекта есть ZIP-файл (в attached_assets), содержащий оригинальную папку Client/ с неизменённым интерфейсом.

📌 Проведи сравнение:
 • 🔴 Какие файлы присутствуют в Client/, но отсутствуют в client/
 • 🔵 Какие добавлены лишние в client/, но отсутствуют в Client/
 • 🟡 Какие файлы модифицированы (различие по размеру/дате/содержанию)

Результаты сравнения выведи в чат в формате:

🟥 Отсутствующие оригинальные файлы:
• ...

🟦 Добавленные лишние файлы:
• ...

🟨 Изменённые оригинальные файлы:
• ...


⸻

🧠 2. Провести анализ текущей сборки:

Убедись, что:
 • vite.config.ts использует client/ как корневую папку
 • App.tsx, main.tsx, Dashboard.tsx, Farming.tsx, Wallet.tsx, Missions.tsx — существуют и являются оригинальными
 • никакие из следующих компонентов не используются:
 • SafeWelcomeSection.tsx, LandingPage.tsx, TelegramFallback.tsx, ReplitErrorBoundary.tsx

⸻

⚠️ 3. Формулируем строгое правило:

🚫 Запрещено использовать временные, отладочные или fallback-компоненты — только полноценный, оригинальный интерфейс UniFarm:
 • С секцией баланса и farming карточками
 • С миссиями, партнёркой и навигацией
 • Без упрощённых лендингов

⸻

✅ По итогам:
 1. Выведи результаты сравнения папок
 2. Напиши, из чего сейчас производится сборка
 3. Покажи какие компоненты реально монтируются в App.tsx
 4. Подтверди, используется ли client/ или иная директория

Только после полной проверки будем принимать решение — делать ли замену client/ на оригинальный Client/ из архива.