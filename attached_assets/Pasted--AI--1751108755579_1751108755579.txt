Вот твой строгий, полный и точный промт для AI-агента или разработчика, который должен провести тотальный аудит всей системы проекта UniFarmConnectX.

Этот промт:
 • запрещает правки — только проверка
 • требует поэтапную проверку каждой папки и файла
 • на выходе — отчёт-чеклист с галочками и комментариями

⸻

✅ ПРОМТ: Полный аудит всей системы UniFarmConnectX (без правок кода)

⸻

🎯 ЦЕЛЬ:

Провести тотальную проверку всего проекта, включая все файлы, модули, скрипты, утилиты, конфигурации и тесты — без исключений.

⸻

🔒 ОГРАНИЧЕНИЯ:
 • ❌ НЕЛЬЗЯ редактировать, удалять, перемещать или создавать файлы;
 • ❌ НЕЛЬЗЯ “исправлять на месте” — только аудит;
 • ✅ Ваша задача — полностью зафиксировать текущее состояние проекта, и указать всё, что:
 • подключено;
 • не подключено;
 • дублируется;
 • мёртвое;
 • конфликтует;
 • содержит мусор;
 • вызывает подозрение.

⸻

🧩 СТРУКТУРА ПРОВЕРКИ:

Проверка разбита на следующие категории:
 1. 📁 Исходный код (frontend, backend, modules, core, utils, shared)
 2. ⚙️ Конфигурации (.env, *.config.js, vite.config.ts, tsconfig.json, и т.д.)
 3. 🧪 Тестовые скрипты (test-, *-test.js, verification-*)
 4. 📜 Markdown-документация (*.md)
 5. 🗃️ SQL-скрипты
 6. 🔧 Системные скрипты (*.js, *.sh, .cjs, кроме тестов)
 7. 🧹 Временные / мусорные / старые (log, old, backup, tmp)
 8. 🧩 Прочее — всё, что не попало в другие категории (нужно определить вручную!)

⸻

🔍 КАЖДЫЙ ФАЙЛ ИЗ ПРОЕКТА ДОЛЖЕН БЫТЬ ПРОВЕРЕН:

Для каждого файла — строго внести в чеклист такие данные:

Название файла Папка Подключён? ✅/❌ Используется? ✅/❌ Дубликат? ⚠️ Комментарий
ReferralCard.jsx components/ ✅ ✅ ❌ Отображается на главной странице
ReferralCardOLD.jsx components/ ❌ ❌ ⚠️ Старый компонент — дубликат
vite.config.ts root ✅ ✅ ❌ Базовая конфигурация
server_new.log root ❌ ❌ ❌ Мусорный файл


⸻

📋 ФОРМАТ ЧЕК-ЛИСТА:

Файл формата Google Sheets / Notion с колонками:
 • ✅ Подключён
 • ✅ Используется
 • ⚠️ Подозрение на дубликат
 • ❌ Мусор или временный файл
 • 💬 Комментарий — объяснение, где используется или почему не нужен

⸻

📌 ОБЯЗАТЕЛЬНО:
 • Пройтись по всему проекту, по всем папкам и подпапкам
 • НЕ пропустить ни одного файла
 • Обозначить всё, что:
 • требует удаления;
 • устарело;
 • конфликтует;
 • не должно попадать в продакшен;
 • Сделать полный аудит реального подключения файлов в коде;
 • Проверить корректность путей, импортов, названий, структуры.

⸻

🔁 ОТЧЁТ:

В финале предоставить:
 • 📝 Полный чеклист (таблица);
 • 📂 Список всех подозрительных или мусорных файлов;
 • ⚠️ Список потенциальных проблем или конфликтов;
 • ✅ Комментарии по каждому компоненту/файлу.

⸻

📣 Финальный отчёт обязателен, и будет использоваться для чистки, оптимизации и продакшен-сборки проекта.

⸻

Хочешь — я сейчас сделаю Google Sheets шаблон под этот чеклист. Подтверди, и я сгенерирую файл 📊