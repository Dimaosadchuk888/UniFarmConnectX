Отлично, ты хочешь провести **глубокий технический аудит всей цепочки обновления баланса UniFarming**, **не ограничиваясь первой найденной проблемой**, а пройти **всю архитектуру — от бизнес-логики и БД до UI**, чтобы:

* выявить все возможные точки обрыва,
* зафиксировать **реальные**, а не предполагаемые, ошибки,
* исключить "симптоматические" решения.

Вот идеальный **промпт-запрос для технического агента** под такой аудит:

---

## 🧠 ГЛУБОКОЕ ИССЛЕДОВАНИЕ СИНХРОНИЗАЦИИ БАЛАНСА UNI FARMING

### 🧭 Цель:

Выполнить **полный анализ всей цепочки синхронизации баланса** после начислений из UniFarming, чтобы:

* Выявить **точные места**, где нарушается синхронизация.
* Подтвердить, **какие поля в базе данных реально обновляются**.
* Проверить, **что передаётся по WebSocket** и получает ли UI правильные данные.
* Убедиться, что **вся архитектура согласована и данные протекают корректно по всей цепочке**: от backend до фронтенда.
* **Никаких предположений** — только факты, подтверждённые данными, логами или чтением кода.

---

### 🔐 Условия:

* ❌ **Категорически запрещается** изменять код, базу данных или конфигурации — это **аудит**, не отладка.
* ✅ Все выводы должны быть **доказательными**: ссылки на строки кода, схемы, фрагменты логов, структуру БД.
* ⛔ Не писать отчёт "по первым симптомам". Нужно пройти **всю цепочку от начала до конца** и выявить **все точки, где возможна несогласованность или поломка.**

---

### 📂 Что нужно проверить:

#### 1. **База данных**

* Какие поля (balance\_uni, balance\_ton, farming\_rewards, deposits и т.д.) обновляются при начислении farming?
* Создаются ли соответствующие транзакции (`FARMING_REWARD`, `FARMING_DEPOSIT`, другие)?
* Какие значения реально пишутся в `users.balance_uni`?
* Есть ли расхождения между транзакциями и итоговым балансом?
* Есть ли триггеры, функции или отложенные обновления, которые могут быть причиной задержки/рассинхронизации?

#### 2. **Backend**

* Где происходит начисление farming-наград (файл, метод)?
* Как формируются и отправляются WebSocket уведомления (файл, строка)?
* Как именно формируется payload (структура, поля)?
* Используется ли `BalanceManager` или `BatchBalanceProcessor`, и согласованы ли они?
* Где регистрируются WebSocket соединения и в каком сервисе они хранятся?
* Какие поля ожидает получить фронт?
* Есть ли асинхронные вызовы, которые могут привести к задержкам или потере данных?

#### 3. **WebSocket**

* Где и как подключается клиент (userId, тип события)?
* Что реально отправляется в WebSocket (`balance_update`, `transaction_update` и т.д.)?
* Есть ли расхождение между ожидаемыми и фактическими событиями?

#### 4. **Frontend (UI/UX)**

* Где отображается баланс?
* Как он обновляется — по WebSocket или через refetch?
* Получает ли компонент правильные данные?
* Что приходит в `console.log` при новых farming событиях?
* Реагирует ли компонент на тип события `balance_update`?

---

### 🧾 Ожидаемый результат:

Агент должен предоставить **структурированный отчёт** по следующей форме:

#### A. Архитектурная цепочка

* 🔁 Полный путь данных: *"scheduler → processor → db → notification → websocket → UI"*
* 🧱 Указание точек входа/выхода на каждом этапе (файлы и строки)

#### B. Найденные обрывы или конфликты

* ❌ Точно где цепочка рвётся (строка, причина)
* 🛠️ Что из передаваемых данных отсутствует/неверное
* 🔎 Сравнение между фактическими и ожидаемыми значениями

#### C. Возможные причины вторичных эффектов

* Потенциальные race condition
* Проблемы с актуальностью кэша
* Несогласованность между batch и live слоями

#### D. Вывод и рекомендации (без изменений в коде!)

* Только **набор точек**, где потенциально требуется правка
* **Ничего не менять**, просто выявить, **что не синхронизировано** и где архитектура нарушена

---

💡 *При необходимости можешь приложить JSON-примеры, SQL-запросы, чтобы зафиксировать, что именно передаётся.*

Если хочешь — я помогу после этого аудита оформить **план действий на исправление**, уже на базе этих фактов.
