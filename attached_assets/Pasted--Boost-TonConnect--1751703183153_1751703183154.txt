⸻

📋 ТЗ: Глубокий аудит системы оплаты Boost через TonConnect

🎯 Цель:

Провести полное исследование и аудит цепочки оплаты Boost пакетов через внешние TON-кошельки (Tonkeeper, Wallet и т.д.), без изменения кода, чтобы:
 • Убедиться, что транзакции обрабатываются корректно
 • Все этапы оплаты проходят
 • Сервер фиксирует оплату, записывает её в нужные таблицы
 • Пользователь получает Boost

⸻

🔍 Что нужно проверить:

1. ConnectWallet + manifest
 • Файл tonconnect-manifest.json:
 • Существует ли
 • Обслуживается ли сервером (status: 200)
 • Нет ли дубликатов
 • Проверить manifestUrl в TonConnectUIProvider
 • Убедиться, что кошелек подключается и адрес сохраняется

⸻

2. Frontend логика оплаты Boost
 • Найти компонент, отвечающий за оплату (например, BoostPackagesCard.tsx или аналог)
 • Проверить:
 • Создается ли транзакция
 • Отправляется ли на кошелек (TON Connect)
 • В каком формате уходит: amount, recipient, comment, tx_hash
 • Показывается ли пользователю уведомление

⸻

3. Backend: Проверка логики фиксации оплаты
 • Проверить, что после успешной транзакции:
 • Вызывается verify-ton-payment или аналогичный endpoint
 • Сервер:
 • Проверяет транзакцию в TON-сети
 • Сопоставляет tx_hash, user_id, boost_id, comment
 • Подтверждает успешность оплаты
 • Записывает в:
 • transactions
 • boost_purchases
 • обновляет users.ton_boost_package

⸻

4. Проверка в базе данных
 • Создаются ли записи в:
 • transactions с нужными параметрами (currency=TON, type=boost, status=success)
 • boost_purchases — с user_id, boost_id, timestamp, status=confirmed
 • Соответствуют ли суммы и ID пользователю
 • Обновляется ли колонка ton_boost_package в users

⸻

5. TON API: Подтверждение транзакции
 • Как именно backend проверяет факт транзакции?
 • Используется ли сторонний TON API (toncenter, Tonkeeper, Tonhub и др)
 • Проверяется ли адрес отправителя, получателя и сумма?
 • Проверяется ли статус: success / confirmed

⸻

6. Логирование и ошибки
 • В логах сервера: выводятся ли данные tx_hash, user_id, response от TON API
 • Ловятся ли ошибки?
 • Есть ли catch-блоки?

⸻

⚠️ Условия выполнения:
 • Никаких изменений в коде
 • Только исследование: просмотр, логика, сопоставление
 • Все названия файлов и путей могут отличаться — нужно найти, как реализовано у нас
 • Оформить отчет в виде таблицы или поэтапного плана:
 • Где начинается процесс
 • Какие файлы участвуют
 • Что работает корректно, что нет

⸻

✅ Цель в конце:

Получить уверенность, что:
 • Кошелек подключается
 • Оплата проходит
 • Сервер фиксирует Boost
 • Транзакция записана в базу
 • Все компоненты (frontend + backend) синхронизированы