# ✅ Техническое задание агенту  
Тип проверки: Регрессионный аудит + аудит интеграции TON Connect Wallet  
Проект: UniFarm  
Цель: Проверить, что система работает по ключевым точкам, а также удостовериться, что ConnectWallet (TON Connect) полностью готов к приёму пополнений TON в личный кабинет.

---

## 🔍 ЗАДАЧА 1. РЕГРЕССИОННАЯ ПРОВЕРКА СИСТЕМЫ

Проверить следующие модули только по фактам, без гипотез и выдумок:

| Модуль | Что проверить | Комментарии |
|--------|---------------|-------------|
| 🔗 ConnectWallet | Проходит ли соединение, сохраняется ли адрес, подгружается ли при перезагрузке страницы | Проверить localStorage + API /connect-wallet |
| 💼 Баланс | Отображается ли реальный баланс UNI/TON, совпадает ли с БД | Проверка balances, transactions |
| 💸 Депозит | Происходит ли фиксация TON-депозита (пополнение) после отправки с кошелька | Есть ли API /ton-deposit, создаётся ли transactions, обновляется ли баланс |
| 🧾 Транзакции | Создаются ли transactions при действиях | Проверить типы, поля, валюта, статусы |
| 🤝 Рефералы | Отрабатывает ли start_param через initData, записывается ли в referrals | Только факты — есть ли запись при переходе по ссылке |
| 🔄 Автовывод / Заявка | Создаются ли заявки на вывод, фиксируются ли в таблице withdraw_requests, статус pending | Не проверять реальные отправки, только факт записи |

---

## 🔍 ЗАДАЧА 2. АУДИТ CONNECTWALLET (TON CONNECT)

### 🔹 Проверить следующее:

| Пункт | Проверка |
|-------|----------|
| Подключение кошелька | Используется ли библиотека @tonconnect/sdk, вызывается ли TonConnectUI |
| Сессия | Сохраняется ли кошелёк между сессиями, что хранится в localStorage |
| Получение адреса | wallet.account.address доступен в UI и backend |
| Отправка TON | Есть ли UI/блок для создания transfer через wallet.sendTransaction() |
| Поддержка протокола | Используется ли TON Connect v2 — сверить с [официальной документацией](https://docs.ton.org/develop/dapps/ton-connect/overview) |
| Безопасность | Нет ли hardcoded ключей, всё ли на стороне пользователя |

---

## 📎 Что важно

- Не править код, только читать.
- Не использовать допущения. Только зафиксированные факты, подтверждённые файлами, логами, API-ответами или схемой.
- В отчёте обязательно указывать:
  - Название файла
  - Строку / функцию
  - API (если вызов идёт)
  - Ответ (если это network)
  - Что отработало / не отработало

---

## 🧩 Финальный отчёт должен содержать

1. Таблицу из 6–8 пунктов (регрессия)
2. Таблицу по ConnectWallet (TON Connect v2)
3. Указание на возможные несоответствия с официальной документацией (если есть)
4. Вывод: «Готов к приёму TON в личный кабинет» / «НЕ готов, причина: …»