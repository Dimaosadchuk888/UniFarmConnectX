✅ Цель:

Проверить готовность всей цепочки файлов для оплаты ToneBoost через внешний TON-кошелёк (через ConnectWallet).

⸻

📂 Ключевые цепочки: от UI → до записи в БД

1. Frontend

Файл Ответственность
components/Boost/BoostPurchase.tsx (или ToneBoostPurchase.tsx) Кнопка «Купить Boost» + выбор способа оплаты
components/Wallet/ConnectWallet.tsx Подключение кошелька (Tonkeeper, Tonhub)
utils/ton/tonConnect.ts или tonConnectUtils.ts Конфигурация TonConnect SDK
services/boostService.ts или api/boost.ts Вызов API: POST /api/boost/purchase или createExternalBoostTx()

🟠 Проверить:
 • Есть ли активный адрес TON-кошелька?
 • Есть ли формирование payload/tx для оплаты?
 • Вызывается ли API на бэкенд при оплате?

⸻

2. Backend (API Layer)

Файл Ответственность
routes/boost.ts или routes/boostPurchase.ts Роут POST /api/boost/purchase или GET /check-tx
controllers/boostController.ts Принимает txHash, проверяет статус оплаты
services/boostService.ts Обрабатывает бизнес-логику: проверка транзакции, создание буста
services/tonService.ts или utils/ton.ts Проверяет транзакции на TON-сети: через TonCenter, TonAPI, TonConnect
db/boostRepository.ts Создаёт запись в user_boosts, transactions

🟠 Проверить:
 • Есть ли методы проверки txHash?
 • Подключена ли проверка TON-кошельков (from, to, amount, memo)?
 • Готова ли логика создания Boost-пакета?

⸻

3. Database (Supabase / PostgreSQL)

Таблица Что хранит
transactions user_id, amount, type='BOOST_PURCHASE', tx_hash, ton_wallet
user_boosts user_id, boost_level, start_date, end_date, from_wallet, activated
ton_wallets (если есть) Список подключённых кошельков
boost_logs или wallet_payments (если есть) Технические логи всех попыток оплаты

🟠 Проверить:
 • Есть ли поля tx_hash, wallet_address, type, status?
 • Есть ли создание записей при внешней оплате?
 • Есть ли проверки на дублирование транзакций?

⸻

📌 Быстрая проверка: что готово, что нет

Компонент Готовность Комментарий
ConnectWallet компонент ✅ / ❌ Есть SDK? Работает подключение?
Кнопка “Купить Boost за TON” ✅ / ❌ Есть ли переход к оплате?
Формирование TON-ссылки ✅ / ❌ Есть ли payload с ID пользователя и Boost?
API /boost/purchase ✅ / ❌ Работает ли маршрут?
Проверка транзакции через tonService ✅ / ❌ Есть ли запрос на TonCenter/TonAPI?
Создание записи в user_boosts ✅ / ❌ Есть ли SQL/ORM-запрос?
Создание транзакции в transactions ✅ / ❌ Есть ли привязка к user_id и tx_hash?
Отображение Boost-пакета в UI ✅ / ❌ Подгружается ли активный Boost после покупки?


⸻

🛠 Что делаем сейчас?
 1. Пройтись по этим цепочкам файлов и определить:
 • Есть ли они вообще?
 • Заполнены ли функциями?
 • Готовы ли к приёму внешней TON-транзакции?
 2. Пометить:
 • 🟢 Готово
 • 🟡 Частично
 • 🔴 Отсутствует / требует правок

⸻

Хочешь — могу помочь:
 • Просканировать твою структуру проекта и сверить с этим чек-листом
 • Составить JSON-карту зависимостей: компонент → сервис → контроллер → БД
 • Подготовить список: какие файлы нужно создать или доработать