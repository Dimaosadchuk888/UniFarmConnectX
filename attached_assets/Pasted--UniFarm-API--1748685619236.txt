Старший технический аудитор UniFarm)

⸻

🔍 ВСТУПИ В РОЛЬ СЕНЬОР-РАЗРАБОТЧИКА / API-АРХИТЕКТОРА МИРОВОГО УРОВНЯ
Твоя задача — провести глубокий технический аудит проекта UniFarm, полностью проверить все API, контроллеры, бизнес-логику и архитектуру, разделить проект на функциональные блоки, и по каждому блоку дать:
 1. Сводку: какие есть файлы, за что они отвечают
 2. Проблемы: неиспользуемые, конфликтующие или дублирующие файлы
 3. Проверку: работает ли этот блок корректно
 4. Рекомендации: что нужно исправить

⸻

🧩 РАЗДЕЛИ ПРОЕКТ НА БЛОКИ И ПРОВЕРЬ КАЖДЫЙ ОТДЕЛЬНО:
 1. Регистрация / Telegram интеграция
 • Проверь: /api/v2/register/telegram, createUserFromTelegram, передачу telegram_id, username, ref_code
 • Убедись, что WebApp корректно получает initData
 • Убедись, что ref_code сохраняется и используется
 • Проверь, не происходит ли fallback на guest_id
 2. Пользовательская система
 • /api/v2/user/info, getUserInfoById, getUserByTelegramId
 • Убедись, что ID пользователя корректно используется в каждом запросе
 • Проверь поля ref_code, ref_by, created_at
 3. Фарминг TON / UNI
 • /api/v2/farming, /api/v2/ton-farming
 • Проверь контроллеры, схемы начислений, авторизацию пользователя
 • Убедись, что все данные подтягиваются из базы
 4. Партнёрская программа
 • /api/v2/referral/info, calculateReferralEarnings
 • Проверь генерацию и передачу ref_code
 • Убедись, что структура из 20 уровней реализована
 • Проверь расчёт % с фарминга
 5. Boost и Миссии
 • /api/v2/missions, /api/v2/boost-packages
 • Убедись, что миссии подтягиваются и отображаются
 • Проверь буст-пакеты, цену, доход, отображение в базе
 6. Wallet и Баланс
 • /api/v2/wallet/balance, /api/v2/wallet/withdraw
 • Проверь корректность расчёта баланса
 • Убедись, что данные приходят по user_id
 7. Deployment и Лишние файлы
 • Проверь дубли, мёртвые импорты, неиспользуемые роуты
 • Удали test-, check-, старые .ts копии, старые routes

⸻

🧾 ОЖИДАЕМЫЙ ОТЧЁТ (ПО КАЖДОМУ БЛОКУ):
 • 🔹 Название блока
 • 📂 Файлы и контроллеры
 • 🧪 Проверка: какие ошибки, что работает/не работает
 • ❌ Конфликты/дубли
 • ✅ Рекомендации
 • 📊 Итоговая оценка блока (0–10)

⸻

🎯 ЦЕЛЬ: Получить ПОЛНЫЙ аудит всех блоков системы UniFarm, выявить все причины ошибок (500, падения Telegram, ref_code инициализации, wallet багов), и построить план по стабилизации архитектуры.