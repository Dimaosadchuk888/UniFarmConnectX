⸻

Привет.
Нужно провести полное исследование корректности вывода средств в TON в разделе Wallet.
Никаких изменений или правок кода делать не нужно.
Твоя задача — только изучить, как сейчас работает система, и дать развёрнутый отчёт по каждому пункту ниже.

⸻

📌 Проверь следующие блоки:

1. 💰 Списание с баланса TON
 • Происходит ли мгновенное списание суммы с пользовательского баланса после подачи заявки?
 • Если есть комиссия — учитывается ли она при списании?
 • Обновляется ли UI: видно ли списание сразу после отправки заявки?

⸻

2. 🧾 Создание транзакции/заявки в базе данных
 • Создаётся ли запись о выводе в таблице (transactions, withdrawals, ton_requests и т.д.)?
 • Какие поля фиксируются: user_id, amount, currency, status, address, created_at?
 • Статус новой заявки: pending, processing и т.д. — корректно ли?

⸻

3. 🛡️ Валидация и защита
 • Можно ли подать заявку на сумму, превышающую текущий баланс?
 • Есть ли защита от повторных нажатий (двойных заявок)?
 • Проверяется ли формат TON-кошелька (валидный / невалидный)?

⸻

4. 🖼 Интерфейс и отклики
 • Есть ли визуальное подтверждение отправки заявки (toast, сообщение)?
 • Обновляется ли баланс на UI без перезагрузки страницы?
 • Не появляется ли баг при двойном клике на кнопку?

⸻

5. ⚙️ Фоновая обработка (если используется)
 • Есть ли система очередей (cron-задачи, async jobs) для обработки вывода?
 • Переходит ли заявка в статус success или failed?
 • Если обработка не проходит — возвращаются ли TON на баланс?

⸻

6. 🔐 Безопасность
 • Можно ли вручную через API отправить запрос и попытаться вывести больше, чем есть?
 • Есть ли защита: JWT, CSRF, user check и т.д.?

⸻

7. 📋 Логи и админ-панель
 • Логируются ли действия пользователя по выводу средств?
 • Есть ли интерфейс или логика на админке для обработки заявок (или проверка вручную)?

⸻

📝 Формат отчёта:
 • Опиши каждый пункт из списка — работает ли он корректно?
 • Если что-то не работает, опиши в чём именно проблема, какой файл или компонент за это отвечает, на каком этапе возникает сбой.
 • Ничего не меняй в логике системы. Только исследование и отчёт.

⸻