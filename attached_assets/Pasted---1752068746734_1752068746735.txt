
⚠️ **ВАЖНО:** Агенту запрещено:

* Вносить изменения в код
* Использовать тестовые аккаунты
* Обходить систему безопасности

---

## ✅ ЦЕЛЬ: Проверить работоспособность всех модулей **в рамках "smoke testing"**, то есть убедиться, что все критические функции:

1. Доступны
2. Не падают
3. Возвращают корректный ответ

---

# 🔥 SMOKE TEST PLAN (по ROADMAP.md)

## 1. 🔐 АВТОРИЗАЦИЯ

* [ ] Telegram initData проходит валидацию
* [ ] JWT токен создается и используется во всех защищенных endpoint
* [ ] GET `/api/v2/auth/session` возвращает текущую сессию
* [ ] POST `/api/v2/auth/refresh` обновляет токен
* [ ] POST `/api/v2/auth/logout` удаляет сессию

## 2. 👤 USER MANAGEMENT

* [ ] GET `/api/v2/user/profile` — корректные данные профиля
* [ ] POST `/api/v2/user/ref-code` работает
* [ ] GET `/api/v2/user/stats` возвращает статистику
* [ ] GET `/api/v2/user/sessions` работает, и видно текущую сессию

## 3. 💰 WALLET

* [ ] GET `/api/v2/wallet/balance` возвращает UNI и TON баланс
* [ ] POST `/api/v2/wallet/deposit` — проходит ли депозит?
* [ ] POST `/api/v2/wallet/withdraw` — создается заявка?
* [ ] POST `/api/v2/wallet/transfer` — перевод UNI работает?

## 4. 🌾 UNI FARMING

* [ ] POST `/api/v2/farming/start` — начинается ли фарминг?
* [ ] POST `/api/v2/farming/claim` — начисляются ли UNI?
* [ ] GET `/api/v2/farming/status` возвращает актуальные данные
* [ ] POST `/api/v2/farming/deposit` работает

## 5. ⚡ TON BOOST

* [ ] GET `/api/v2/boost/packages` — список отображается
* [ ] POST `/api/v2/boost/purchase` — проходит ли покупка?
* [ ] GET `/api/v2/boost/farming-status` — есть ли активный пакет?
* [ ] Доход начисляется планировщиком (см. историю транзакций)

## 6. 👥 РЕФЕРАЛЫ

* [ ] GET `/api/v2/referral/chain` — видна ли структура?
* [ ] GET `/api/v2/referral/stats` — видно начисления?
* [ ] Генерируется ли код при регистрации?

## 7. 🎯 МИССИИ

* [ ] GET `/api/v2/missions/list` — отображаются все 5 заданий
* [ ] POST `/api/v2/missions/complete` — можно ли завершить?
* [ ] GET `/api/v2/missions/user/:id` — есть ли история?

## 8. 🎁 DAILY BONUS

* [ ] GET `/api/v2/daily-bonus/status` — доступен ли бонус?
* [ ] POST `/api/v2/daily-bonus/claim` — начисляется ли 10 UNI?
* [ ] GET `/api/v2/daily-bonus/streak` — считает ли серию?

## 9. 🎉 AIRDROP

* [ ] GET `/api/v2/airdrop/active` — есть ли активные кампании?
* [ ] GET `/api/v2/airdrop/eligibility` — можно ли участвовать?
* [ ] POST `/api/v2/airdrop/claim` работает?

## 10. 🧾 TRANSACTIONS

* [ ] GET `/api/v2/transactions/history` — видна история?
* [ ] POST `/api/v2/transactions/export` — экспорт работает?
* [ ] GET `/api/v2/transactions/stats` — есть статистика?

## 11. 🧠 DEBUG

* [ ] POST `/api/v2/debug/decode-jwt` — JWT декодируется?
* [ ] GET `/api/v2/debug/check-user/:id` — данные читаются?

## 12. 📊 MONITORING

* [ ] GET `/api/v2/monitor/health` — статус OK?
* [ ] GET `/api/v2/monitor/status` — отображает живые модули?

## 13. 🤖 TELEGRAM BOT (frontend + backend)

* [ ] initData читается
* [ ] Кнопки Telegram работают
* [ ] WebApp реагирует на изменения баланса
* [ ] Команды бота (`/start`, `/help`, `/app`) доступны
* [ ] Не отображаются лишние кнопки (например, "перевод языка", "закрыть")

## 14. 🛠️ ADMIN BOT

Проверить через @unifarm\_admin\_bot (авторизация по username):

* [ ] `/stats` работает
* [ ] `/users` выводит список
* [ ] `/withdrawals` показывает заявки
* [ ] `/approve`, `/reject` — обрабатывают заявки корректно
* [ ] `/ban`, `/missions` работают
* [ ] Никакие команды не дублируются

---

## ✅ УСЛОВИЯ

* Все действия логируются в консоль
* Если endpoint не отвечает — ошибка фиксируется
* Если UI не отрисовывается — это тоже фиксируется
* Никаких изменений в код или БД!

---

Готов передать этот список агенту и запустить smoke-тестирование — подтвердить?