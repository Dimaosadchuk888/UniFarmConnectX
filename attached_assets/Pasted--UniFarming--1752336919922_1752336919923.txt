🧪 ЗАДАНИЕ: Углублённый аудит модуля UniFarming

🎯 Цель:
Провести детальную проверку всей цепочки работы UniFarm, начиная с UX, через backend до базы данных, и обратно.

✅ Что нужно проверить:
1. Открытие пакетов (депозитов):

Подтвердить, что открытие UniPacket (основного пакета) корректно фиксируется в БД
Убедиться, что каждый открытый пакет привязан к текущему пользователю
Проверить, что данные по пакету (сумма, время старта, ставка) точно сохраняются
2. Цепочка начислений UNI:

Проверить, запускается ли процесс начислений сразу после открытия депозита
Убедиться, что расчёты выполняются по правилам:
reward = base_rate * deposit_amount * time_passed
Проверить файл rewardCalculation.ts и весь маршрут, вплоть до записи начислений
3. Обновление баланса:

Убедиться, что начисленные UNI:
✅ сохраняются в базе
✅ добавляются в баланс пользователя
✅ обновляются в кошельке (и UI показывает обновление)
Подтвердить, что работает автообновление через WebSocket (или polling)
🔍 Где и что нужно исследовать:
🔁 Полная цепочка:

UX:
Что именно вызывается при нажатии "Открыть пакет"
Как формируется запрос (проверь Farming.tsx и UniFarmingCard)
Frontend → Backend:
Какой endpoint вызывается (POST /api/farming/start)
Что передаётся в запросе (userId, сумма, пакет, и т.д.)
Backend:
Контроллер: modules/farming/controller.ts
Сервис: modules/farming/service.ts
Расчёт: rewardCalculation.ts
Проверить, создаётся ли farming-entry в БД и как считается yield
База данных:
Проверить таблицы farming, users, transactions
Подтвердить:
Депозит закреплён за нужным user_id
Начисления записываются
Баланс увеличивается
Обратная передача на frontend:
Обновление данных по WebSocket (если есть)
UI показывает свежий баланс и активные пакеты
🧾 Дополнительно:
Агент НЕ ДОЛЖЕН менять код
Только анализ и отчёт: где что вызывается, что сохраняется, как обновляется
Агент должен зафиксировать все открытые мною пакеты
Проверить, что нет конфликтов в логике и дубликатов начислений
Отдельно указать, если какие-то поля или связи могут вызвать ошибки
📌 Результат:
Агент должен предоставить:

Подтверждение того, что вся цепочка работает корректно
Привязка депозитов к моему аккаунту в базе
Подтверждение начислений
Обновление баланса
Вывод: готов ли UniFarming для реального теста